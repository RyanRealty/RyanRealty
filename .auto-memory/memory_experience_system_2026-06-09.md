# Experience System program — locked 2026-06-09 (Matt directive)

**Mission:** "Thoroughly polished, smarter UI/UX, exciting and interactive, no tile-grid boredom, do this for EVERY page, rework the menu, stay optimized for tracking and getting found, think further ahead than others so when the traffic comes we absolutely dominate."

**The system:** six page archetypes (Geo, Hub, Listing, Tool, Content, LP) in the approved homepage-v3 design language, composed EDITORIALLY not as tile grids:
- Listings as ledger rows (huge Amboqia price, hairline rules, edge-bleed photos), never uniform card grids twice in a row
- Stats as typography (one giant living number owns the section), never stat-box rows
- Split-scroll spines (map/flyover pinned while content flows), full-bleed breaks, overlap/layering between sections
- Shared interactive modules: FlyoverHero (26 area videos rendered + gated in out/neighborhood-flyover/), LiveMarketBand, scrubbable price-history chart, map-linked listing filtering (price-pill map shipped), inline payment slider, pulse ticker, StickyActionRail, SectionNav
- Engagement telemetry on every section (section_view, scroll_depth, module_interact, dwell heartbeats) through lib/tracking.ts so improvement runs on behavior

**Menu rework:** mega-menu elevated to the same language — live counts/medians beside areas, price-drops + market entries, editorial layout.

**Sequencing (the anti-overwhelm contract):** ONE review gate — the Tetherow community-page exemplar (before/after screenshots) + blueprint docs/EXPERIENCE_SYSTEM.md. Matt calibrates once, THEN waves roll per archetype family across every route, each migration updating its parity contract to the new standard in the same change (gates enforce the destination, never block it). Builder agent ad6e49ad9bed20ffb ran the structural pass; a de-tiling + interaction pass applies before the review.

**Dominate-when-traffic-comes stack (keep green through every rework):** JSON-LD/G34 (21+ surfaces), llms.txt + AI-crawler allowlist (G39), canonical integrity (G40), sitemap freshness, CWV budgets (G10/G15), the E2E nightly walker, lead capture rails on every archetype, the measurement loop + site_improvement_ledger learning which pages convert.
