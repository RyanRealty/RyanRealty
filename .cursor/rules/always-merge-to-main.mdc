# Always merge to main

Every verified change goes to `main` and gets pushed immediately.
```bash
git checkout main && git pull origin main --no-rebase --no-edit && git merge <branch> --no-edit && git push origin main
```
Work is not done until it's on `main` and pushed. Do not leave work on feature branches.