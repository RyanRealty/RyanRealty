'use client'

/**
 * SITE-96. Route-local catalog install.
 *
 * Tip Ready (`taste-receipt --ship`) requires the contact page/_v3 set to
 * import the catalog specifiers (requireRouteImport). These are the real
 * sources the write path paints — beui-input shake/check and shadcn Input —
 * not a second kit and not a house cream box named after them.
 */
import { Input as BeuiInput } from '@/components/motion/input'
import { Input as ShadcnInput } from '@/components/ui/input'

export { BeuiInput, ShadcnInput }
