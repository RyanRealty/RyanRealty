'use client'

/**
 * SITE-103. Route-local catalog install.
 *
 * ci:catalog-install checks the house primitive on each install spec.
 * Tip Ready (`taste-receipt --ship`) also requires this route's page/_v3 set
 * to import the same specifiers (requireRouteImport). These are the real
 * sources the fold already paints — InsightPager on the year overlay,
 * AnimatedNumber on MOS and lead figures — not a second kit.
 */
import { InsightPager } from '@/components/motion/insight-pager'
import { AnimatedNumber } from '@/components/motion/number'

void InsightPager
void AnimatedNumber
