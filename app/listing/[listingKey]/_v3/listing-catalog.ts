'use client'

/**
 * SITE-99. Route-local catalog install.
 *
 * ci:catalog-install checks the house primitive named on each install spec.
 * Tip Ready (`taste-receipt --ship`) also requires the listing page/_v3 set
 * to import the same specifiers (requireRouteImport). These are the real
 * sources the fold already paints — carousel, button group, sheet, dialog,
 * tabs, action-swap, modal motion, photo skeleton — not a second kit.
 */
import { Carousel } from '@/components/ui/carousel'
import { ButtonGroup } from '@/components/ui/button-group'
import { Sheet } from '@/components/ui/sheet'
import { Dialog } from '@/components/ui/dialog'
import { Tabs } from '@/components/motion/tabs'
import { ActionSwapText } from '@/components/motion/action-swap'
import { TRANSITIONS_MODAL_SURFACE } from '@/components/motion/transitions-modal'
import { PhotoSkeleton } from '@/components/motion/photo-skeleton'

void Carousel
void ButtonGroup
void Sheet
void Dialog
void Tabs
void ActionSwapText
void TRANSITIONS_MODAL_SURFACE
void PhotoSkeleton
