'use client'

/**
 * SITE-92. Route-local catalog install.
 *
 * Tip Ready (`taste-receipt --ship`) requires the cities page/_v3 set to
 * import each claimed catalog specifier (requireRouteImport). These are the
 * installed sources the fold paints — not cream-box aliases.
 *
 *   beui-combobox            → V3MosCompare city overlay
 *   beautifului-insight      → CitiesInsight pager + year Liveline scrub
 *   beui-number              → V3Number on MOS bar values (V3MosBars)
 *   beui-infinite-masonry    → featured-city photo field
 */
import {
  Combobox,
  ComboboxList,
  ComboboxTrigger,
} from '@/components/motion/combobox'
import { InfiniteMasonry } from '@/components/motion/infinite-masonry'
import InsightCards from '@/components/motion/insight-cards'
import { AnimatedNumber } from '@/components/motion/number'

void Combobox
void ComboboxList
void ComboboxTrigger
void InfiniteMasonry
void InsightCards
void AnimatedNumber

export const citiesCatalogReady = true
