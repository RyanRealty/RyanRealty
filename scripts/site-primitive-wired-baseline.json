{
  "note": "ci:site-primitive-wired (G73) — SHRINK-ONLY. Barrel components from components/site/v3/index.ts with no importer outside app/dev/**. A component leaves this list by being wired into the public route its SITE node owes; no component may be added without recording that node id. Regenerate with `node scripts/check-site-primitive-wired.mjs --write-baseline`.",
  "generatedAt": "2026-09-08T12:57:58.223Z",
  "seedFinding": "Seeded 2026-09-08 against the tree at 1409fe74. V3StickyAsk and V3ProofBlock — the two that caused this gate — are correctly ABSENT: both were wired onto /sell in 65d4dbc8. Run against 65d4dbc8^ the gate names exactly those two with their dev-only importers, so it is proven load-bearing on the incident it exists for. The seven below are a DIFFERENT and milder shape than that incident and none was found by the queue: five are sub-primitives a wired barrel sibling composes (so a visitor does reach them, through the parent), and two are orphan exports with no consumer anywhere. No SITE node owns any of them yet — each carries the parent that reaches it, or the fact that nothing does.",
  "unwired": {
    "V3ChartHover": "sub-primitive: composed by V3Chart (components/site/v3/V3Chart.tsx:38), which IS wired, so visitors reach it through the parent. Never imported directly by a page. SITE node that owes the wiring: none filed — either a node claims it as a page-level control or the barrel stops exporting it.",
    "V3CourseMapControl": "sub-primitive: composed by V3CourseMap (components/site/v3/V3CourseMap.tsx:44), which IS wired, so visitors reach it through the parent. Never imported directly by a page. SITE node that owes the wiring: none filed — either a node claims it as a page-level control or the barrel stops exporting it.",
    "V3Figure": "ORPHAN EXPORT — no consumer anywhere in the repo under this name. The only V3Figure usage is components/site/v3/V3Instrument.tsx importing it from './atoms' directly, so the barrel re-export at index.ts:71 reaches nobody. SITE node that owes the wiring: none filed — a node either wires it into a public route or drops it from the barrel.",
    "V3FooterFold": "sub-primitive: composed by V3Footer (components/site/v3/V3Footer.tsx:52), which IS wired, so visitors reach it through the parent. Never imported directly by a page. SITE node that owes the wiring: none filed — either a node claims it as a page-level control or the barrel stops exporting it.",
    "V3JsonLd": "ORPHAN EXPORT — nothing imports the name V3JsonLd. Its alias MetadataBlock IS wired (app/communities/[slug]/types/[type]/page.tsx imports it from the barrel), and every other page imports a DIFFERENT MetadataBlock from '@/components/site/MetadataBlock'. Two components share one name across two registers. SITE node that owes the wiring: none filed — resolving the name collision is the real work here.",
    "V3Placeholder": "sub-primitive: composed by V3Loading (components/site/v3/V3Loading.tsx:20), which IS wired, so visitors reach it through the parent. Never imported directly by a page. SITE node that owes the wiring: none filed — either a node claims it as a page-level control or the barrel stops exporting it.",
    "V3ProofStars": "sub-primitive: composed by V3ProofBlock (components/site/v3/V3ProofBlock.client.tsx:43), which IS wired onto /sell, so visitors reach it through the parent. Never imported directly by a page. SITE node that owes the wiring: none filed — either a node claims it as a page-level control or the barrel stops exporting it."
  }
}
