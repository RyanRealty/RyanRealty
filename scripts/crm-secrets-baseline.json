{
  "note": "Known CRM cron routes present under app/api/cron but NOT registered in vercel.json (they never run). KNOWN DEBT pending schedule-or-delete. Count may only SHRINK. Regenerate with `npm run ci:crm-secrets:baseline`.",
  "generated_by": "check-crm-secrets.mjs --write-baseline",
  "orphan_crons": []
}
