{
  "route": "app/compare/page.tsx",
  "mockup": "design_system/ryan-realty/ui_kits/compare/index.html",
  "visualLanguage": "design_system/public/PUBLIC_UI.md (locked 2026-08-11). The index.html in this directory is the RETIRED KB-era mockup and is kept only because ci:mockup-coverage requires a parity.json beside any index.html. It is not the visual target. Comparison is a Sheet job. CompareClient is the interactive working surface (photos, feature table, best-in-class, map, copy link, PDF). The barrel Sheet's static compare block cannot host that without dropping those controls, so CompareClient stays and the page chrome moves to v3. Not a sixth pattern.",
  "competitiveTarget": "Utility comparison page: side-by-side property table. noindex — not an SEO surface.",
  "note": "E-HOMES-SEARCH 2026-08-13. Migrated /compare chrome onto the v3 barrel. Chrome: layout mounts V3Chrome (sticky, in flow). This page does not remount it. V3Breadcrumb belowNav=false. V3Footer outside <main>. Page contract carried unchanged: robots noindex,follow, revalidate 60, canonical /compare, DAL fetch (getListingTiles by listNumbers AND listingKeys, dedup, getListingDetailPhotos), CompareClient props, BreadcrumbList JSON-LD, KbSectionTracker pageType=compare. Shared /compare?ids= links keep resolving. Dual objectives unchanged. Capture does not live on this route. KB-era deletions: SmoothScrollProvider, KbBreadcrumb, KbFooter, kb-root, kb.css, navy header band. AICompare is built but not wired — HOLD, same note as before. MetadataBlock and KbSectionTracker stay on non-v3 registers.",
  "requiredComponents": [
    {
      "name": "KbSectionTracker",
      "section": "Tracking — section_view + scroll_depth events (page contract G52), pageType=compare. Analytics wiring, not visual language.",
      "blocking": true
    },
    {
      "name": "MetadataBlock",
      "section": "JSON-LD — BreadcrumbList structured data (Home > Compare). No WebPage JSON-LD on a noindex utility route.",
      "blocking": true
    },
    {
      "name": "V3Breadcrumb",
      "section": "Home / Compare homes. Surface tone, belowNav=false because chrome is sticky in flow.",
      "blocking": true
    },
    {
      "name": "V3Heading",
      "section": "H1 Compare homes. Display face via the barrel atom.",
      "blocking": true
    },
    {
      "name": "V3Quiet",
      "section": "Empty state when ?ids= is missing: how to add homes, plus a Search homes exit.",
      "blocking": true
    },
    {
      "name": "CompareClient",
      "section": "interactive comparison surface — photo row, side-by-side feature table with best-in-class highlighting, Google Maps locations embed, Copy Link + Download PDF actions, empty/loading state",
      "blocking": true
    },
    {
      "name": "V3Footer",
      "section": "Chrome. V3_FOOTER_COLUMNS. Rendered OUTSIDE <main>.",
      "blocking": true
    }
  ],
  "layoutComponents": [
    {
      "name": "V3Chrome",
      "section": "renders from app/layout.tsx, position:sticky in flow",
      "note": "ci:mockup-parity auto-satisfies a V3Chrome requiredComponent via layout. This page does not remount it."
    }
  ],
  "deferredComponents": [
    {
      "name": "AICompare",
      "reason": "Built but not wired. Wire-or-delete is a HOLD from the compare-homes process, not this lease."
    }
  ]
}
