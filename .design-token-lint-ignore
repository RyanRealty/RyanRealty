# Existing violations tracked for incremental cleanup.
# New files should not be added without a cleanup plan.
# Rental calculator tool + its SVG equity chart: chart height, axis label sizes,
# tooltip width, and SVG stroke/fill navy have no locked-ladder equivalent (same
# class of exception as the admin report/chart pages below).
components/tools/RentalCalculator.tsx
components/tools/EquityProjectionChart.client.tsx
app/actions/generate-market-report.ts
app/admin/(protected)/audit-log/page.tsx
app/admin/(protected)/crm/page.tsx
app/admin/(protected)/crm/[id]/page.tsx
app/admin/(protected)/email/campaigns/page.tsx
app/admin/(protected)/expired-listings/page.tsx
app/admin/(protected)/geo/AssignCommunity.tsx
app/admin/(protected)/geo/NeighborhoodForm.tsx
app/admin/(protected)/geo/area-guide-upload/AreaGuideUploadClient.tsx
app/admin/(protected)/listings/page.tsx
app/admin/(protected)/query-builder/AdminQueryBuilderForm.tsx
app/admin/(protected)/reports/CityReportSection.tsx
app/admin/(protected)/reports/brokers/page.tsx
app/admin/(protected)/reports/custom/CustomReportBuilder.tsx
app/admin/(protected)/resort-communities/page.tsx
app/admin/(protected)/site-pages/TeamImageForm.tsx
app/admin/(protected)/sync/SyncHistoryTable.tsx
app/admin/(protected)/sync/SyncHistoryTest.tsx
app/admin/(protected)/sync/SyncRunLog.tsx
app/admin/(protected)/sync/page.tsx
app/api/og/route.tsx
app/components/admin/AdminBrokerForm.tsx
app/components/admin/AdminUsersList.tsx
app/dashboard/page.tsx
app/layout.tsx
app/reports/sales/[city]/[period]/page.tsx
components/ListingMapGoogle.tsx
components/SearchFilterBar.tsx
components/activity/ActivityFeedSection.tsx
components/admin/DashboardSyncPanel.tsx
components/compare/CompareClient.tsx
components/listing/ListingGallery.tsx
components/listing/showcase/ShowcaseHero.tsx
components/listing/ListingHero.tsx
components/listing/ListingNav.tsx
components/open-houses/OpenHousesClient.tsx

# Bend LP — Google Maps API requires literal hex strings for polygon
# strokeColor/fillColor (not CSS variables). Also includes data-table HTML
# that's being migrated to shadcn <Table> in Wave 3 page rebuild per
# docs/EXECUTION_PLAN.md. Tracked exceptions, not unbounded.
app/lp/bend/_components/BendInteractiveMap.tsx
app/lp/bend/page.tsx

# Central Oregon Golf LP — bespoke marketing LP with self-contained
# <style jsx> blocks. Uses brand navy/cream hex literally because the
# inline styles can't reference CSS vars defined elsewhere in the doc.
# The golf-table HTML is comparative data (course-by-course matrix);
# migrating to shadcn <Table> would lose the print-specific layout
# the golf vertical needs. Tracked exception per Wave 3 plan.
app/lp/central-oregon-golf/page.tsx

# Admin analytics action-required dashboard — uses 4 distinct severity
# colors (amber/blue/orange/red) for ranked alert dots. The design system
# carries warning + destructive but lacks an info or stretch-warning token
# to cleanly separate "warn vs urgent vs critical". Internal-only page,
# not user-facing; tracked exception.
app/admin/(protected)/analytics/action-required/page.tsx

# Admin producers spec viewer — renders SKILL.md markdown to HTML.
# The <hr> reference is inside a runtime markdown replacer
# (`.replace(/^---$/gm, '<hr>')`), not JSX. Internal-only page.
app/admin/(protected)/producers/[slug]/page.tsx

# Seller home-value LP — uses Google brand hex (#4285F4, #34A853,
# #FBBC05, #EA4335) for the official Google sign-in button. Google's
# brand guidelines require these exact colors; cannot be CSS-var'd.
app/lp/seller-home-value/page.tsx

# CMA email route — renders the HTML email body + preview-page wrapper
# as inline-style HTML. Email clients don't support CSS variables so
# brand-color hex literals (#102742, #5f6c7c, #f5f3ed, #faf8f4) are
# unavoidable. Tracked exception.
app/api/cma/[slug]/email/route.ts

# Clustered search map — price-pill markers are SVG data-URIs and the
# listing popup renders inside a Google Maps InfoWindow, both isolated
# from Tailwind/CSS-vars, so brand hex literals (#102742 navy, white,
# charcoal, status red) are required inline. Same case as
# components/ListingMapGoogle.tsx above.
components/SearchMapClustered.tsx

# Shared map marker/InfoWindow primitives — SVG data-URI price-pill markers
# and InfoWindow HTML builders require literal hex because Google Maps
# isolates these from Tailwind/CSS-var inheritance. Canonical source of
# truth for all inline hex on map surfaces; all map components import from here.
lib/maps/markers.ts

# NeighborhoodMap — the InfoWindow renders inside Google Maps' isolated DOM
# context and therefore needs literal hex for brand colors, same pattern as
# SearchMapClustered.tsx and ListingMapGoogle.tsx.
components/site/NeighborhoodMap.client.tsx

# Map-search filter UI (SearchFilters) + map-search shell (MapSearchView) —
# migrated to design-system components (P1.20, 2026-06-09). Files removed from
# the ignore list so DISALLOWED_PRIMITIVES scanning resumes.
