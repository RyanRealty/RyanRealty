# Existing violations tracked for incremental cleanup.
# New files should not be added without a cleanup plan.
# Rental calculator tool + its SVG equity chart: chart height, axis label sizes,
# tooltip width, and SVG stroke/fill navy have no locked-ladder equivalent (same
# class of exception as the admin report/chart pages below).
components/tools/RentalCalculator.tsx
components/tools/EquityProjectionChart.client.tsx
app/actions/generate-market-report.ts
app/admin/(protected)/audit-log/page.tsx
app/admin/(protected)/crm/page.tsx
app/admin/(protected)/crm/[id]/page.tsx
app/admin/(protected)/email/campaigns/page.tsx
app/admin/(protected)/expired-listings/page.tsx
app/admin/(protected)/geo/AssignCommunity.tsx
app/admin/(protected)/geo/NeighborhoodForm.tsx
app/admin/(protected)/geo/area-guide-upload/AreaGuideUploadClient.tsx
app/admin/(protected)/listings/page.tsx
app/admin/(protected)/query-builder/AdminQueryBuilderForm.tsx
app/admin/(protected)/reports/CityReportSection.tsx
app/admin/(protected)/reports/brokers/page.tsx
app/admin/(protected)/reports/custom/CustomReportBuilder.tsx
app/admin/(protected)/resort-communities/page.tsx
app/admin/(protected)/site-pages/TeamImageForm.tsx
app/admin/(protected)/sync/SyncHistoryTable.tsx
app/admin/(protected)/sync/SyncHistoryTest.tsx
app/admin/(protected)/sync/SyncRunLog.tsx
app/admin/(protected)/sync/page.tsx
app/api/og/route.tsx
app/components/admin/AdminBrokerForm.tsx
app/components/admin/AdminUsersList.tsx
app/dashboard/page.tsx
app/layout.tsx
app/reports/sales/[city]/[period]/page.tsx
components/ListingMapGoogle.tsx
components/SearchFilterBar.tsx
components/activity/ActivityFeedSection.tsx
components/admin/DashboardSyncPanel.tsx
components/compare/CompareClient.tsx
components/listing/ListingGallery.tsx
components/listing/showcase/ShowcaseHero.tsx
components/listing/ListingHero.tsx
components/listing/ListingNav.tsx
components/open-houses/OpenHousesClient.tsx

# Bend LP — Google Maps API requires literal hex strings for polygon
# strokeColor/fillColor (not CSS variables). Also includes data-table HTML
# that's being migrated to shadcn <Table> in Wave 3 page rebuild per
# docs/EXECUTION_PLAN.md. Tracked exceptions, not unbounded.
app/lp/bend/_components/BendInteractiveMap.tsx
app/lp/bend/page.tsx

# Central Oregon Golf LP — bespoke marketing LP with self-contained
# <style jsx> blocks. Uses brand navy/cream hex literally because the
# inline styles can't reference CSS vars defined elsewhere in the doc.
# The golf-table HTML is comparative data (course-by-course matrix);
# migrating to shadcn <Table> would lose the print-specific layout
# the golf vertical needs. Tracked exception per Wave 3 plan.
app/lp/central-oregon-golf/page.tsx

# Admin analytics action-required dashboard — uses 4 distinct severity
# colors (amber/blue/orange/red) for ranked alert dots. The design system
# carries warning + destructive but lacks an info or stretch-warning token
# to cleanly separate "warn vs urgent vs critical". Internal-only page,
# not user-facing; tracked exception.
app/admin/(protected)/analytics/action-required/page.tsx

# Admin producers spec viewer — renders SKILL.md markdown to HTML.
# The <hr> reference is inside a runtime markdown replacer
# (`.replace(/^---$/gm, '<hr>')`), not JSX. Internal-only page.
app/admin/(protected)/producers/[slug]/page.tsx

# Seller home-value LP — uses Google brand hex (#4285F4, #34A853,
# #FBBC05, #EA4335) for the official Google sign-in button. Google's
# brand guidelines require these exact colors; cannot be CSS-var'd.
app/lp/seller-home-value/page.tsx

# CMA email route — renders the HTML email body + preview-page wrapper
# as inline-style HTML. Email clients don't support CSS variables so
# brand-color hex literals (#102742, #5f6c7c, #f5f3ed, #faf8f4) are
# unavoidable. Tracked exception.
app/api/cma/[slug]/email/route.ts

# Clustered search map — price-pill markers are SVG data-URIs and the
# listing popup renders inside a Google Maps InfoWindow, both isolated
# from Tailwind/CSS-vars, so brand hex literals (#102742 navy, white,
# charcoal, status red) are required inline. Same case as
# components/ListingMapGoogle.tsx above.
components/SearchMapClustered.tsx

# Shared map marker/InfoWindow primitives — SVG data-URI price-pill markers
# and InfoWindow HTML builders require literal hex because Google Maps
# isolates these from Tailwind/CSS-var inheritance. Canonical source of
# truth for all inline hex on map surfaces; all map components import from here.
lib/maps/markers.ts

# NeighborhoodMap — the InfoWindow renders inside Google Maps' isolated DOM
# context and therefore needs literal hex for brand colors, same pattern as
# SearchMapClustered.tsx and ListingMapGoogle.tsx.
components/site/NeighborhoodMap.client.tsx

# Map-search filter UI (SearchFilters) + map-search shell (MapSearchView) —
# migrated to design-system components (P1.20, 2026-06-09). Files removed from
# the ignore list so DISALLOWED_PRIMITIVES scanning resumes.

# Transaction Coordinator deal dashboard — shipped in TC system v1 (commit
# c605e562). Admin-only surface; uses bg-white + pixel sizing for the deal
# detail layout (max-w-[680px] max-h-[420px] text-[10px]). Tracked exception;
# migrate to locked ladder in Wave 3 TC polish pass.
app/admin/(protected)/deals/[key]/page.tsx
app/admin/(protected)/deals/page.tsx

# Experience System hero modules — FlyoverHero, LiveMarketBand, SectionNav,
# InlineValuationHook render white text/overlays on top of photo/video media
# (full-bleed backgrounds). The design system has no token for "white text on
# photo" (primary-foreground is navy) — these are the same class of exception
# as ListingHero, ShowcaseHero, and the LP hero components. Also uses
# pixel-level sizing (w-[7px] for the live-dot, min-h-[580px] for the hero
# viewport height) with no locked-ladder equivalent. Experience System
# modules are tracked exceptions, not unbounded; each new module is listed.
components/site/experience/FlyoverHero.tsx
components/site/experience/LiveMarketBand.tsx
components/site/experience/SectionNav.tsx
components/site/experience/InlineValuationHook.tsx
# v3.1 de-tiling modules -- same class of exceptions:
# ListingLedger: white text on photo scrim (featured listing overlay), pixel-level
#   thumb height, tracking overrides for badge label, hand-rolled badge span
#   (inline on a photo overlay -- Badge component doesn't compose well at this
#   position inside an <a> with complex hover state).
# CommunityMapLedgerPane: fractional max-w for the 52/48 split layout (no locked
#   equivalent in the ladder -- this is an asymmetric editorial grid).
# PriceHistoryScrubber.client: chart uses raw navy/white hex for SVG gradient stops
#   (recharts doesn't accept CSS custom properties in fill/stroke) -- same exception
#   class as EquityProjectionChart.client.tsx.
# PriceHistoryScrubber: h-[260px] for the loading skeleton (fixed chart height).
# PaymentSlider: range input with inline gradient for track fill (no token equivalent
#   for a slider track), hex for the gradient stops, label+input primitives
#   (no shadcn slider component that matches this inline pattern).
components/site/experience/ListingLedger.tsx
components/site/experience/CommunityMapLedgerPane.tsx
components/site/experience/PriceHistoryScrubber.tsx
components/site/experience/PriceHistoryScrubber.client.tsx
components/site/experience/PaymentSlider.tsx
# Homepage v3 Experience Rollout components (2026-06-10, Family 2):
#   - text-white/* variants required on photo/dark backgrounds (hero, ticker, mosaic,
#     market band) — no cream/navy token alternative works on a dark image scrim.
#   - SVG path fill in NeighborhoodMap uses #102742 directly (SVG attrs can't use CSS vars).
#   - fine-grain text sizes (text-[11px] etc) for stat micro-labels — no locked-ladder step.
#   - raw <input>/<button> in hero search form — no shadcn equivalent for inline hero search.
#   - link-as-button in mosaic/map — CTA links that are visually button-like.
#   Cleanup plan: once the design system adds photo-overlay tokens, migrate here.
# cities/page.tsx modifications (cities family rollout, photo-on-dark text-white variants):
app/cities/page.tsx
# communities index rebuild (P0-5, same Experience System hub language as
# app/cities/page.tsx: navy hero with text-white/* on-dark variants, live-dot
# h-[7px], micro-label text-[11px]/tracking ladder — same exception class):
app/communities/page.tsx
# admin pages with pre-existing raw-input/bg-white (other session work):
app/admin/(protected)/crm/sequences/page.tsx
app/admin/(protected)/crm/approvals/page.tsx
app/admin/(protected)/crm/workflows/page.tsx
app/admin/(protected)/sign-off/page.tsx
components/site/HomepageHeroV3.tsx
components/site/HomepagePulseTicker.tsx
components/site/HomepageListingsMosaic.tsx
components/site/HomepageNeighborhoodMap.tsx
components/site/HomepageMarketBand.tsx
components/site/HomepageVoiceBlock.tsx
components/site/HomepageToolsRow.tsx
components/site/HomepageTrustBand.tsx
# Admin sidebar: sticky-below-61px-header needs h-[calc(100vh-61px)] — no
# locked-ladder step for viewport-minus-header heights (same exception class
# as the admin report/chart pages above).
app/components/admin/AdminSidebar.tsx
# Mobile bottom tab bar: iOS safe-area inset requires pb-[env(safe-area-inset-bottom)]
# — no locked-ladder step can express env() (same exception class as the
# sticky-header calc heights above).
app/components/admin/AdminMobileTabBar.tsx
# Recharts LP performance charts need literal SVG fill/stroke colors (navy + mid-navy),
# same exception class as the rental EquityProjectionChart.

# Kinetic-brutalist (KB) surface — components/site/kb/* + app/concept/kb/*.
# This is the NEW locked bespoke design language (navy/cream, Amboqia/Geist,
# GSAP/Lenis) being rolled across the site, NOT the shadcn system this floor
# enforces. It carries its own token layer (components/site/kb/kb.css:
# --navy/--cream/--edge) and gets a dedicated KB structural gate (Phase 3).
# Raw hero search input/button + full-bleed navy/cream sections — same exception
# class as the V3 hero search above. Each KB island is listed as added.
components/site/kb/
app/concept/kb/
