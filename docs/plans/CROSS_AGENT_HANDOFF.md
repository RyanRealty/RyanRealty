# Current — 2026-09-24 (Vault: PRs #354, #356, #357, #358, #359 merged; dashboard + file workspace, SkySlope daily intake, reader memory fix and deal terms in the follow-up PR. PR #355 visibility follow-up: leases, split search, sort order, About AEO, CRM enrollment fix. Visibility pass PR #352 merged + deployed)

**Vault (branch `claude/transaction-management-optimization-6jdt16`).**

Surface: Claude cloud session. Merged: PR #354 `9ae0438`, #356 `ea11690`, #357 `9f8431a`, **#359 `ef093de`** (transactions dashboard `/admin/closings` + tabbed file workspace, daily SkySlope → Vault intake cron `skyslope-vault-intake` 06:50Z, mail rules v3, principal review held in SkySlope until cutover). Follow-up PR on the same branch (after #359): principal review mode `/admin/sign-off/review`; intake carries SkySlope checklist status onto untouched Vault items; closed files show no warnings; the document reader's out-of-memory fix; deal terms read from the contract. Matt: "You always have access. Don't stop to ask for SQL access": `.claude/settings.json` DENIES `mcp__Supabase__execute_sql`; DB work goes through service-role tsx scripts.

Matt's directives 2026-09-24 (standing): documents must never be wrong (identify the form and release, confirm filled out and fully executed); every email reviewed and mapped to deal and broker; audit-ready for the principal broker rules; Matt reviews in SkySlope until the cutover; closed transactions carry no warnings; deal information fills itself from the executed contract, "bulletproof" (two independent readers must agree, `docs/TC_SYSTEM.md` "Deal terms from the contract").

State:
- **Document reader outage 2026-09-24 09:35Z → follow-up deploy:** `/api/cron/tc-document-read` was killed for memory on every run (a one-page poster-size plan, "17130 Mayfield Dr", rendered to 4.2 GB); 744 live documents unread. Fix in the follow-up PR (render cap, interrupted-read marker, 3-attempt cap). The backlog drains on the cron after deploy; `tc-deal-terms` texts Matt if the reader stalls 2 h again.
- **Deal terms** (`lib/tc/terms`, `lib/data/tc/deal-terms.ts`, cron `tc-deal-terms` every 20 min): Claude (native PDF) + Grok (renders); a term is written only when both agree, only into EMPTY cycle fields; conflicts wait on the file's Overview ("From the contract"). The Grok half was checked on Beaumont's 15 contract documents; the Claude half has no key in the cloud container: check its first production run with `GET /api/cron/tc-deal-terms?doc=<id>&dry=1`.
- **SkySlope intake** not yet applied in production: first run is the 06:50Z cron after the follow-up deploys, unless Matt says "apply the SkySlope pull now". Dry run: 1 new deal (909 NW Delaware), 4 cycles, 64 distinct documents (every one uploaded after the 6/10 import), 3 stages (Beaumont → Closed 2026-07-09), 2 checklist statuses.
- **Records audit** `/admin/closings/audit`; **mail review** Paul/Rebecca done, Matt's walk resumes from its cursor (`review-all --mailbox matt@ryan-realty.com --model-stage`, then `review-retry`).

**Next:** watch the follow-up PR to green and merge; confirm `tc-document-read` reads again and `tc-deal-terms` dry run on Beaumont's Nicoll agreement; SkySlope intake first run.
- Skills read: `.claude/skills/tc-builder/SKILL.md`, `.claude/skills/skyslope-form-compliance/`, `design_system/admin/ADMIN_UI.md`, CLAUDE.md §0/§1/§3/§4/§6/§7/§8.

**Visibility follow-up (PR #355, branch `claude/admiring-feynman-7lgwc3`), on top of PR #352 (merged to main as `d8741c2`, deployed).**

Surface: Claude Code cloud session. Standing rule from Matt 2026-09-24 (CLAUDE.md §8): every ask ends live on production; the session merges its own PR once CI is green, confirms the deploy, applies its held migrations, and leaves no branch or worktree behind.

- **Landed on PR #355:** commercial space for lease (a closing section on every place page plus `/commercial-space-for-lease`; the rent unit is read per listing from MLS `Lease Rate Options`, a rate never prints without its unit); `/homes-for-sale` opens as split map + list on desktop and list on phones, framed by `CENTRAL_OREGON_BOUNDS`; `newest` means newest LISTED on every path, the Sold scope's `newest` means most recently SOLD, and "new in N days" reads the on-market date (all in `lib/search/search-sort-order.ts` + `searchPredicates.ts listedWithinDaysCutoff`); the region Atlas core is cached as a head plus chunks under Next's 2MB entry limit; the dial's thumbnail rail sits on the left (Jax covered it on the right); /about gained the AEO playbook sections below Matt's 09-21 lock (no competitor named, "same business day" reply promise); the site-wide Organization JSON-LD `foundingDate` is 2014 (Ryan Realty LLC) and /about shows the recorded closings as a count only (Matt 2026-09-24); the transitional `/api/cron/refresh-mvs` route is deleted (both `mv_refresh_state` stamps verified 2026-09-24 02:2xZ, pg_cron owns both duties).
- **Tests and taste on PR #355.** 18 test files matched no vitest project and never ran (one, `app/about/_v3/about-playbook.test.tsx`, had failed since it was written: "multi-family" tripped its protected-class check). Each vitest project now owns its include list (the root list was concatenated onto every project, so `test:int` ran all 1,283 files and `npm test` ran every unit test twice), and G78 `ci:tests-wired` fails a test file no project runs or two projects run. `/commercial-space-for-lease` is a taste class with a seeded content floor; its taste receipt is still the 2026-09-23 one at 66, because two fresh evaluator runs on the same judge scored 54 and 59 (findings in its `parity.json` `openDefects`, incl. the site-wide Jax button over "13 for lease" at 375). Re-judge after the next craft pass on that page.
- **Database.** Every migration #352 wrote is applied, including the held `20260923014700_pulse_withhold_unverified_closed_side` (applied after #352 deployed; 28 neighborhood rows null on the closed side, city 30-close floor live). **Held on purpose:** `20260923230000_search_sort_newest_listed` (the RPC list path's newest-listed / recently-sold order and on-market "new in N days"): apply after #355 deploys, EXPLAIN ANALYZE on production first.
- **CRM, done 2026-09-24.** Buyer sequence id 2 step 0 (email) and step 1 (SMS + fallback email) rewritten live (Matt approved). Janell (person 63952, 1130 Canter Ct, Sisters): Matt met her; she is not selling for now. She stays in Nurture (the streamlined stage that replaced "C - Cold"; `crm_stages` has 9 active stages, while `lib/crm/constants.ts CRM_STAGES` still lists the retired ones), her seller enrollment 62 is paused, the 09-14 call task is closed, and her CMA `cma-1130-canter-sisters-usa` is finalized at $675,000 to $705,000 (the flagged `cma-1130-canter-sisters` draft is archived). Nothing was sent to her.
- **CRM enrollment defect, fixed.** Since 2026-06-13 the engine wrote status `awaiting_broker_next` that the table's CHECK refused; `finish()` ignored the error, so any enrollment whose next step is confirm:true re-ran its current step every 15 minutes (no re-send: the send claim held) and never parked. Migration `20260924052000_crm_enrollment_status_awaiting_broker_next` is APPLIED; the engine now throws on a refused write; `lib/crm/enrollment-status.ts` + its test hold the code's statuses equal to the CHECK. Buyer person 64134 was the other looping enrollment: its duplicate "step 1 skipped / Next step ready" timeline rows can be removed once it has parked.
- **Database load (not fixed, Matt's to prioritize).** pg_cron `refresh_listing_search_mv` averages 253s per 15-minute run and `refresh_listing_tile_mv` 481s per 30-minute run (pg_stat_statements, 2026-09-24), so those two refreshes alone occupy 28% (253/900) and 27% (481/1800) of wall time. That is when builds prerender degraded pages and search RPCs time out (`search_listings_advanced` Bend active page 1 read 9.6s to 18s at 03:40Z). The p09 addition (`neighborhood_year_pricing_mv`) costs about 10s per run. The fix is incremental maintenance of those two views instead of full refreshes.
- **Matt's calls still open.** Keep or retire the GA4 Measurement Protocol mirror; Turnstile on /contact if the honeypot does not stop the bots; a 301 of the plain `/homes-for-sale/sunriver` if the canonical alone does not consolidate; `CRM_SEQ_SUPPRESSED_SMS_FALLBACK_EMAIL` stays OFF (turning it on emails leads more, not approved).
- **Could not clean from the session:** sixteen remote snapshot branches `claude/admiring-feynman-7lgwc3--wip-p00..p15` are superseded (every package merged with #352, `docs/plans/VISIBILITY_2026-09-22/README.md`), but the session's git access refuses deleting any branch other than its own. Delete them in GitHub.
- **Blocked on 28-day GSC windows to 2026-10-21:** SITE-181/188 Brasada, SITE-182/189 Tetherow, SITE-183 Broken Top, SITE-184/191 Black Butte Ranch, SITE-185 Bend luxury, SITE-186/187 Sunriver, SITE-190 Bend and SITE-192 Redmond. Re-pull with query×page through the GSC service account when the windows open.
- **Not done, no node.** `WebPage.primaryImageOfPage` is omitted on the split branch. Plat pages build `/communities/${resortSlug}` from the durable slug (a Pronghorn plat links `/communities/pronghorn`, a 308) at five call sites. `/housing-market/black-butte-ranch` and `/housing-market/sisters/black-butte-ranch` are two self-canonical market URLs for one place. Crooked River Ranch joined the self-city set by derivation; excluding it needs a registry field. The chrome Homes panel still lists Sunriver as a search city.
- **Rules landed 2026-09-22 (standing).** The contact's assigned broker signs a CMA, BPO, market report or newsletter; Matt signs only when nobody is assigned; the published business line is the number on the document. `/communities/tetherow` title says "Tetherow real estate", heading stays "Tetherow homes for sale". A listing that is not ours shows the listing brokerage name, never that broker's phone, email or personal name.
- **Sandbox facts for cloud fires.** `deploy:verify` needs `VERCEL_TOKEN` (absent in the cloud sandbox); the Vercel connector reads deployments instead. `ci:runtime-gates` in the sandbox fails six `heroImageNatural` floors on remote-media egress; GitHub CI is the judge. At most two `next dev` servers on the box: `/tmp/claude-0/devslot.sh <port> <dir>` (sets `RR_TURBOPACK_ROOT` for worktrees). Heavy commands under `flock /tmp/claude-0/heavy.lock`, `CI_GATES_CONCURRENCY=1`. Chromium reaches production through the agent proxy only with `--disable-http2 --disable-quic --ignore-certificate-errors` and `proxy: { server: HTTPS_PROXY }`. A stale `.next/dev/types/validator.ts` fails `tsc` after a route is deleted; it is generated, delete it. The commit-msg hook wants `Node: <id>` or `Node: none (<reason>)` on app/** changes, merge commits included. GitHub re-runs are 403 from the session. The Supabase connector's `execute_sql` is DENIED in `.claude/settings.json` (Matt 2026-09-24, "Please stop asking"): read the database through service-role tsx scripts; `apply_migration` stays allowed.
- Skills read: `docs/RUN_LOOP.md`, CLAUDE.md §0/§1/§2/§3/§6/§7/§8, `design_system/public/TASTE.md`, `design_system/public/PUBLIC_UI.md`, `marketing_brain_skills/brand-voice/VOICE.md`.

**Cloud environment review (Matt 2026-09-24: "connectors always allow, minimum interruption"; branch `claude/ecstatic-gates-k4utik`, draft PR).**

- Found in a fresh cloud session: no `node_modules`, brand fonts, ffmpeg, git hooks or pinned Chromium, so the setup script's output was absent. With `core.hooksPath` unset git ran no hooks (no commit-msg approval gate, no pre-push marker check) and `ci:hooks-installed` skipped instead of failing; it now fails. `GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` was pasted multi-line into the environment variables field and split into 27 stray variables (26 key lines and the END line), so service-account calls cannot authenticate from cloud sessions. Playwright 1.58.2 wants Chromium 1208; the image ships 1194 (`executablePath: '/opt/pw-browsers/chromium'` works).
- On the branch: `.claude/hooks/session-start.sh` (idempotent: `npm ci` on lockfile change, husky hooks, brand fonts, a one-line note when Playwright's pinned browser is missing; measured 65 s bare), the matching marker in `scripts/cloud-setup.sh`, `scripts/check-hooks-installed.mjs` failing a hookless clone, `.husky/pre-commit` running only the unit tests a commit touches (`--changed`; the full suite took 320 s per commit and still runs in CI; `PRECOMMIT_UNIT_FULL=1` for all), `docs/CLOUD_ENVIRONMENT_SETUP.md` §2, §3, §5, §6, and `.auto-memory/feedback_connector_permissions_2026-09-24.md`.
- **Applied on Matt's explicit approval (2026-09-24):** `.claude/settings.json` allows all 14 connector servers (Supabase `execute_sql` and `apply_migration` included, which Matt confirmed again when merging main), asks on 12 send/spend/pause/restore/delete-branch tools as `mcp__*<Server>__<tool>` globs, has an empty `deny`, and registers the SessionStart hook. The auto-mode classifier refuses an agent editing its own permissions unprompted; a future change needs the same explicit go-ahead. Still Matt-side: connector tool permissions, re-paste and rotate the Google key, delete `ANTHROPIC_API_KEY` and the retired CRM's variables, add `VERCEL_TOKEN`, check the setup script field, enable the `engineering`, `design` and `data` plugins, connect Sentry.
- Skills read: `session-start-hook`; CLAUDE.md §0, §1, §6, §7, §8.

**Rule for this file.** ONE `# Current` block. At session end, replace it (surface, SHA, what landed, what is next, skills read) and carry forward every still-open Matt directive; never stack a new one on top. `ci:handoff-current` fails a second `# Current`. Older blocks move verbatim to `docs/archive/`. Boot doc for "run the loop": `docs/RUN_LOOP.md`.

**Still open from the 51 blocks archived 2026-09-23** (each checked against the tree and `loop_work_nodes` on 2026-09-23 08:50Z; every other directive in those blocks is on the tree or its node is done, including SITE-153 and SITE-155, the 2026-09-22 header and dog directives):

- SITE-93 city, SITE-95 compare, SITE-104 neighborhood, SITE-112 subdivision: the work is on main (PR #253, merge `4d0905f5a`); each node is blocked only on a grok-4.6 rejudge to confirm `demoMatch`. Matt 2026-09-23 made `demoMatch` a recorded note, so each closes on the `docs/RUN_LOOP.md` §4 accept test with evidence from the live page. SITE-104's 375 shots predate the phone dock; SITE-112 also owes `/subdivisions/keystone-terrace`, 14px wider than a 375 screen (`.srch-chip-actions`).
- CMA letter `cma-20506-murphy` (Matt 2026-09-22: the home's own photo, evidence columns, alignment): built, not sent, `auto_send` off. Sending it is Matt's per-action yes (CLAUDE.md §1). No node.
- Content-floor re-seed proposed with SITE-119 (done): cities + community `sectionDepth.atlas.items` 8 to 4 once aria-hidden swatches stopped counting. Waiting on Matt's approval; no parity file carries `sectionDepth` yet. No node.

**Matt p0 nodes blocked on a person (from the work graph, not the archived blocks).** SITE-146 dog floater crop rematch #3: land `wt/site-146-20260920` @ `82dcaa345`; Matt's phone is the done gate. SITE-147 homepage "Central Oregon right now" pulse: land only `app/_v3/home-pulse.ts`, its test and the four pulse shots from `wt/site-147-20260920` @ `7c472ac5f`, never the full cherry-pick. SITE-63: Matt picks the contact fold (`design_system/public/references/contact-decision-sheet.html`).

**Standing holds.** No owner email from a land. CMA `auto_send` stays off. Do not reopen SITE-151/152 (Matt 2026-09-21). Matt dropped the typeahead city-to-place-page change (2026-09-19, "never mind"); a city pick still opens search results. Place-craft Tip Ready keeps the competitor first-look and map-drives-hierarchy locks (`ci:place-craft`, SITE-128).

**Graph at 2026-09-23 08:50Z (public-ux SITE nodes not done or killed): 34.** 24 blocked: 15 on dated measurement windows (SITE-01 to 12, 02b, 23, 32), SITE-186 and 187 on 28-day GSC windows, and the 7 on a person above. 2 in progress (SITE-184, 185, `cloud-grinder-2026-09-23-08`). 8 open GSC gaps (SITE-181, 182, 183, 188 to 192).

# Prior

Every earlier block is verbatim in `docs/archive/CROSS_AGENT_HANDOFF-2026-09-23-stacked.md` (2026-09-22/23, seven blocks),
`docs/archive/CROSS_AGENT_HANDOFF-2026-09-15-through-2026-09-22.md`
(2026-09-15 to 2026-09-22) and `docs/archive/CROSS_AGENT_HANDOFF-through-2026-09-12.md` (2026-06 to
2026-09-12). Read them only for a named SHA.
