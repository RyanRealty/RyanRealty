# Current — 2026-09-15 (SITE-103 — official Compare + MOS DigitSwapPreview)

Surface: Cursor cloud `cursor-cloud-site103-20260915` on `cursor/site-103-market-report-region-a8f3` (PR #244). Do not merge. Do not mark SITE-103 done. Do not invent `demoMatch`.

- **Done:** Mini `--ship` 1 on `aad7d006b` (57, `demoMatch: false`). Tip to rejudge: `53121822b` — official Compare (prose/pill outside card, no cream `.v3` wrap, series columns, Trend snapshot, Snapshot, ChartTooltip, stage marks off) + MOS DigitSwapPreview (barrow hover changes value+key, duration 1.15, rest stays live). SITE-92 / 110 left alone.
- **Blocked:** `--ship` stays 1 until Cos Mini writes the envelope. This VM cannot Mini-judge.
- **Next:** Cos Mini rejudge this tip + `--ship` only. If Mini `--ship` ≠ 0, install the **named** `replaceWith`.
- **Node:** `4de56cc6-c112-4db6-b785-a76bb0488b5a` in_progress. Skills read: `.cursor/skills/site-queue/SKILL.md`, `.claude/skills/site-queue/SKILL.md`.
