# Current — 2026-09-15 (SITE-103 — Insights N + DigitSwap mask, awaiting Mini)

Surface: Cursor cloud `cursor-cloud-site103-20260915` on `cursor/site-103-market-report-region-a8f3` (PR #244). Do not merge. Do not mark SITE-103 done. Do not invent `demoMatch`.

- **Done:** Mini `--ship` 1 on `c52ce1fc` (55, `demoMatch: false`, replaceWith insight / number). Installed the named interactions: InsightCards owns Insights N ‹ › (not InsightPager YEAR); scrub writes the same sourced face into claim + DigitSwap + label ($664K Aug rest → $650K Apr hover); Animate masks that face ($664K ↔ $•••K). Compare `keysToggle: false`. SITE-96 / SITE-106 / CMA left alone. Tip `0c711aad2` + recaptured shots.
- **Blocked:** `--ship` stays 1 until Cos Mini writes the envelope.
- **Next:** Cos Mini rejudge + `--ship` only. If Mini `--ship` ≠ 0, install the **named** `replaceWith`.
- **Node:** `4de56cc6-c112-4db6-b785-a76bb0488b5a` in_progress. Skills read: `.cursor/skills/site-queue/SKILL.md`, `.claude/skills/site-queue/SKILL.md`, `TASTE.md`.
