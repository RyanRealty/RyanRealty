# Current — 2026-09-15 (SITE-103 — Insights Compare/Anomaly/Allocation + DigitSwap slots)

Surface: Cursor cloud `cursor-cloud-site103-20260915` on `cursor/site-103-market-report-region-a8f3` (PR #244). Do not merge. Do not mark SITE-103 done. Do not invent `demoMatch`.

- **Done:** Mini `--ship` 1 on `4053963df` (60, `demoMatch: false`). This tip installs the named replaceWith: beautifului-insight paged Insights (Compare / Anomaly / Allocation) and beui-number DigitSwap 1ch glyph slots. year-open clicks Anomaly (not month scrub). `$689K`/`$664K` are slotted glyphs, not display posters. SITE-96 / 106 / 107 / 109 / 110 / CMA left alone.
- **Blocked:** `--ship` stays 1 until Cos Mini writes the envelope.
- **Next:** Cos Mini rejudge the tip after this push + `--ship` only. If Mini `--ship` ≠ 0, install the **named** `replaceWith`.
- **Node:** `4de56cc6-c112-4db6-b785-a76bb0488b5a` in_progress. Skills read: `.cursor/skills/site-queue/SKILL.md`, `.claude/skills/site-queue/SKILL.md`, `TASTE.md`.
