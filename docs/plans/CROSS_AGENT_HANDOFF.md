# Current — 2026-09-15 (SITE-103 — official Insights + DigitSwap slots)

Surface: Cursor cloud `cursor-cloud-site103-20260915` on `cursor/site-103-market-report-region-a8f3` (PR #244). Do not merge. Do not mark SITE-103 done. Do not invent `demoMatch`.

- **Done:** Mini `--ship` 1 on `a911dd9b6` (58, `demoMatch: false`). Tip to rejudge: `111ce7fca` — official `Insights N ‹ ›` only (no kinds tabs); Anomaly is Closings/Sale pills not year chips; DigitSwap uses official 1ch glyph slots (no house underlines); Compare + Anomaly keep hover/scrub. SITE-96 / 106 / 107 / 109 / 110 / CMA left alone.
- **Blocked:** `--ship` stays 1 until Cos Mini writes the envelope.
- **Next:** Cos Mini rejudge this tip + `--ship` only. If Mini `--ship` ≠ 0, install the **named** `replaceWith`.
- **Node:** `4de56cc6-c112-4db6-b785-a76bb0488b5a` in_progress. Skills read: `.cursor/skills/site-queue/SKILL.md`, `.claude/skills/site-queue/SKILL.md`, `TASTE.md`.
