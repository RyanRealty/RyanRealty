# Current — 2026-09-24 (Vault: PR #356 merged + deployed; form check against the printed form, every message reviewed, records audit in the next PR; on top of main's visibility pass PR #352 + cloud grinder rounds)

**Vault (branch `claude/transaction-management-optimization-6jdt16`).**

Surface: Claude cloud session. PR #354 merged `9ae0438`; PR #356 (form registry, mail rule fixes, connector `execute_sql` denied) merged `ea11690`, deployed READY on ryan-realty.com (`dpl_DWHJegPA…`). Matt: "You always have access. Don't stop to ask for SQL access" and "Please stop asking": `.claude/settings.json` DENIES `mcp__Supabase__execute_sql`; DB work goes through service-role tsx scripts (`.auto-memory/feedback_sql_access_2026-09-24.md`). Never re-allow it or send a subagent to it.

Matt's directives this session (2026-09-24), all open until shipped:
- "We cannot get the documents incorrect ... identify the document and confirm that everything is filled out correctly and fully executed." / "Some of the older forms ... might be different from the newer forms. We just can't be confused by that." / "The PDFs that are coming through in the emails are not actually forms ... if you're trying to use the real forms, as I would expect, they're not working."
- "All emails in the vault ... reviewed to determine whether they're part of a deal ... mapped to that deal and to that broker." / "fully compliant with the principal broker requirements ... audit my business ... see everything."

What landed on the branch (next PR, after `ea11690`):
- **Checked against the printed form** (`lib/tc/form-match`, `docs/TC_DOCUMENT_READER.md` "Checked against the printed form"): emailed PDFs are flattened prints (garbage text layer, no fields). Library templates from the 301 licensed blanks; learned templates for older releases from our own copies (2+ deals). Match at ≥ 0.98 (same release 0.98-1.00; other releases 0.92-0.97), a page whose footer prints another release never matches it. Per line: signed / dated / printed; initials; missing pages. Migration `20260924040000_tc_form_templates` APPLIED. `tc_document_checks` checker `form-check-v3-2026-09-24`.
- **Two reads must agree** (`lib/tc/doc-read/cross-check.ts`): a reader "fully executed" contradicted by the printed form (missing page, named party with no mark) goes to a person; unsigned buyer/seller lines the printed page does not have are dropped (OREF 020 p7). verdict.ts: SPDS buyer acknowledgment of an exclusion claim is conditional.
- **Every message reviewed** (`tc_mail_reviews`, migration `20260924030000` APPLIED, `docs/TC_MAIL_FILING_RULES.md` "Every message reviewed"): full-history walker per mailbox with a model stage (files ≥ 0.9, never dismisses). Paul's 8,661 done; Matt's (61,340) and Rebecca's (2,452) were walking at 05:30Z (`review-all --mailbox x --model-stage`, resumes from `tc_mail_review_cursors`). `review-retry` re-indexes errors (NUL-byte fix landed after 25 errors).
- **Records audit** `/admin/closings/audit` (`lib/tc/audit/records.ts`, `docs/TC_RECORDS_AUDIT.md`): per deal, each record Oregon requires with the rule. Principal broker review (OAR 863-015-0140(4)) is recorded in `tc_principal_reviews` via Sign-off; none recorded yet.
- New crons: `/api/cron/tc-form-templates` daily 12:50Z; `tc-document-read` checks new documents before reading.

**Next:** rebuild finished (library → check → learn → check `--all`, log `scratchpad/rebuild.log`) → `tc-doc-read-backfill.ts plan` → verify each superseded archive against the form check → `apply` → review-all finished for Matt/Rebecca + `review-retry` → archive the alias test deal's re-filed test documents (`[TC TEST …]`, deal 99001 Alias Test Loop) → PR CI green → merge → deploy verify.
- Nothing open for Matt on forms. Optional: Google Vault (Workspace eDiscovery) export of mail brokers deleted needs the `ediscovery` OAuth scope (per-action class, §1); nothing uses Google Vault today.
- Skills read: `.claude/skills/tc-builder/SKILL.md`, `.claude/skills/skyslope-form-compliance/` (SKILL + references), CLAUDE.md §0/§1/§3/§4/§7/§8.

**Visibility pass + grinder (PR #352, merged to main as `d8741c2`).**


Surface: Claude Code cloud session on `claude/admiring-feynman-7lgwc3` (draft PR #352), plus the cloud grinder's rounds on `main` (`fc3649b`, `582b8a9`, `738be2a`, `8b890f2`, `95c77df`), which PR #352 now carries through merges of `main` up to `d194fb88a`. Seven stacked Current blocks from 2026-09-22/23 are verbatim in `docs/archive/CROSS_AGENT_HANDOFF-2026-09-23-stacked.md`; every open item from them is below.

- **Visibility pass (PR #352).** Fifteen fix packages from the 2026-09-22 audit are merged on the branch; spec, evidence and resume procedure are in `docs/plans/VISIBILITY_2026-09-22/README.md`. Where the grinder's one-winner rounds overlapped them (Bend luxury, Sunriver, Black Butte Ranch, Broken Top, Tetherow), main's helpers won (`bendLuxuryHomesDoor()`, `lib/communities/self-city-community.ts`, `getPlatParentCommunity`) and the packages' additions sit on top (browse-pair emission, type-twin and area canonicals, phase-family city).
- **Database (applied 2026-09-23 through MCP migrations).** `place_membership_incremental_refresh` (pg_cron `refresh_place_membership_15min`), `mv_refresh_pg_cron_owns_neighborhood_year_pricing`, and `visitor_automation_flag_and_gsc_full_store` (visitor `is_automated`, GSC page/query store, `target_query_benchmark` on the full store, the `unmeasurable` ledger verdict). **Held on purpose:** `20260923014700_pulse_withhold_unverified_closed_side.sql` nulls neighborhood closed-side pulse figures, and the readers on production turn NULL into 0 (`getMarketPulse` `?? 0`, the SMS agent's market tool would say "0 closed"). Apply it only after the null-preserving reader fix (merged on PR #352 as `9051cb7e8`) is deployed to production. After `mv_refresh_state('neighborhood_year_pricing_mv')` shows stamps at :05/:20/:35/:50, delete `app/api/cron/refresh-mvs` and its `vercel.json` entry (p09).
- **In build (Matt 2026-09-23, /cities/bend district map).** Selected district labelled on the map and shaded; the map zooms to it and shows only its homes, with a way back to all of Bend; street names at street zoom; tax-lot lines only if parcel geometry already exists in the project; a tennis-ball cursor over the map (off-palette on Matt's word, cursor only); Jax's eyes follow the pointer (still under reduced motion). Branch `feat/district-map-play`. Node: none.
- **Blocked on 28-day GSC windows to 2026-10-21:** SITE-181/188 Brasada, SITE-182/189 Tetherow, SITE-183 Broken Top, SITE-184/191 Black Butte Ranch, SITE-185 Bend luxury, SITE-186/187 Sunriver, SITE-190 Bend and SITE-192 Redmond (round 3, `95c77df`: the plain city search's map-split branch now ships WebPage / BreadcrumbList / Place / ItemList / Dataset / FAQPage and the SEO tail; the depth read runs beside the viewport fetch with a 1.5 s grace). Re-pull with query×page through the GSC service account when the windows open.
- **Queue at 2026-09-23 11:50Z (round 3):** every SITE node done or blocked. Empty of eligible is not a stop: the next fire runs the measurer (now `/api/cron/loop-weekly-measure`, p11) and seeds from it.
- **Not done, no node.** `WebPage.primaryImageOfPage` is omitted on the split branch (the banner read is grid-only). Plat pages build `/communities/${resortSlug}` from the durable slug (a Pronghorn plat links `/communities/pronghorn`, a 308) at five call sites. `/housing-market/black-butte-ranch` and `/housing-market/sisters/black-butte-ranch` are two self-canonical market URLs for one place (needs a routing decision in `app/housing-market/[...slug]` + `ci:market-city-slug-canon`). Crooked River Ranch joined the self-city set by derivation; excluding it needs a registry field. The chrome Homes panel still lists Sunriver as a search city.
- **Matt's calls.** A 301 of the plain `/homes-for-sale/sunriver` (it would bounce the search app's city switcher) if the canonical alone does not consolidate. From the visibility pass: approve p06's rewrite of buyer sequence step 0; re-run the one real 2026-09-14 seller enrollment (not the internal alias or the vendor pitch); keep or retire the GA4 Measurement Protocol mirror; Turnstile on /contact if the honeypot does not stop the bots.
- **Rules landed 2026-09-22 (standing).** The contact's assigned broker signs a CMA, BPO, market report or newsletter; Matt signs only when nobody is assigned; the published business line is the number on the document. `/communities/tetherow` title says "Tetherow real estate", heading stays "Tetherow homes for sale". A listing that is not ours shows the listing brokerage name, never that broker's phone, email or personal name.
- **Sandbox facts for cloud fires.** `deploy:verify` needs `VERCEL_TOKEN` (absent in the cloud sandbox). `ci:runtime-gates` in the sandbox fails six `heroImageNatural` floors (cities, oregon-city, place-type-community, price-drops, reviews, search) on remote-media egress every run; GitHub CI is the judge. Build one at a time (`--max-old-space-size=11264`). Chromium reaches production through the agent proxy only with `--disable-http2 --disable-quic --ignore-certificate-errors` and `proxy: { server: HTTPS_PROXY }`. Run `tsc` solo (`NODE_OPTIONS=--max-old-space-size=6144`); `next dev` dies under memory pressure next to `ci:gates`, verify on `next build && next start`. A stale `.next/dev/types/validator.ts` can fail `tsc` after a route is deleted; it is generated, delete it. The Supabase connector asks Matt to approve every `apply_migration` unless he sets it to "Always allow" at claude.ai/customize/connectors; batch migrations into one call.
- Skills read: `.claude/skills/site-queue/SKILL.md`, `docs/RUN_LOOP.md`, CLAUDE.md §0/§1/§2/§3/§6/§7, PAGE_OUTLINE "One winner".

**Rule for this file.** ONE `# Current` block. At session end, replace it (surface, SHA, what landed, what is next, skills read) and carry forward every still-open Matt directive; never stack a new one on top. `ci:handoff-current` fails a second `# Current`. Older blocks move verbatim to `docs/archive/`. Boot doc for "run the loop": `docs/RUN_LOOP.md`.

**Still open from the 51 blocks archived 2026-09-23** (each checked against the tree and `loop_work_nodes` on 2026-09-23 08:50Z; every other directive in those blocks is on the tree or its node is done, including SITE-153 and SITE-155, the 2026-09-22 header and dog directives):

- SITE-93 city, SITE-95 compare, SITE-104 neighborhood, SITE-112 subdivision: the work is on main (PR #253, merge `4d0905f5a`); each node is blocked only on a grok-4.6 rejudge to confirm `demoMatch`. Matt 2026-09-23 made `demoMatch` a recorded note, so each closes on the `docs/RUN_LOOP.md` §4 accept test with evidence from the live page. SITE-104's 375 shots predate the phone dock; SITE-112 also owes `/subdivisions/keystone-terrace`, 14px wider than a 375 screen (`.srch-chip-actions`).
- CMA letter `cma-20506-murphy` (Matt 2026-09-22: the home's own photo, evidence columns, alignment): built, not sent, `auto_send` off. Sending it is Matt's per-action yes (CLAUDE.md §1). No node.
- Content-floor re-seed proposed with SITE-119 (done): cities + community `sectionDepth.atlas.items` 8 to 4 once aria-hidden swatches stopped counting. Waiting on Matt's approval; no parity file carries `sectionDepth` yet. No node.

**Matt p0 nodes blocked on a person (from the work graph, not the archived blocks).** SITE-146 dog floater crop rematch #3: land `wt/site-146-20260920` @ `82dcaa345`; Matt's phone is the done gate. SITE-147 homepage "Central Oregon right now" pulse: land only `app/_v3/home-pulse.ts`, its test and the four pulse shots from `wt/site-147-20260920` @ `7c472ac5f`, never the full cherry-pick. SITE-63: Matt picks the contact fold (`design_system/public/references/contact-decision-sheet.html`).

**Standing holds.** No owner email from a land. CMA `auto_send` stays off. Do not reopen SITE-151/152 (Matt 2026-09-21). Matt dropped the typeahead city-to-place-page change (2026-09-19, "never mind"); a city pick still opens search results. Place-craft Tip Ready keeps the competitor first-look and map-drives-hierarchy locks (`ci:place-craft`, SITE-128).

**Graph at 2026-09-23 08:50Z (public-ux SITE nodes not done or killed): 34.** 24 blocked: 15 on dated measurement windows (SITE-01 to 12, 02b, 23, 32), SITE-186 and 187 on 28-day GSC windows, and the 7 on a person above. 2 in progress (SITE-184, 185, `cloud-grinder-2026-09-23-08`). 8 open GSC gaps (SITE-181, 182, 183, 188 to 192).

# Prior

Every earlier block is verbatim in `docs/archive/CROSS_AGENT_HANDOFF-2026-09-23-stacked.md` (2026-09-22/23, seven blocks),
`docs/archive/CROSS_AGENT_HANDOFF-2026-09-15-through-2026-09-22.md`
(2026-09-15 to 2026-09-22) and `docs/archive/CROSS_AGENT_HANDOFF-through-2026-09-12.md` (2026-06 to
2026-09-12). Read them only for a named SHA.
