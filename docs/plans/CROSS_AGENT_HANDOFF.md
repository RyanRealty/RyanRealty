# Current — 2026-09-15 (SITE-103 market-report-region — year-open framed, awaiting Mini)

Surface: Cursor cloud `cursor-cloud-site103-20260915` on `cursor/site-103-market-report-region-a8f3` (PR #244). Do not merge. Do not mark SITE-103 done. Do not invent `demoMatch`.

- **Done:** Mini `--ship` 1 on `517dff97b` (56, `demoMatch: false`). Catalog `year-open` is now `#market-insights .v3-chart__hover@#market-insights!hover`; take-route-shots hover no longer steals the ANCHOR frame. Recaptured shots: DigitSwap + Animate + Insights + scrubber/crosshair in year-open; 375 fold shows scrubber; stale `year-page-*` overwritten so YEAR 2024 / `$749,900` two-up is not in the kit. Source dates both Sep 15. SITE-96 / SITE-106 / CMA left alone.
- **Blocked:** `--ship` stays 1 until Cos Mini writes the envelope. JSON-LD is in HTML (Dataset), not a screenshot widget.
- **Next:** Cos Mini rejudge + `--ship` only. If Mini `--ship` ≠ 0, install the **named** `replaceWith`.
- **Node:** `4de56cc6-c112-4db6-b785-a76bb0488b5a` in_progress. Skills read: `.cursor/skills/site-queue/SKILL.md`, `.claude/skills/site-queue/SKILL.md`, `TASTE.md`.
