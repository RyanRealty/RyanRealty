# Current — 2026-09-15 (SITE-103 — rest-revealed DigitSwap + card-framed year-open)

Surface: Cursor cloud `cursor-cloud-site103-20260915` on `cursor/site-103-market-report-region-a8f3` (PR #244). Do not merge. Do not mark SITE-103 done. Do not invent `demoMatch`.

- **Done:** Mini `--ship` 1 on `996ce99c1` (52, `demoMatch: false`, replaceWith beui-number + beautifului-insight). Install is `0f1099670`: rest shows live DigitSwap faces (`$664K` / `1,502` / `325`); Animate still toggles the mask. `#market-insights` is the insight card (claim + DigitSwap + scrub + pill), not the Insights N pager. SITE-96 / 106 / 107 / 109 / 110 / CMA left alone.
- **Blocked:** `--ship` stays 1 until Cos Mini writes the envelope.
- **Next:** Cos Mini rejudge `0f1099670` + `--ship` only. If Mini `--ship` ≠ 0, install the **named** `replaceWith`.
- **Node:** `4de56cc6-c112-4db6-b785-a76bb0488b5a` in_progress. Skills read: `.cursor/skills/site-queue/SKILL.md`, `.claude/skills/site-queue/SKILL.md`, `TASTE.md`.
