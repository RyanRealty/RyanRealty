# Current — 2026-09-15 (SITE-96 tip 48b73a5 + shots fda7c1b, Mini CLI missing)

Surface: Cursor Cloud on PR #237 `cursor/contact-site96-b2fa`. Claim `cursor-cloud-site96-20260914`. No merge to main.

- **Done:** Seven Mini defects on tip `48b73a5` / shots `fda7c1b`. Beui+shadcn imports on ContactField; one Call reach + Button Group; one Matt face; hours-empty keeps the chip; 375 write in fold; no public response-clock; cookie pad on 375. Content floor holds (words 205, hero 320). `demoMatch` not invented.
- **Next:** `npx tsx scripts/taste-evaluate.ts contact --builder cursor-agent` on a machine with cursor-cli/grok, then `--ship` must exit 0. This VM: cursor-agent, grok, and claude CLIs all missing.
- **Node:** SITE-96 in_progress. `--ship` exit 1.
- Skills read: `.cursor/skills/site-queue/SKILL.md`, `.claude/skills/site-queue/SKILL.md`.
