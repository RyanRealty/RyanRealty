# Current — 2026-09-15 (SITE-103 market-report-region tip — Mini 56, catalog reinstall)

Surface: Cursor cloud `cursor-cloud-site103-20260915` on `cursor/site-103-market-report-region-a8f3` (PR #244). Do not merge. Do not mark SITE-103 done.

- **Done:** Mini `--ship` 1, score 56, `demoMatch: false`, `competitiveBriefPass: false`. Cream-box failed (`YEAR 2026 3/3` ≠ insight; disclosure ≠ digit-swap; AUG tip covers marks). Installed real `beautifului-insight` (`InsightCards` + distinct pages + scrubber) and `beui-number` (`DigitSwap` on instrument + insight figures). Cities MOS rows gone. Chart hover reading is under the plot, not a cream box on marks. SITE-96 / SITE-106 left alone.
- **Blocked:** This VM cannot judge (`cursor-agent` not logged in; grok/claude CLIs missing). Did not invent `demoMatch`. `--ship` exit 1 until Cos Mini writes the envelope.
- **Next:** Recapture shots. Cos Mini `--ship` only. If Mini `--ship` ≠ 0, install the **named** `replaceWith` — no house patch.
- **Node:** `4de56cc6-c112-4db6-b785-a76bb0488b5a` in_progress. Skills read: `.cursor/skills/site-queue/SKILL.md`, `.claude/skills/site-queue/SKILL.md`, `TASTE.md`.
