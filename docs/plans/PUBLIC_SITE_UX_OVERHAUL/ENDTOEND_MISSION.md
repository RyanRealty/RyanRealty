# /endtoend mission — Public Site UX Overhaul

**Started:** 2026-08-11  
**Goal (done means):** A real buyer, seller, or trust-seeker can use ryan-realty.com end to end on mobile and desktop without Frankenstein chrome, with every public page and section dispositioned, one design system under navy/cream + Amboqia/Geist, money-path journeys converting, and competitive lead on local brokerages with no portal-class embarrassment on find/sell.

## Measurable bar

1. `PAGE_LEDGER` — 0 `unaudited` (every route scored + dispositioned + competitor note).  
2. `SECTION_LEDGER_CANONICAL` — 0 `unaudited` (every pattern dispositioned).  
3. Journey A/B/C audit docs exist with defects linked to ledger IDs.  
4. Conversion map covers every P0–P2 marketing template.  
5. IA recommendation package written; working IA lock applied for execution (Matt may veto).  
6. Visual + section library package written; working visual lock applied for execution (Matt may veto).  
7. Site v2 primitives exist under `components/site/v2/` (or documented path) with brand tokens only.  
8. At least **chrome + homepage spine** shippable or clearly blocked only on deploy/auth — not on missing design.  
9. Progress logged; residual queue honest.

## Hard limits

- Brand colors/fonts only sacred.  
- §0 data accuracy.  
- No invented metrics.  
- LPs: conversion plan before rewrite.  
- Admin out of scope.

## Stop conditions

- Matt veto of working locks.  
- Missing credentials for prod deploy verify.  
- Context exhaustion → handoff with queue.

## Execution posture

Matt ordered full remainder + `/endtoend`. Locks that the plan marked “await Matt” are produced as **RECOMMENDED working locks** with full rationale so build can continue; they remain reversible by writing a veto in `decisions.md`.
