# Open defects after wave 1 and wave 2 (verifier output, 2026-08-11)

Each entry is a fresh-context verifier finding that survived the wave repair round. Close every one. Never close a defect by loosening a gate or deleting an assertion.

## app/months-of-supply/page.tsx (2)

1. Undeclared user-facing deletions. /Users/matthewryan/RyanRealty/app/months-of-supply/page.tsx lines 58-82 assert a complete ledger ("DELETED BY THIS MIGRATION, each stated with where the information went"), but two removed copy blocks are absent from it: the H2 "Right now" and its lede "Pulled live from the same feed every market report on this site reads from. Refreshed every 10 to 15 minutes from Oregon Data Share." (both deleted in the diff), and the H2 "The formula" (now the Quiet heading "What months of supply measures" with a "Formula" term row). No data-accuracy impact: the 10-15 minute cadence still ships in FAQ 4 and each section carries its own "updated" stamp. The defect is that the self-authored deletion ledger is incomplete, so a reviewer checking it line by line would conclude nothing else was cut.

2. Untraced live figure in the city footnote. app/months-of-supply/page.tsx:274-280 builds `${label} shows ${snapshot.active_count.toLocaleString('en-US')} active with no published months of supply`, pushed into closingItems at 328-334 and rendered by the closing <V3Quiet id="faq"> at 478-484, which accepts no `source` and no `updated`. Because MOS_CITY_LABELS is ['Bend'], that string can only render when the city Ledger took its empty branch (438-448), whose type forbids `source`/`updated` — so every time this active count prints it is the page's only city figure and it carries neither the city query's trace nor the city query's freshness stamp. That contradicts the file's own invariant 3 ('ONE TRACE PER QUERY, ONE STAMP PER TRACE') and the v3 barrel's stated rule that a live number belongs beside its source line. Fix: keep the fact ('no published months of supply') in the Quiet block and move the count into a section that carries the city trace, or drop the count from the footnote.


## SHARED (36)

1. Latent href hole in the new route-local constants. /Users/matthewryan/RyanRealty/app/months-of-supply/_v3/mos-constants.ts:20 types the map as `MOS_CITY_REPORT: Record<string, string>` rather than keying it to `MOS_CITY_LABELS` (line 17, `as const`). page.tsx:237 does `href: MOS_CITY_REPORT[label]` straight into V3Ledger, which renders `<Link href={row.href}>` (components/site/v3/V3Ledger.tsx:288). Adding a city to MOS_CITY_LABELS without a matching report path type-checks and ships `href: undefined` into next/link at render. No current impact (only 'Bend', which is mapped). Fix: `Record<(typeof MOS_CITY_LABELS)[number], string>`.

2. OUT OF SCOPE, not caused by this route, reported so the wave owner sees it: the working tree fails `ci:reachable-exports` with 12 new orphan modules (components/site/explore/CommunityGolfLinks.tsx, PlaceInventoryMap.tsx, components/site/kb/KbAreaGuideVideo.tsx, KbBuyCta.client.tsx, KbOutOfAreaReferral.client.tsx, KbPopularSearches.tsx, KbResortOverview.tsx, KbSchools.tsx, components/site/sell/SellMobileSticky.client.tsx, components/site/sell/spine/index.ts, lib/kb/city-page-config.ts, lib/kb/neighborhood-photos.ts) and `ci:file-size-budget` breaches on app/lp/seller-home-value/SellerLPForm.tsx (626 vs baseline 618). None trace to months-of-supply: FAQBlock (14), PageBreadcrumb (7) and SiteFooter (32) all retain other consumers, so this migration's "nothing was orphaned" claim holds.

3. INVARIANT 5's JUSTIFICATION IS FALSE AT MOBILE — the page ships no primary ask at 390px. page.tsx:35-37 and the inline comment at :469-470 demote the Instrument's action to `variant: 'ghost'` on the stated ground that "The sticky public header carries a filled valuation CTA at every scroll position." Measured in the browser at 390x844 on the rendered route: all three header anchors whose text matches /value|valuation/ resolve to zero width and height, one with `display: none` and the other two under a `display:none` ancestor. The header at that width renders only the wordmark, SEARCH, and MENU +. So on mobile the page's sole conversion affordance is a secondary outline button, where the KB page carried KbSell's filled "Value my home" button plus an address field at every width. Either the ghost demotion needs to be width-conditional or the stated premise has to be corrected to "at desktop widths".

4. §0 TRACE DOES NOT COVER ONE OF ITS FIGURES — the cities Ledger. page.tsx:498-500 sets `source={v3Text('live MLS through Oregon Data Share, active single-family listings, one row per city')}`, but each row also publishes a pace figure: :230-233 `detail: v3Text(`${snapshot.median_days_to_pending} days to pending`)`, rendering e.g. "18 days to pending" for Bend. Median days-to-pending is not an attribute of active listings — it is derived from listings that went pending — so the population phrase in the source line does not cover it. This is a NEW figure on this page: the KbExploreTowns tiles it replaces carried only `activeCount` and `medianPrice` (KbTownItem), so the trace was inherited rather than widened to match what the rows now print. The region Instrument's trace ("single-family homes across the Central Oregon region") is broad enough to cover its five; this one is strictly narrower than its three.

5. INVARIANT 2 IS APPLIED TO ONE FIGURE OUT OF FIVE. page.tsx:25-29 states "ONE GUARD PER FIGURE, SHARED WITH ITS CONSUMER," and :136-139 implements it for months of supply (`> 0`, matching buildMarketFaq's own condition). The other two pulse figures do not share their consumer's guard: :168 pushes the median on `regionPulse?.medianListPrice != null` and :175 pushes the count on `regionPulse != null`, while lib/site/market-faq.ts:110 and :117 both require `> 0`. At a stored 0 the Instrument prints "$0 / median list price" and "0 / homes for sale", each linked to the listings browse path, with no FAQ answer and no Dataset variable behind either — the same shape as the defect the page says it closed for the supply figure, and a synthesized-looking zero under a live-MLS source line (invariant 4). Narrow window, but the guard is one comparison and the file already declares the rule.

6. The declared Section 0 fix does not hold at the route level, and the header states it as fact. page.tsx:43 asserts 'The health bucket is not published here.' The page still passes `regionSnapshot={regionSnapshot}` to `<PulseFeed>` (page.tsx:380), and components/pulse/MarketSnapshotCard.tsx:47,65 renders `market_health_label` for that same region row. Live on localhost:3000/pulse right now the page ships BOTH the new H1 'The Central Oregon market pulse: a balanced market' and, inside the feed, 'Central Oregon / Warm / Months of supply 5.8 mo' with no source line. The migration made these two diverge: before it, the KB strip's 'Market' cell and the interstitial both read 'Warm', so the page carried one label; now it carries two verdict-shaped labels from two different metrics, and the un-sourced one is the composite heat bucket the header set out to stop publishing. Either scope the claim to the Instrument, or suppress `market_health_label` in MarketSnapshotCard for this route.

7. Barrel contract misuse — the Ledger's `when` slot carries an inventory count. page.tsx:247 sets `when: v3Text(`${snapshot.active_count.toLocaleString('en-US')} for sale`)`, while components/site/v3/V3Ledger.tsx:78-82 documents that slot as the row's date ('for example "Aug 3"… Dates arrive already formatted through lib/format/date') and the primitive's data rules name `when` alongside `updated` as a date field. It renders acceptably today because the slot is a plain `<span class="v3-ledger__when">`, so this is not a visible break — but the moment that column becomes a `<time>` element or picks up a date-format assertion, ten inventory counts get marked up as dates. Either the row uses `detail`/`value` for the count or V3Ledger's `when` contract is restated to allow a non-date lead-in.

8. Minor — the two event-count figures are doors that do not open onto the population they name. page.tsx:210 and :215 point 'new listings, last 7 days' (104) and 'sold, last 30 days' (365) at bare `#pulse-feed`, whose default state is 'All events' — clicking either lands the reader in an unfiltered mix of price drops, pendings, and solds. PulseFeed already parses `types` from the URL (components/pulse/PulseFeed.tsx:98, `parseCsv(searchParams.get('types')).filter(isPulseEventType)`), so `?types=new_listing#pulse-feed` and `?types=status_closed#pulse-feed` were available at no cost. Note the sold figure additionally has no matching feed window (the feed is not scoped to 30 days).

9. UNDECLARED DELETION of a user-facing capture surface. KbSell — the address-capture form (`<input placeholder="Enter your home address">` handing off to the valuation flow) plus its three live proof figures (medianListPrice, medianDaysToPending, soldCount30d; the old call site passed `regionPulse?.closedLast30Days`, which the new page now reads and never renders) — is removed, and so is SmoothScrollProvider. Nothing on disk declares either: the new docblock's 'THE PAGE CONTRACT, carried across unchanged' list (page.tsx:17-27) names generateMetadata, canonical, revalidate, the JSON-LD payloads, KbSectionTracker and the capture contract, and the coverage comment (page.tsx:243-246) declares MarketSources — but neither KbSell nor its address prefill appears anywhere, and this route has no design_system/ryan-realty/ui_kits/<surface>/parity.json to hold a deletions block. The standard the sibling set is explicit: design_system/ryan-realty/ui_kits/market-report-region/parity.json enumerates 'KbSell - its three figures ... are all figures in the Instrument above ... KbSell's ADDRESS PREFILL FIELD is deleted outright, the one capture affordance this migration does not carry across'. Here the same deletion is silent.

10. CHECK 4 (section-0 traces) — CONFIRMED LIVE. The closed-sales window printed on screen is one day earlier than the same page's own machine-readable window. /Users/matthewryan/RyanRealty/app/housing-market/annual-review/_v3/annual-sections.ts:229-233 builds the trace with bare formatDate(detail.periodStart) / formatDate(detail.periodEnd); lib/format/date.ts defaults to timeZone America/Los_Angeles, and a date-only column ('2025-08-12') parses as UTC midnight and formats back to Aug 11 in Pacific. Live fetch of http://localhost:3000/housing-market/annual-review renders the #closed-sales source line as 'the trailing 12 months (Aug 11, 2025 to Aug 11, 2026)' while the Dataset JSON-LD emitted from page.tsx:405 publishes "temporalCoverage":"2025-08-12/2026-08-12" off the identical two fields. Two windows for one set of figures on one citable page, and the printed one is not the window the cache job computed. The repo's own convention for these date-only period columns is an explicit UTC zone (lib/kb/place-sections.ts:146 monthLabel, lib/data/crm/getMarketReportData.ts:198). Fix: formatDate(detail.periodStart, { timeZone: 'UTC' }) for both ends. Note: the retired KB page carried the same shift in its Caption, so this is carried across rather than introduced — but it is live on the route now and the parity contract claims the trace 'states the period the figures were computed over'.

11. CHECK 1/4 (JSON-LD vs on-screen number) — the Dataset payload publishes a median list price that is not the number printed for it. The rendered page shows '$730,000 median list price' in the #market Instrument (built by _v3/annual-sections.ts:90-96 through formatPrice, which rounds to the nearest $1,000) and repeats '$730,000' in the visible FAQ answer, while the Dataset variableMeasured entry emitted from the same buildMarketFaq call publishes {"name":"Median List Price","value":729900} (lib/site/market-faq.ts:115). The route's own rule, written into _v3/annual-constants.ts:87-100 and repeated in parity.json, is 'One number per fact, on the page and in the payload' — it is why the trailing-12-month median deliberately bypasses formatPrice for formatWholeDollars — and parity.json's jsonLd note asserts 'each variableMeasured value must equal the on-screen number'. The sibling figure was never brought onto that rule. The Months-of-Supply variable in the same builder was already fixed this way (it publishes Number(displayMos), market-faq.ts:154), so the shape is known. Same behaviour on the retired KB page and shared with other market surfaces, so the fix belongs in buildMarketFaq (or in this page's figure formatting), not only here.

12. CHECKS THAT PASSED, with the evidence: (1) METADATA/JSON-LD — generateMetadata is byte-identical to the KB page's (diff of the two blocks shows only a trailing blank line); the live fetch renders canonical /housing-market/annual-review, the KB title/description/keywords, and all five payloads (BreadcrumbList, WebPage, Article with dateModified=2026-08-12T19:03:54Z off the pulse row, Dataset, FAQPage). FAQPage moved emitter only: FAQBlock passed {type:'faqPage', items} to MetadataBlock (FAQBlock.tsx:99) and page.tsx:412 passes the same shape from the same faqs array. (2) METRICS AND READS — all four reads render (regionPulse, regionDetail, citySnapshots, cityDetails); no catch wraps them (KB's four .catch(()=>null) swallows are gone, matching the resilient-cache reasoning); every KB metric survives (activeCount, medianListPrice, MoS, medianSalePrice, soldCount, medianDom, avgSaleToList, medianPricePerSqft, all yoy_* fields, plus medianDaysToPending inherited from KbSell); the one dropped figure, closedLast30Days, is declared in parity.json deletions and needs no read of its own. (3) REVALIDATE AND CAPTURE — revalidate 300 unchanged; AnnualInquirySheet calls submitMarketPageInquiry with variant 'inquiry' and name/email/message, and LeadCaptureBlock.tsx:82-85,143-144,225 confirms the KB inquiry variant collected exactly those three and no phone. (4) VERDICTS — one derivation, guar

13. CAPTURE PAYLOAD CHANGED (criterion 3): the page header claims 'the capture contract (`submitOutOfAreaReferral`, keys citySlug / name / email / notes)' is carried across unchanged, but the KB form sent FIVE keys — `submitOutOfAreaReferral({ citySlug, name, email, notes, company })` (components/site/kb/KbOutOfAreaReferral.client.tsx:31) — and the replacement sends four (app/oregon/[city]/_v3/OutOfAreaReferralSheet.client.tsx:103-108). `company` was the form's ONLY bot deterrent: app/actions/out-of-area-referral.ts has no rate limit, no captcha, and its honeypot branch (`if (input.company?.trim()) return { ok: true }`, line 52) is now unreachable from its only caller. Every scripted POST therefore creates a real CRM person + origin note + a 240-minute broker task + a queued SMS to Matt's cell (ensureNativeLead / createNativeTask / queueBrokerAlert). It is declared (OutOfAreaReferralSheet.client.tsx:14 'ONE FIELD IS GONE'; parity.json deletions), so criterion 5 is not tripped — but the stated justification does not survive the barrel's own contract: components/site/v3/index.ts:3 reads 'THIS FILE IS THE PRESSURE VALVE. A migration that needs a control this set does not have ADDS A PRIMITIVE HERE' and 'The six patterns are closed. The atoms are not', so a hidden-field variant on V3SheetField was the sanctioned move, not a raw <input> in the route. Fix: add the hidden field to the bar

14. PARITY CONTRACT STATES A FALSE FACT ABOUT ITS OWN DELETION (criterion 7, doc arm): design_system/ryan-realty/ui_kits/oregon-city/parity.json:77 justifies deleting KbAbout's facts rail with 'Three of the four were the same snapshot figures the hero printed, twice on one page with no trace under either.' Only TWO of the four were — KbHero renders `data.activeCount` and `kbMoneyFull(data.medianListPrice)` and nothing else (components/site/kb/KbHero.client.tsx:257-273), and the old page passed `medianDaysToPending: null`. 'Single-family active' (city.activeSfrCount) appeared exactly once, in the facts rail, so it was never printed twice and the duplication argument does not cover it. The figure does survive in the Instrument, so this is a wrong rationale in the file the page header names as 'the parity contract, not this comment', not a lost figure. Correct the count to two and state separately why the single-family count moved.

15. MOBILE HAS NO PRIMARY SELLER CTA, AND THE STATED PREMISE FOR THAT IS FALSE. page.tsx:54-58 asserts invariant 5: "The sticky public header carries a filled valuation CTA at every scroll position, so the Instrument's ask is a GHOST and V3Footer carries no button at all." Verified in a real browser at 390x844 on /cities/bend/old-bend: the header's only `a.nav-cta` ("Get your home's value" -> /sell#get-value) computes `display:none`; the second copy lives in the closed overlay menu at `visibility:hidden`, top -107. The visible header is logo + "Search" + "Menu +". The only visible body controls are `v3-btn v3-btn--ghost` "Value my Old Bend home" and the alert sheet's `v3-btn--primary` submit. So on mobile the money route ships zero filled seller CTA, where the KB page carried KbHero's primary CTA, `ctaSecondary={{href:'/sell/valuation',label:'Value my home'}}`, AND a full KbSell block (all three deleted, page.tsx:398-404 + parity.json removedComponents). The ghost downgrade at page.tsx:400-404 is justified by a header behaviour that does not exist at 390. At 1280 the header CTA IS visible, which is why this survives desk review.

16. SECTION 0: TWO CONTRADICTING ACTIVE-INVENTORY COUNTS PUBLISHED IN ADJACENT SECTIONS, WITH THE SMALLER ONE IN THE H1 AND IN THE STRUCTURED DATA. On /cities/bend/southern-crossing the level-1 Instrument prints "2 / homes for sale", "$587,000 / median list price", "1.7 / months of supply" under the trace "live MLS through Oregon Data Share, active single-family listings (PropertyType A) in Southern Crossing, Bend"; the V3Field directly below prints "16 / active single-family listings inside the Southern Crossing boundary" and renders all 16 doors, $499,000 to $2,499,000 (visible median approx $811,000). The FAQPage JSON-LD and the visible Q&A both say "There are 2 active single-family listings in Southern Crossing as of August 2026", the Dataset carries `variableMeasured` Active Listings = 2, and the seller's-market verdict is derived from that 2. Confirmed against the source: market_pulse_live geo_slug='bend-southern-crossing' has active_count=2, months_of_supply=1.71. The same split runs across the family (old-bend 7 vs 8, river-west 24 vs 34, century-west 20 vs 41, southwest-bend 29 vs 41, summit-west 71 vs 85). The diff is what created the adjacency: KB resolved ONE number (`inBoundaryCount ?? pulse?.activeCount ?? …`) for the hero; page.tsx:233-250 now publishes the pulse row as the headline while page.tsx:425-433 publishes the boundary set one section down. Section 0 rule 5 

17. SECTION 0 TRACE COVERAGE: `median days to pending` is published under a trace that does not cover it. page.tsx:256-259 emits `'live MLS through Oregon Data Share, active single-family listings (PropertyType A) in ' + `${placeName}, ${cityName}.`` and appends MOS_METHODOLOGY_CLAUSE + MOS_THRESHOLD_CLAUSE only for months of supply. But `liveFigures` (neighborhood-sections.ts:92-98) also publishes `median days to pending` (rendered as "76 / median days to pending" on /cities/bend/old-bend), which comes from market_pulse_live.median_days_to_pending and is measured on listings that WENT PENDING, not on the active set the trace names. Every other figure got explicit coverage (the count and median are active-listing facts; MoS got its own methodology clause); this one is the only figure on the page whose population the source line contradicts.

18. THE FIELD DISPLAYS INDIVIDUAL MLS LISTING DATA WITH A SOURCE LINE THAT NEVER NAMES THE MLS, WHILE THE PARITY CONTRACT CLAIMS IT DOES. page.tsx:430 sets the count source to "the recorded ${placeName} boundary polygon joined to the live listing index, narrowed to active single-family (PropertyType A)" — rendered verbatim, no Oregon Data Share. That section is the one rendering 8 to 24 individual listings with street address, price, beds, baths and sqft. parity.json justifies deleting `MarketSources` on the grounds of "the per-section source lines, which name Oregon Data Share on every figure block" — false for the block that displays actual listing records. MarketSources' explanatory clause ("the regional MLS cooperative behind the live listing and market data on this page") is also gone; only a bare anchor survives, buried 30+ rows deep in the closing Quiet block.

19. DEAD DEGRADED-STATE BRANCH: the "No live figures right now" fallback can never render. page.tsx:389 branches on `firstLive`, destructured from `liveSet` (page.tsx:233-251). In the pulse branch `liveFigures` unconditionally pushes the activeCount figure (neighborhood-sections.ts:78-84) and `MarketPulse.activeCount` is typed `number` (lib/data/types/market.ts:33). In the no-pulse branch the first element is `neighborhood.activeCount`, typed `number` (app/actions/cities.ts:415, computed at :437). `liveSet` therefore always has length >= 1, `firstLive` is always defined, and the entire `<V3Quiet id="market" headingLevel={1}>` block at page.tsx:406-419 — including its copy "Neither the market row nor the listing index answered for X on this refresh, so this page is not printing an inventory count or a median" — is unreachable. The page has no state in which it withholds the inventory figure, contrary to what that branch asserts.

20. THE THIRD POPULATION SHIPS WITH NO STAMP, CONTRADICTING THE FILE'S OWN INVARIANT. page.tsx:30-36 (invariant 1) and parity.json both state each of the three populations gets "its own section, its own source line, and its own timestamp". The count object at page.tsx:425-433 passes only `value`, `label` and `source`. `V3FieldProps.count` accepts `updatedAt` (V3Field.tsx:104-117) and renders it through V3SourceLine (V3Field.tsx:~370). Verified in the rendered page: both Instruments end "· updated Aug 12, 2026"; the Field's source line ends with no stamp at all. The boundary/tiles population is the only live figure block on the page a reader cannot date.

21. THE ENTIRE OUTBOUND LINK GRAPH IS FILED UNDER A HEADING THAT MISDESCRIBES IT. page.tsx:462-467 renders one `V3Quiet id="explore"` with `eyebrow="Common questions"` and `heading={`${placeName} real estate questions`}`, whose `items` (page.tsx:298-318) are 4 FAQ prose rows followed by ~33 navigation links: the full-inventory door, city overview, city inventory, city market report, months-of-supply, 4 subdivisions, 12 peer neighborhoods, the city index, open houses, price drops, 3 blog posts, /sell, and the ODS citation. Verified on /cities/bend/old-bend: 4 Q&A then 33 link rows inside `<section id="explore" aria-labelledby="explore-heading">`. The KB page labelled these as separate ledgers (KbExploreTowns "Subdivisions", "Explore nearby neighborhoods", "Other cities on the list", KbArticles "Old Bend guides"); the migration collapsed four distinctly-named sections into one section whose accessible name says "questions", so the majority of the section's content sits under a landmark name that does not describe it.

22. CAPTURE CONTRACT CHANGED: the honeypot is gone from this public write path. NeighborhoodAlertsSheet.client.tsx:65-69 sends `company: ''` as a hardcoded constant, so `submitSearchAlertSignup`'s first defence (app/actions/search-alert-capture.ts:41-43, `if (typeof input.company === 'string' && input.company.trim() !== '') return { ok: true }`) can no longer fire from this surface — the KB block bound it to a real hidden `<Input name="company">` with user state (KbCommunityAlerts.client.tsx:44, 123-133). The email/filters payload is otherwise identical (`{ city, propertyType: 'A' }`, no subdivision key) and the three success side effects run in the same order, and the removal is declared in parity.json, but this is a real reduction in a public server-action write path's defences; only the per-IP limit, email regex and narrowing-filter check remain. The post-submit "Sign in to manage alerts" link was also dropped.

23. DISCLOSED TRADE-OFF, still a live regression — /Users/matthewryan/RyanRealty/app/zip/[zip]/_v3/ZipAlertsSheet.client.tsx: the KB band's hidden `company` honeypot is gone and the field is now always sent as the empty string, so the bot trap is inert on this capture surface until the v3 barrel grows a hidden-field kind. Documented in the component header and in design_system/ryan-realty/ui_kits/zip/parity.json; the action's other four defenses (per-IP rate limit, email validation, native dedup, compliance gate) are intact. Minor related narrowing: the sheet caps the email input at maxLength 200 where the KB input allowed 254 (the server action's own limit is 254).

24. OUT OF SCOPE (does not trace to app/zip, but `npm run ci:gates` is currently RED and aborts before the later gates): ci:hydration-safety fails with 1 NEW violation at components/site/sell/spine/SellSectionTracker.client.tsx:12 (readRrSessionId() called in render); ci:file-size-budget fails on app/lp/seller-home-value/SellerLPForm.tsx (626 lines vs baseline 618); ci:reachable-exports fails with 11 new orphan modules including components/site/sell/spine/index.ts and components/site/sell/SellMobileSticky.client.tsx. All belong to the untracked sell-spine work in the tree. Every one of the 30 route-relevant gates run individually for /zip passes, and tsc --noEmit is clean.

25. SECTION 0 — A VERDICT AND A COUNT THAT CONTRADICT EACH OTHER, IN THE H1, LIVE NOW. http://localhost:3000/communities/vandevert-ranch renders: H1 "Vandevert Ranch homes for sale: a buyer's market", then one figure row containing "0 homes for sale" and "72.0 months of supply", then a source line that prints the formula "Months of supply = active listings divided by average monthly closings over the last 6 months". With 0 active listings that formula yields 0, not 72.0, so the page's own printed math refutes the figure beside it — the precise hazard lib/market/classify.ts's docblock exists to prevent. The verdict does not match the number it sits next to (CLAUDE.md section 0: "a 'seller's market' verdict next to 4.3 months of supply is a fail"); zero inventory is definitionally not a buyer's market. The cause is page.tsx:331-338: `buildLiveFigures` merges the live-pair count (`activeCount`, from resolveLivePair's alias branch) and `mosDisplay` (from the pulse row, whose own active count is a DIFFERENT and non-zero number) into ONE figure set under ONE headline verdict, and page.tsx:478 derives the H1 verdict from `mosRaw` with no reference to `activeCount`. The KB page carried the same two numbers but in a mid-page HUD and with an H1 of "Vandevert Ranch, Homes for Sale" — this migration promotes an unsupported market claim into the H1 of a licensed broker's page. Immediately below

26. THE COUNT'S OWN DOOR PUBLISHES A DIFFERENT COUNT, AND A NEW SENTENCE ASSERTS OTHERWISE. page.tsx:328 sets `browseHref = homesForSalePath(cityName, community.subdivision)` = `/homes-for-sale/bend/tetherow`, and page.tsx:331-338 hrefs BOTH live figures plus the Instrument's CTA at it. /communities/tetherow publishes "36 homes for sale" (alias-aware, correct); /homes-for-sale/bend/tetherow publishes "Homes for sale in Tetherow — 33 homes for sale" because that route filters on the literal MLS subdivision name, which is the exact undercount the alias-aware count exists to correct. Verified by curl against both routes. The community page's own markup proves the gap: it links 19504 Century Drive as /homes-for-sale/bend/century-west/roald-west/19504-century-220214751 — subdivision "Roald West", counted in the 36, absent from the 33. NEW in this diff: page.tsx:517-521 `footNote` states "Listed here: the 24 highest-priced of the 36 homes on the map. Every one of them is on the browse page." That sentence is false, and it is a new claim the KB page never made. Same block has a second arithmetic hole: `fieldItems` is sliced from `rows` (every tile) while the count and the footnote's denominator are `pins.length` (tiles carrying coordinates), so when any listed row lacks lat/lng the footnote names rows that are not on the map, and when `pins.length < 24 < rows.length` the condition `fieldI

27. AN UNDECLARED USER-FACING DELETION: THE VISIBLE FRESHNESS SIGNAL. The KB page rendered `<p className="community-freshness-signal">Market data updated {asOfLabel}</p>` (old page.tsx:794-798), added as an explicit section 0 / crawlability fix. The v3 page has no equivalent that fires in practice: page.tsx:482 `updated={live.stamp ? v3Text(formatDate(live.stamp)) : undefined}`, and `resolveLivePair` returns `stamp: null` on the alias branch, the reliable-boundary branch, and the subdivision-name branch (_v3/community-figures.ts:69-92) — i.e. on every resort and every plain subdivision. Verified: `grep -c "Market data updated|Updated "` on the rendered /communities/tetherow HTML returns 0, while `asOfLabel` still exists and is still published into the Dataset JSON-LD name and dateModified. The page therefore asserts a machine-readable dateModified it no longer shows a human. design_system/ryan-realty/ui_kits/community/parity.json lists the phone line, the second-home note, and the hero facts strip among removedComponents, but never this element; the only "freshness" mention is V3Field's note about the map count. Per check (5) this is a user-facing element deleted without being declared.

28. THE CAPTURE CONTRACT DID CHANGE, AND PART OF THE CHANGE IS UNDECLARED. parity.json and _v3/CommunityAlertSheet.client.tsx both assert "the capture contract is unchanged" except the honeypot. Two more things left with it, neither declared: (a) the standing disclosure. KbCommunityAlerts.client.tsx:155 always rendered "One email per new listing. Unsubscribe any time." and, post-success (lines 96-102), "Sign in to manage alerts" (a link to /login?returnUrl=/account/saved-searches) plus "Pause from any alert email." The rendered v3 sheet contains none of it — verified against the live #alerts section, whose entire text is "New listings | Get new Tetherow listings by email | Where should new Tetherow listings go? | Email | Get alerts". An email-capture form on a licensed broker's public site now collects an address with no frequency statement, no unsubscribe statement, and no route to manage the subscription. (b) the honeypot deletion IS declared, but it is still a live anti-abuse regression on a public write path — the `company` field was the naive-bot filter in FRONT of the per-IP limiter, and it is gone with only a comment promising a future V3SheetField kind. ci:capture-scope passes because it checks the action and payload shape, not the disclosure.

29. CHECK 4 (no horizontal overflow at 390) WAS NOT RE-RUN — it needs a rendered viewport and this pass was read-only, so I am recording it unverified rather than green. Checks 2 and 3 are green by construction only, not by measurement: V3Breadcrumb's belowNav defaults true on the surface tone (components/site/v3/V3Breadcrumb.tsx:105) and the page passes neither prop; the Instrument's action is variant 'ghost' (page.tsx:470), V3Field and V3Footer render no V3Button, and V3Sheet's primary submit sits in the fourth section, so the header's nav-cta is the only filled control in the first viewport at both widths. Both still want a browser pass before ship. Checks 1, 5, 6, 7 are proven: one <main> (:439) with <footer> outside it (:536); mosRaw is classified raw by marketVerdict and rendered by formatMonthsOfSupply, and buildMarketFaq receives the raw value and runs the identical pair (:197-229), so H1 verdict, Instrument figure, visible FAQ, FAQPage JSON-LD and Dataset variable cannot diverge; KbSectionTracker observes '.kb-root section[id], .v3 section[id]' and every primitive renders <section id> inside main.v3 with unique ids (market/homes/about/alerts/faq); every door resolves live — /housing-market/{slug} 200 for bend, la-pine, powell-butte, terrebonne, /open-houses/la-pine 200, plus /cities, /months-of-supply, /sell/valuation and homesForSalePath. Note for the owner of the OTHER r

30. SYNTHESIZED ZERO UNDER A LIVE TRACE (§0). _v3/subdivision-figures.ts:89-93 pushes the third parent figure with no guard — `figures.push({ value: v3Text(pulse.closedLast30Days.toLocaleString('en-US')), label: v3Text('closed in the last 30 days'), href })` — while medianListPrice and medianDaysToPending directly above it are both null-guarded. lib/data/market/getMarketPulse.ts:60 reads `closedLast30Days: (row.sold_count_30d as number) ?? 0`, so a NULL column arrives as the number 0 and the page cannot tell it from a real zero. It then prints that 0 under parentMarketTrace, which explicitly claims "closings from the last thirty days" as a live-MLS figure. This is exactly the hazard the page's own header doctrine names ("ABSENT IS NOT ZERO") and applies to activeCount via featuredOk/boundaryRead.ok, but not here. platStatsFigures() in the same file guards all four of its figures; the asymmetry is the bug.

31. DEAD READ CONTRADICTING ITS OWN COMMENT. page.tsx:372-373 introduces the comment "Sales history, the plat cached statistics, schools, and the parent pulse rows. Every result below reaches the screen." directly above the Promise.all. It is false for the fourth element: page.tsx:405-406 sets `const parentFigures = platFigures.length > 0 ? [] : parentPulseFigures(parentPulse, parentHref)`, so whenever the plat has its own market_stats_cache row, BOTH getMarketPulse calls inside fetchSubdivMarketExtras (community + city, 3000ms each) render nothing. Verified live: /subdivisions/sunrise-village renders the plat 'Year to date' Instrument and no parent market band at all, yet both pulse queries ran.

32. UNDECLARED DELETION OF LISTING PHOTOGRAPHY. parity.json's DELETIONS list names only "KbFeatured's photo rail", but the boundary path (e.g. tetherow-phase-1, useSplit true) rendered PlaceMapListSplit, not KbFeatured, and that component renders a photo per row — components/site/explore/PlaceMapListSplit.client.tsx:114-117 `{row.photoUrl ? <img src={row.photoUrl} ...>}` fed by `photoUrl: t.photoUrl` in splitRowsFromTiles — plus map photo stamps (line 166, and the deleted page passed the subtitle "Zoom in for photo stamps"). V3FieldItem carries no image field and SubdivisionFieldPin is {id, href, priceLabel, title, lat, lng}, so both are gone. Verified on the rendered page: zero <img> elements inside <main>; rows are price + address + '4 bd · 3 ba' text only. The parity contract promises deletions are enumerated "each with where the information went"; this one is not listed anywhere.

33. TWO THRESHOLDS RESTATED IN USER-FACING COPY, NEITHER MECHANICALLY TIED. SubdivisionSchools.tsx now prints "A school appears only when at least 70 percent of at least ten of those listings name it." Both numbers live as exported constants in lib/data/subdivisions/getSubdivisionSchools.ts:41-44 (`SCHOOL_MIN_SAMPLES = 10`, `SCHOOL_MIN_AGREEMENT = 0.7`) with no gate binding the prose to them. The KB copy restated only the 70 percent; this change adds the sample floor, doubling the drift surface. Changing either constant leaves the public page asserting a threshold the DAL no longer applies.

34. MINOR — DANGLING FIGURE IN THE FIELD FOOTNOTE. page.tsx:538-542 emits `The map plots all ${plotted.length} that carry coordinates.` with no antecedent noun; live output reads "24 homes shown here … The map plots all 26 that carry coordinates." directly under an Instrument that says "26 homes for sale … every property type the plat holds." Three different 24/26 populations in one viewport with the linking noun omitted from the only sentence meant to reconcile two of them.

35. CHECK 5 RESIDUE — the Dataset JSON-LD description names four metrics the payload does not carry, which is repair defect #2 one field over. app/communities/[slug]/_v3/community-metadata.ts, buildCommunitySchemas, hardcodes the Dataset description 'Live single-family home market data for <name> in <city>, Oregon. Median list price, active inventory, months of supply, and median days to pending.' while the repair deliberately drops the Months of Supply variable everywhere the pulse row's own numerator is not the count on screen. Rendered evidence (localhost dev, browser UA, 2026-08-12): /communities/tetherow publishes that sentence with variableMeasured = [Median List Price=1474500, Active Listings=36, Median Days to Pending=20.5, Homes Sold (12 months)=35] and NO Months of Supply; /communities/eagle-crest the same; /communities/vandevert-ranch publishes the identical sentence with variableMeasured = [Homes Sold (12 months)=1] ONLY — none of the four named metrics is in the dataset it describes. The figure, the H1 verdict, the FAQ question and the variable now drop together from one derivation; the sentence describing them still does not. Fix: derive the description from the datasetVariables actually emitted (the same way the FAQ and the variables are), so a machine-readable claim cannot outrun the payload.

36. ci:reachable-exports IS RED, and 5 of its 12 new orphans are modules this route's own parity contract declares removed — recipe section 9 trap 9 ('a deletion you did not declare reads as a deletion you did not notice, and the module it orphaned will fail ci:reachable-exports in the next agent's session'). `node scripts/check-reachable-exports.mjs` exits 1: components/site/explore/CommunityGolfLinks.tsx, components/site/explore/PlaceInventoryMap.tsx, components/site/kb/KbResortOverview.tsx, components/site/kb/KbSchools.tsx and components/site/kb/KbAreaGuideVideo.tsx are each named in design_system/ryan-realty/ui_kits/community/parity.json removedComponents and are now imported by nothing (the other 7 — KbBuyCta.client, KbPopularSearches, lib/kb/city-page-config, lib/kb/neighborhood-photos, SellMobileSticky.client, sell/spine/index, lib/site/valuation-href — come from the sibling cities/zip/sell units). The declaration in decisions.md is not the mechanism; the gate is. Each of the five needs deletion, a 'reachability: entry-point <reason>' marker in its first 10 lines, or a live import. (Separately red and NOT this route's: ci:file-size-budget on app/lp/seller-home-value/SellerLPForm.tsx, 626 vs baseline 618, unchanged pre-existing.)


## app/housing-market/central-oregon/page.tsx (4)

1. §0 VERDICT/NUMBER CONTRADICTION — the migration dropped the boundary-safe months-of-supply formatter. app/housing-market/central-oregon/page.tsx:140 `const mosDisplay = mosRaw == null ? null : Math.round(mosRaw * 10) / 10` and :184 `value: v3Text(mosDisplay.toFixed(1))`. The KB page this replaces printed `formatMonthsOfSupply(raw)` in BOTH places it showed the figure (the deleted lede, and components/site/kb/KbMarketHud.client.tsx:162,199). lib/format/months-of-supply.ts exists solely to stop naive rounding from crossing a threshold the raw value does not: `if (rounded === 4 && mos > 4) return '4.1'; if (rounded === 6 && mos < 6) return '5.9'` — its header names this as design-audit P2. With the naive rounding, a stored 4.02 renders the figure "4.0" and the H1 "Central Oregon market report: a balanced market" and, in the source line directly beneath both, this page's own MOS_THRESHOLD_CLAUSE: "4 months of supply or less is a seller's market." Three surfaces in one viewport, one of them contradicting the other two, on a licensed-broker page. The file header claims invariant 1 closes this class ("4.02 rounds to 4.0, `4.0 <= 4` prints 'a seller's market'") — it closed the CLASSIFY half and reopened the DISPLAY half. The sibling v3 migration kept the formatter (app/housing-market/annual-review/_v3/annual-sections.ts). Fix: `v3Text(formatMonthsOfSupply(mosRaw))`.

2. TRACE NARROWER THAN THE ROW, on the Instrument itself (app/housing-market/central-oregon/page.tsx:260-264). regionTrace reads only 'live MLS through Oregon Data Share, single-family homes across the Central Oregon region' plus the two MOS clauses. But the market_pulse_live region row prints figures about TWO populations, exactly like the city rows: 'median days to pending' (23) is not an attribute of an active listing — supabase/migrations/20260526140535_refresh_market_pulse_advisory_lock.sql computes median_days_to_pending as the median list-to-pending time of single-family homes with StandardStatus='Closed' and CloseDate within 90 days, at a five-sale minimum — and 'closed in the last 30 days' (367) is sold_count_30d. The migration widened the CITY Ledger's source line for precisely this reason and says so (page.tsx:460-469, parity.json 'THE TRACE COVERS BOTH POPULATIONS THE ROW PRINTS'), and the file's own invariant 3 (page.tsx:43-49) states the rule, but the region trace one section above was left on the inherited active-inventory wording. Nothing else on the page closes the gap: the summary Quiet's methodology names the 90-day closed window only for 'each city's pace figure' (page.tsx:297), and the FAQ answer says only 'took a median of 23 days to go pending'. So the region's pace figure publishes with no stated population or window anywhere on a licensed-broker surface. F

3. DECLARED RHYTHM 'no two adjacent alike' breaks in the degraded state the page explicitly designs for (app/housing-market/central-oregon/page.tsx:485-539). The comment at 276-281 guarantees only that the summary Quiet keeps the cities and closed-sales Ledgers apart, but two other adjacencies are reachable: (a) when regionPulse is null — the state the level-1 Quiet fallback at 440-452 exists to handle — buildMarketFaq returns an empty faqs array (every guard is null-checked), so the FAQ Quiet at 512 does not render and V3Ledger id='closed-sales' sits directly adjacent to V3Ledger id='guides'; (b) when closedSeries yields no qualifying row, V3Ledger id='closed-sales' is absent and V3Quiet id='summary' sits directly adjacent to V3Quiet id='faq'. parity.json states the rhythm as part of the contract ('Rhythm: four of the six patterns, no two adjacent alike, chrome exempt'), so the contract is not held in states the page itself anticipates. Both are fixed the same way the first adjacency was: give one of the two conditional sections an unconditional degraded form, or make the guides Ledger's placement depend on what actually rendered above it.

4. STATED INVARIANT IS FALSE AT MOBILE WIDTH. page.tsx:56-58 asserts unconditionally: 'ONE PRIMARY PER VIEWPORT (PUBLIC_UI.md section 1). The sticky public header carries a filled valuation CTA at every scroll position, so both Instrument asks are ghost.' Measured in-browser at 390x844 on http://localhost:3000/housing-market/annual-review: every header element whose text matches /value|valuation/ resolves to width 0 / height 0, one of them `display: none`; the visible header at 390 is SEARCH + MENU+ only. So below the breakpoint the two ghost Instrument asks are the only asks in their viewports and the justification for making them ghost does not hold. The sibling file states the caveat rather than absorbing it (app/housing-market/central-oregon/page.tsx:55-61: "'carried at every scroll position' is true at 880px and up, false at 390px"). Consequence is benign here because the closing Quiet carries the valuation door as a text edge, but the invariant as written is inaccurate.


## app/zip/[zip]/page.tsx (5)

1. SELLER-LEAD ATTRIBUTION REGRESSION, UNDECLARED — the valuation door lost its `?from=`. page.tsx:471-475 ships `action={{ label: v3Text('Get a free written valuation'), href: valuationPath(), variant: 'ghost' }}`; `valuationPath()` is the bare '/sell/valuation' (lib/slug.ts:97). Both valuation doors the KB page carried passed the originating path: components/site/kb/KbSell.client.tsx:52-55 (`params.set('from', pathname)`) and components/site/kb/KbMarketHud.client.tsx:290 (`?from=${encodeURIComponent(pathname)}`). app/home-valuation/actions.ts:93 resolves the lead's stored `source_url` from the referer's `from` param and, when it is absent, falls back to `refUrl.pathname` — which is now '/sell/valuation'. Every seller lead this page produces will record the valuation page as its own origin. That comment block names the regression by date: "2026-07-15 conversion audit — the hardcoded '/home-valuation' erased which page produced every seller lead." Two sibling v3 migrations in this same working tree kept it (app/zip/[zip]/page.tsx:401, app/subdivisions/[slug]/page.tsx:499 both build `${valuationPath()}?from=…`). Neither the `from` param nor KbSell's address-prefill input is named in the parity contract's deletion list, which says only "KbSell — ... the valuation ask is the Instrument's ghost action"; the sibling hub contract (ui_kits/market-report/parity.json) does explicitly name 

2. app/zip/[zip]/page.tsx:220 — the page's LEAD FIGURE is now a door to a destination that does not carry the ZIP. The migration makes `activeCount` a link (`href: `/search?keywords=${encodeURIComponent(zip)}``, comment: 'The same door the KB page's neighborhood CTA carried'). Verified: `/search?keywords=97756` 308s to `/homes-for-sale?keywords=97756`, which applies a `Showing Bend only` saved-area filter and renders 'No homes in this map view / No homes match these filters here'. Same on production ryan-realty.com, so it is standing site behavior, not a local artifact — but the KB page only hung this href on its subdivision CTA, never on the headline count. Result: on nine of the ten canonical ZIPs (every non-Bend ZIP: 97756, 97759, 97739, 97707, 97741, 97754, 97760, plus the Bend-adjacent ones) the page publishes e.g. '392 active single-family listings' under a live Oregon Data Share trace and clicking that figure lands on an empty result set. On 97703 the door disagrees quantitatively instead: the page says 256 and the destination says '241 homes in this map view'. §0 reconciliation fails at the click. The neighborhood Ledger action at page.tsx:454 (`All homes in ${zip}`) carries the identical broken href — that one is inherited verbatim, the figure link is new. Fix is to point both at a ZIP-scoped search (a real postalCode filter, the same filter the alert payload already uses

3. app/zip/[zip]/page.tsx:437 — the degraded branch instructs the visitor to use pins that cannot exist. `mapNote` is unconditional (`Every listing in ${zip} that reports coordinates. Select a pin to open that home.`) while the `emptyMessage` two lines below it (page.tsx:438-442) reads, on `live === false`, 'The listing feed did not answer on this refresh, so this frame is not claiming an inventory.' In that branch `fieldItems` is empty and the map plots nothing, so the same frame simultaneously disclaims an inventory and tells the reader to select a pin. Every other figure on the page is correctly gated on `tilesRead.ok`; this one string is not. Gate `mapNote` on `live` the way `emptyMessage` already is.

4. BLOCKING — /Users/matthewryan/RyanRealty/app/zip/[zip]/_v3/ZipFieldMapImpl.tsx:113-115: the marker-building useEffect bails at `const map = mapRef.current; if (!ready || !map) return`. mapRef.current is assigned only inside onLoad (line 99), which sets a ref and causes no re-render, and the effect deps are [ready, plotted, frame] — so on the render where `ready` flips true the effect runs before @react-google-maps/api has created the map instance, and nothing ever re-runs it. Zero pins are ever created. Verified in a real browser against the running dev server at 390x844 and 1280x900: /zip/97760 (72 rows), /zip/97703 (256 rows) and /zip/97756 (392 rows) all render the Google basemap, correctly framed on the ZIP, with 0 markers (no element inside .v3-field__map carries a price title; no google.maps.Marker deprecation warning fires on a fresh load, i.e. no Marker is ever constructed). Control in the same session: /communities/awbrey-glen renders 8 pins for 8 rows using the sibling app/communities/[slug]/_v3/CommunityFieldMapImpl.tsx pattern (useState `fitted` flipped in onLoad + declarative <Marker> children). Consequence: PUBLIC_UI Pattern 2's map<->list binding does not exist on this route (no hover-both-ways, no pin door), and app/zip/[zip]/page.tsx:324-329 still renders the note 'Every listing in {zip} that reports coordinates. Select a pin to open that home.' — the exact 'an

5. PUBLIC-FACING NAMING — /Users/matthewryan/RyanRealty/app/zip/[zip]/page.tsx:338-362 with SUBDIVISION_NOISE in /Users/matthewryan/RyanRealty/app/zip/[zip]/_v3/zip-constants.ts: the neighborhood Ledger publishes raw MLS subdivision codes as place names. On /zip/97760 four of the eight rows are 'Crr' (20 for sale, $582,000), 'Crr 2' (5), 'Crr 3' (3) and 'Crr3_C' (3) — Crooked River Ranch fragmented into four 'neighborhoods' with four different medians, one of them literally named 'Crr3_C'. The noise set excludes only five exact strings ('', n/a, none, unknown, other) and the row name never passes through displaySubdivision(), which the same file uses for tile display and which additionally drops '***'-masked private values and 'na'/'n/a.'. Carried over from the KB page rather than introduced here, but it contradicts this file's own stated rule that a group earns a row only when it has 'a name the feed did not fill with noise'. Counts and doors still reconcile.


## app/pulse/page.tsx (4)

1. SECTION 0 — the Ledger's price figure is never named. app/pulse/page.tsx:253 pushes `value: v3Text(formatPrice(snapshot.median_list_price))` and the row renders as `<span class="v3-ledger__value">$750,000</span>` (verified in the live DOM). The section's only trace is `'live MLS through Oregon Data Share, active single-family listings, one row per city, ordered by inventory'` — it never says the dollar figure is a MEDIAN LIST PRICE. V3Ledger renders no column header (confirmed: `hasHeader: false`), so nothing on screen names the metric. A reader can read $750,000 as a median sale, an average, or a list price. The Instrument three sections above labels the identical field 'median list price'; the Ledger holds a weaker standard for the same number. V3Ledger already exports an unused `note` prop ('One sentence under the heading when the list needs a stated basis') — the fix is one line in `source` or `note`.

2. SECTION 0 — the Ledger's `days to pending` figure contradicts the population its own trace states. app/pulse/page.tsx:249-252 prints `${Math.round(snapshot.median_days_to_pending)} days to pending` under a source line whose population is 'active single-family listings'. Days-to-pending is measured on listings that WENT pending, not on active inventory, and no window is stated for it. The same file is explicit about exactly this distinction 60 lines earlier (page.tsx:198-199: 'Unsold inventory age, NOT days to pending. The two are different populations and differ by a factor of three on this region row, so the label says which'), so the page applies two different standards to the same class of figure. Provenance itself is fine (ci:days-to-pending-source exits 0; the field is market_pulse_live.median_days_to_pending) — the defect is that the trace above the figure does not cover it.

3. app/pulse/page.tsx:339-341 + :356 — the "new listings, last 7 days" figure names a population the column does not carry, and the trace does not correct it. market_pulse_live.new_count_7d is COUNT(*) FILTER (WHERE "StandardStatus" IN ('Active','Coming Soon') AND "OnMarketDate" >= now - 7 days) (supabase/migrations/20260526140535_refresh_market_pulse_advisory_lock.sql lines 133-137), so it counts listings that came on market in the last 7 days AND ARE STILL ON THE MARKET — a home listed 5 days ago that went pending yesterday is not in the 105. The regionTrace clause at line 356 says only 'New listings covers the last 7 days', while the same trace explicitly qualifies the neighboring figures ('counts active and coming-soon listings', 'their age together with listings under contract, so it is inventory age, not days to pending'). By the file's own rule at lines 73-83 ('EVERY FIGURE IS NAMED WHERE IT IS PRINTED'), this is the one printed figure whose population is left unnamed, and it understates true 7-day new-listing volume by whatever share already went pending. Fix: add the restriction to the clause (e.g. 'New listings counts homes that came on market in the last 7 days and are still active or coming soon') or relabel the figure.

4. app/pulse/page.tsx:437-439 (and the header claim at :82) — the Ledger trace states the days-to-pending suppression rule more narrowly than the SQL applies it. The trace says 'a city with fewer than five of those closings shows no figure for it', but refresh_market_pulse suppresses on CASE WHEN COUNT(*) FILTER (WHERE COALESCE(days_to_pending, pending_timestamp::date - "OnMarketDate"::date) IS NOT NULL) >= 5 — five closings WITH A MEASURABLE list-to-pending time, not five closings. A city with 20 closings of which only 3 carry a pending timestamp also shows a blank, and the trace would then state a reason that is not the operative one. Today the table happens to agree (Terrebonne, the only blank row, has sold_count_90d = 0; Culver shows a figure on exactly 5), so this is latent rather than currently publishing a wrong reason — but it is the stated basis for a blank cell on a licensed-broker surface and should read 'fewer than five of those closings with a recorded list-to-pending time'.


## app/housing-market/annual-review/page.tsx (3)

1. ATTRIBUTION REGRESSION (confirmed live): the valuation door lost its origin parameter. Old page (git show HEAD:app/housing-market/annual-review/page.tsx:573) rendered `<KbSell data={{...}} eyebrow="Sell in Central Oregon" />`, and components/site/kb/KbSell.client.tsx:48-55 pushes `if (pathname) params.set('from', pathname)` -> `/sell/valuation?address=...&from=/housing-market/annual-review`. New page (app/housing-market/annual-review/page.tsx:311) emits `edges.push({ label: 'Get a free written valuation', href: valuationPath() })`, and lib/slug.ts:98 `valuationPath()` returns the bare `'/sell/valuation'`. Verified in the rendered DOM: the only /sell/valuation href inside <main> carries no query string. app/home-valuation/actions.ts:93 reads `refUrl.searchParams.get('from')` and, when absent, falls back to `refUrl.pathname` — which at submit time is /sell/valuation itself. Every seller lead this page produces now records source_url = /sell/valuation instead of /housing-market/annual-review. The sibling migration guarded exactly this: app/housing-market/central-oregon/page.tsx:135 `const VALUATION_HREF = `${valuationPath()}?from=${encodeURIComponent(PAGE_PATH)}`` with the docblock naming the 2026-07-15 conversion audit as the reason.

2. CHECK 5/7 (undeclared deletion, contract vs code) — the level-2 closed-sales Instrument silently disappears in a degraded state, and that degrade is declared nowhere. app/housing-market/annual-review/page.tsx:486-501 renders `{firstClosedFigure ? <V3Instrument .../> : null}`; V3Instrument.tsx:124 types figures as a non-empty tuple, so when regionDetail returns no figure the whole section vanishes. Every other section on the page has a declared degrade — the level-1 Instrument becomes a level-1 Quiet (page.tsx:449-462) and both Ledgers render a named empty state — and design_system/ryan-realty/ui_kits/market-report-annual/parity.json declares exactly those two, while its sectionOrder entry (line 163) lists 'V3Instrument (level 2, region trailing 12 months)' unconditionally, unlike the level-1 entry which carries '— or V3Quiet level 1 when the pulse row is absent'. In that state the page also ships V3Ledger #cities-now immediately followed by V3Ledger #cities-year, which the same contract's rhythm rule ('no two adjacent alike', parity.json note and the page docblock lines 6-9) forbids. Either give the section a declared absent-state (a Quiet stating the closed-sales row did not return, matching the opening section's pattern) or declare the conditional removal in the deletions/sectionOrder blocks.

3. CONTEXT, NOT A DEFECT OF THIS ROUTE: npm run ci:file-size-budget currently fails on app/lp/seller-home-value/SellerLPForm.tsx (626 vs baseline 618), an unrelated modified file. The annual-review route itself is 548 LOC, under the 600 floor, and every other gate run came back green for this route: ci:mockup-parity ([OK] app/housing-market/annual-review/page.tsx, required 9 / missing 0 / baselined 0), ci:mockup-coverage (0 violating), ci:default-chrome-footer ('every public page resolves to exactly one footer'), ci:public-ui, ci:heading-display, ci:rsc-fn-props, ci:page-dal, ci:dal-boundary, ci:market-formula, ci:brand-voice, ci:design-tokens, ci:date-format, ci:report-geo-registry, ci:market-city-slug-canon, ci:count-degraded-read, ci:kb-breadcrumb-overlay, ci:seo-shell, and npx tsc --noEmit (exit 0).


## app/cities/[slug]/page.tsx (7)

1. METRIC SUBSTITUTED AND PUBLISHED: communityCount. /Users/matthewryan/RyanRealty/app/cities/[slug]/page.tsx line 254 and line 371 changed `communityCount: communitySnapshots.length` -> `communityCount: snapshot.communityCount`. These are different queries: the retired one is `getCityCommunitySnapshots()` (geo_snapshot_mv, geo_type='community', geo_key LIKE '<city>:%', active_sfr_count > 0, LIMIT 40 — lib/data/geo/getGeoSnapshot.ts:300-307); the new one is the city row's `community_count`, defined as count(DISTINCT "SubdivisionName") FILTER (WHERE not null AND <> 'N/A' AND "StandardStatus"='Active') over ALL property types, uncapped (supabase/migrations/20260730003000_geo_snapshot_mv_drop_volatile_split.sql:119). Verified live against geo_snapshot_mv: bend 40 -> 343, terrebonne 18 -> 24, powell butte 8 -> 12. This value is consumed by buildDataDrivenCityAbout (lib/city-content.ts:168 `${communityCount} communities and subdivisions`), so it is PUBLIC COPY, and it renders on every city with no hand-written description — i.e. every slug outside PRIMARY_CITIES, reachable via `dynamicParams = true`. Confirmed rendered at http://localhost:3000/cities/terrebonne: "Right now the Terrebonne market shows 6 active listings, a median list price of $685,000, 24 communities and subdivisions" where the retired page published 18. It also changes the GA4 `view_city` `community_count` param for ev

2. FIGURES RENDERED WITH NO TRACE AND NO STAMP, contradicting the file's and the contract's own stated invariant. The About V3Quiet in /Users/matthewryan/RyanRealty/app/cities/[slug]/page.tsx lines 432-439 renders `aboutParagraphs` from buildDataDrivenCityAbout, which emits live figures inline (lib/city-content.ts:166-172: active listing count, median list price, community count). Confirmed live at http://localhost:3000/cities/terrebonne: "Right now the Terrebonne market shows 6 active listings, a median list price of $685,000, 24 communities and subdivisions" — inside a V3Quiet, with no source line and no updated stamp. This falsifies three explicit claims shipped in this diff: (a) parity.json's "No figures in either block"; (b) V3Quiet's own contract in components/site/v3/V3Quiet.tsx:9-10 ("it never carries a primary action and never carries a figure") and :121-122 ("A number belongs in an Instrument with its source line, not here"); (c) the page's invariant 3 / inventory comment at lines 186-189 ("derived once and printed once ... the page never states how many homes are for sale twice") — the inventory count is stated a second time here, untraced, directly under a V3Field that states it with a trace. Compounding it, the median in this untraced sentence uses a different fallback chain (`cityPulse?.medianListPrice ?? snapshot.medianListPrice`, line 253) than the traced Instrumen

3. FALSE PROVENANCE IN THE NO-PULSE SOURCE LINE. /Users/matthewryan/RyanRealty/app/cities/[slug]/page.tsx lines 196-200 print, as the V3Field's source trace: "live MLS geo snapshot, active single-family listings with a <City> address. The 15-minute market row did not return on this refresh, so this count is the snapshot's own", with `updatedAt: snapshot.refreshedAt`. But getGeoSnapshot applies withPulseOverride on every city read (lib/data/geo/getGeoSnapshot.ts:147-148 and 102-113), which replaces `activeSfrCount` with `market_pulse_live.active_count` and `refreshedAt` with that row's `updated_at`. getMarketPulse is a separate resilient-cached read that falls back to `null` independently (lib/data/market/getMarketPulse.ts:71-79), and getGeoSnapshot's pulse map keys on BOTH geo_slug and geo_label (a broader match than getMarketPulse's `eq('geo_slug', geoSlug)`). So in the exact branch this sentence is written for, the printed count and the printed timestamp can both be the market_pulse_live row's, while the trace asserts the market row did not return and that the count is the snapshot's own. That is a named-source claim the code cannot guarantee (CLAUDE.md §0 rule 1), and it also violates the page's own invariant 3, line 51-52: "Neither borrows the other's clock."

4. CROSS-SURFACE VERDICT DIVERGENCE INTRODUCED BY PRE-ROUNDING BEFORE CLASSIFICATION. /Users/matthewryan/RyanRealty/app/cities/[slug]/page.tsx lines 155-159 compute `mosDisplay = Math.round(raw*10)/10` and then `marketVerdict(mosDisplay)`, and line 172 hands the ROUNDED value to buildMarketFaq, whose classification therefore also reads the rounded number. The retired KB page passed the RAW `pulse.monthsOfSupply` through KbMarketHud, so this is new behavior on this route. lib/site/market-faq.ts — modified in this same changeset — was rewritten specifically to prevent this ("Rounding first and then comparing the rounded number to the threshold moves the verdict across a boundary in two directions ... marketType() therefore reads the RAW value"), and this page defeats that protection by rounding upstream. Result: for raw MoS in (4.00, 4.05] the city page publishes "4.0 months of supply" + "a seller's market" while /housing-market/<slug> publishes "4.1" + "balanced market" from the identical market_pulse_live row on the same day; for raw MoS in [5.95, 6.00) the city page publishes "6.0" + "a buyer's market" against the hub's "5.9" + "balanced". The divergence propagates into the FAQPage JSON-LD answer and the Dataset `Months of Supply` variableMeasured, so two public pages and two structured-data payloads assert contradictory market verdicts for the same geo. parity.json acknowledges 

5. CHECK 8 (empty-state honesty) FAILS — the no-figure branch publishes two false sentences and contradicts the Field beside it. app/cities/[slug]/page.tsx:454 keys the branch on `firstMarketFigure`, not on `cityPulse`, so a pulse row that RETURNED with all three figures null falls into the V3Quiet at :473-486 whose body (:482) states 'The {City} market row did not return on this refresh' and 'The homes below carry their own live list prices.' Meanwhile `inventory` (:251-261) keys on `cityPulse`, so the same render takes the pulse branch and the Field prints its count under 'the 15-minute market row for {City}' (:254) — one page asserting both that the row did not return and that the count came from it. VERIFIED LIVE, not hypothetical: market_pulse_live has geo_type='city', geo_slug='warm springs', property_type='A' with active_count=0 and median_list_price / months_of_supply / median_days_to_pending ALL null; geo_snapshot_mv has the matching 'warm springs' city row so getGeoSnapshot resolves (getGeoSnapshot normalizes hyphens to spaces for cities, lib/data/geo/getGeoSnapshot.ts:130-132) and the page renders instead of 404ing; 'warm-springs' is admitted by CENTRAL_OREGON_CITY_SLUGS (lib/central-oregon.ts:18); https://ryan-realty.com/cities/warm-springs returns 200. listing_tile_mv has ZERO Active tiles of any property_type for city='Warm Springs', so 'The homes below carry their o

6. ci:reachable-exports FAILS (commit-blocking) and 4 of its 11 orphans were created by this migration. `node scripts/check-reachable-exports.mjs` exits 1. Modules whose ONLY importer was HEAD's app/cities/[slug]/page.tsx and which nothing imports now: components/site/kb/KbAreaGuideVideo.tsx, components/site/kb/KbPopularSearches.tsx, lib/kb/city-page-config.ts (CITY_HERO_VIDEO / CITY_MARQUEE_COMMUNITIES / communityVideoUrl), and lib/kb/neighborhood-photos.ts (only lib/kb/__tests__/neighborhood-photos.test.ts still references it). All four are declared in design_system/ryan-realty/ui_kits/city/parity.json removedComponents, but declaring is not the whole rule — migration-recipe.md section 7 / trap 9 requires the orphan be deleted or wired, which is what the subdivision unit did with its three. The remaining 7 orphans belong to sibling units in flight (PlaceInventoryMap, CommunityGolfLinks, KbResortOverview, KbSchools, KbBuyCta.client, SellMobileSticky.client, sell/spine/index.ts), not to this route.

7. MINOR, same class as the repaired defects: the Field's emptyMessage (app/cities/[slug]/page.tsx:503) asserts 'No active single-family listing carried a {City} address on this refresh', but the branch that renders it only proves that no tile survived cityFieldItems' price-and-street filter (app/cities/[slug]/_v3/city-field-items.ts:36-46), and the pull is propertyType 'A', which this same file states at :173-184 is the residential BUCKET (single family, townhome, condo), not the SFR sub-type. When tiles exist but every one lacks a list price or a street, the page publishes an absence claim about the world that the query does not support. Low probability, but it is the exact standard this file holds itself to elsewhere.


## app/communities/[slug]/page.tsx (2)

1. MONTHS-OF-SUPPLY DISPLAY BYPASSES THE CANONICAL BOUNDARY-SAFE FORMATTER — the page prints two different numbers for one statistic. app/communities/[slug]/page.tsx:288 `const mosDisplay = mosRaw == null ? null : Math.round(mosRaw * 10) / 10`, rendered at _v3/community-figures.ts:141 as `input.mosDisplay.toFixed(1)`. This is the exact expression that lib/site/market-faq.ts:146 REPLACED in this same changeset, with the reason written in its own comment: "Math.round reopened the contradiction from the far end." lib/format/months-of-supply.ts:23-28 is the single boundary-safe rule (4.02 prints 4.1, 5.97 prints 5.9), and the old KB page went through it via KbMarketHud (`formatMonthsOfSupply(data.monthsSupply)` beside `verdictOf(data.monthsSupply)`), so this is a regression, not a carry-over. FAILURE: raw MoS 4.02 -> the Instrument prints "4.0 months of supply" under an H1 reading "...: a balanced market", with the threshold clause in the source line directly beneath saying "4 months of supply or less is a seller's market"; the same page's FAQ row (buildMarketFaq, fed mosRaw at page.tsx:392) prints "has 4.1 months of supply"; and the Dataset JSON-LD variableMeasured publishes 4.1. Three values for one statistic on one page. Same at 5.97 -> "6.0" vs "5.9" beside a "balanced market" verdict and a printed "6 or more is a buyer's market". Ungated: ci:market-formula only scans for the wron

2. CHECK 3 FAILS AT 390 — the first viewport carries ZERO visible filled controls, and the reasoning recorded in the source is the exact one the locked visual language forbids. Measured in the live dev render (390x844, scrollY=0, tetherow): visible filled controls = 0; at 1280x800 = 1 (header a.nav-cta, top 18px). The header CTA 'Get your home's value' is display:none below 880px (components/site/kb/kb.css:204 — `@media(min-width:880px){.topbar a.nav-cta{display:inline-flex;}}`), and its mobile twin sits in the closed drawer at visibility:hidden (.menu-cta-row, kb.css:212). The only v3-btn--primary on the page is the alert sheet's 'Get alerts' button at top ~8796px. The page demotes its own ask to ghost on purpose — app/communities/[slug]/page.tsx invariant 5 ('ONE PRIMARY PER VIEWPORT. The sticky public header carries a filled CTA at every scroll position, so the Instrument's ask is a ghost'), the V3Instrument `action` prop passes `variant: 'ghost'` (page.tsx, the Instrument's action arg), and design_system/ryan-realty/ui_kits/community/parity.json repeats it in the V3Instrument entry. design_system/public/PUBLIC_UI.md section 1 (LOCKED 2026-08-11) names this precise failure: 'at 390 the chrome's valuation CTA sits inside the collapsed menu, so a page that demotes its own ask to ghost "because the header carries the primary" ships a first viewport with no ask at all. A content pa


## app/housing-market/history/page.tsx (1)

1. BROKEN DOOR + FALSE COPY (new, live-verified). SubdivisionSalesHistory.tsx now makes every Ledger year a link — `href: `${HISTORY_PATH}?year=${year.year}${cityQuery}`` — and states in the section note: "Each year opens the Central Oregon closed-sales explorer at that year." app/housing-market/history/page.tsx:53 clamps: `const year = Math.min(2030, Math.max(1998, Number(one(sp.year)) || 2024))`. /subdivisions/sunrise-village renders a 1997 row ("1997 6 closed sales $242,000"); its door is /housing-market/history?year=1997&city=Bend, which I fetched and it renders "Active query 1998 · Bend" with 1998 aggregates (Closes 2,484, Median close $114,200). A 1997 plat figure therefore opens a page of 1998 data, and the sentence asserting otherwise ships on a licensed broker's public surface. The KB table rows were plain <td>s with no destination, so both the door and the claim are introduced by this change.


## app/cities/[slug]/[neighborhoodSlug]/page.tsx (5)

1. INVARIANT 4 SURVIVOR, SERP: /Users/matthewryan/RyanRealty/app/cities/[slug]/[neighborhoodSlug]/page.tsx:163-166 — generateMetadata prints `${neighborhood.activeCount} single-family homes for sale in ...` (and the second branch 'Active single-family homes in ...'). neighborhood.activeCount comes from getNeighborhoodBySlug (/Users/matthewryan/RyanRealty/app/actions/cities.ts:431-437), which filters listing_tile_mv by isResidentialInventoryType — single_family OR condo_townhome OR manufactured_mobile OR other (/Users/matthewryan/RyanRealty/lib/inventory-filters.ts:58-61) — matched on the neighborhood NAME, not the polygon. That is exactly the population the page's own liveFallbackTrace admits 'counts every residential inventory type rather than the Single Family Residence subtype alone'. So the meta description publishes a non-single-family count under the words 'single-family homes for sale', and on any neighborhood that HAS a pulse row that number also contradicts the page's own H1 figure (Southern Crossing: pulse 2 SFR vs ~16 in the bucket, per the header comment's own verified figures). Invariant 4 claims this was fixed 'in the H1, in the visible Q&A, and in the FAQPage and Dataset markup' — the meta description was left on the old wording.

2. INVARIANT 4 SURVIVOR, VISIBLE Q&A + JSON-LD: /Users/matthewryan/RyanRealty/app/cities/[slug]/[neighborhoodSlug]/page.tsx:326-330 — in the no-pulse branch the page feeds buildMarketFaq `{ activeCount: neighborhood.activeCount, medianListPrice: neighborhood.medianPrice }`, the all-residential-types numbers. buildMarketFaq then renders 'There are N active single-family listings in {place}' and 'The median list price for a single-family home in {place} is $X' (/Users/matthewryan/RyanRealty/lib/site/market-faq.ts:110-125), and those strings ship three ways: the visible V3Quiet rows (page.tsx:334), the FAQPage JSON-LD (page.tsx:415), and the Dataset variableMeasured (page.tsx:401-411). The same page, in the same branch, prints a source line saying that number is NOT the single-family subtype (liveFallbackTrace, /Users/matthewryan/RyanRealty/app/cities/[slug]/[neighborhoodSlug]/_v3/neighborhood-sections.ts:171-180) and deliberately labels the Instrument figure 'homes for sale' rather than 'single-family homes for sale'. One number, two contradictory names, on a licensed broker's surface.

3. FALSE UI CLAIM: /Users/matthewryan/RyanRealty/app/cities/[slug]/[neighborhoodSlug]/page.tsx:463-467 — the Field footNote reads 'The {24} highest-priced are listed. The figure above opens the full set.' Nothing above it opens anything. V3Field's count prop carries only { value, label, source, updatedAt } with no href (/Users/matthewryan/RyanRealty/components/site/v3/V3Field.tsx:111-119) and renders as a bare <V3Figure> (V3Field.tsx:308-312), and V3Figure has no href either (/Users/matthewryan/RyanRealty/components/site/v3/atoms.tsx:198-247). Read instead as the Instrument's headline figure, the sentence is still false: that figure links to browsePath (/homes-for-sale/{city}/{neighborhood}), a boundary_neighborhood search over a different population, not the in-boundary PropertyType-A set the Field capped at 24. The note fires whenever tiles.length > 24, which is reachable (boundary pin cap 200, tiles limit 250).

4. INVARIANT 4, EMPTY BRANCH: /Users/matthewryan/RyanRealty/app/cities/[slug]/[neighborhoodSlug]/page.tsx:314 — the success-path empty message says 'No active single-family listing sits inside the recorded {place} boundary right now.' The Field's set is NOT single-family: getListingTiles narrows to propertyType 'A' only (page.tsx:254), the bucket the page itself documents as holding townhouses and condominiums, which is why the count label reads 'active homes inside the {place} boundary' (page.tsx:457) and fieldCountTrace spells out A_BUCKET_PROSE. This is the one empty branch that fires on a healthy read, and it names the wrong population — the same mislabel the repair removed everywhere else in the block.

5. INVARIANT 2, NON-CANONICAL DISPLAY RULE: /Users/matthewryan/RyanRealty/app/cities/[slug]/[neighborhoodSlug]/page.tsx:272-274 computes mosAgrees against Math.round(mos*10)/10, and neighborhood-sections.ts:268 renders the figure with the same Math.round, instead of formatMonthsOfSupply (/Users/matthewryan/RyanRealty/lib/format/months-of-supply.ts:24-29), the boundary-safe formatter every other public surface and buildMarketFaq already use. The comment's premise ('There is no third number to print, so the figure is withheld') is false — formatMonthsOfSupply IS that number (4.02 prints 4.1, 5.97 prints 5.9), and it is already printed on this very page. Observable result at mos = 4.02: the level-1 Instrument silently drops months of supply and the page also drops the '/months-of-supply' edge (page.tsx:339), while the closing V3Quiet, from the same pulse row, states '{place} has 4.1 months of supply, which is a balanced market'. The page suppresses a figure it is simultaneously publishing thirty rows below.

