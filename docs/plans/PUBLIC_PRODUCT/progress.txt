2026-08-11T00:00:00Z P0 BOOT: Public Product OS scaffolded — constitution (PUBLIC-PRODUCT-OS.md), runner skill (.claude/skills/public-product-os/SKILL.md), seven memory artifacts, package registration row in docs/DEVELOPMENT_PROCESS.md, PUBLIC_SITE_UX_OVERHAUL demoted to evidence (product decisions absorbed into decisions.md; statuses void per 2026-08-11 audit: home-spine/pixel-pass/endtoend-ship claims had no disk evidence), experience-rollout skill bannered superseded. Founding directives recorded from Matt (chat 2026-08-11): dual page objectives + exits, seamless exploration graph, lead-gen machine without acting like it, explorable market knowledge present/past/future, continuous+fluid context carry, >=3 section patterns (closed set), replicate admin OS machinery. Phase -> P1_REGISTRY. Next: registry-pass.
2026-08-11T00:30:00Z P0 BOOT-FIX: push gate (materialized-tree ci:gates) caught regressions from the prior session unpushed commit 2c11ec83 -- design-token ratchet 253 vs 252. Fixed the class: deleted components/site/v2 (zero imports, superseded; register count 5 -> 4), converted flagged arbitrary utilities to style props in PlaceMapListSplit.client + ListingLot3D.client (rendering preserved), reasoned ignore entry for app/search/loading.tsx (skeleton must mirror baselined MapSearchView pane widths). Also landed the granted one-spine nav retarget (/sell#get-value) whose stale test was blocking every commit. Ratchet re-verified 252 = baseline. Next: registry-pass.
2026-08-11T01:00:00Z MISSION (endtoend, Fable 5): Grind P1+P2 to the P3 lock in one run. Done means: process-registry.json holds every real visitor+machine process with in-session file:line evidence (seed catalog confirmed/denied + code discovery); page-inventory.json maps every public route to a process; ALL confirmed processes have complete 12-section PDS files under processes/ (exclusive file per parallel agent), each adversarially verified by a fresh-context refuter (evidence pointers resolve, sections non-empty, no shape inheritance); P3 decision package (KEEP/MERGE/KILL/DEFER + <=5 questions) written; state flushed awaiting_lock=process; committed + pushed. Parallel deepen note: the one-process-at-a-time efficiency rule governs a single context, not parallel agents each owning one whole PDS exclusively (Matt directed max parallelism this run). Stop token: BLOCKED_ON_MATT: process.
2026-08-11T02:00:00Z P1 COMPLETE + P2 PARTIAL (endtoend mission, 75 agents): P1 registry done — 17 parallel agents, 34 confirmed process rows (15 seeds -> 27 confirmed/split + 7 discovery finds: sign-transaction-documents, view-client-valuation-doc, newsletter-lifecycle, track-outbound-engagement, pwa-offline-resilience, join-the-brokerage, compare-homes), 3 denied, all evidence file:line proven. page-inventory.json: 141 routes mapped, 7 UNMAPPED, 5 REDIRECT. P2: 25/34 PDS written to processes/ before the session usage limit killed the remaining agents (resets 19:30 PT); ALL adversarial verifies outstanding — no PDS is verified yet. process-registry.json statuses honest (deepened-unverified vs in_deepening). Next: p2-resume-verify (exact resume command in work-queue.json).
2026-08-11T03:30:00Z P2 COMPLETE + P3 PACKAGE (endtoend mission, 105 agents total, 0 errors on resume): all 34 PDS deepened; adversarial verify 32/34 clean after one repair round; 2 residual defects (capture-and-attribute Meta-suppression overclaim, sign-transaction-documents decline-token semantics) fixed by hand against code and re-read. p3-process-lock-package.md written: 6 proposed merges (instant-cma->written-cma, poi->evaluate-a-place, hunt-price-cuts->find-a-home, broker-attributed-lead->written-cma, sms-shortlink-click->track-outbound-engagement, compare-homes->find-a-home), 28 KEEP, 5 questions. state: phase P3_PROCESS_LOCK, awaiting_lock=process. Next: BLOCKED_ON_MATT: process.
2026-08-11T04:00:00Z P3 PROCESS LOCK GRANTED (Matt, structured answers): 28 KEEP / 6 MERGE locked; keep /videos fold /feed; PWA minimal recovery; full scope. Merged PDS files bannered; registry rows locked. Phase -> P4_DATA. Next: p4-data-chains.
2026-08-11T05:00:00Z P4 COMPLETE (29 agents, 0 errors): data-atlas.md written — 239 writer->store->reader->outcome chains across the 28 locked processes; 191 hold, 48 broken with gap statements (headline: engagement_metrics.view_count write-only — buyer demand signal dark; dead featured-rail consumer). Completeness critic rolled gaps into 5 buckets for a later correctness phase. Phase -> P5_IA. Next: p5-ia-package.
2026-08-11T06:00:00Z P5 PACKAGE COMPLETE (8 agents: 7 route families + live GSC pull): ia-lock.md — 6 job-traced destinations (Homes/Places/Market/Sell/Saved/About), continuity spec (place+search+intent+identity carry), amnesia test recorded. page-inventory.json: 142/142 routes with destination+visitor_objective+machine_objective+exits. cut-list.md: ~40 candidates each with LIVE Search Console 90d evidence (service-account API; no fabricated numbers). awaiting_lock=ia. Next: BLOCKED_ON_MATT: ia.
2026-08-11T07:00:00Z P5 IA LOCK GRANTED (Matt, structured answers): 6 destinations locked (Homes/Places/Market/Sell/About + Saved affordance); /buy folds into Homes; calculators dissolve EXCEPT /tools/appreciation which keeps standing (608 impr/90d); /price-drops survives the deal-signals merge (motivated-sellers 301s, 11 URLs); ALL /lp/* become noindex off-graph paid surfaces (ends Tetherow/Bend/golf cannibalization). cut-list frozen w/ amendments; ledger updated. Phase -> P6_VISUAL. Next: p6-visual-package (closed pattern set + MOVING prototype).
2026-08-11T08:00:00Z P6 PACKAGE COMPLETE: design_system/public/PUBLIC_UI.md written (thesis, 7 externally-cited foundations, SIX closed section patterns with rhythm rule + per-destination openings, computed AA contrast table w/ 2 rejected pairs, motion spec, recorded amnesia test, craft scorecard 8.7). MOVING prototype at app/dev/public-v3 (noindex, @no-parity): all six patterns on REAL DAL data (Bend pulse 485 active / $750,000 median / 4.0 MoS / 18 DTP + 6 live tiles), count-up on real figures, pin<->row binding verified by real hover, two-step capture carrying the address forward, owned Old Mill video, reduced-motion path. Browser-verified 390 AND 1280. Three defects found and fixed during verification: (1) getListingTiles status enum is lowercase and validates SYNCHRONOUSLY so .catch never attached (error-swallowing removed entirely per canon), (2) verdict derived from raw MoS while display rounded -> could print 4.0 beside balanced; now both derive from the rounded value, (3) UPDATED NOT AVAILABLE eyebrow copy replaced with omission. awaiting_lock=visual. Next: BLOCKED_ON_MATT: visual.
2026-08-11T09:00:00Z P6 VISUAL LOCK GRANTED (Matt, on the live prod prototype): six closed section patterns + motion rule + AA table locked as the public visual language. Phase -> P7_PRIMITIVES. Queue: p7-primitives, p8-litmus, p9-roll-first-family (ratchet lands with the first roll). Next: build the v3 barrel.
2026-08-11T10:00:00Z P7 COMPLETE (26-agent barrel run + orchestrator repairs): components/site/v3 shipped — tokens.css + atoms.tsx + six pattern primitives (Instrument/Field/Ledger/Stage/Sheet/Quiet) + index.ts barrel; accessible names enforced IN THE TYPE (v3Text brand rejects empty strings; a nameless region does not compile). Root-caused the font defect the whole program inherited: --font-display is undefined app-wide, so v3 now resolves --font-amboqia-safe. Barrel proof at app/dev/public-v3/barrel rebuilds the Market node with ZERO bespoke markup on real data (3.6 MoS, correct verdict). GATE: scripts/check-public-v3.mjs written by the fixer (standalone imports via AST, var(--v3-*)-only color, no formatting inside a primitive, full barrel export), wired into ci:gates, break-tested 3 ways (fires on forbidden import, raw hex, unexported primitive; green after each restore). Design-token exemptions added for the barrel (justified by that gate) and app/dev prototypes; fixed the last arbitrary utilities in the tracked PlaceMapListSplit and ratcheted the baseline DOWN 252 -> 250. Phase -> P8_LITMUS.
2026-08-11T10:30:00Z MISSION (endtoend, P8+P9): DONE MEANS — (a) L1 and L2 litmus spans MEASURED this session in a real browser at 390 on both the current production path and the barrel path, taps + seconds recorded, no fake CRM lead created (measure to the submit control, never through it); (b) ci:public-ui shrink-only ratchet EXISTS, is AST-based, wired into ci:gates, break-tested three ways, seeded at today real counts (per-register import counts per public page + legacy-page count); (c) the first production family (Market) MIGRATED onto components/site/v3 in the same commit as the ratchet, with metadata/canonical/JSON-LD/metrics proven unchanged by a fresh-context adversarial verifier reading git diff; (d) browser-verified at 390 AND 1280 on real data with counts reconciling; (e) committed, pushed, deploy verified READY, live URL checked. Stop tokens only for: missing credential, destructive ambiguity, conflicting requirements.
2026-08-11T11:00:00Z P8 LITMUS MEASURED (production, 390x844, both spans stopped BEFORE submit so no fake CRM lead or listing_alerts row was created): L1 /sell cold -> valuation armed = TTFB 14ms, DOM interactive 1219ms, load complete 4032ms, 1 scroll to the field, 1 field, 2 taps, page height 23515px. L2 /homes-for-sale cold -> saved search armed = TTFB 14ms, DOM interactive 435ms, load complete 3243ms, 1 tap to capture, 924ms from tap to a usable email field, 1 field. Written to docs/plans/PUBLIC_PRODUCT/litmus.md with proposed targets (L1 under 2.5s and no scroll to the ask; L2 field usable within 300ms) for Matt sign-off. ALSO FOUND AND FIXED during the walkthrough, a locked-decision violation live on the money page: /sell advertised the retired 2.5%-3.5% tiers, named Essential/Elite, carried the negotiability line, and shipped a three-tier answer inside the indexed FAQ JSON-LD. Replaced with the single 3% plan across /sell, /sell/valuation, /our-homes, SellCommission rewritten, the tier matrix swapped for the already-built-but-unimported SellPlanSingle, and the FAQ + its structured data corrected together. Rendered page verified: zero occurrences of 2.5, 3.5, Essential, Elite, or negotiable.
2026-08-11T12:00:00Z P9 MECHANICAL HALF SHIPPED, FIRST MIGRATION REVERTED (honest): ci:public-ui ratchet live and wired (123 public pages, 688 non-v3 import sites, 84 legacy pages, by register kb=528 legacy=131 primitives=19 explore=10), break-tested 4 ways by its author + rule C rewritten and re-break-tested by me. The /housing-market migration was BUILT, then REFUTED by two fresh-context verifiers (17 defects: 4 commit-blocking gate failures + section-0 problems incl. a verdict that could contradict the pages own FAQ JSON-LD, a source line overstating its window, synthesized zero rows under a live-MLS trace, a discarded getSurfaceImage read, and an undisclosed deletion of KbHero property search), and REVERTED unshipped. Root finding recorded in decisions.md: the KB-era gates (mockup-parity, seo-shell, kb-breadcrumb-overlay) enforce the OLD destination, so every family migration must update its route gate contracts in the same change. Next: p9-market-family-v2 with that prerequisite, then p9-chrome-unit which unblocks every family at once.
2026-08-11T13:00:00Z MISSION (endtoend, finish P9): DONE MEANS — (a) v3 chrome primitives (nav, footer, breadcrumb) exist in the barrel with accessible names in the type and every current nav href preserved; (b) the gate contracts that bind the Market family are MAPPED and updated to the v3 destination in the same change that migrates the route; (c) /housing-market ships on v3 with all 17 previously-found defects closed, verified by two fresh-context refuters (behavior-preservation first) and by a real browser at 390 and 1280; (d) the rest of the Market family follows the same proven recipe; (e) ci:public-ui numbers go DOWN and are re-seeded in the shipping commit; (f) committed, pushed, deploy READY, live URL checked. Stop tokens only for missing credential, destructive ambiguity, conflicting requirements.
2026-08-11T13:20:00Z LITMUS CORRECTED: the L2 tap-to-field figure was re-measured and is 8ms, not 924ms. The original number was an artifact of the probe (a fixed 700ms sleep before looking), not the product. litmus.md keeps both numbers and explains the error rather than overwriting it; the 300ms L2 target is withdrawn as written against a wrong baseline. Lesson recorded in SESSION_BOOT.
2026-08-11T14:00:00Z P9 FIRST FAMILY SHIPPED (/housing-market on v3): ratchet 688->674 non-v3 import sites, 84->83 legacy pages, page itself 16 -> 2 non-v3 imports (MetadataBlock for JSON-LD + KbSectionTracker for analytics, both deliberate, neither carries visual language). Chrome primitives added to the barrel (V3Chrome/V3Footer/V3Breadcrumb) reading every href from lib/site-nav.ts: 0 destinations lost, proven by server-rendering the components and diffing href sets. gate-contracts.md maps every gate binding the Market family so the next route hits zero surprises; the mockup-parity contract for this route moved to the v3 destination in the same change. Two verifier rounds + a repair closed 12 defects; I then fixed 3 more myself: KbSectionTracker only observed .kb-root so every section_view on a v3 page was silently dropped (now both registers), MOS_THRESHOLD_CLAUSE said "Under 4" while marketVerdict classifies <=4 as sellers (contradiction publishable on 5 market pages, now one canonical sentence used by the FAQ builder too), and the FAQ test asserted a bare substring that the shared clause legitimately contains (tightened to the verdict clause). Browser-verified 390 + 1280: one main, one footer, one filled CTA per viewport, no overflow, verdict matches the figure beside it.
2026-08-11T15:00:00Z MISSION (endtoend, FINISH THE ROLLOUT): Matt asked for everything remaining, max parallelism. DONE MEANS every public page family migrated onto components/site/v3 with its gate contracts moved in the same change, ci:public-ui driven toward zero legacy pages, each route adversarially verified (behavior preservation first) and browser-verified, committed and pushed in waves with the ratchet re-seeded per wave. Waves: (1) rest of Market family, (2) Places, (3) Homes/listing, (4) Sell + trust + content, (5) residual + P10 gate hardening. Stop tokens only for missing credential, destructive ambiguity, conflicting requirements.
2026-08-11T16:00:00Z WAVES 1+2 REPAIRED AND VERIFIED: ratchet 674 -> 527 non-v3 import sites (-147), 83 -> 73 legacy pages (-10), by register kb 518 -> 399. Ten routes migrated (market: central-oregon, annual-review, months-of-supply, pulse; places: cities/[slug], cities/[slug]/[neighborhoodSlug], communities/[slug], subdivisions/[slug], zip/[zip], oregon/[city]). 69 verifier defects consolidated to docs/plans/PUBLIC_PRODUCT/open-defects.md, then 74 closed by a repair wave and the residue closed by hand. Systemic fixes that came out of it: lib/site/valuation-href.ts is now the ONE builder for a valuation link (?from= is CRM source attribution and four routes had silently dropped it); PUBLIC_UI section 1 now says the one-primary count is of VISIBLE filled controls because at 390 the chrome CTA sits inside the collapsed menu and every wave-1 page had shipped a first viewport with no ask; Dataset descriptions are derived from the variables actually emitted (Vandevert Ranch had promised four metrics over a one-variable payload). Shared cleanup: 13 orphaned modules deleted or rewired (untracked sell-spine WIP archived to scratchpad first), 12 city contract tests restored to green (29/29). Gates: reachable-exports green, public-v3 green, design tokens 250 -> 248, all three ratchets re-seeded at the improved numbers. Every migrated route smoke-tested 200 (subdivisions/tetherow 308 is pre-existing prod behavior, verified against production).

2026-08-12T23:00:00Z FOLDED INTO BROKER OS. Plan of record is docs/plans/ADMIN_PRODUCT/BROKER-OPERATING-SYSTEM-PLAN.md v0.13. Where we are: docs/plans/ADMIN_PRODUCT/EXECUTION.md. This program's state.json/work-queue.json are not authority. Process/IA/visual locks still bind. Implementation is quarry (11 mixed routes are not done; 0 v3-only pages because layout still mounts PublicNav). Next land on go: E-CHROME. Do not start Public Product OS 2.

2026-08-13T22:45:00Z TRACK 1 OPEN HOUSES LOOK GREEN. feat 96a26b1b READY. /open-houses and /open-houses/bend first viewport is photographed open houses that open the listing. Field before Instrument. CTA Value my home. Looks in docs/plans/PUBLIC_PRODUCT/looks/2026-08-13-open-houses/. Next: About (faces). Track 2 not started.

2026-08-13T23:05:00Z TRACK 1 ABOUT LOOK GREEN. feat bc680a7a READY. /about first viewport is Matt, Rebecca, and Paul as canonical /images/brokers/*.png cutouts. No card, no wash, no box. CTA Value my home. Looks in docs/plans/PUBLIC_PRODUCT/looks/2026-08-13-about/. Next: Reviews (Google quotes first). Track 2 not started.

2026-08-13T23:40:00Z TRACK 1 REVIEWS LOOK GREEN. feat c07f5638 READY. /reviews first viewport is live Google quotes as written (MJB, E Oster; Audra on 1280). Attribution stays. No star HUD, no paraphrase, no ticker. CTA Value my home. Looks in docs/plans/PUBLIC_PRODUCT/looks/2026-08-13-reviews/. Next: Market (figures + chart allowed). Track 2 not started.

2026-08-13T23:50:00Z TRACK 1 MARKET LOOK GREEN. Already green. Production `c07f5638` READY. `/housing-market` first viewport is live figures and the chart atom (Instrument + V3Chart year overlay). CTA Value my home. v3 chrome kept. Looks in docs/plans/PUBLIC_PRODUCT/looks/2026-08-13-market/. Next: Track 1 §4 SEO / analytics / GSC / LLM. Track 2 not started.

2026-08-13T24:10:00Z TRACK 1 §4 SEO/LLM DIALED. llms.txt golf hub off noindex LP. Sitemap no longer submits /lp/* or /feed. Three query URLs proven live. Evidence looks/2026-08-13-seo-llm/EVIDENCE.md. Track 1 §4 done. Track 2 not started.

2026-08-14T15:35:00Z PAGE GRADE 1 (look only, dc7540e6). Sample 8/8 FAIL. Send 0. Top class: wrong-job-chrome (7 routes). Must-win TRAIL Zillow+Compass on homes/places. Cards in looks/2026-08-14-page-grade/cards/. No product code. Next: Matt says fix <class>.

2026-08-14T15:45:00Z FIX wrong-job-chrome. Chrome fills Value my home only on /sell*. Local 390: home/homes/about CTA 0, sell CTA 1. PUBLIC_UI amended. Pending regrade.

2026-08-14T20:25:00Z PAGE GRADE v2.4 locked. PUBLIC_UI §3: one shop, five place rhythms (city / neighborhood / master-plan / subdivision / listing). Rubric auto-fails 26/27. Universe + skill grind. No product code. Next: `page grade` runs the new universe.

2026-08-14T20:55:00Z PAGE GRADE 2 (v2.4 universe, prod 33c3d556). 40/40 FAIL. Send 0. One-shop PASS. Wrong-grain FAIL (city, neighborhood, master-plan, plat, zip, indexes). Top class wrong-job-chrome 39/40. Local regrade: home 390 chrome CTA gone; sell keeps it. Prod still fills seller ask. Next class after ship: cookie-on-the-thing / number-as-hero / wrong-grain.

2026-08-14T22:10:00Z FIX WAVE ALL OPEN CLASSES (local, not shipped). 7 parallel agents. Chrome seller-ask only on Sell. Cookie hidden until scroll (3s chip). About faces + boutique cut. Sell one Value ask. Home/browse Field. City/zip Field not 494-hero. Neighborhood pace then Field. Master-plan Stage then Field. Plat Ledger not 0-hero. Open houses / price drops / luxury / videos / activity Field. tsc clean. 306 wave tests pass. mockup-parity 28/28. Next: ship pathspec, then regrade universe.

2026-08-14T22:30:00Z FIX WAVE SHIP PREP. seo-shell H1s restored (open houses / price drops Layer A). shadcn burndown excludes sell/_v3 + *.test.ts. Pathspec commit on top of 7541ff16. Next: push + deploy:verify + universe regrade.
