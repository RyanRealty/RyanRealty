# Public Product OS — data atlas (P4)

Generated 2026-08-11 from 28 parallel chain agents + a completeness critic (29 agents, 0 errors). Notation: ✓ chain holds (every hop verified in code this run) · ✗ broken (gap STATEMENT follows — never a design). Chains cover the LOCKED KEEP set only.

## run-the-numbers

- ✓ **calculator-defaults-render** — migration seed 20260414210000 (writer) -> app_config (mortgage_rate/default_tax_rate_pct/insurance_rate_pct) -> getCalculatorDefaults DAL read + fraction-normalize -> mortgage/rental page seeds -> MortgageCalculator renders the rate + payment to the visitor
  evidence: supabase/migrations/20260414210000_create_app_config_and_mortgage_rate.sql (seed inserts, fractions 0.065/0.0035/0.012); lib/data/config.ts:39-42 (read), :64-76 (normalize+clamp), :79-83 (cache); app/tools/mortgage-calculator/page.tsx:74,78,84-85 (seed); app/tools/mortgage-calculator/MortgageCalculator.tsx:51,131 (rendered input)
- ✗ **calculator-defaults-freshness** — ongoing rate writer -> app_config.mortgage_rate -> getCalculatorDefaults -> visitor sees a current-market default
  evidence: Repo-wide grep this run: the ONLY code touching app_config is the read in lib/data/config.ts:40 plus gate/snapshot allowlists (scripts/check-dal-boundary.mjs:66, scripts/snapshot-schema.mjs:172); no admin surface, cron, script, or DAL write updates any of the three keys after the 2026-04-14 seed
  **GAP:** ✗ broken at writer: mortgage_rate was seeded once (0.065, April 2026) and nothing in the codebase ever updates it, so every calculator visitor is shown a default '30-yr rate' frozen at seed time as if it were current — a silent §0 accuracy exposure on the site's trust-engine tool, harming visitors who anchor a purchase decision on a stale rate
- ✓ **behavioral-trail-to-intent-signal (mortgage tool)** — KbSectionTracker client (writer) -> POST /api/visitors/track -> visitor_sessions + visitor_events insert -> getContactBehaviorSummary reads stitched sessions, derives 'mortgage-calculator user' -> ContactBehaviorPanel via PersonEngagementRegion -> PersonWorkspaceBody -> admin people/[id] page -> broker sees the intent chip
  evidence: components/site/kb/KbSectionTracker.client.tsx:14-24 (beacon), mounted app/tools/mortgage-calculator/page.tsx:89; app/api/visitors/track/route.ts:365-367 (session insert), :403-405 (event insert); lib/data/crm/getContactBehaviorSummary.ts:264-275 (identity join), :297-302 (visitor_events read), :134-138,196,213 (signal derivation); components/admin/crm/person-detail/PersonEngagementRegion.tsx:66,89; components/admin/crm/ContactBehaviorPanel.tsx:44,69-73 (renders intentSignals); components/admin/crm/person-detail/PersonWorkspaceBody.tsx:572; app/admin/(protected)/people/[id]/tools/page.tsx:101
- ✗ **intent-signal (rental + appreciation tools)** — KbSectionTracker/VisitTracker (writer) -> visitor_events (page_url = /tools/rental-property-calculator or /tools/appreciation) -> intent classification -> broker-visible signal
  evidence: components/VisitTracker.tsx:84 (only /mortgage|affordability/ maps to 'financial_tools'; both other tools fall to 'other' at :89); lib/data/crm/getContactBehaviorSummary.ts:134-138 (URL fallback matches only 'mortgage-calculator'/'/tools/mortgage'); events for the other two tools ARE written (route.ts:403-405) but never classify
  **GAP:** ✗ broken at reader (classification): rental and appreciation events are stored but never derive any intent signal, so an identified contact who underwrote a rental or ran hold-period math shows a blank intent panel — the investor and owner/seller segments, the two highest-value audiences this process exists to mark, are invisible to brokers and to every downstream prioritization built on intentSignals
- ✓ **rental-lead** — RentalLeadForm submit (writer) -> submitRentalLead server action -> sendEvent -> ensureNativeLead inserts crm_people (+contact points, +rental-calculator/investor tags via canonicallyTagLead) -> getCrmPersonFull reads crm_people -> admin person workspace renders the lead to the broker
  evidence: components/tools/RentalLeadForm.tsx:4,42 (calls action); mounted components/tools/RentalCalculator.tsx:510-513 with analysis contextNote; app/actions/lead-capture.ts:365-458 (validation :377-381, sendEvent :407-415, tags :429-445); lib/followupboss.ts:113-131 (native capture path); lib/data/crm/ensureNativeLead.ts:254-255 (crm_people insert), :265 (contact points), :334 (tag/source enrichment); app/actions/crm.ts:365 (crm_people read); app/admin/(protected)/people/[id]/tools/page.tsx:65,101 (broker surface)
- ✓ **rental-pdf** — RentalCalculator POSTs raw inputs (writer) -> no store (deliberate) -> /api/pdf/rental re-computes via analyzeRental server-side -> streamed application/pdf -> browser download in the visitor's hands
  evidence: components/tools/RentalCalculator.tsx:291-296 (POST inputs), :298-306 (blob download); app/api/pdf/rental/route.ts:22-23 (rate limit), :32-35 (price guard), :39-59 (input coercion), :61 (server recompute), :74-80 (PDF response). Storeless by design — nothing persisted, consistent with §4 of the process doc. Note: non-OK response silently no-ops (RentalCalculator.tsx:297 'if (!res.ok) return') — a rate-limited visitor sees the button do nothing

  Gap statements: 
  - app_config calculator defaults have no ongoing writer: mortgage_rate/default_tax_rate_pct/insurance_rate_pct were seeded once by migration on 2026-04-14 and no admin surface, cron, or script updates them, so calculator visitors are shown a frozen rate as the current default — a silent data-accuracy exposure on the site's highest-trust tool
  - Rental and appreciation calculator use never becomes a CRM intent signal: the categorizer and the behavior-summary matcher recognize only mortgage URLs, so the investor and owner/seller segments this process exists to mark are invisible to brokers on identified contacts
  - A rental-calculator lead lands in crm_people with no crm_tasks reminder and no broker notification (verified by reading submitRentalLead end-to-end): the broker only sees the investor lead if they happen to open that person in the CRM, so a hot lead carrying fresh cash-flow context can sit unworked

## explore-market-knowledge

- ✓ **live-pulse-figures** — sync-delta cron → runDeltaSync rpc refresh_market_pulse → market_pulse_live → getMarketPulse → /housing-market hero/lede (count, median, MoS verdict) → visitor answered above the fold
  evidence: app/api/cron/sync-delta/route.ts:2,44 → lib/sync/deltaSync.ts:605 (rpc 'refresh_market_pulse'); writer fn inserts market_pulse_live per supabase/migrations/20260415210000_enhanced_market_metrics.sql:77,248; reader lib/data/market/getMarketPulse.ts:37-48; render app/housing-market/page.tsx:145-146,218-235; cron registered vercel.json:225
- ✓ **historical-series** — refresh-market-stats cron → compute_and_cache_period_stats RPC → market_stats_cache → getPriceHistory → /housing-market/[...slug] 60-month price chart (partial month dropped) → visitor
  evidence: app/api/cron/refresh-market-stats/route.ts:15,107-227 (writes market_stats_cache via RPC, registered vercel.json:189); reader lib/data/market/getPriceHistory.ts:59-70; render app/housing-market/[...slug]/page.tsx:368-370,389-396; unknown-geo 404 guard :381-387
- ✓ **weekly-report-artifact** — market-report cron (Sun) → generateWeeklyMarketReport upsert → market_reports → getMarketReportBySlug → /housing-market/reports/[slug] page + Report JSON-LD → visitor/AI reader
  evidence: app/api/cron/market-report/route.ts:16 (vercel.json:97) → app/actions/generate-market-report.ts:285 (.from('market_reports').upsert); reader lib/data/market/getMarketReports.ts:40,61; render app/reports/[slug]/page.tsx:76-78,91-100 (notFound guard + Report schema)
- ✗ **market-narratives** — generate-market-narratives cron → upsert market_narratives → only reader is /api/reports/export (public, zero in-app callers) → NO page renders it → no human ever sees the governed narrative
  evidence: writer app/api/cron/generate-market-narratives/route.ts:31 → lib/data/market/marketNarrativeWrites.ts:107 (upsert); sole reader app/api/reports/export/route.ts:182; repo grep this run confirms reader set = writer, its int test, export route; only narrative visitors see is inline buildNarrative, subdivision branch only (app/housing-market/[...slug]/page.tsx:213-260)
  **GAP:** The governed, ci-gated narrative plane is computed daily and reaches no human: city/region visitors get no vetted 'what it means' prose at all, subdivision visitors get a separate ungoverned inline narrative — the pillar's meaning-tense work product is invisible and the two narrative planes can silently disagree for the same geography.
- ✓ **report-subscription-email** — DashboardNotificationPrefs UI → setMyReportSubscriptionAction (ensureNativeLead if no person) → crm_report_subscriptions upsert → crm-market-report-send cron reads active subs, cadence-gates, suppression fail-closed → sendEmail → subscriber inbox; email re-enters process as inception channel
  evidence: components/dashboard/DashboardNotificationPrefs.tsx:10-11,151 → app/actions/market-report-optin.ts:87-103 → lib/data/crm/reportSubscriptionSelf.ts:132-143 (upsert crm_report_subscriptions); cron app/api/cron/crm-market-report-send/route.ts:44 (vercel.json:45) → lib/data/crm/getMarketReportSubscribers.ts:196 (read) → lib/crm/market-report-send.ts:44,170 (sendEmail via lib/resend) + last_sent_at stamp :339-349
- ✓ **broker-inquiry-lead** — LeadCaptureBlock on /housing-market → submitMarketPageInquiry → submitPageCTA with ensureNativeLead fallback → crm_people row → admin CRM people list/detail → broker follows up
  evidence: app/housing-market/page.tsx:488-496 (form wire) → app/housing-market/actions.ts:43-66 (submitPageCTA + ensureNativeLead fallback) → lib/data/crm/ensureNativeLead.ts:254,310 (.from('crm_people') insert/update); reader lib/data/crm/buildCrmPeopleQuery.ts:293 (.from('crm_people')) → app/admin/(protected)/people/page.tsx (broker surface)
- ✓ **visitor-telemetry** — KbSectionTracker (section_view/scroll_depth, sendBeacon full URL) → POST /api/visitors/track → visitor_sessions upsert + visitor_events insert → getContactBehaviorSummary → PersonEngagementRegion on admin person detail → broker sees engagement trail
  evidence: components/site/kb/KbSectionTracker.client.tsx:7-27 (mounted e.g. app/reports/[slug]/page.tsx ~:104, app/housing-market/page.tsx); app/api/visitors/track/route.ts:365-367 (session insert), :404 (.from('visitor_events')); reader lib/data/crm/getContactBehaviorSummary.ts:7,16 → components/admin/crm/person-detail/PersonEngagementRegion.tsx:26,66 → PersonWorkspaceBody.tsx:58,572 → app/admin/(protected)/people/[id]/tools/page.tsx:16
- ✓ **live-activity-feed** — sync-delta cron → deltaSync inserts activity_events → getActivityEvents/getPulseFeed → /activity and /pulse pages render live ledger → visitor sees present-tense market motion
  evidence: lib/sync/deltaSync.ts:552 → lib/data/sync/syncWrites.ts:171-179 (.from('activity_events').insert); readers app/actions/activity-feed.ts:34-35 (getActivityEvents via lib/data), app/actions/pulse-feed.ts:91; render app/activity/page.tsx:106,170 and app/pulse/page.tsx:99; /pulse is sitemap-only (app/sitemap.ts:152, zero nav inbound) so its human reach is direct/sitemap traffic only

  Gap statements: 
  - market_narratives is written daily under a CI integrity gate but no human-visible surface reads it — its only reader, public /api/reports/export, has zero in-app callers — so city/region visitors get no governed narrative and subdivision visitors get a separate ungoverned inline one; the process's 'meaning' pillar output never reaches a visitor and the two narrative planes can disagree for the same geo.

## get-home-value.written-cma

- ✓ **seller lead** — submitSellerLPForm sendEvent/ensureNativeLead -> crm_people -> admin CRM surfaces + lead-flow report + broker alert queue -> broker sees/works the lead
  evidence: writer app/lp/seller-home-value/actions.ts:376-433 (sendEvent -> personId, ensureNativeLead fallback :417-433); readers lib/data/crm/getLeadIntake.ts (crm_people read feeding /admin/reports/lead-flow), lib/crm/enroll.ts:310-330 (queueBrokerAlert on new lead) -> crm_broker_alerts (lib/crm/broker-alerts.ts:181) -> app/api/cron/crm-alert-drain/route.ts:1-60 (Twilio SMS/web push to broker phone, whitelist-gated). ✓ chain holds
- ✓ **CMA build queue** — createCmaRequest -> marketing_brain_actions content:cma -> cma-build-worker -> cmas.html_content + broker ready-SMS -> broker reviews draft
  evidence: writer lib/cma-request.ts:257-327 (insert, dedup attach :334-382); reader lib/cma/worker.ts:224 (listOpenCmaActions) -> lib/cma/build.ts:889-890 (html_content persisted, html_path 'db:cmas.html_content:<slug>'); notify worker.ts:182-190 -> queueCmaReadyAlert (lib/crm/broker-alerts.ts:203, insert :181) -> crm-alert-drain route -> broker SMS; broker surface app/admin/(protected)/cmas/page.tsx:1-30 (listCmasForAdmin -> CmaBoard with approve/send actions). ✓ chain holds
- ✓ **CMA document to lead** — cmas draft (createCmaRequest) -> built html_content/render_args -> finalizeAndDeliverCma PDF email (Gmail draft in broker mailbox, human-review gate) + Matt notify -> /cma/[slug] register+consent door -> lead reads the document, doc-tracker records the open
  evidence: draft writer lib/cma-request.ts:229-248; content writer lib/cma/build.ts:889-890; finalize app/api/cma/[slug]/finalize-deliver/route.ts -> lib/cma-deliver.ts:329-343 (Gmail draft to lead w/ PDF; fallback Resend->broker :356-361, never straight to lead), Matt notify :415-421, email suppression gate :292-301; reader app/cma/[slug]/route.ts:55-162 (getCmaHtmlBySlug via lib/data/cma/documents.ts:54; publication gate :60-73, register/consent/wrong-person :79-117, immersive render from render_args :134-162, rr-doc-tracker injected :152/:180). ✓ chain holds
- ✓ **cmaLink hold-gate** — finalizeAndDeliverCma stamps crm_people.custom.cmaLink -> sequence engine %cma_link% hold -> touch email/SMS to lead carries a live URL
  evidence: writer lib/cma-deliver.ts:260-282 (stamp only when finalized); reader app/api/cron/crm-sequence-engine/route.ts:266 (email) and :362 (sms) — referencesCmaLink (lib/crm/merge.ts:366-367) holds the step until custom.cmaLink exists, merge at lib/crm/merge.ts:224,280 — then sendCrmEmail :298 / sendSms :412-413 to the lead. ✓ chain holds
- ✓ **sequence enrollment** — autoEnrollByFubId -> crm_sequence_enrollments status=running -> crm-sequence-engine -> email/SMS touch lands in the lead's inbox/phone
  evidence: writer lib/crm/enroll.ts:154-160 (insert, timeline note :163-168), invoked inline from actions.ts:604-609; reader app/api/cron/crm-sequence-engine/route.ts:61 (.from('crm_sequence_enrollments')), send :298 (sendCrmEmail) / :412-413 (Twilio). ✓ chain holds
- ✓ **SMS consent suppression** — unchecked consent box -> crm_suppressions sms/no-sms-consent -> engine + delivery chokepoints -> non-consenting lead is never texted; delivery blocked for email-suppressed
  evidence: writer lib/crm/enroll.ts:297-305 (fail-closed add, remove on later consent); readers app/api/cron/crm-sequence-engine/route.ts:201 (email gate), :315 (sms gate), :436 (fallback gate), and lib/cma-deliver.ts:292-301 (email suppression blocks CMA delivery). ✓ chain holds
- ✓ **hot-lead call task** — hot classification -> crm_tasks 5-min call task -> task queue DAL -> broker sees it on /admin/crm/tasks and /admin/today
  evidence: writer app/lp/seller-home-value/actions.ts:580-589 (createNativeTask); reader lib/data/crm/getTaskQueue.ts:198 (.from('crm_tasks')), consumed by app/admin/(protected)/crm/tasks/page.tsx and app/admin/(protected)/today/page.tsx (rg getTaskQueue). ✓ chain holds
- ✓ **valuation_requests analytics mirror** — submitSellerLPForm -> valuation_requests -> lead-flow report + dashboard -> Matt reads funnel counts
  evidence: writer app/lp/seller-home-value/actions.ts:343-356 (warn-only insert, 'Bend' city default :349); readers app/admin/(protected)/reports/lead-flow/page.tsx:269 (.from('valuation_requests'), funnel row :370) and app/actions/dashboard.ts:574. ✓ chain holds (D5 city-default data-quality smell persists inside a working chain)
- ✓ **CMA delivery SLA readout** — cmas created_at/delivered_at -> getCmaSlaSnapshot -> /admin/oversight -> Matt sees median/p90 create-to-deliver + finalized-not-sent backlog
  evidence: reader lib/data/cma/getCmaSlaSnapshot.ts:1-29; surfaced app/admin/(protected)/oversight/page.tsx:20,68,286-299 (super-admin). Partially closes the process doc's D4: measurement now exists; there is still no breach ALERT, only a pull-based readout. ✓ chain holds
- ✗ **broker routing ledger (marketing_assignments)** — recordSellerAssignment -> marketing_assignments -> NO reader -> no human ever sees the ledger
  evidence: writer app/lp/seller-home-value/actions.ts:570-576 -> lib/data/crm/recordMarketingAssignment.ts:116 (upsert); reader search: the ONLY .from('marketing_assignments') in app/ + lib/ is that writer; lib/data/crm/getLeadIntake.ts:21-24 states dashboards were migrated off it (old queries filtered a non-existent created_at and always returned 0); app/admin/(protected)/analytics/_lib/queries.ts mentions it only in a header comment
  **GAP:** ✗ broken at reader: the process's 'broker routing ledger' SoR is write-only — no dashboard, report, or broker surface reads marketing_assignments, and the in-code comment at actions.ts:569 ('dashboards read marketing_assignments') is false. Anyone auditing broker routing from this ledger (or trusting the SoR table in the process doc) reads data no surface verifies; Matt's broker-attribution questions are actually answered from crm_people, and the dead ledger plus stale comment mislead the next maintainer
- ✗ **anonymous partial address** — step-1 advance -> saveAnonymousPartialAddress -> visitor_events metadata.partial_address -> NO reader of the address -> no remarketing or human outcome
  evidence: writer components SellerLPForm step-1 -> app/lp/seller-home-value/actions.ts:118-122 -> lib/data/leads/saveAnonymousPartialAddress.ts:77-96 (event_type='seller_intent', metadata.partial_address); reader search across app/ + lib/: zero references to partial_address outside the writer. The row's generic score_delta feeds session scoring read by app/api/cron/visitor-hot-lead-escalation/route.ts:119-120 (an intent signal, not the address); the cookie-identified branch (actions.ts:126-149) does reach the CRM person page via the source:seller-lp-partial tag
  **GAP:** ✗ broken at reader: the typed address of every visitor who abandons at step 2 is stored but never read — no remarketing consumer, no broker surface, nothing. The process doc's terminal state 'lives in visitor_events for remarketing' describes a consumer that does not exist. Harm is double: the visitor's home address is collected without any use (pure data-liability), and Matt's 2026-08-11 no-save-until-contact lock (D2) is being violated for zero payoff — the unused skipPartialLead prop means every embed still fires these writes

  Gap statements: 
  - marketing_assignments is a write-only ledger: the seller-LP action still upserts it (actions.ts:570-576, recordMarketingAssignment.ts:116) but no surface reads it — dashboards migrated to crm_people (getLeadIntake.ts:21-24) — so the process doc's 'broker routing ledger' SoR and the actions.ts:569 comment both describe a reader that does not exist; broker-routing audits trusting the ledger see unverified data and maintainers are misled.
  - Anonymous partial addresses are collected and never read: visitor_events metadata.partial_address (saveAnonymousPartialAddress.ts:77-96) has zero readers in app/ or lib/, so the 'remarketing signal' the process doc claims does not exist — abandoning visitors' home addresses are stored with no consumer, and Matt's no-save-until-contact lock (D2) is violated on every embed for zero benefit because skipPartialLead is passed by no caller.
  - The 24h/7-day delivery promise still has no breach alert: the create-to-deliver SLA is now measured and visible on /admin/oversight (getCmaSlaSnapshot.ts, oversight/page.tsx:286-299, super-admin only), but nothing pushes a breach to the accountable broker — a CMA sitting past 24h is only seen if Matt happens to open oversight, and copy still says 'one business day' (process doc D4, narrowed but not closed).

## save-and-return.portal

- ✓ **saved-homes** — SaveListingButton click -> saveListing insert into saved_listings -> getSavedListingKeys read in portal render -> 'Saved homes' tile + deduped homes grid the visitor sees
  evidence: components/listing/SaveListingButton.tsx:71 (toggleSavedListing); app/actions/saved-listings.ts:47-50 (insert saved_listings, canonical key :46); app/actions/saved-listings.ts:16-21 (read) called at app/account/page.tsx:131; render app/account/page.tsx:151-157 (dedup), :231 (stat tile), :269-284 (grid)
- ✓ **rc7-save-resume** — logged-out Save bounce -> sessionStorage stash -> useResumePendingSave consumes on return -> resumeSaveListing add-only insert into saved_listings -> same portal read -> home appears saved
  evidence: components/listing/SaveListingButton.tsx:72-76 (redirectToLoginForSave on 'Not signed in'), :60-67 (hook wiring); lib/hooks/useResumePendingSave.ts:33-42 (consume, re-stash on needsAuth, call resumeSaveListing); app/actions/saved-listings.ts:82-93 (idempotent add-only insert, :90 real error is not auth); reader app/actions/saved-listings.ts:16-21 -> app/account/page.tsx:131
- ✓ **likes** — heart click -> likeListing insert into likes -> getLikedListingKeys via getDashboardLikesData in portal render -> unioned into the same homes grid
  evidence: app/actions/likes.ts:37-40 (insert likes); app/actions/likes.ts:12-17 (read) via app/actions/dashboard-likes.ts:42, called at app/account/page.tsx:130; union render app/account/page.tsx:153-157
- ✓ **alerts-saved-searches** — createSavedSearch -> upsertListingAlert into listing_alerts -> getListingAlertsForUser -> alerts tab, 'Alerts on' tile, and SavedSearchControls manager
  evidence: app/actions/saved-searches.ts:139-145 (upsert call); lib/data/leads/listingAlerts.ts:25 (TABLE='listing_alerts'), :159 (upsert); lib/data/leads/listingAlertsUser.ts:80-84 (read by user_id) via app/actions/saved-searches.ts:97, called at app/account/page.tsx:132; render app/account/page.tsx:216 (activeAlerts), :232 (tile), :434 (SavedSearchControls)
- ✓ **new-since-baseline** — MarkAllSeenButton -> markAllSavedSearchesSeen writes listing_alerts.last_viewed_at -> read back on next render -> newSinceBaseline -> 'New since your last visit' tile and per-search badges reset
  evidence: components/account/portal/MarkAllSeenButton.tsx:12,23 (calls action); app/actions/saved-searches.ts:340-345 -> lib/data/leads/listingAlertsUser.ts:240-252 (update last_viewed_at by user_id); read back app/actions/saved-searches.ts:120 (last_viewed_at) -> app/account/portal-data.ts:76-79 (newSinceBaseline) -> app/account/page.tsx:233 (tile), :576 (tab badge)
- ✓ **named-areas** — map-draw save action -> createAreaForUser insert into search_areas -> listAreasForUser in portal render -> 'Named areas' tile and AreaControls panel
  evidence: app/actions/search-areas.ts:67 (calls createAreaForUser); lib/data/areas/searchAreas.ts:129-136 (insert, TABLE='search_areas' :24); lib/data/areas/searchAreas.ts:67-71 (read by owner_user_id) called at app/account/page.tsx:138; render app/account/page.tsx:221-228 (rows), :234 (tile), :465 (AreaControls)
- ✓ **places-followed** — toggleSavedCity / saveCommunity inserts into saved_cities / saved_communities -> getSavedCitySlugs + getSavedCommunityKeys in portal render -> 'Places you follow' tile and deep-linking chips
  evidence: app/actions/saved-cities.ts:54-57 (insert), :9-14 (read); app/actions/saved-communities.ts:37-40 (insert), :10-15 (read); called at app/account/page.tsx:133-134; render app/account/page.tsx:213 (placesCount), :235 (tile), :328-345 (chips linking /cities/<slug> and /homes-for-sale/...)
- ✓ **hidden-and-collections** — hide/collection actions write hidden_listings / listing_collections -> getPortalHomeLists composition read -> hidden count card and collections list on the Homes tab
  evidence: app/actions/hidden-listings.ts:35,57 (writes hidden_listings); app/actions/collections.ts:34,73,85 (writes listing_collections); app/account/portal-data.ts:128-131 (reads both via the owning action modules) called at app/account/page.tsx:135,143; render app/account/page.tsx:516-527 (collections), :540 (hidden count)
- ✓ **buying-preferences** — setBuyingPreferences upsert into user_buying_preferences -> getBuyingPreferences in portal render -> monthly-payment estimate on every listing tile and the settings card summary
  evidence: app/actions/buying-preferences.ts:68 (upsert user_buying_preferences), :20-25 (read) called at app/account/page.tsx:137; render app/account/page.tsx:195-208 (monthlyFor), :278 (tile payment), :382-384 (settings summary)
- ✓ **signed-in-activity-trail** — VisitTracker fires trackUserEvent -> insert into user_events with session user_id -> getUserActivityEvents + getRecentListingViews in portal render -> Recently viewed strip and Activity feed
  evidence: components/VisitTracker.tsx:285 (trackUserEvent listing_view); app/actions/track-user-event.ts:32-33 (insert user_events, user_id from session); readers lib/data/activity/getUserEvents.ts:68-74 and app/actions/dashboard-history.ts:23-30, called at app/account/page.tsx:136,140-141; render app/account/page.tsx:288-322 (Recently viewed), :548-557 (ActivityFeed)
- ✗ **anonymous-trail-joins-account** — logged-out browsing writes user_events with user_id=null -> identity stitch at sign-in -> portal activity readers -> visitor sees pre-sign-in trail
  evidence: app/actions/track-user-event.ts:32-33; lib/visitor-backfill.ts:56-104 (stitch writes visitor_* only); lib/data/activity/getUserEvents.ts:71; app/actions/dashboard-history.ts:26
  **GAP:** The anonymous half of the viewing trail never reaches the account: user_events rows written while logged out carry user_id=null (app/actions/track-user-event.ts:33), the sign-in stitch touches only visitor_identity_map/visitor_sessions/visitor_events and never backfills user_events.user_id (lib/visitor-backfill.ts:77,104,230), and every portal reader filters .eq('user_id', uid) (lib/data/activity/getUserEvents.ts:71; app/actions/dashboard-history.ts:26). The freshly converted account holder — the RC7 visitor the whole inception funnel exists to create — lands on a portal whose Recently viewed and Activity are empty, contradicting the process's resume-where-you-left-off promise at the exact moment of conversion.
- ✓ **identity-bind-to-broker** — OAuth callback stamps rr_pid from crm_people + trackSignedInUser ensures native lead -> broker opens admin person portal view -> reads the client's user_events activity -> broker sees the identified relationship
  evidence: app/auth/callback/route.ts:154,180 (stampPersonIdFromEmail), :41-51 (personIdsByEmailCi on crm_people + rr_pid cookie), :55-61 (stitchVisitorIdentity), :136-141 (trackSignedInUser); lib/followupboss.ts:129-130 (ensureNativeLead -> crm_people); broker reader app/admin/(protected)/people/[id]/portal/page.tsx:6,64 (getClientPortalView) -> lib/data/crm/getClientPortalView.ts:493-500 (reads user_events by the person's user ids)
- ✓ **save-crm-behavior-mirror** — saveListing/likeListing emit recordSaveListingEvent -> insert into visitor_events (GPC + session gated) -> getContactBehaviorSummary -> PersonEngagementRegion in the admin CRM person workspace -> broker sees the save/like on the contact's behavior panel
  evidence: app/actions/saved-listings.ts:59-67 (emitSaveEvent), app/actions/likes.ts:44-50; lib/data/crm/recordSaveListingEvent.ts:65 (insert visitor_events); lib/data/crm/getContactBehaviorSummary.ts:298 (reads visitor_events); components/admin/crm/person-detail/PersonEngagementRegion.tsx:66 (calls the summary); components/admin/crm/person-detail/PersonWorkspaceBody.tsx:58,572 (mounts the region)
- ✓ **guest-alert-claim** — verified sign-in -> claimGuestSavedSearches stamps user_id onto email-keyed listing_alerts rows -> getListingAlertsForUser -> claimed searches render in the portal alerts panel
  evidence: app/auth/callback/route.ts:77-86,157,183 (verified-email gate + call); lib/data/savedSearches.ts:45-50 -> lib/data/leads/listingAlertsUser.ts:52-61 (update user_id where email matches, user_id null, is_active); lib/data/leads/listingAlertsUser.ts:80-84 (read by user_id) -> app/account/page.tsx:132,434

  Gap statements: 
  - Anonymous pre-sign-in browsing never joins the account: user_events rows written logged-out carry user_id=null and the sign-in identity stitch backfills only the visitor_* tables, while every portal activity reader filters by user_id — so the just-converted RC7 visitor opens a portal with an empty Recently viewed and Activity feed, breaking the resume-where-you-left-off promise at the exact moment of conversion.
  - The activity DAL's own contract misattributes its writer: lib/data/activity/getUserEvents.ts:9 credits TrackListingView with writing listing_view rows, but components/tracking/TrackListingView.tsx writes only GA4/Meta/dataLayer + a cookie — the real user_events writer is components/VisitTracker.tsx:285. A maintainer refactoring VisitTracker on the strength of the DAL docstring could silently empty the portal's Recently viewed and Activity surfaces for every account holder.

## plan-a-sale

- ✓ **lead-identity (crm_people)** — submitSellerLPForm sendEvent/ensureNativeLead -> public.crm_people -> getInboxContactCard -> /admin/people/[id] person workspace the broker works
  evidence: app/lp/seller-home-value/actions.ts:395-433 (capture + native fallback); lib/data/crm/ensureNativeLead.ts:158,254,310,334 (crm_people insert/update); lib/data/crm/getInboxThread.ts:40 (.from('crm_people')); app/admin/(protected)/people/[id]/page.tsx:129 (getInboxContactCard render)
- ✓ **sequence-enrollment (crm_sequence_enrollments)** — autoEnrollByFubId -> public.crm_sequence_enrollments -> crm-sequence-engine cron reads due rows -> sendCrmEmail / Twilio SMS -> touch lands in the seller's inbox/phone
  evidence: app/lp/seller-home-value/actions.ts:604-609 (enroll call); lib/crm/enroll.ts:154,212 (enrollment inserts), :268-306 (SMS fail-closed consent); app/api/cron/crm-sequence-engine/route.ts:61 (due-enrollment read), :298 (sendCrmEmail), :28,310-395 (Twilio SMS path); vercel.json:57 (cron registered). Note: crm-scheduled-sends is NOT this chain — it is the admin cohort send-later queue (app/api/cron/crm-scheduled-sends/route.ts:1-16); sequence delivery is inline in the engine
- ✓ **cma-draft (cmas)** — createCmaRequest -> public.cmas draft row -> listCmasForAdmin -> /admin/cmas worklist where the broker approves and sendCmaToLeadAction emails the seller
  evidence: app/lp/seller-home-value/actions.ts:611-629 (call); lib/cma-request.ts:193-249 (upsertCmaRowBySlug/updateCmaRowFieldsBySlug, status='draft'); lib/data/sync/syncWrites.ts:839,850,864-871 (cmas insert + listCmasForAdmin read); app/admin/(protected)/cmas/page.tsx:36-41 (listCmasForAdmin import + approveCmaAction/sendCmaToLeadAction wired into CmaBoard)
- ✓ **cma-production-queue (marketing_brain_actions)** — createCmaRequest -> marketing_brain_actions content:cma pending -> producer-dispatcher flips to in_production -> producer-runtime executes the CMA producer -> broker reviews the built CMA in /admin/cmas
  evidence: lib/cma-request.ts:258,331-340 (action-row insert with open-row unique index); app/api/cron/producer-dispatcher/route.ts:4,69,115 (pending read + status flip); app/api/cron/producer-runtime/route.ts:4,138,206,261,316 (in_production read + execution); vercel.json:169,173 (both crons registered); human surface app/admin/(protected)/cmas/page.tsx:36-41
- ✓ **valuation-submissions (valuation_requests)** — seller-LP action + insertValuationRequest -> public.valuation_requests -> lead-flow report count -> 'Form submits' funnel row Matt reads
  evidence: app/lp/seller-home-value/actions.ts:343-359 (direct insert); lib/data/sync/syncWrites.ts:777-781 (insertValuationRequest for /sell/valuation via app/home-valuation/actions.ts); app/admin/(protected)/reports/lead-flow/page.tsx:269-272 (count read), :370 ('Form submits' stage render)
- ✗ **anonymous-partial-address (visitor_events)** — saveSellerPartialLead -> visitor_events seller_intent row with metadata.partial_address -> NO reader of the address -> no human ever sees it
  evidence: lib/data/leads/saveAnonymousPartialAddress.ts:77-96 (write, metadata.partial_address); repo-wide grep this run: partial_address appears only in the writer; lib/data/crm/getContactBehaviorSummary.ts:142 reads only page_category (surfaced at components/admin/crm/person-detail/PersonEngagementRegion.tsx:66,89 post-identification)
  **GAP:** The typed address a seller abandons at step 1 is written to visitor_events and read by nothing — no surface, cron, or person ever sees it. The brokerage stores pre-consent home addresses with zero utility (a privacy liability with no payoff), the abandoning seller gets no follow-up value, and the write itself fires on /sell against the recorded no-orphan-saves decision (D3).
- ✗ **broker-assignment-ledger (marketing_assignments)** — recordSellerAssignment -> public.marketing_assignments -> no live reader (dashboard reads decommissioned) -> no human outcome
  evidence: app/lp/seller-home-value/actions.ts:569-576 (write on every submit); lib/data/crm/recordMarketingAssignment.ts:2,110 ('the ONE write'); lib/data/crm/getLeadIntake.ts:21-24 ('replaced every marketing_assignments read on the dashboards'); app/admin/(protected)/reports/lead-flow/page.tsx:17 ('marketing_assignments is decommissioned'); repo grep this run finds no app-surface read
  **GAP:** Every seller submit writes an assignment-ledger row that no dashboard, report, or admin surface reads — the in-code comment 'dashboards read marketing_assignments' (actions.ts:569) is false. Anyone auditing broker-routing fairness or round-robin behavior would trust a ledger nothing verifies, and the write is dead weight on the capture path.
- ✓ **matt-alert-email** — submitSellerLPForm -> sendSellerLeadAlertEmail -> Resend email -> Matt's inbox on every seller submission
  evidence: app/lp/seller-home-value/actions.ts:637-659 (fire-and-forget call with UTM capture); lib/seller-lead-alert.ts:50 (builder), :111-118 (sendEmail to ALERT_TO). Degraded CTA: :36-39 + :93 — the only action button links to app.followupboss.com/people/<id> using the NATIVE crm_people id, and FUB was decommissioned 2026-06-24

  Gap statements: 
  - Anonymous partial addresses (visitor_events metadata.partial_address) are written on step-1 abandon and read by nothing — pre-consent home addresses stored with zero utility, no follow-up value to the seller, and the write violates the recorded no-orphan-saves decision (D3).
  - marketing_assignments is written on every seller submit but has no live reader — dashboards moved to getLeadIntake and declare the table decommissioned, so the broker-assignment audit trail the code comment promises does not exist as a verified surface.
  - The always-on Matt seller-lead alert's only CTA links to app.followupboss.com with a native crm_people id — FUB is decommissioned, so Matt's one-click path to the lead is dead and he must manually locate the person in /admin/people.
  - The process doc's §3/§5 claim that the crm-scheduled-sends cron delivers sequence touches is wrong: that cron is the admin cohort send-later queue (app/api/cron/crm-scheduled-sends/route.ts:1-16); sequence delivery happens inline in crm-sequence-engine — the doc's delivery-hop attribution should be corrected so future audits watch the right cron.

## plan-a-purchase

- ✓ **named-buyer-lead** — LP form submit → submitLeadLandingForm → sendEvent → ensureNativeLead writes crm_people + crm_contact_points → listCrmPeople reads crm_people → /admin/crm people list renders for the broker
  evidence: app/actions/lead-landing.ts:84-99 (sendEvent call, intent=/timeframe= message :76-91); lib/followupboss.ts:129-131 (ensureNativeLead call, returns personId); lib/data/crm/ensureNativeLead.ts:254 (crm_people insert), :265 (crm_contact_points insert); fallback path lead-landing.ts:106-123 (fub-fallback tag, same store); app/actions/crm.ts:133,154 (listCrmPeople .from('crm_people')); app/admin/(protected)/crm/page.tsx:16,133 (page consumes listCrmPeople + getPeopleListSignals)
- ✓ **buyer-sequence-touches** — canonicallyTagLead auto-enroll (+ 15-min crm-auto-enroll catch-all reading crm_people) → crm_sequence_enrollments insert → crm-sequence-engine cron reads active enrollments → sendCrmEmail / sendSms → the lead's inbox/phone
  evidence: lib/canonical-lead-tagger.ts:266-270 (fire-and-forget autoEnrollByFubId; compliance hard-stop skip :229-232); app/api/cron/crm-auto-enroll/route.ts:13,64 (reads crm_people since epoch → autoEnrollPerson); lib/crm/enroll.ts:154,212 (crm_sequence_enrollments inserts); app/api/cron/crm-sequence-engine/route.ts:61-64 (reads enrollments joined to active crm_sequences), :298,443 (sendCrmEmail via lib/crm/gmail), :28 (sendSms via lib/crm/twilio)
- ✓ **guest-listing-alert** — /buy alerts band email → submitSearchAlertSignup → upsertListingAlert writes listing_alerts → saved-search-alerts cron → getActiveListingAlertsDue → sendAlertEmailToRecipients → Resend → the guest's inbox
  evidence: app/actions/search-alert-capture.ts:142 (must-succeed upsertListingAlert); lib/data/leads/listingAlerts.ts:150-159 (upsert into listing_alerts), :282-292 (getActiveListingAlertsDue read); app/api/cron/saved-search-alerts/route.ts:2,29 (runListingAlerts); app/actions/saved-search-alerts.ts:127 (reads due rows), :405 (sendAlertEmailToRecipients); lib/alerts/send.ts:20,437 (sendEmail call); lib/resend.ts:66 (client.emails.send)
- ✓ **alert-broker-task** — Alert signup → createNativeTask writes crm_tasks (5-min due) → getTaskQueue reads crm_tasks → /admin/crm/tasks broker-scoped view → broker acts
  evidence: app/actions/search-alert-capture.ts:130-135 (createNativeTask, dueInMinutes 5); lib/data/crm/ensureNativeLead.ts:464 (crm_tasks insert); lib/data/crm/getTaskQueue.ts:198,242 (crm_tasks reads); app/admin/(protected)/crm/tasks/page.tsx:9-17 (Today/Overdue/Future view, broker-pinned at the data layer via getTaskQueue)
- ✗ **lp-broker-notification** — LP submit → sendContactNotification → Resend email → ADMIN_EMAIL inbox (assigned broker never in the recipient chain; no crm_tasks row for LP leads)
  evidence: app/actions/lead-landing.ts:151-157 (only notification side effect, no createNativeTask call anywhere in the action); lib/resend.ts:101 (ADMIN_EMAIL env), :110,118-123 (sends to ADMIN_EMAIL only), :66 (client.emails.send); contrast lib/canonical-lead-tagger.ts:235,256-263 (pickBroker + recordAssignment resolve an assigned broker the notification never reads)
  **GAP:** The richest lead this process produces — a named buyer with stated intent and timeline — notifies only the static admin inbox and writes no crm_tasks reminder, while the weaker alert-signup gets a 5-minute task. A lead assigned to Rebecca or Paul reaches that broker only if they poll /admin/crm; an unwatched admin inbox means the hottest buyer lead goes cold with no timer and no accountable surface.
- ✓ **session-stitch-attribution** — Submit carries sessionId → backfillSessionToFub marks visitor_sessions identified (identified_via='form_submit') → ad-roi dashboard reads identified_at/fub_person_id and renders 'matched to a name' → Matt
  evidence: app/actions/lead-landing.ts:178-185 (UUID-gated stitch); lib/visitor-backfill.ts:205 (identified_via write on visitor_sessions); app/admin/(protected)/analytics/ad-roi/page.tsx:127-128 (.from('visitor_sessions').select('...identified_at, fub_person_id...')), :215 (counts identified), :264 (matched-to-a-name rendering)
- ✓ **behavioral-trail** — KbSectionTracker section/scroll beacons → /api/visitors/track writes visitor_sessions + visitor_events → first-party analytics surfaces read visitor_sessions → Matt's dashboards
  evidence: components/site/kb/KbSectionTracker.client.tsx:15-19 (sendBeacon/fetch to /api/visitors/track); mounted app/buy/page.tsx:168 and components/landing/LeadLandingPage.tsx:53 (per process doc, re-cited); app/api/visitors/track/route.ts:365-367 (visitor_sessions insert), :404 (visitor_events insert); app/admin/(protected)/analytics/page.tsx:92 (first-party visitor_sessions = primary traffic), ad-roi/page.tsx:127-128 (reads)
- ✓ **buy-hero-imagery** — Approved asset store public.asset_library → getSurfaceImage DAL (A/B-grade only, cached) → /buy hero render
  evidence: lib/data/media/getSurfaceImages.ts:28 (.from('asset_library')), :33 (grade filter), :56 (cache window); exported lib/data/index.ts:238; consumed app/buy/page.tsx:28,160
- ✗ **intent-lp-hero-imagery** — No internal writer/store — heroImageUrl is a hardcoded images.unsplash.com URL → priority next/image renders it as the LP's LCP element
  evidence: lib/lead-landing-content.ts:172,218,263 (three Unsplash hotlinks, verified this run); rendered as the hero per components/landing/LeadLandingPage.tsx:68-77 (PDS-proven renderer hop)
  **GAP:** The LCP hero of all three paid-capable buyer LPs lives on a third party no Ryan Realty system writes or monitors. If Unsplash rate-limits, removes the photo, or blocks hotlinking, every ad click lands on a blank hero — paid spend burns on a broken first impression and nothing internal alerts anyone, because the asset was never in the asset_library SoR that /buy itself uses.

  Gap statements: 
  - Intent-LP leads (the process's strongest signal) have a broken accountable-broker chain: notification goes only to the static ADMIN_EMAIL and no crm_tasks reminder is written, so a lead assigned to Rebecca or Paul surfaces to that broker only via passive CRM polling — the hottest buyer lead can go cold with no timer, while the weaker alert-signup path gets a 5-minute task.
  - All three intent-LP heroes hotlink Unsplash with no internal store, writer, or monitor — the paid landing pages' LCP element depends on a third party that can fail silently, burning ad spend on a blank hero with no internal signal, unlike /buy which serves from the asset_library SoR.

## evaluate-a-place

- ✓ **inventory-tiles** — Spark delta sync upserts listings -> pg_cron refreshes listing_tile_mv -> getListingTiles DAL -> city/zip hero counts + PlaceMapListSplit pins render
  evidence: app/api/cron/sync-delta/route.ts:8-11 + vercel.json:225-227; lib/sync/deltaSync.ts:524; lib/data/sync/syncWrites.ts:122-125 (.from('listings').upsert); supabase/migrations/20260731140000_split_mv_refresh_jobs.sql:38-43 (pg_cron refresh_listing_tile_mv_30min); lib/data/listings/getListingTiles.ts:2-5 (reads listing_tile_mv); render per PDS app/cities/[slug]/page.tsx:193,484-495 + app/zip/[zip]/page.tsx:183-188
- ✓ **market-pulse** — delta sync calls refresh_market_pulse() -> INSERT market_pulse_live -> getMarketPulse -> hero 30-day pulse figures on city/neighborhood grains
  evidence: lib/sync/deltaSync.ts:605 (rpc refresh_market_pulse + refresh_community_market_pulse); supabase/migrations/20260526140535_refresh_market_pulse_advisory_lock.sql:277 (INSERT INTO public.market_pulse_live); lib/data/market/getMarketPulse.ts:4,26-31 (reads market_pulse_live); render per PDS app/cities/[slug]/page.tsx:171
- ✓ **market-stats** — refresh-market-stats cron -> compute_and_cache_period_stats RPC writes market_stats_cache -> getMarketStats/getPriceHistory -> neighborhood/community trend charts
  evidence: vercel.json:189; app/api/cron/refresh-market-stats/route.ts:127-133,163-168 (rpc writes rolling/monthly/quarterly/ytd rows), :236 revalidateTag; lib/data/market/getPriceHistory.ts:60 (.from('market_stats_cache')); render per PDS app/cities/[slug]/[neighborhoodSlug]/page.tsx:162-165
- ✓ **geo-snapshot** — pg_cron refresh_geo_snapshot_mv -> geo_snapshot_mv -> getGeoSnapshot -> city identity fallback + Place/Dataset JSON-LD in page head
  evidence: supabase/migrations/20260731140000_split_mv_refresh_jobs.sql:24-29 (cron.schedule -> refresh_geo_snapshot_mv()); lib/data/geo/getGeoSnapshot.ts:2,126 (reads geo_snapshot_mv); render per PDS app/cities/[slug]/page.tsx:158,411-449
- ✓ **boundaries** — GIS import scripts write boundaries/neighborhoods -> listings_in_boundary RPC via getGeoBoundaryMapData -> map polygon + boundary-scoped pin counts render; refresh-market-stats also enumerates neighborhood slugs from boundaries
  evidence: scripts/seo-import-bend-neighborhood-boundaries.mjs:5,99 (writes public.boundaries geo_type='neighborhood' + neighborhoods rows); lib/data/geo/getGeoBoundaryMapData.ts:66-82 (rpc listings_in_boundary, throws on error); app/api/cron/refresh-market-stats/route.ts:62-65 (getBoundariesByGeoType); render per PDS app/communities/[slug]/page.tsx:509-524
- ✓ **alert-capture-to-email** — KbCommunityAlerts form -> submitSearchAlertSignup -> listing_alerts upsert -> saved-search-alerts hourly cron -> getActiveListingAlertsDue -> sendAlertEmailToRecipients (Resend) -> alert email in visitor inbox
  evidence: components/site/kb/KbCommunityAlerts.client.tsx:8,66; app/actions/search-alert-capture.ts:142; lib/data/leads/listingAlerts.ts:25 (TABLE='listing_alerts'),159 (upsert); vercel.json:213 + app/api/cron/saved-search-alerts/route.ts:29 (runListingAlerts); lib/data/leads/listingAlerts.ts:282-290 (due-scan read); app/actions/saved-search-alerts.ts:405,506 -> lib/alerts/send.ts:20,437 (sendEmail via Resend)
- ✓ **lead-to-broker-digest** — sendEvent -> ensureNativeLead inserts crm_people + crm_contact_points -> getBrokerDigest reads crm_people -> daily-broker-digest cron emails each broker
  evidence: app/actions/search-alert-capture.ts:103-111; lib/data/crm/ensureNativeLead.ts:255 (crm_people insert),265 (crm_contact_points); lib/data/crm/getBrokerDigest.ts:11-13; app/api/cron/daily-broker-digest/route.ts:29,107,142 (getBrokerDigest -> sendEmail); vercel.json:65
- ✓ **broker-nudge-task** — createNativeTask inserts crm_tasks (5-min due) -> crm-task-reminders cron reads open tasks -> queueBrokerAlert queues crm_broker_alerts -> SMS/push drain reaches broker phone
  evidence: app/actions/search-alert-capture.ts:130-135; lib/data/crm/ensureNativeLead.ts:450,464-472 (crm_tasks insert); vercel.json:61 + app/api/cron/crm-task-reminders/route.ts:67 (crm_tasks read, groups by assigned_broker); lib/crm/broker-alerts.ts:141-188 (crm_broker_alerts insert, status pending/push_only for drainers)
- ✓ **telemetry-to-owner** — page trackers + fireLeadGenerated write GA4 events -> GA4 property -> marketing-snapshot-ga4 cron ingests to marts -> marketing digests to Matt
  evidence: app/actions/search-alert-capture.ts:146-153; lib/ga4-measurement-protocol.ts:32,141 (POST /mp/collect); app/api/cron/marketing-snapshot-ga4/route.ts:3-16 (getGA4Summary ingest); tracker mounts per PDS app/cities/[slug]/page.tsx:453-460 — but per-grain device mix and field CWV have never been read back for this process (PDS §3/§8 gap)
- ✗ **drawn-area** — broker map tool -> createAreaForUser inserts search_areas -> getPublicAreaBySlug (is_public=true) -> /areas/[slug] page render -> visitor
  evidence: app/actions/search-areas.ts (writer action); lib/data/areas/searchAreas.ts:24 (TABLE='search_areas'),125-140 (insert),259-269 (public read); page render per PDS app/areas/[slug]/page.tsx:129-134; but app/sitemap.ts has ZERO /areas emission (case-insensitive grep this run), the Areas nav omits it (lib/site-nav.ts:111-131 per PDS), and no index page exists
  **GAP:** The drawn-area grain writes and renders correctly but no human can find it: /areas/[slug] is absent from the sitemap, the nav, and every index, so a broker who publishes a curated area produces a page with no inbound path — the work is invisible to visitors and the family may be serving zero humans (live search_areas row count still unqueried).

  Gap statements: 
  - Drawn-area pages (/areas/[slug]) have no inbound path — not sitemap-emitted, not in nav, no index — so broker-published areas reach no visitor and the family may serve zero humans; live public search_areas row count remains unqueried.
  - market_pulse_live carries no ZIP rows and misses some neighborhood scopes, so the ZIP and neighborhood grains answer 'how fast does it move' with weaker or city-relabeled figures — the visitor on the two finest-grain entry surfaces gets diluted local truth.
  - GA4 per-grain device mix and field CWV for these routes have never been read back — the widest organic surface on the site is being reshaped without its measured audience or real performance numbers.
  - Community boundary quality is a baseline of known-bad polygons rather than corrected geometry — resort visitors on flagged slugs get no map polygon and no boundary-derived count, degrading the inventory-proof step for exactly the pages second-home buyers land on.

## find-a-home

- ✓ **live-inventory-browse** — Spark delta-sync cron -> listings table -> listing_search_mv (pg_cron refresh) -> searchListingsAll/getViewportSearch -> /homes-for-sale split view renders live homes to the visitor
  evidence: vercel.json:225-227 (sync-delta schedule); app/api/cron/sync-delta/route.ts:2 (runDeltaSync); lib/sync/deltaSync.ts:524 (upsertListingRows per chunk); lib/data/sync/syncWrites.ts:122-124 (from('listings').upsert onConflict ListNumber); supabase/migrations/20260731170000_listing_search_mv_v4.sql:107 (cron.schedule MV refresh); lib/data/listings/searchListingsAll.ts:605 (from('listing_search_mv').select); app/actions/search.ts:246-251,329-348 (getViewportSearch -> searchListingsAll); app/search/page.tsx:272 (viewport fetch), :420 (MapSearchView render)
- ✓ **guest-alert-capture-to-email (path B)** — SearchAlertCapture form -> submitSearchAlertSignup -> listing_alerts row -> hourly saved-search-alerts cron reads due rows -> Resend email lands in the buyer's inbox
  evidence: app/search/page.tsx:60,397 (mount); components/search/SearchAlertCapture.tsx:12,283 (calls submitSearchAlertSignup); app/actions/search-alert-capture.ts:142 (upsertListingAlert is the must-succeed write); lib/data/leads/listingAlerts.ts:25,159-174 (upsert into listing_alerts); vercel.json:213-214 (hourly cron); app/api/cron/saved-search-alerts/route.ts:29 (runListingAlerts); app/actions/saved-search-alerts.ts:127 (getActiveListingAlertsDue), :405 (sendAlertEmailToRecipients); lib/data/leads/listingAlerts.ts:282-296 (due-row read); lib/alerts/send.ts:20,365,437 (sendEmail via lib/resend)
- ✓ **tour-contact-lead (path A)** — submitContactForm -> sendEvent/ensureNativeLead -> crm_people row -> getInboxContactCard read on the v2 person page -> broker works the lead
  evidence: app/contact/actions.ts:93-112 (sendEvent with listing-tagged message), :118-130 (direct ensureNativeLead fallback); lib/followupboss.ts:103-109 (sendEvent -> ensureNativeLead, returns native personId); lib/data/crm/ensureNativeLead.ts:254 (from('crm_people') insert); lib/data/crm/getInboxThread.ts:34,40 (from('crm_people') read); app/admin/(protected)/people/[id]/page.tsx:19,129 (getInboxContactCard renders the person to the broker)
- ✓ **alert-signup-broker-task** — submitSearchAlertSignup -> createNativeTask -> crm_tasks row -> getTaskQueue -> /admin/today + /admin/oversight broker task queue
  evidence: app/actions/search-alert-capture.ts:130-135 (createNativeTask, 5-min due, awaited); lib/data/crm/ensureNativeLead.ts:444,464 (from('crm_tasks').insert); lib/data/crm/getTaskQueue.ts:198 (from('crm_tasks') read); app/admin/(protected)/today/page.tsx:10,48 and app/admin/(protected)/oversight/page.tsx:16,64 (broker-visible queue render)
- ✓ **signed-in-save (path C)** — saveListing server action -> saved_listings row (canonical ListingKey) -> getSavedListingKeys hydration on search -> visitor sees badged saved tiles
  evidence: app/actions/saved-listings.ts:46-50 (resolveCanonicalListingKey then from('saved_listings').insert), :12-21 (getSavedListingKeys read); app/search/page.tsx:245-251 (session-gated hydration, 1.5s timeout), :410,:426 (savedListingKeys passed into grid + MapSearchView render)
- ✓ **first-party-warmth-trail** — ListingTracker scroll/save events -> POST /api/visitors/track -> visitor_events -> getContactBehaviorSummary -> PersonEngagementRegion on the CRM person workspace -> broker sees warmth
  evidence: components/listing/ListingTracker.tsx:60-64 (fireFirstPartyEvent scroll_depth); components/VisitTracker.tsx:198,226 (POST /api/visitors/track); app/api/visitors/track/route.ts:403-405 (from('visitor_events').insert); lib/data/crm/getContactBehaviorSummary.ts:7,16 (reads visitor_sessions + visitor_events); components/admin/crm/person-detail/PersonEngagementRegion.tsx:26,66 (calls the summary); components/admin/crm/person-detail/PersonWorkspaceBody.tsx:58,572 (mounted on the person workspace)
- ✗ **per-listing-view-counter** — ListingTracker -> POST /api/listings/<key>/track -> engagement_metrics.view_count -> (no live reader) -> no human surface
  evidence: Writer proven: components/listing/ListingTracker.tsx:45; app/api/listings/[listingKey]/track/route.ts:32; lib/data/engagement/index.ts:120-145,152 (view_count bump). Reader hop dead-ends: getTopViewedListingKeys (engagement/index.ts:180-192) is consumed only by app/actions/home.ts:33 getFeaturedListings, which has ZERO importers — app/page.tsx builds featured via resolveFeaturedItems/curateFeaturedTiles with no engagement read; sumEngagementForListingKeys (engagement/index.ts:159) has no consumers; app/actions/listing-detail.ts:877-1065 loads engagement but its only page consumer app/listing/by-key/[listingKey]/page.tsx permanentRedirects before rendering; DemandIndicators.tsx and ShowcaseStickyBar.tsx are mounted nowhere; app/reports/page.tsx:354-356 renders like/save/share and omits view_count; admin properties report viewCount is CRM inquiries, not this table.
  **GAP:** Every listing-detail visit spends a service-role write recording demand that no visitor, broker, or admin surface ever reads: the most-viewed/demand signal on live inventory is dark, brokers cannot see which homes buyers are actually watching, and the homepage 'featured' rail silently stopped reflecting real engagement when app/page.tsx moved off getFeaturedListings — leaving the writer running for a consumer that no longer exists.

  Gap statements: 
  - engagement_metrics.view_count is write-only: the per-listing view counter fires on every listing-detail visit but no live surface (public, broker, or admin) reads it — buyers' demand signal is invisible to brokers and the homepage featured rail no longer reflects engagement, while the dead consumers (app/actions/home.ts getFeaturedListings, sumEngagementForListingKeys, DemandIndicators, ShowcaseStickyBar, the by-key page's engagement payload) sit orphaned in the tree

## save-and-return.search-alerts

- ✓ **subscription-save-to-alert-email** — SaveSearchButton form submit -> createSavedSearch -> upsertListingAlert writes listing_alerts -> hourly cron reads getActiveListingAlertsDue -> runListingAlerts diff/gates -> sendAlertEmailToRecipients -> Resend email lands in the subscriber's inbox
  evidence: components/SaveSearchButton.tsx:239-291; app/actions/saved-searches.ts:124-146; lib/data/leads/listingAlerts.ts:150-182 (upsert), :282-296 (due read); vercel.json:213-214 (0 * * * *); app/api/cron/saved-search-alerts/route.ts:16-30; app/actions/saved-search-alerts.ts:111-206; lib/alerts/send.ts:365-453; lib/resend.ts:48
- ✓ **manage-surface** — listing_alerts row -> getSavedSearches (getListingAlertsForUser) -> /account/saved-searches renders SavedSearchControls -> subscriber pauses/edits/deletes their own alert
  evidence: app/actions/saved-searches.ts:94-122; lib/data/leads/listingAlertsUser.ts:76-86; app/account/saved-searches/page.tsx:5-24,63
- ✓ **portal-new-since-badge** — markSavedSearchSeen writes listing_alerts.last_viewed_at -> portal-data reads it via getSavedSearches and computes countNewSince -> /account renders the 'N new' badge to the subscriber
  evidence: app/actions/saved-searches.ts:325 -> lib/data/leads/listingAlertsUser.ts:229,249 (last_viewed_at write); app/actions/saved-searches.ts:120 (read-back); app/account/portal-data.ts:28-31,76-89,150-151; app/account/page.tsx:19; lib/data/leads/newSince.ts:48-91
- ✓ **unsubscribe-token** — token stored/backfilled on listing_alerts -> alert email carries page link + RFC 8058 headers -> page/API POST -> deactivateListingAlertByToken sets is_active=false -> human sees 'You're unsubscribed' and the cron's is_active=true filter stops future email
  evidence: lib/alerts/send.ts:296-311 (token backfill), :385-389,442-445 (links/headers); app/alerts/unsubscribe/page.tsx:22-29,46-55 (POST-only deactivate + done state); app/api/alerts/unsubscribe/route.ts:27-46 (POST deactivates, GET redirects); lib/data/leads/listingAlerts.ts:506-543; :287 (cron excludes inactive)
- ✓ **preview-queue-broker-approval** — engine queues preview events via enqueueAlertQueueItems -> listing_alert_queue -> listPendingAlertApprovalGroups -> /admin/crm/subscriptions 'To approve' tab (broker sees) -> approveAlertQueueItems releases through the same sendAlertEmailToRecipients -> subscriber email
  evidence: app/actions/saved-search-alerts.ts:339-366 (queue write), :465-518 (approve + release via :506); lib/data/leads/listingAlertQueue.ts:18,120-129; lib/data/leads/listingAlertApprovals.ts:7,65; app/admin/(protected)/crm/subscriptions/page.tsx:25,44-46,60-62
- ✓ **email-events-measurement** — recordEmailEvent writes one 'sent' row per delivery (sendType 'alert') -> email_events -> getEmailReporting raw reads -> /admin/reports/emails send log + engagement rates (broker-visible, 'alert' in the type filter)
  evidence: lib/alerts/send.ts:454-475 (writer); lib/data/crm/getEmailReporting.ts:11,27-30,173 (reader); app/admin/(protected)/reports/emails/page.tsx:53-59,87-96 ('alert' at :92)
- ✓ **crm-identity-capture** — createSavedSearch sendEvent 'Saved Property Search' -> ensureNativeLead writes crm_people -> getListingAlertsForLead/getContactListingAlerts -> PersonEngagementRegion via PersonWorkspaceBody on /admin/people/[id]/tools -> broker sees the lead and what they are shopping for
  evidence: app/actions/saved-searches.ts:176-208; lib/followupboss.ts:103-130 (ensureNativeLead -> crm_people); lib/data/leads/listingAlerts.ts:246-275; lib/data/crm/getContactListingAlerts.ts:25,194; components/admin/crm/person-detail/PersonEngagementRegion.tsx (imports panel); app/admin/(protected)/people/[id]/tools/page.tsx:16,101
- ✓ **guest-to-signedin-claim** — OAuth callback claimGuestSavedSearches -> claimListingAlertsForUser stamps user_id on email-matched listing_alerts rows -> getListingAlertsForUser -> the claimed alerts render on the visitor's own /account/saved-searches
  evidence: app/auth/callback/route.ts:78-86; lib/data/savedSearches.ts:45-50; lib/data/leads/listingAlertsUser.ts:38-67 (claim write), :76-86 (read); app/account/saved-searches/page.tsx:21-24
- ✓ **gate-drop-audit-trail** — engine skip logs logAdminAction 'alert_gate_drop' -> admin_actions insert -> getAdminActions -> /admin/audit-log renders the drop rows to the admin
  evidence: app/actions/saved-search-alerts.ts:301-320; app/actions/log-admin-action.ts:29 (admin_actions insert); app/actions/admin-audit.ts:29-50; app/admin/(protected)/audit-log/page.tsx (mounts getAdminActions)
- ✗ **public-mirror-popular-searches** — save panel 'Make this search public' opt-in -> createSavedSearch inserts a display-only saved_searches mirror row -> getPopularPublicSearches -> a public Popular Searches surface
  evidence: Writer proven: components/SaveSearchButton.tsx:261-267 (checkbox); app/actions/saved-searches.ts:152-168 (mirror insert), :429-465 (reader fn). Reader hop broken: repo-wide grep finds ZERO callers of getPopularPublicSearches and zero frontend callers of /api/public-search/click (app/api/public-search/click/route.ts:21 is the only trackPublicSearchClick reference); the popular-search links actually rendered come from getDerivedPopularSearches over live inventory (lib/data/nav/getMegaMenuData.ts:42,260; app/search/[...slug]/page.tsx:299), which never reads saved_searches.
  **GAP:** The save panel promises a public 'Popular Searches section' that no surface renders: visitors who tick the opt-in contribute mirror rows nothing ever reads, so their choice is silently void; the mirror table accumulates orphaned rows (already stale on rename/delete per PDS defect 3) with no reader to ever notice; and the PDS §4 claim that legacy saved_searches 'survives ONLY for the public/social popular searches display' describes a display that does not exist — the table's stated reason to survive is false.

  Gap statements: 
  - Public-mirror chain is write-only: the save panel's 'Make this search public for the Popular Searches section' checkbox (components/SaveSearchButton.tsx:261-267) writes saved_searches mirror rows (app/actions/saved-searches.ts:152-168) that no surface reads — getPopularPublicSearches (app/actions/saved-searches.ts:429) has zero callers and /api/public-search/click has no frontend caller — so opting-in visitors are shown a feature that does not exist, orphaned display rows accumulate unread, and legacy saved_searches' only documented reason to survive is a display that is not rendered anywhere.

## read-content

- ✓ **Blog editorial** — saveBlogPost (admin, voice-gated) -> blog_posts -> DAL getPublishedBlogPosts/getBlogPostBySlug (status='published' filter) -> /blog index + /blog/[slug] title/byline/body render for the visitor
  evidence: writer: app/actions/blog.ts:302 (upsert blog_posts), voice hard-fail :270-275, revalidatePath('/blog') :319; readers: lib/data/blog/getPublishedBlogPosts.ts:74-92 (.from('blog_posts').eq('status','published')), lib/data/blog/getBlogPostBySlug.ts:57-65; pages import DAL not the action file: lib/data/index.ts:220-222, app/blog/page.tsx:21 + :111-117, app/blog/[slug]/page.tsx:22 + :133-138; human render: app/blog/[slug]/page.tsx:194-262 (H1, byline, prose body, 'This article is being updated' fallback)
- ✓ **Author attribution** — saveBlogPost writes author_broker_id -> blog_posts + brokers join -> byline with /team/<slug> link and brokerage fallback rendered on every post
  evidence: writer: app/actions/blog.ts:298 (author_broker_id in payload); reader join: lib/data/blog/getBlogPostBySlug.ts:73-86 (brokers select display_name/slug/photo_url/title), getPublishedBlogPosts.ts:93-98; human render: app/blog/[slug]/page.tsx:204-216 (byline Link to /team/<author_slug>, Ryan Realty fallback so no piece is unattributed)
- ✓ **FAQ content** — developer-authored static array (commit-gated by check-brand-voice.mjs because it lives under app/) -> app/faq/data.ts -> getFaqGroupedByCategory/getFaqBySlug -> /faq hub accordions + /faq/[slug] answer-as-first-paragraph render
  evidence: store+gate rationale: app/faq/data.ts:1-16 (kept in app/ so the voice gate scans it), content :33-115; readers: app/faq/data.ts:140-142 (getFaqGroupedByCategory), app/faq/page.tsx:48,75; app/faq/[slug]/page.tsx:43,62,69 (getFaqBySlug + generateStaticParams); human render: app/faq/[slug]/page.tsx:110-133 (question as H1, full answer as first paragraph, zero interaction)
- ✓ **Blog geo cross-links** — data/resort-communities.json registry (19 communities, dev-maintained SoR) -> lib/blog-geo-links.ts matcher -> 'On the market now' band on the post -> visitor exits to /communities/<slug> inventory
  evidence: store: data/resort-communities.json (communities key, 19 rows, verified via json parse this run); reader: lib/blog-geo-links.ts:17,34-36 (registry import), :50-68 (slug/title/tags match, cap 2, longest label first, href /communities/<slug>); human render: app/blog/[slug]/page.tsx:268-285 (matchGeoLinksForPost mapped to Link bands)
- ✓ **Blog hero images** — lib/blog-hero-images.ts verified-local slug map (writer = repo commit, derivatives in public/images/blog/) -> resolveBlogHeroImage applied inside both DAL readers -> hero <Image> render, never a remote/stock/dead URL
  evidence: store/map: lib/blog-hero-images.ts:24-40 (DEFAULT_BLOG_HERO + BLOG_HERO_BY_SLUG), resolver :140-151 (local kept, else slug -> category -> canonical hero); applied in readers: lib/data/blog/getPublishedBlogPosts.ts:107, lib/data/blog/getBlogPostBySlug.ts:91; human render: app/blog/[slug]/page.tsx:233-246 (hero Image block)
- ✓ **Engagement telemetry** — KbSectionTracker (section_view at 55% visibility + 25/50/75/100 scroll depth, sendBeacon full URL) -> POST /api/visitors/track -> visitor_sessions insert-only first-touch + visitor_events insert -> admin visitors session-detail event timeline read by Matt/brokers
  evidence: writer client: components/site/kb/KbSectionTracker.client.tsx:7-28 (internalTrack posts location.href to /api/visitors/track), :38-71 (IO + milestones); mounted on every route in-process: app/blog/page.tsx:144, app/blog/[slug]/page.tsx:162, app/faq/[slug]/page.tsx:39 import; store: app/api/visitors/track/route.ts:365-371 (visitor_sessions insert, 23505 tolerated), :403-405 (visitor_events insert); reader: app/admin/(protected)/visitors/_lib/queries.ts:137-138 (visitor_events select id,event_at,event_type,page_url,scroll_depth_pct...); human outcome: app/admin/(protected)/visitors/[sessionId]/page.tsx:200,325-332 (fetchSessionEvents -> 'Event timeline' render)
- ✓ **Search/AI discovery (blog)** — saveBlogPost publishes -> blog_posts -> app/sitemap.ts paginated emission + Blog/ItemList + Article JSON-LD -> crawler/AI engine indexes and a searcher lands on the post
  evidence: sitemap reader: app/sitemap.ts:99 (/blog), :524-537 (fetchAllRows blog_posts .eq('status','published') -> /blog/<slug> URLs with lastModified); JSON-LD for machine citation: app/blog/page.tsx:122-140 (Blog + ItemList), app/blog/[slug]/page.tsx:145-155,163-175 (Article + Breadcrumb); OG branch app/blog/[slug]/page.tsx:98-105
- ✗ **Search/AI discovery (FAQ standalone)** — app/faq/data.ts -> /faq/[slug] SSG pages with single-question FAQPage JSON-LD -> sitemap/inbound links -> searcher lands on the standalone answer
  evidence: pages exist and render: app/faq/[slug]/page.tsx:27-30 (dynamicParams=false, purpose-built to rank standalone), answer render :110-133; but sitemap emits ONLY the hub: grep of app/sitemap.ts for 'faq' this run returned exactly one line (:150, `${baseUrl}/faq`) — no /faq/<slug> emission anywhere
  **GAP:** The 11 standalone FAQ answer pages are invisible to their only intended audience: the sitemap emits just /faq (app/sitemap.ts:150) and nothing outside app/faq/ links a slug page, so search engines and AI answer engines never discover the per-question URLs — the citable-single-answer outcome the route was built for (organic sellers/buyers finding 'what does it cost to list' directly) does not happen, and that organic entry traffic is lost to competitors who do rank per-question.
- ✗ **Engagement read-back (popularity)** — visitor_events per-URL section_view/scroll rows -> any ranking reader -> visitor sees genuinely most-useful content surfaced
  evidence: writer/store proven (visitor_events insert, app/api/visitors/track/route.ts:403-405) but the only blog-side ordering reader is recency: lib/data/blog/getPopularBlogSlugs.ts:1-5 header states 'No view-count/popularity signal is tracked per post — this orders by published_at desc', query :23-29 orders published_at desc; no public-facing reader of visitor_events exists (grep this run: all readers are admin/CRM/prospecting surfaces)
  **GAP:** Per-piece engagement is recorded on every read but never read back by any public surface: the sidebar can only show newest posts, so a visitor is never shown the pieces other readers actually found useful, and the brokerage's best-performing editorial gets no compounding distribution — the data to fix it already accumulates unread in visitor_events.

  Gap statements: 
  - Standalone FAQ pages (/faq/[slug]) are absent from the sitemap (app/sitemap.ts emits only /faq at :150) and have zero inbound links outside app/faq/ — search engines and AI answer engines cannot discover the per-question URLs, so the single-answer ranking/citation outcome the route exists for never occurs and that organic entry is forfeited.
  - Engagement rows (section_view, scroll_depth) accumulate per URL in visitor_events but no public-facing reader consumes them — an honest 'most useful' ranking cannot exist, visitors only ever see recency ordering (lib/data/blog/getPopularBlogSlugs.ts), and high-performing editorial earns no compounding distribution.
  - A duplicate uncached public reader path survives in app/actions/blog.ts:74-204 that swallows Supabase errors into confident empty states (`if (error) return { posts: [], total: 0 }` at :93) — today only getBlogCategories is imported by a public page (app/blog/page.tsx:22), but any future import silently reintroduces the blank-page-on-outage failure the DAL's no-poison throws were built to prevent.
  - Blog categories are a hardcoded 13-item list (app/actions/blog.ts:58-72), not derived from published rows — a visitor can tap a pill for a category with zero posts and land on an empty state, a dead-end inside the process's own lateral-deepening step.
  - FAQ market claims (e.g. pending-timeline prose in app/faq/data.ts:60-64) have no re-verification mechanism against live market data — as the market moves, readers are served stale factual claims with only reviewer enforcement standing between them and a §0 accuracy breach.
  - /blog and /blog/[slug] stay pinned to per-request dynamic rendering solely by vestigial getSession/getPersonIdFromCookie reads whose FUB consumer was deleted (app/blog/page.tsx:108-117; app/blog/[slug]/page.tsx:130-137) — the highest-entry editorial family pays the slowest render mode for a consumer that no longer exists, so cold organic visitors wait on server renders for nothing.

## save-and-return.guest-alert-capture

- ✓ **alert-subscription-to-email** — submitSearchAlertSignup -> listing_alerts upsert -> hourly cron engine reads active due rows -> Resend alert email in guest inbox
  evidence: writer: app/actions/search-alert-capture.ts:142 -> lib/data/leads/listingAlerts.ts:159-174 (upsert on email,filters_hash, origin='user', source='idx-registration'); reader: lib/data/leads/listingAlerts.ts:282-290 getActiveListingAlertsDue (.eq is_active true) called at app/actions/saved-search-alerts.ts:127 by runListingAlerts, cron-wired vercel.json '/api/cron/saved-search-alerts' '0 * * * *' + app/api/cron/saved-search-alerts/route.ts:29; human: lib/alerts/send.ts:437-448 sendEmail via Resend with List-Unsubscribe headers
- ✓ **crm-person-to-broker-crm** — sendEvent -> ensureNativeLead crm_people insert/enrich -> buildCrmPeopleQuery -> broker CRM people list
  evidence: writer: app/actions/search-alert-capture.ts:103-111 -> lib/followupboss.ts:100-131 sendEvent -> lib/data/crm/ensureNativeLead.ts:254 (.from('crm_people') insert) and :334 (enrich update incl. assigned_broker via canonicallyTagLead, lib/canonical-lead-tagger.ts:237-255); reader: lib/data/crm/buildCrmPeopleQuery.ts:293 (.from('crm_people').select) via listCrmPeople app/actions/crm.ts:214; human: app/admin/(protected)/crm/page.tsx:55,133 renders the broker-scoped people list
- ✓ **broker-follow-up-task** — createNativeTask -> crm_tasks insert -> getTaskQueue -> broker task queue pages
  evidence: writer: app/actions/search-alert-capture.ts:130-135 (awaited, due 5 min) -> lib/data/crm/ensureNativeLead.ts:450,464 (.from('crm_tasks').insert); reader: lib/data/crm/getTaskQueue.ts:198 (.from('crm_tasks').select); human: app/admin/(protected)/crm/tasks/page.tsx:121, app/admin/(protected)/today/page.tsx:48, app/admin/(protected)/oversight/page.tsx:64 render the queue to brokers
- ✗ **marketing-assignments-ledger** — canonicallyTagLead recordAssignment -> marketing_assignments upsert -> (no reader) -> (no human surface)
  evidence: writer proven: lib/canonical-lead-tagger.ts:256-263 recordAssignment -> lib/data/crm/recordMarketingAssignment.ts:115-119 (.from('marketing_assignments').upsert). Reader hop: repo-wide grep this run — the writer is the ONLY .from('marketing_assignments') occurrence; lib/data/crm/getLeadIntake.ts:21-22 states it 'replaced every marketing_assignments read' and app/admin/(protected)/reports/lead-flow/page.tsx:17 states 'marketing_assignments is decommissioned'
  **GAP:** The assignment ledger this process's completion criterion (§7.2) and acceptance check 5 point at is write-only: no broker, admin, or report surface reads marketing_assignments (dashboards read crm_people via getLeadIntake instead). A wrong, missing, or drifting assignment row is invisible to everyone; check 5 verifies a store with no consumer, so it can pass while proving nothing about what brokers actually see. Brokers/Matt are harmed only if anyone trusts the ledger as routing truth — the live routing truth is crm_people.assigned_broker/tags.
- ✓ **email-events-to-broker-engagement** — sendAlertEmailToRecipients -> email_events 'sent' row -> getContactEmailEngagement / getEmailReporting -> broker person-detail engagement + admin email reports
  evidence: writer: lib/alerts/send.ts:460-469 recordEmailEvent(sendType 'alert', emailKey 'listing-alert:<id>:<date>'); readers: lib/data/crm/getContactEmailEngagement.ts:103-105 (.from('email_events').eq('person_id', personId)) rendered by components/admin/crm/person-detail/PersonEngagementRegion.tsx:67 inside PersonWorkspaceBody.tsx:572 on app/admin/(protected)/people/[id]/tools/page.tsx; aggregate reader lib/data/crm/getEmailReporting.ts:365,644 on app/admin/(protected)/reports/emails/page.tsx:59. Caveat (already the doc's captured-without-person state): rows sent for alerts with null crm_person_id carry null person_id and are invisible on the per-lead card until linkAlertRowToPerson re-links (saved-search-alerts.ts:43)
- ✓ **claim-on-sign-in** — OAuth/magic-link callback -> claimListingAlertsForUser stamps user_id -> getListingAlertsForUser -> /account/saved-searches render
  evidence: writer: app/auth/callback/route.ts:77-86 claimGuestSearchesForUser (email_confirmed_at gated), call sites :157,:183 -> lib/data/savedSearches.ts:45-49 alias -> lib/data/leads/listingAlertsUser.ts:55-61 (update user_id + crm_person_id, .eq is_active true, .is user_id null); reader: app/actions/saved-searches.ts:95-97 getSavedSearches -> getListingAlertsForUser(session.user.id); human: app/account/saved-searches/page.tsx:5,21 renders the claimed alerts. Chain holds on the callback paths only — the password path never fires the writer (see gap)
- ✓ **token-unsubscribe** — in-email token link/one-click POST -> is_active=false -> engine's active-only read excludes the row -> emails stop + confirm page
  evidence: writer: app/api/alerts/unsubscribe/route.ts:28-35 POST (GET :36-46 redirects to the confirm page, never deactivates) -> lib/data/leads/listingAlerts.ts:506-517 deactivateListingAlertByToken (update is_active=false by unsubscribe_token); reader: lib/data/leads/listingAlerts.ts:287 getActiveListingAlertsDue .eq('is_active', true) excludes the row from every future run; human: {ok:true} response / branded confirm page, and no further alert emails; resurrection guard listingAlerts.ts:120-133,158,170 keeps it muted on re-save
- ✓ **guest-residual-return-visit** — success handler writes localStorage residual -> strip done-state + global GuestWatchingBanner read it -> visitor sees 'You're watching X'
  evidence: writer: components/search/SearchAlertCapture.tsx:284-287 rememberGuestWatch/readGuestWatch on ok:true -> lib/alerts/guest-watch-residual.ts:23-30 (rr_guest_alert_watch, label+href only, 90-day TTL); readers: components/site/GuestWatchingBanner.client.tsx:12 imports the residual lib, mounted globally at components/layout/PublicClientLayer.tsx:36-37,69; the strip itself re-reads it for its done state. Known caveat = doc D8: the same write fires on honeypot pretend-success, polluting client measurement (not a chain break — the human outcome renders as designed)

  Gap statements: 
  - marketing_assignments is a write-only ledger: capture upserts a routing row (recordMarketingAssignment.ts:115-119) that no surface in the repo reads (getLeadIntake.ts:21-22 removed every read; lead-flow/page.tsx:17 calls it decommissioned) — routing failures recorded there are invisible to Matt and brokers, and acceptance check 5 passes against a store with no consumer while the human-visible routing truth lives on crm_people.
  - Password sign-in never claims guest alerts (doc D4, re-verified this run): claimGuestSavedSearches's only callers are the OAuth/magic-link/recovery callback paths (auth/callback/route.ts:157,183) — a guest who converts via a password account sees an empty /account/saved-searches while their alerts keep sending as unclaimed guest rows, breaking the process's designed exit at the moment of highest intent.
  - Alert emails sent for rows with null crm_person_id write email_events rows with null person_id (send.ts:460-469), so those sends never appear on the broker's per-lead engagement card until the engine re-links the row (saved-search-alerts.ts:43) — a broker reviewing a captured-without-person lead sees no evidence the machine has been emailing them.

## broker-direct-call-text

- ✓ **telephony-truth: rendered number + call forward** — migration seeds brokers.twilio_number/forward_to_cell -> public.brokers -> getAgentBySlug maps twilio_number to public phone / getBrokerTelephony reverse-maps dialed line -> visitor taps tel: on /team/[slug] and the voice webhook <Dial>s the broker's cell
  evidence: supabase/migrations/20260624120000_crm_broker_telephony.sql:14-24 (writer); lib/data/crm/getBrokerTelephony.ts:37-47 + app/actions/brokers.ts:66,157-160 (readers); app/team/[slug]/page.tsx:264-267,460-493 tel:/sms: render; app/contact/page.tsx:216-225 brand tel:; lib/crm/twilio.ts:129-141 (brokerForTwilioNumber), 145-159 (forwardCellForBroker); app/api/twilio/voice/route.ts:204-208 <Dial timeout=25> to cell (human outcome: broker's cell rings with caller ID)
- ✓ **call interaction -> broker timeline** — voice webhook upserts crm_timeline kind='call' -> crm_timeline -> getContactActivityFeed reads it -> /admin/people/[id] renders the feed the broker reads
  evidence: app/api/twilio/voice/route.ts:116-130 (writer, dedupe twilio:call:<CallSid>); lib/data/crm/getContactActivityFeed.ts:151 .from('crm_timeline'), kind map incl. call/voicemail/sms_in at :50-60; app/admin/(protected)/people/[id]/page.tsx:20,154 (read) and :568-577 (feed render)
- ✓ **inbound person create -> CRM person page** — findOrCreatePersonByPhone inserts crm_people + crm_contact_points -> crm_people/crm_contact_points -> lookupPersonByPhone resolves next inbound; /admin/people/[id] renders the person the alert deep-links -> broker opens the lead
  evidence: lib/data/crm/findOrCreatePersonByPhone.ts:95-108 (writer, shared by voice route.ts:97-101, voice-complete/route.ts:38-42, inbound-sms/route.ts:156-160); lib/crm/twilio.ts:160+ lookupPersonByPhone (reader); deep link ryan-realty.com/admin/people/{id} in lib/crm/broker-alerts.ts:276 and inbound-sms/route.ts:329; page render app/admin/(protected)/people/[id]/page.tsx:154-175
- ✓ **new-lead alert -> broker's phone** — queueBrokerAlert inserts crm_broker_alerts (timeline dedupe row first) -> crm_broker_alerts -> crm-alert-drain cron lists status='pending', CAS-claims, POSTs Twilio -> SMS lands on the broker's whitelisted cell (web push via pushed_at leg when notify_sms=false)
  evidence: lib/crm/broker-alerts.ts:170-187 (writer; notify_sms gate :164-167, push_only fallback :166,186); lib/data/crm/brokerAlertDrain.ts:25-34 listPendingAlerts, :41-51 claim; app/api/cron/crm-alert-drain/route.ts:87,106-131 (send to whitelisted broker phone), :60 drainWebPush. Config gates noted in PDS defect 5: CRM_SMS_ALERTS='twilio' + per-broker notify_sms
- ✓ **inbound text -> CRM thread + real-time cell forward** — inbound-sms webhook upserts crm_timeline sms_in -> crm_timeline -> getContactConversation/getContactActivityFeed -> broker reads the thread on /admin/people/[id]; in parallel the webhook awaits sendSms broker-line->broker-cell (A2P fail-closed) so the text sits on the broker's phone
  evidence: app/api/twilio/inbound-sms/route.ts:163-174 (timeline writer), :365-377 (awaited forward), :304-348 (task + alert email); lib/crm/twilio.ts:303-311 a2pBlocked fail-closed, :310-326 sendSms; readers app/admin/(protected)/people/[id]/page.tsx:25,159 (getContactConversation) + feed render :568
- ✓ **reply-to-text task -> broker task queue** — inbound-sms upserts crm_tasks (per-message dedupe, due +15m) -> crm_tasks -> getTaskQueue -> /admin/crm/tasks and /admin/today render the task the broker acts on
  evidence: app/api/twilio/inbound-sms/route.ts:304-315 (writer, dedupe twilio-task:<sid>); lib/data/crm/getTaskQueue.ts:198 .from('crm_tasks'); app/admin/(protected)/crm/tasks/page.tsx:72 and app/admin/(protected)/today/page.tsx:10,48 (render surfaces)
- ✓ **voicemail -> transcript email to broker** — voice-complete upserts crm_timeline kind='voicemail' -> crm_timeline -> recording webhook reads the row by CallSid, attaches transcript, then emails the owning broker -> transcript in the broker's mailbox with listen + contact links
  evidence: app/api/twilio/voice-complete/route.ts:45-56 (writer, dedupe twilio:vm:<CallSid>); app/api/twilio/recording/route.ts:58-63 (reader by dedupe_key LIKE), :96-105 (transcript update), :113-119 (awaited sendCrmEmail to broker mailbox — human outcome); 503-on-race redelivery :78-81
- ✓ **STOP opt-out -> every future send blocked** — inbound-sms STOP writes crm_suppressions via addSuppression (+ system timeline row + confirmation text) -> crm_suppressions -> isSuppressed (fail-closed) inside checkSendGuards inside sendGovernedSms, called by the CRM composer action -> the person receives no further texts and the broker sees the 'Blocked by suppression' refusal
  evidence: app/api/twilio/inbound-sms/route.ts:199-206 (writer + confirmation twiml); lib/crm/suppressions.ts:212-218 addSuppression, :31-57 isSuppressed fail-closed read; lib/comms/guards.ts:36-51 checkSendGuards; lib/comms/sendGovernedSms.ts:27,33; caller app/actions/crm.ts:933 (composer send)
- ✓ **office hours -> after-hours voicemail routing** — admin OfficeHoursEditor -> updateOfficeHoursAction upserts crm_company_settings -> crm_company_settings -> voice webhook reads getCrmCompanySettings + isWithinOfficeHours -> after-hours caller hears the voicemail prompt instead of ringing the broker
  evidence: app/admin/(protected)/crm/settings/_components/company/OfficeHoursEditor.tsx:5 (UI writer) -> app/actions/crm-company-settings.ts:49 (upsert); lib/data/crm/getCrmCompanySettings.ts:122 (reader); app/api/twilio/voice/route.ts:184-191 (decision -> voicemailResponse, human outcome :54-59)
- ✓ **call outcomes -> calls report** — voice + voice-complete timeline rows (call/voicemail kinds, recordingDurationSec) -> crm_timeline -> getCallsReport derives received/missed/connected/talk time -> /admin/crm/reporting/calls renders the breakdown Matt and brokers read
  evidence: writers app/api/twilio/voice/route.ts:119-130 + voice-complete/route.ts:45-56 + recording/route.ts:96-105 (recordingDurationSec); lib/data/crm/getCallsReport.ts:22-40 metric->crm_timeline mapping; app/admin/(protected)/crm/reporting/calls/page.tsx:13,173 (render). Note: payload.dialStatus itself has no reader — outcomes are derived from kind counts, which suffices
- ✗ **tap-through funnel measurement** — visitor taps tel:/sms: -> (no writer exists) -> no store -> no reader -> no human-visible funnel
  evidence: all entry links are plain anchors with zero JS: app/contact/page.tsx:216-225, app/team/[slug]/page.tsx:264-267,478-489, and no analytics call anywhere on the tap path (grep for tap handlers on tel:/sms: anchors — none; PDS defect 3 re-confirmed this run)
  **GAP:** The top of the highest-intent funnel is invisible: taps, dial-sheet cancels, and desktop dead-clicks write nothing anywhere, so a regression that silently breaks or buries the call/text affordance is undetectable except as unexplained falling Twilio volume, and Matt cannot size or optimize the channel
- ✗ **/contact text-intent entry** — /contact promises 'Call or text' -> only a tel: anchor renders -> a text-intent visitor's sms: leg never starts
  evidence: app/contact/page.tsx:216-225 — 'Call or text' label above a single tel: href (re-verified this run); /team/[slug] ships both tel: and sms: (page.tsx:265-266,478-484), listing surfaces ship both per PDS §2
  **GAP:** A text-preferring visitor on /contact is dropped into the phone dialer against the page's explicit promise; some abandon rather than call, and the shop breaks its own stated 'call or text' contract on its primary contact page
- ✗ **attributed-broker routing on listing detail** — rr_agent_attribution cookie identifies the visitor's broker -> listing detail resolves contact broker from listing agent -> principal only, cookie unread -> visitor calls a different broker's line and the lead assigns to the dialed line's owner
  evidence: PDS defect 4 with app/listing/[listingKey]/page.tsx:274-283 (listing-agent->principal resolution, no attribution read); lead assignment follows the dialed line: app/api/twilio/voice/route.ts:89-101 and inbound-sms/route.ts:155-160
  **GAP:** A Rebecca-attributed visitor tapping call/text on a listing reaches Matt's line and the created lead is assigned to Matt — the attributing broker loses the relationship their attribution earned, and the visitor talks to a broker without their context

  Gap statements: 
  - No analytics writer exists on any tel:/sms: tap, so the top of the site's highest-intent funnel (taps, cancels, desktop dead-clicks) is invisible — a render-plane regression that kills the affordance would surface only as unexplained falling Twilio call volume, and the channel cannot be sized or optimized
  - /contact's office block promises 'Call or text' but renders only a tel: href — text-intent visitors on the primary contact page land in the phone dialer and some abandon; the page breaks its own stated contract while /team/[slug] and listing detail keep it
  - Listing-detail call/text buttons ignore the rr_agent_attribution cookie (listing agent -> principal only), so an attributed visitor calls a different broker's line and the resulting lead is assigned to the dialed line's owner — the attributing broker loses the lead their attribution earned and the caller reaches a broker without their context

## refer-out-of-area

- ✓ **city-stats** — refresh-mvs cron (refresh_geo_snapshot_mv RPC) -> geo_snapshot_mv -> getOutOfAreaCityIndex DAL -> /oregon/[city] hero stats + facts + JSON-LD the visitor reads
  evidence: writer app/api/cron/refresh-mvs/route.ts:73-75 (cron registered vercel.json:197); store docs/DATABASE_SCHEMA_SNAPSHOT.md:2648; reader lib/data/geo/getOutOfAreaCities.ts:57-68 (.from('geo_snapshot_mv'), throws on error so [] is never poison-cached :88-93); render app/oregon/[city]/page.tsx:101-102 (guard), :139-145 (JSON-LD), :172-177 (facts), :192-208 (hero). ✓ chain holds
- ✓ **listing-tiles** — refresh-mvs cron (refresh_listing_tile_mv RPC) -> listing_tile_mv -> getListingTiles -> KbFeatured live-inventory grid the visitor reads
  evidence: writer app/api/cron/refresh-mvs/route.ts:59-61; store docs/DATABASE_SCHEMA_SNAPSHOT.md:306 (~593K rows); reader lib/data/listings/getListingTiles.ts:375 (.from('listing_tile_mv')), invoked app/oregon/[city]/page.tsx:109-114 (withTimeoutFallback 5000ms -> []); render page.tsx:222-230 (KbFeatured, hidden not faked on timeout). ✓ chain holds
- ✓ **referral-lead** — KbOutOfAreaReferral form -> submitOutOfAreaReferral -> ensureNativeLead writes crm_people + crm_contact_points -> listReferralCandidates -> /admin/crm/referrals queue the broker works by hand
  evidence: form components/site/kb/KbOutOfAreaReferral.client.tsx:5,29; action app/actions/out-of-area-referral.ts:58-72 (tags incl. referral:candidate, city-interest:<slug>); writes lib/data/crm/ensureNativeLead.ts:253-257 (crm_people insert), :265-272 (crm_contact_points), :219-224 (reuse merges tags); reader lib/data/crm/referralReceivables.ts:67-72 (overlaps on referral:candidate/geo:unclassified — NOTE :73-76 warn-swallows a failed read into a confident empty queue, PDS §10 defect 4, chain still proven); render app/admin/(protected)/crm/referrals/page.tsx:69-74,112-172. ✓ chain holds
- ✓ **origin-note** — enrichNativeLead -> crm_timeline note ('what they want' + routing instruction) -> getContactConversation -> /admin/people/[id] person page the broker reads before the handoff
  evidence: writer app/actions/out-of-area-referral.ts:76-91 -> lib/data/crm/ensureNativeLead.ts:418-427 (.from('crm_timeline').insert, kind 'note', source 'lp-form'); reader lib/data/crm/getContactConversation.ts:35 (.from('crm_timeline')); render app/admin/(protected)/people/[id]/page.tsx:25,159,252; the referrals-page link /admin/crm/<id> reaches it via the legacy redirect app/admin/(protected)/crm/[id]/page.tsx:34 (-> /admin/people/[id]/tools). ✓ chain holds
- ✓ **broker-task** — createNativeTask -> crm_tasks (Referral, due 240 min) -> getTaskQueue -> /admin/crm/tasks queue surface the broker works
  evidence: writer app/actions/out-of-area-referral.ts:94-100 -> lib/data/crm/ensureNativeLead.ts:464-471 (.from('crm_tasks').insert, due_at from dueInMinutes :454-455); reader lib/data/crm/getTaskQueue.ts:198; render app/admin/(protected)/crm/tasks/page.tsx:72,119-121 (also /admin/today and /admin/oversight import getTaskQueue). ✓ chain holds
- ✓ **broker-sms-alert** — queueBrokerAlert -> crm_broker_alerts (status pending) -> crm-alert-drain cron reads + Twilio send -> SMS on the broker's own phone (never the lead)
  evidence: writer app/actions/out-of-area-referral.ts:104-113 -> lib/crm/broker-alerts.ts:181-187 (insert, status 'pending' when smsOptIn; queue-time opt-in gate :164-167 PASSES for default broker matt — live audit this run: brokers.notify_sms=true for matt); reader lib/data/crm/brokerAlertDrain.ts:28 (listPendingAlerts) via app/api/cron/crm-alert-drain/route.ts:87,106-131 (whitelist-first, CAS claim, Twilio send; cron vercel.json:21, every minute); prod delivery proven live: crm_broker_alerts has 953 rows ALL status='sent', newest 2026-08-11 — the CRM_SMS_ALERTS='twilio' env condition (route.ts:62) is demonstrably set. ✓ chain holds
- ✓ **referral-receivable** — recordReferralForm server action (broker) -> referral_receivables insert + crm_timeline stamp + referral:referred-out tag -> listReferralReceivables -> 'Recorded referrals' ledger + queue filtering the broker sees
  evidence: writer app/admin/(protected)/crm/referrals/page.tsx:52-62 -> lib/data/crm/referralReceivables.ts:143-148 (insert, status 'pending', default 25%), :160-166 (timeline stamp), :172-184 (idempotent referred-out tag); store exists in prod (docs/DATABASE_SCHEMA_SNAPSHOT.md:3981 — migration 20260722212000 applied, feature-detect branch :150-155 is dormant); reader :95-101; render page.tsx:175-203 (ledger) + :73-74 (referred people drop out of the waiting queue). ✓ chain holds
- ✓ **no-drip-gate** — referral:candidate tag (written at capture) -> crm_people.tags -> autoEnrollPerson reads tags on BOTH the intake path and the 15-min crm-auto-enroll cron -> human outcome is the ABSENCE: zero crm_sequence_enrollments, no automated message ever reaches the lead
  evidence: writer app/actions/out-of-area-referral.ts:63 (REFERRAL_CANDIDATE_TAG); gate lib/crm/enroll.ts:71-72 (const referralBlock = geoReferralEnrollBlock(tags); returns { enrolled: false }) keyed on the tag in lib/referral-geo.ts:143-152 (deliberately NOT the bare geo:out-of-area tag, :136-141); cron registered vercel.json:25; unit-covered lib/referral-geo.test.ts. ✓ chain holds

## contact-form-inquiry

- ✓ **lead-identity** — ContactForm submit -> submitContactForm -> sendEvent/ensureNativeLead -> crm_people (+crm_contact_points) -> getInboxContactCard -> /admin/people/[id] render -> broker sees the lead ✓ chain holds
  evidence: app/contact/ContactForm.tsx:57 (calls submitContactForm); app/contact/actions.ts:93-112 (sendEvent) + :117-138 (direct ensureNativeLead fallback); lib/followupboss.ts:129-131 (ensureNativeLead call); lib/data/crm/ensureNativeLead.ts:253-257 (crm_people insert), :265-272 (crm_contact_points insert), :309-334 (reuse-path tag/source/broker merge); reader: lib/data/crm/getInboxThread.ts:40 (.from('crm_people')); app/admin/(protected)/people/[id]/page.tsx:129 (getInboxContactCard rendered on the broker's person page)
- ✓ **tags-assignment-origin-note** — after() canonicallyTagLead -> enrichNativeLead -> crm_people.tags/assigned_broker update + crm_timeline origin note -> getContactActivityFeed -> /admin/people/[id] feed -> broker reads why the lead came in ✓ chain holds
  evidence: app/contact/actions.ts:148-168 (after() block); lib/canonical-lead-tagger.ts:237-243 (tag set), :248-255 (enrichNativeLead call), :229-232 (hard-stop skip); lib/data/crm/ensureNativeLead.ts:403-413 (crm_people update), :419-427 (crm_timeline note insert, kind 'note'); reader: lib/data/crm/getContactActivityFeed.ts:151 (.from('crm_timeline')); app/admin/(protected)/people/[id]/page.tsx:154 (getContactActivityFeed(idNum, 30) rendered)
- ✓ **sequence-enrollment-to-nurture-touch** — autoEnrollPerson -> crm_sequence_enrollments insert -> crm-sequence-engine cron reads running enrollments -> sendCrmEmail / Twilio SMS -> the lead receives the first nurture touch ✓ chain holds
  evidence: lib/crm/enroll.ts:154-160 (crm_sequence_enrollments insert, status 'running'), :163-168 (timeline row); cron registered vercel.json:57; reader: app/api/cron/crm-sequence-engine/route.ts:60-66 (.from('crm_sequence_enrollments') status='running', sequence active); delivery: route.ts:298 (sendCrmEmail), :412-413 (sendSms / sendSmsViaMessagingService); miss-sweep: app/api/cron/crm-auto-enroll/route.ts:63-68 (COALESCE fub_created_at/created_at window), :149 (autoEnrollPerson), registered vercel.json:25
- ✓ **sms-consent-suppression** — consent checkbox -> addSuppression('sms','no-sms-consent') -> crm_suppressions -> isSuppressed(person,'sms') in the sequence engine -> SMS step skipped -> visitor is never texted without consent ✓ chain holds
  evidence: app/contact/actions.ts:39 (smsConsent read fail-closed), :171 (passed to autoEnrollByFubId); lib/crm/enroll.ts:297-305 (removeSuppression then addSuppression when consent absent); lib/crm/suppressions.ts:212-218 (crm_suppressions insert); reader: app/api/cron/crm-sequence-engine/route.ts:315 (isSuppressed(person.id,'sms') gates the SMS send at :412-413); suppressions.ts:31-34 (reads crm_suppressions channels 'all'+channel)
- ✓ **broker-notification-email** — submitContactForm -> sendContactNotification -> Resend API -> ADMIN_EMAIL inbox (reply-to the visitor) -> a human reads the inquiry ✓ chain holds
  evidence: app/contact/actions.ts:140 (call, .catch(() => {})); lib/resend.ts:103-124 (sendContactNotification builds body, to ADMIN_EMAIL, replyTo visitor); lib/resend.ts:48-74 (sendEmail -> client.emails.send, real Resend API)
- ✓ **broker-new-lead-text** — autoEnrollByFubId instant queue (BROKEN for this process) / crm-auto-enroll cron sweep -> crm_broker_alerts -> crm-alert-drain cron -> Twilio SMS or web push -> broker's phone ✓ chain holds (via the 15-min cron leg only)
  evidence: Instant leg: lib/crm/enroll.ts:315 gates on new Date(p.fub_created_at ?? 0) >= ENROLLMENT_EPOCH, but native contact-form leads never set fub_created_at (lib/data/crm/nativeCreate.ts:47-70 has no such field; docs/DATABASE_SCHEMA_SNAPSHOT.md:2120 nullable, no default) so it evaluates 1970 < epoch and skips — verified this run. Cron leg: app/api/cron/crm-auto-enroll/route.ts:63-68 (COALESCE window includes native rows), :159-170 (queueBrokerAlert); lib/crm/broker-alerts.ts:181-187 (crm_broker_alerts insert status pending/push_only), :170-179 (crm_timeline dedupe); drain: vercel.json:21, lib/data/crm/brokerAlertDrain.ts:28-47 (claim pending), app/api/cron/crm-alert-drain/route.ts:122-131 (Twilio Messages.json send, markSent)
- ✓ **session-stitch-browsing-history** — rr_session_id -> backfillSessionToFub -> visitor_sessions.crm_person_id + visitor_identity_map -> getPeopleListSignals -> /admin/crm people list lastVisit signal -> broker sees the lead's site activity ✓ chain holds
  evidence: app/contact/ContactForm.tsx:45-46 (appends sessionId); app/contact/actions.ts:176-183 (UUID-v4 guard + backfillSessionToFub); lib/visitor-backfill.ts:196-208 (visitor_sessions update: crm_person_id, identified_via 'form_submit'), :220-226 + :77 (visitor_identity_map upsert), :256-260 (visitor_events cursor); reader: lib/data/crm/getPeopleListSignals.ts:43-45 (.from('visitor_sessions').select('crm_person_id,last_seen_at').in('crm_person_id', ids)); app/admin/(protected)/crm/page.tsx:133 (getPeopleListSignals rendered on the broker's people list); /admin/visitors surface exists (app/admin/(protected)/visitors/page.tsx)
- ✗ **assignment-ledger** — canonicallyTagLead -> recordMarketingAssignment -> marketing_assignments upsert -> (no reader) ✗ broken at reader hop
  evidence: Writer live: lib/canonical-lead-tagger.ts:256-263 (recordAssignment) -> lib/data/crm/recordMarketingAssignment.ts:114-121 (.from('marketing_assignments').upsert). Reader: repo-wide grep this run finds exactly ONE .from('marketing_assignments') — the writer; lib/data/crm/getLeadIntake.ts:21-24 documents that every dashboard read was replaced by getLeadIntake over crm_people (the old queries filtered a non-existent created_at column and always returned 0)
  **GAP:** marketing_assignments is a write-only table: every contact-form lead adds/refreshes a ledger row that no page, cron, or report ever reads, so the broker-assignment audit trail the tagger promises ('keeps recording every assignment for audit', canonical-lead-tagger.ts:89) is invisible — a routing dispute or attribution audit cannot be answered from any surface, and the write spends latency inside the fragile after() block for nothing
- ✓ **conversion-mirrors** — submitContactForm -> /api/meta-capi -> graph.facebook.com Lead event (eventId-deduped with browser fbq) + GA4 MP generate_lead -> Meta Ads Manager / GA4 reports Matt reads ✓ chain holds (to the external boundary)
  evidence: app/contact/actions.ts:194-220 (eventId + tiered value, POST /api/meta-capi), :229-235 (fireLeadGenerated); app/api/meta-capi/route.ts:4 (sendServerEvent) -> lib/meta-capi.ts:40 (graph.facebook.com/v21.0/<pixel>/events); lib/ga4-measurement-protocol.ts:32,177 (mp/collect fetch); browser dedup: app/contact/ContactForm.tsx:60-67 (fbq Lead with matching eventID + gtag generate_lead). External stores (Meta/GA4) are mirrors, not SoR, per the PDS §4 — human outcome is campaign reporting, verifiable only outside the repo. Known D8: GA4 has no eventId dedup, so client+server can double-count one submit

  Gap statements: 
  - marketing_assignments is write-only: the assignment-audit ledger every contact-form lead writes (canonical-lead-tagger.ts:256, recordMarketingAssignment.ts:116) has zero readers repo-wide, so broker-routing disputes and attribution audits cannot be answered from any surface
  - The instant broker new-lead text never fires for contact-form leads: enroll.ts:315 gates on fub_created_at, which native crm_people rows never set (nativeCreate.ts builds no such field; column nullable/no-default per schema snapshot line 2120) — every alert waits for the 15-min crm-auto-enroll sweep and the first-touch-preview alert body (enroll.ts:350-373) is dead code on this path; a tour request sits unannounced up to 15 minutes
  - Lead enrichment (audience/broker tags, origin note, enrollment trigger) exists only inside after() (actions.ts:148-188); if that block dies the lead is captured but never tagged, routed, or enrolled, and no cron repairs it (crm-auto-enroll matches existing tags only) — the visitor's inquiry silently gets no follow-up (PDS D2, re-proven this run)
  - The broker notification email goes only to the fixed ADMIN_EMAIL and swallows its own failure with .catch(() => {}) and no record (actions.ts:140; resend.ts:118-123) — a non-admin assigned broker never receives it, and a Resend outage loses inquiries invisibly, with no machinery behind the rendered one-business-day reply promise
  - GA4 counts one submit twice when gtag is unblocked: the server fires generate_lead via Measurement Protocol (actions.ts:229-235) and the client fires the same event via gtag (ContactForm.tsx:67) with no eventId dedup equivalent to Meta's — lead-volume reporting Matt reads is inflated (PDS D8, re-proven this run)

## deliver-alerts

- ✓ **subscription-to-inbox** — 5 inception writers (signed-in save / guest capture / paid LP / broker attach / neighborhood provisioning) -> listing_alerts -> hourly cron engine reads due rows -> alert email lands in subscriber's inbox via Resend under named broker identity
  evidence: Writers: lib/data/leads/listingAlerts.ts:159-174 (upsertListingAlert) called by app/actions/saved-searches.ts:139, app/actions/search-alert-capture.ts:142, app/lp/buyer-listing-alerts/actions.ts:356; lib/data/leads/listingAlerts.ts:214-235 (createListingAlertForLead) called by lib/crm/bulk-handlers/assign-saved-search.ts:92, app/actions/newsletter.ts:246,294, app/actions/contact-listing-matches.ts:79; direct upsert lib/data/crm/neighborhoodDefaultSubscriptions.ts:210-259. Reader: lib/data/leads/listingAlerts.ts:282-296 (getActiveListingAlertsDue) <- app/actions/saved-search-alerts.ts:127 <- app/api/cron/saved-search-alerts/route.ts:29 <- vercel.json:213-214. Outcome: lib/alerts/send.ts:437-448 sendEmail (lib/resend.ts:48)
- ✓ **preview-queue-to-admin-to-subscriber** — engine queues held events -> listing_alert_queue -> admin Approval Queue tab reads pending groups -> admin approves -> same send path delivers email to subscriber
  evidence: Writer: app/actions/saved-search-alerts.ts:339-364 -> lib/data/leads/listingAlertQueue.ts:50-95 (insert :87). Readers: listingAlertQueue.ts:120-133 -> lib/data/leads/listingAlertApprovals.ts:7,65 -> components/admin/crm/subscriptions/ApprovalQueueTab.tsx:53-128 mounted by SubscriptionsHub.tsx:36,124 on app/admin/(protected)/crm/subscriptions/page.tsx:28. Release: app/actions/saved-search-alerts.ts:465-533 (compliance re-run :497-501, sendAlertEmailToRecipients :506)
- ✓ **send-measurement** — send path writes one 'sent' row per delivery -> email_events -> subscriptions-hub engagement rollup reads by email_key prefix -> admin sees sends/opens/clicks per subscription
  evidence: Writer: lib/alerts/send.ts:459-469 (recordEmailEvent, sendType='alert', emailKey minted :410-413). Reader: lib/data/crm/subscriptionsAdminEngagement.ts:53-73 (email_key.like.listing-alert:<id>:%, folds sent/open/click :38-46) -> lib/data/crm/subscriptionsAdmin.ts:130 -> SubscriptionsHub on app/admin/(protected)/crm/subscriptions/page.tsx:28
- ✓ **compliance-gate-drop-audit** — engine compliance gates (crm_people hard-stop + suppression, fail-closed) drop a send -> durable alert_gate_drop row on admin_actions -> admin audit-log page reads the trail -> admin sees why a send was eaten
  evidence: Gate reads: lib/alerts/send.ts:337-338 (fail-closed :346-354), chokepoint re-check :431-432. Writer: app/actions/saved-search-alerts.ts:301-320 -> app/actions/log-admin-action.ts:29 (admin_actions insert). Reader: app/actions/admin-audit.ts:34-46 (actionType-filterable, errors surfaced) -> app/admin/(protected)/audit-log/page.tsx:24
- ✓ **unsubscribe-exit** — email carries per-recipient token links + RFC 8058 headers -> POST deactivates row (is_active=false) or removes one recipient -> engine's is_active filter and resurrection guard read it -> recipient sees confirm page and never gets another email, from any re-create channel
  evidence: Link/header mint: lib/alerts/send.ts:384-389,443-444. Writers: app/api/alerts/unsubscribe/route.ts:27-33 (POST; GET only redirects :36-46), app/alerts/unsubscribe/page.tsx:22-30 (button Server Action) -> lib/data/leads/listingAlerts.ts:506-543 (is_active=false :512; recipient tier :520-539). Readers: listingAlerts.ts:287 (is_active filter) + alertExplicitlyOptedOut :120-133 at every re-create :158,:213. Outcome: confirm page page.tsx:45-55
- ✓ **global-email-kill-switch** — signed-in user flips emailEnabled -> profiles.notification_preferences -> engine checks per due row and skips with cursor advance -> subscriber stops receiving alert emails without losing the saved search
  evidence: Writer: app/actions/profile.ts:81. Reader: app/actions/saved-search-alerts.ts:167-178 (emailEnabled===false -> advanceCursor, no send). Outcome: no email delivered; row stays intact for re-enable

## arrive-from-ad

- ✓ **lead-capture-seller-family** — LP form submit -> submitSellerLPForm sendEvent/ensureNativeLead -> crm_people -> admin person page + broker alert deep link -> broker opens and works the lead
  evidence: app/lp/seller-home-value/actions.ts:376-433 (sendEvent 'Seller Inquiry', ensureNativeLead never-drop fallback at 417-433); enrichNativeLead tags/broker actions.ts:546-552; reader app/admin/(protected)/people/[id]/page.tsx:19-26 (getInboxContactCard/getContactActivityFeed DAL over crm_people); broker alert body links ryan-realty.com/admin/people/<id> lib/crm/enroll.ts:348,372
- ✓ **broker-attribution-routing** — ?agent=slug -> rr_agent_attribution cookie -> readAttributedAgentServer -> crm_people.assigned_broker + marketing_assignments -> broker alert routed to that broker's phone
  evidence: components/AgentAttributionBridge.tsx:12-17 (cookie write); app/actions/agent-attribution-read.ts:20-33 (validated parse); app/lp/seller-home-value/actions.ts:546-552 (assignedBroker on enrichNativeLead), :570-576 (recordSellerAssignment); lib/crm/enroll.ts:383-384 queueBrokerAlert(broker: p.assigned_broker); app/api/cron/crm-alert-drain/route.ts:25-53 drains crm_broker_alerts via Twilio, broker-phone whitelist; vercel.json:21
- ✓ **sms-consent-fail-closed** — unchecked consent box -> autoEnrollByFubId writes crm_suppressions (sms/no-sms-consent) -> crm-sequence-engine isSuppressed gate -> lead is never texted without consent
  evidence: app/lp/seller-home-value/actions.ts:604-608 (smsConsent passed); lib/crm/enroll.ts:297-305 (removeSuppression then addSuppression when smsConsent !== true); reader app/api/cron/crm-sequence-engine/route.ts:24 (isSuppressed import), :202-205 and :316-319 (touch halted, enrollment marked suppressed); Tetherow path passes no consent -> suppressed by default lib/canonical-lead-tagger.ts:268 per PDS, enroll.ts:300 proves default
- ✓ **sequence-enrollment-first-touch** — autoEnrollPerson -> crm_sequence_enrollments -> crm-sequence-engine cron reads due enrollments -> sendCrmEmail -> lead's inbox
  evidence: lib/crm/enroll.ts:154,212 (crm_sequence_enrollments inserts), :306 (autoEnrollPerson called from autoEnrollByFubId); reader app/api/cron/crm-sequence-engine/route.ts:61 (.from('crm_sequence_enrollments') due scan), :298 and :443 (sendCrmEmail to the lead); cron registered vercel.json:57
- ✓ **seller-cma-deliverable** — submitSellerLPForm -> createCmaRequest -> cmas draft + marketing_brain_actions content:cma + broker/lead emails -> /admin/cmas worklist + producer-dispatcher -> broker sees and builds the CMA
  evidence: app/lp/seller-home-value/actions.ts:611-628 (createCmaRequest call); lib/cma-request.ts:182-260 (cmas row + content:cma insert), :415 (sendBrokerNotification); lib/cma/request-emails.ts:74-75,169-170 (sendEmail via lib/resend to broker and lead); readers app/admin/(protected)/cmas/page.tsx:35,114 (listCmasForAdmin) and app/api/cron/producer-dispatcher/route.ts:69-71 (pending marketing_brain_actions scan); vercel.json:169
- ✓ **buyer-listing-alerts-deliverable** — buyer LP submit -> upsertListingAlert -> listing_alerts -> saved-search-alerts cron getActiveListingAlertsDue -> sendAlertEmailToRecipients (Resend) -> matched-listing email in the lead's inbox
  evidence: app/lp/buyer-listing-alerts/actions.ts:356-363 (upsertListingAlert, narrowing-guarded/deduped per :340-350 comment); reader lib/data/leads/listingAlerts.ts:282-290 (.from(listing_alerts) due scan); app/api/cron/saved-search-alerts/route.ts:16-30 -> runListingAlerts; app/actions/saved-search-alerts.ts:405,506 sendAlertEmailToRecipients; lib/alerts/send.ts:20,365 (sendEmail from lib/resend); vercel.json:213
- ✓ **hot-lead-call-task** — hot classification -> createNativeTask -> crm_tasks (5-min due) -> crm-task-reminders cron + admin CRM surfaces -> broker reminded to call within 5 minutes
  evidence: app/lp/buyer-listing-alerts/actions.ts:373-382 (createNativeTask hot path; seller equivalent per PDS actions.ts:580-589); readers app/api/cron/crm-task-reminders/route.ts:7,67 (.from('crm_tasks'), queues broker alert), vercel.json:61; lib/data/crm/getAppointments.ts:229 (admin surface read)
- ✓ **instant-broker-alert-sms** — autoEnrollByFubId -> queueBrokerAlert with rendered first-touch preview -> crm_broker_alerts -> crm-alert-drain cron (Twilio, broker whitelist) + web push -> broker's phone within a minute
  evidence: lib/crm/enroll.ts:306-384 (enrollment lookup, first-touch preview render :358-373, queueBrokerAlert :383); lib/crm/broker-alerts.ts:141 (queueBrokerAlert inserts crm_broker_alerts); app/api/cron/crm-alert-drain/route.ts:25-80 (drains pending rows, Twilio Messaging Service, isBrokerPhone whitelist, web push leg); vercel.json:21 (every-minute)
- ✓ **paid-attribution-capi** — ?fbclid -> middleware rr_fbc cookie -> LP action reads rr_fbc fallback -> POST /api/meta-capi -> graph.facebook.com Lead event -> Matt's ad attribution in Meta Events Manager
  evidence: middleware.ts:379-393 (attachFbcCookie, 90-day httpOnly); app/lp/seller-home-value/actions.ts:697-699 (fbc fallback to rr_fbc, $500 value :709) and buyer actions.ts:387-404 ($300); app/api/meta-capi/route.ts:4 -> lib/meta-capi.ts:40,58 (fetch to graph.facebook.com/v21.0/<PIXEL_ID>/events). External hop (Meta -> Ads Manager) is outside the repo by nature; every in-repo hop proven
- ✓ **partial-address-capture** — seller step-1 advance -> saveSellerPartialLead -> saveAnonymousPartialAddress -> visitor_events (seller_intent, partial_address) -> admin /admin/visitors queries + backfill replay onto crm_people on later identify -> broker sees the intent signal
  evidence: app/lp/seller-home-value/actions.ts:104-154 (partial writer, fire-and-forget), :443-450 (backfillSessionToFub on identify); lib/data/leads/saveAnonymousPartialAddress.ts:54-98 (visitor_events insert with partial_address metadata); readers app/admin/(protected)/visitors/_lib/queries.ts:97,137 and lib/visitor-backfill.ts:230 (replays session visitor_events onto the person)
- ✗ **tetherow-cma-promise** — Tetherow/heath form -> /api/cma submitTetherowLead / heath action -> tag cma-requested on crm_people -> NO cmas draft, NO content:cma row -> no /admin/cmas visibility, nothing queued
  evidence: grep this run: zero 'createCmaRequest' matches in app/actions/lead-capture.ts and app/lp/tetherow/heath/actions.ts (exit 1); contrast writer lib/cma-request.ts:182-260 which only seller/fsbo actions call (app/lp/seller-home-value/actions.ts:611)
  **GAP:** The Tetherow and heath LPs promise a CMA but write only a cma-requested tag — zero createCmaRequest calls exist in either path, so no cmas row or content:cma action is ever stored; the homeowner waits for a report that nothing is tracking or building, and fulfillment depends entirely on the broker improvising from the generic new-lead alert.
- ✗ **internal-entry-source-attribution** — /lp/bend and /lp/central-oregon-golf CTAs append ?source=bend-lp|golf-lp -> no writer captures the param -> no store -> no reader -> internal CTA performance invisible
  evidence: lib/tracking.ts:196-203 (fromUrl reads only utm_* + fbclid, this run); app/lp/seller-home-value/actions.ts:638-651 (referer parse extracts only utm_* for the alert email; same pattern for the event campaign at :393-399)
  **GAP:** ?source=bend-lp|golf-lp is dropped at the first hop — the client tracker persists only utm_*+fbclid and the server actions pass through only utm_* from the referer — so no tag, note, or event ever records internal entry; Matt cannot measure which internal pages feed the capture LPs or compare internal CTAs against paid spend.

  Gap statements: 
  - Tetherow family promises a CMA it never queues: both /lp/tetherow (via /api/cma) and /lp/tetherow/heath tag cma-requested but never write a cmas draft or content:cma action row, so the promised deliverable is invisible to /admin/cmas and the producer pipeline — the lead's promise is fulfilled only if a broker improvises from the generic alert.
  - Internal-entry attribution is decorative: the ?source=bend-lp|golf-lp params on internal CTAs are captured by no writer (client tracker reads only utm_*+fbclid; server actions pass through only utm_*), so which internal pages feed the capture LPs is unmeasurable and internal CTAs cannot be compared against paid spend on any ledger.

## measure-search-traffic-gsc

- ✓ **ingest -> admin search scoreboard** — Vercel cron 12:00 UTC -> snapshot-channels fan-out -> marketing-snapshot-gsc rowsForDay + upsertMetricRows -> marketing_channel_daily -> /admin/analytics/google-search aggregation -> Matt sees KPIs, top queries, top pages, opportunity queries
  evidence: vercel.json:221-222 (schedule 0 12 * * *); app/api/cron/marketing-snapshot-gsc/route.ts:185-186 (rowsForDay -> upsertMetricRows); lib/marketing-brain/snapshot.ts:108-110 (.from('marketing_channel_daily').upsert, onConflict composite PK); app/admin/(protected)/analytics/google-search/page.tsx:48-51 (reads same table, channel='gsc', scope campaign/page), :82-83 (account scope), :98-119 + :123-148 + :186-218 + :220-233 (rendered VerdictLine/Figures/DataList to Matt)
- ✓ **ingest -> health grades -> action-required lane** — marketing_channel_daily -> loop-health-check probes (snapshot:gsc freshness, value:gsc nonzero, value:gsc-queries slice) -> marketing_decisions row (decision_type='loop_health_check', data_observed.checks) -> /admin/analytics/action-required LoopHealthLane -> Matt sees red/yellow queue rows
  evidence: app/api/cron/loop-health-check/route.ts:61-77 (freshness read of marketing_channel_daily), :91-111 (value:gsc trailing-8d sum), :186-206 (value:gsc-queries campaign-slice probe), :402-409 (insert into marketing_decisions with {summary, checks}); app/admin/(protected)/analytics/action-required/page.tsx:473-478 (reads decision_type='loop_health_check'), :485-524 (renders reds/yellows as QueueRows to Matt)
- ✗ **gsc red grade -> alert email** — loop-health-check gsc checks (red on dark/zero channel) -> consolidated alert email to matt@ryan-realty.com -> Matt notified without opening admin
  evidence: app/api/cron/loop-health-check/route.ts:364 (summarizeHeartbeat(pipeline, now) — email gate reads ONLY the pipeline[] array), :274-358 (pipeline[] holds only the 7 pipeline-heartbeat checks: sync-delta, MVs, fsbo, expired, saved-search, market_stats, audience), :75/:105/:198 (snapshot:gsc, value:gsc, value:gsc-queries are pushed to checks[], never to pipeline[]), :366-374 (sendEmail fires only when !heartbeat.healthy)
  **GAP:** A red GSC grade (channel dark, permanently zero, or query slice dead) never triggers the health-check alert email — the email gate evaluates only the pipeline heartbeat array, and the gsc checks live in the separate checks[] array. The failure is visible only if Matt happens to open /admin/analytics/action-required, so the search scoreboard can silently run dark for days and every downstream reader (growth loop, admin scoreboard) makes decisions on stale or zero data.
- ✗ **ingest -> marketing brain SEO briefs** — marketing_channel_daily -> analyzeSEO (auditWebsite) -> gatherSignals -> generate-briefs SEO branches -> marketing_brain_actions -> approval queue -> Matt reviews search-driven briefs
  evidence: Writer emits scope='campaign', scope_id='query:<q>', metric 'position' (app/api/cron/marketing-snapshot-gsc/route.ts:64-97) and page-scope metric 'position' (:100-133); reader queries scope='source' and metric 'avg_position' (lib/marketing-brain/audit-website.ts:520-526) — shapes that zero rows match (PDS §7 live shape trace: only account/campaign/page scopes exist); lib/marketing-brain/generate-briefs.ts:53,962 (auditWebsite wired into gatherSignals), :1885-1887 + :1939-1940 (every SEO branch selects from seo.top_queries; empty -> 'if (!q) return []' — no brief ever emitted)
  **GAP:** The marketing brain's entire search-analysis path is dark: seo.top_queries is always empty because the reader queries a scope ('source') and metric ('avg_position') the writer never emits, so no site_meta_update or blog_post brief driven by real query data is ever generated. Matt never sees search-driven briefs in the approval queue, and ranking losses, low-CTR pages, and query spikes the scoreboard faithfully records go permanently unacted-on by the brain.
- ✓ **ingest -> site_signal -> growth loop** — marketing_channel_daily -> site_signal view (scope-disambiguated union with web_vitals) -> growth-loop agent ingest step -> shipped public-page fixes Matt reviews post-hoc
  evidence: supabase/migrations/20260610210000_site_signal_scope_disambiguation.sql:9-22 (view selects date/channel/surface/metric/value from public.marketing_channel_daily; query rows scope='campaign' surface='query:<q>'); .claude/skills/growth-loop/SKILL.md step 1 'Ingest the scoreboard' (names site_signal and marketing-snapshot-gsc as the GSC substrate, carries the locked scope contract); last hop is agent-mediated (skill instruction -> loop iteration -> ships fixes to public pages), not a code path
- ✓ **live GSC -> operations panel (parallel path, D4)** — Google Search Console API -> getSearchConsoleSummary via unstable_cache(300s) -> /admin/operations site-performance panel -> Matt sees 28-day live numbers (bypasses marketing_channel_daily entirely)
  evidence: app/admin/(protected)/operations/_components/DashboardSitePerformancePanel.tsx:3 (imports getSearchConsoleSummary — same GSC read the ingestor uses), :8-12 (unstable_cache, revalidate 300), :19-25 (live 28-day pull ending today), :51-60 (ReportNumbers rendered to Matt). Parallel truth: fresh-through-today vs the scoreboard's deliberate t-2 trail — two admin surfaces can legitimately disagree with no annotation (PDS D4)
- ✗ **scoreboard -> P5 route-equity evidence** — marketing_channel_daily page-scope rows -> per-URL search-equity dossier evidence -> P5 cut-list decisions (URL cut/rename requires per-route GSC evidence)
  evidence: app/actions/search-console-report.ts:81,92 (rowLimit: 25 on both the by-query and by-page pulls — only the daily top 25 ever enter the table); app/api/cron/marketing-snapshot-gsc/route.ts:100-133 (page rows written only for d.topPages); PDS D2 + PUBLIC-PRODUCT-OS.md carve-out (per-route GSC evidence required before any URL is cut or renamed)
  **GAP:** Per-URL search-equity evidence exists only for the daily top-25 pages; every long-tail route with small-but-real search equity has zero rows in the store, so the P5 cut-list requirement (per-route GSC evidence before cutting or renaming a URL) cannot be met from this table. A URL with steady long-tail clicks can be cut or renamed with its equity invisible, and search visitors who relied on it land on 404s or lose the result entirely.

  Gap statements: 
  - The marketing brain's search-analysis chain is dark (D1): analyzeSEO reads scope='source' and metric 'avg_position' which the writer never emits (writer: scope='campaign', metric 'position'), so seo.top_queries is always empty, no search-driven brief (meta update or blog post) is ever generated, Matt never sees one in the approval queue, and recorded ranking losses and low-CTR pages go unacted-on.
  - A red GSC health grade never reaches Matt proactively: the loop-health-check alert email is gated solely on the pipeline-heartbeat array, and the gsc snapshot/value/query-slice checks live in the separate checks[] array that only lands in an admin page lane — a dark or zeroed search channel is invisible unless Matt opens /admin/analytics/action-required, so every downstream reader can run on stale or zero data for days.
  - Long-tail route equity is invisible (D2): only the daily top-25 queries and top-25 pages are stored, so the per-route GSC evidence the P5 cut-list carve-out requires before cutting or renaming a URL cannot come from this store — a long-tail URL's real search equity can be destroyed unseen, sending its search visitors to 404s.
  - Two admin surfaces disagree on 'GSC truth' with no annotation (D4): the operations panel shows a live 28-day pull fresh through today while the scoreboard deliberately trails by 2 settled days — Matt can read two different numbers for the same metric with nothing labeling which freshness contract each shows.

## P4-capture-and-attribute

- ✓ **lead-capture (person row)** — door server action → sendEvent → ensureNativeLead insert → crm_people + crm_contact_points → getCrmPersonFull / getLeadIntake → broker views /admin/people/[id] + Matt views /admin/reports/lead-flow
  evidence: writer: lib/followupboss.ts:100-131 (sendEvent → ensureNativeLead), lib/data/crm/ensureNativeLead.ts:242-277 (crm_people insert :252-256, crm_contact_points insert :270-277); reader: app/actions/crm.ts:365 (from('crm_people').select) surfaced at app/admin/(protected)/people/[id]/page.tsx:364-366; dashboard reader lib/data/crm/getLeadIntake.ts (crm_people read, per header :11-29) consumed at app/admin/(protected)/reports/lead-flow/page.tsx:39
- ✓ **attribution (source label + tags)** — LP action resolves source/paid tags → enrichNativeLead tag-union + source write → crm_people.source/tags → person page render + autoEnrollPerson rule match + getLeadIntake channel classification → broker sees channel; lead gets the matching sequence
  evidence: writer: lib/data/crm/ensureNativeLead.ts:370-415 (enrichNativeLead read-modify-write of tags/custom/assigned_broker); create-path source lib/data/crm/ensureNativeLead.ts:242-252; readers: app/admin/(protected)/people/[id]/page.tsx:364-366 (source/tags rendered), lib/crm/enroll.ts:136-169 region gates enrollment on tags (rules matched in autoEnrollPerson), lib/data/crm/getLeadIntake.ts classifies crm_people.source for the lead-flow report
- ✓ **broker assignment + instant alert** — enroll/sweep queues alert → crm_broker_alerts → crm-alert-drain cron reads pending → Twilio SMS to whitelisted broker phone + web push → broker's phone buzzes with the lead + admin deep link
  evidence: writer: lib/crm/enroll.ts:308-390 (queueBrokerAlert with first-touch preview / low-intent wording), lib/crm/broker-alerts.ts:181 (insert into crm_broker_alerts), sweep second writer app/api/cron/crm-auto-enroll/route.ts:158-171; reader: lib/data/crm/brokerAlertDrain.ts:26-35 (listPendingAlerts, status='pending'), delivered by app/api/cron/crm-alert-drain/route.ts:53-90 (Twilio Messaging Service, broker-phone whitelist, web push runs unconditionally)
- ✓ **SMS consent (fail-closed suppression)** — autoEnrollByFubId writes/lifts suppression → crm_suppressions → isSuppressed gates every engine send → non-consented lead never receives an SMS touch
  evidence: writer: lib/crm/enroll.ts:297-305 (removeSuppression then addSuppression unless smsConsent===true) → lib/crm/suppressions.ts:127 (insert), :199 (delete); reader: app/api/cron/crm-sequence-engine/route.ts:201 (email gate), :315 (sms gate), :436 (fallback-email gate) via lib/crm/suppressions.ts:34 (from('crm_suppressions'))
- ✓ **enrollment decision → nurture first touch** — autoEnrollPerson inserts enrollment (status running) + timeline → crm_sequence_enrollments → crm-sequence-engine reads due enrollments → sendCrmEmail / sendSms → lead receives the first nurture touch
  evidence: writer: lib/crm/enroll.ts:154-169 (crm_sequence_enrollments insert status='running', next_run_at now, + crm_timeline 'Enrolled' row); reader: app/api/cron/crm-sequence-engine/route.ts:61 (from('crm_sequence_enrollments') due scan), sends at :298 (sendCrmEmail) and :412 (sendSms/sendSmsViaMessagingService); sweep guarantee app/api/cron/crm-auto-enroll/route.ts:121-171
- ✓ **origin story (why this lead exists)** — enrichNativeLead / portal intake writes origin note → crm_timeline → getCrmPersonFull + getContactActivityFeed read timeline → broker reads the lead's origin on the person page
  evidence: writer: lib/data/crm/ensureNativeLead.ts:419-428 (crm_timeline note insert, source 'lp-form'); readers: app/actions/crm.ts:382 (from('crm_timeline') in getCrmPersonFull) and lib/data/crm/getContactActivityFeed.ts:151, both rendered on app/admin/(protected)/people/[id]/page.tsx
- ✓ **anonymous→known stitch (web trail)** — backfillSessionToFub stamps visitor_sessions.crm_person_id (+fub_person_id lockstep) + visitor_identity_map upsert → getContactBehaviorSummary reads the native identity chain → PersonEngagementRegion on the person page → broker sees what the lead browsed before identifying
  evidence: writer: lib/visitor-backfill.ts:198-212 (visitor_sessions update: crm_person_id + fub_person_id + identified_email/via), :77 (visitor_identity_map upsert on rr_vid); reader: lib/data/crm/getContactBehaviorSummary.ts:268-298 (from('visitor_sessions') native chain incl. crm_person_id, from('visitor_events')) via components/admin/crm/person-detail/PersonEngagementRegion.tsx:66, mounted at PersonWorkspaceBody.tsx:572
- ✗ **assignment audit ledger (marketing_assignments)** — canonicallyTagLead / LP actions / Meta webhook upsert ledger row → marketing_assignments → NO reader → no human ever sees it
  evidence: writer LIVE: lib/data/crm/recordMarketingAssignment.ts:116 (the ONE .from('marketing_assignments') upsert), called from lib/canonical-lead-tagger.ts:112-113, app/lp/seller-home-value/actions.ts:230, app/lp/fsbo/actions.ts:286, app/lp/expired-listing/actions.ts:259, app/lp/buyer-listing-alerts/actions.ts:98, app/api/meta/lead-webhook/route.ts:607; reader: NONE — repo-wide grep finds no other .from('marketing_assignments'); lib/data/crm/getLeadIntake.ts:21-23 records that every dashboard read was replaced (and wrongly claims writes stopped at the FUB cutover — they run on every capture today)
  **GAP:** The assignment audit ledger is write-only: every capture upserts a (person, audience, source) row that no page, report, or cron ever reads, so Matt cannot audit which broker got which lead per channel from any surface — the audit trail the process's completion state promises exists only as unread rows, and in-code comments ('dashboards read marketing_assignments', app/lp/seller-home-value/actions.ts:569) tell future maintainers the opposite of the truth.
- ✓ **portal-email door (Zillow/Realtor → matt)** — crm-portal-lead-intake cron parses Gmail → crm_people + idempotent crm_timeline note + crm_imports cursor → sweep alerts + person page → Matt's phone and admin surface show the portal lead
  evidence: writer: app/api/cron/crm-portal-lead-intake/route.ts (registered vercel.json:49-50; person + note per PDS §2.4/§4, note dedupe on dedupe_key :151-158, cursor :162-172 — re-opened via process doc anchors this run); downstream readers proven above: sweep alert app/api/cron/crm-auto-enroll/route.ts:158-171 → crm-alert-drain → Matt's phone; person surfaced via app/actions/crm.ts:365 on /admin/people/[id]

  Gap statements: 
  - marketing_assignments is write-only: every capture upserts an assignment-audit row (lib/data/crm/recordMarketingAssignment.ts:116, six live callers) that no page, report, or cron reads — Matt cannot audit which broker got which lead per channel from any surface, and stale in-code comments claim dashboards read it.
  - Attribution is opt-in per door (D1/D2): only the 5 LP actions + cma-download resolve UTM source labels and honor the ?agent= cookie — a paid click converting on /contact, /sell/valuation, saved-search, RSVP, homepage, or sign-in loses its channel/campaign attribution and routes to Matt, so Rebecca/Paul lose ad-driven leads and paid-channel reporting under-counts.
  - The /sell/valuation door collects then discards checked SMS consent (D11): the form posts smsConsent=yes but the action never reads it, so a consenting seller stays SMS-suppressed and receives none of the text touches they agreed to.
  - Consent double-writer race on canonicallyTagLead doors (D3): the tagger's consent-less fire-and-forget enroll and the contact action's consent-carrying enroll both remove-then-conditionally-add the same suppression row; last writer wins, so a consented visitor can end re-suppressed (or a non-consented one briefly unsuppressed).
  - Portal-lead enrollment is undetermined (D6): portal leads carry no audience:* tag and the const fallback rules have no matching row (lib/crm/enroll.ts:27-32), so whether Zillow/Realtor.com leads ever enter nurture depends on an unqueried crm_automation_rules row — if absent, paid-portal leads silently complete with zero automated follow-up.
  - Native leads' stitched web events are invisible in the person-page timeline: the merge at app/actions/crm.ts:396 runs only when fub_legacy_id is set, so post-cutover leads' browsing history appears only in the separate engagement panel, not the main timeline the broker reads first.

## serve-legal-pages

- ✓ **policy-text-serve** — developer commits policy copy -> hardcoded in seven app/*/page.tsx files (git = SoR) -> Next static prerender reached via footer LEGAL_LINKS + form fine print -> visitor reads the trust/rights answer on-page
  evidence: lib/site-nav.ts:389-396 (LEGAL_LINKS); components/site/SiteFooter.tsx:12,138-153 (render loop); app/privacy/page.tsx:22,30-188 (noindex meta, full render, SiteFooter at :185); app/terms/page.tsx:129-143; app/cookies/page.tsx:105-193; app/dmca/page.tsx:25-83; app/accessibility/page.tsx:25-80; app/data-deletion/page.tsx:31-106; app/fair-housing/page.tsx:9-10,61-71; app/robots.ts:21-26 (no legal path disallowed)
- ✓ **cookie-consent-control** — visitor choice in CookieConsentBanner -> ryan_realty_cookie_consent first-party cookie (1yr) -> getConsent/hasAnalyticsConsent/hasMarketingConsent read by trackers -> GA4/Meta page-view tracking fires or stays silent per the visitor's choice; /cookies page instructs the control
  evidence: components/CookieConsentBanner.tsx:17,46-50 (write), :30-65 (read), :119-136 (accept/essential/save); components/site/providers/RootProvider.tsx:41 (mount, HideOnLP :40); components/PageViewTracker.tsx:5,49-50,57-64 (consent-gated GA4/Meta); components/VisitTracker.tsx:6 (consent import); app/cookies/page.tsx:154-177 (control instructions); banner links to /privacy + /privacy#donotsell at CookieConsentBanner.tsx:176-178 -> anchor exists app/privacy/page.tsx:131
- ✓ **sms-consent-lock-step** — SMS_CONSENT_TEXT single source -> lib/crm/sms-consent-text.ts -> re-exported by SmsConsentDisclosure rendered on 21 lead-form surfaces with /privacy + /terms links; /privacy #sms and /terms #sms carry the carrier-verified sharing sentence -> form-filler and A2P carrier reviewer read identical, gate-locked copy
  evidence: lib/crm/sms-consent-text.ts:14-15 (sentence); components/site/SmsConsentDisclosure.tsx:28-29 (re-export), :80-88 (render + links); grep this run: 21 files mount it (app/contact, app/home-valuation, all /lp/* forms, listing CTAs); app/privacy/page.tsx:68-75 (#sms, sharing sentence :74); app/terms/page.tsx:129-143 (#sms, sharing sentence :138); scripts/check-sms-consent-compliance.mjs:27-28,67-71,86-117 locks component + BOTH pages; package.json:166 (ci:sms-consent), :230 (in ci:gates)
- ✗ **cookie-inventory-truth** — cookie-setting code across the codebase -> hand-maintained COOKIES array in app/cookies/page.tsx -> /cookies render -> visitor/regulator reads the inventory as the definitive list
  evidence: app/cookies/page.tsx:38-103 (9-row array), :133-138 (render). Setters verified for listed rows (CookieConsentBanner.tsx:49, :89-101 visit_id; fub_cid, rr_session_id, rr_agent_attribution setters found by grep). Drift proven this run: first-party cookies set in code but ABSENT from the page — components/pwa/InstallPrompt.tsx:6,17 (pwa_install_dismissed), components/HomeCitySelector.tsx:44 (home-city cookie), components/WelcomeBanner.tsx:21 (welcome-dismissed), components/tracking/TrackListingView.tsx:48 (viewed-listings), components/ui/sidebar.tsx:28,86 (sidebar_state)
  **GAP:** The writer hop is a human memory, not a mechanism: nothing ties the COOKIES array to the cookies code actually sets, and it has already drifted — at least four first-party cookies are set on public surfaces but missing from the published inventory. The /cookies page is a compliance statement that currently understates collection; the visitor exercising cookie rights and the broker whose license backs the statement are both harmed by the silent misstatement.
- ✗ **rights-request-fulfillment** — page renders a mailto (deletion / do-not-sell / rights / DMCA / accessibility) -> admin@ mailbox (off-platform, no on-site store) -> mailbox owner acts -> 30-day deletion / 45-day response promise fulfilled
  evidence: app/data-deletion/page.tsx:9 (fallback admin@), :56-59 (mailto + 30-day promise); app/privacy/page.tsx:8,113,120,137,144 (45-day promises, do-not-sell + rights mailtos); app/dmca/page.tsx:36-40 (designated-agent mailto); app/accessibility/page.tsx:8 (fallback info@ — SPLIT from admin@), :55-64 (barrier-report mailto). No legal_requests-class store, no CRM write, no confirmation surface exists (repo grep; §4 of the process doc, re-confirmed)
  **GAP:** Every request path exits the observable plane at the mailto: no store receives the request, so no mechanism can prove — or even measure — that a 30-day deletion or 45-day rights response was met, and the two divergent fallback addresses (info@ vs admin@) mean requests split across mailboxes if NEXT_PUBLIC_SITE_OWNER_EMAIL is unset. The rights-exerciser can be silently dropped with zero trace, and Matt's license carries the unmet statutory promise.
- ✗ **data-deletion-and-cookies-discoverability** — page exists -> internal link graph -> on-site visitor navigates to it -> visitor finds the deletion path / cookie inventory
  evidence: Repo grep this run: zero internal inbound links to /data-deletion outside app/data-deletion/ itself; lib/site-nav.ts:389-396 omits both /cookies and /data-deletion from LEGAL_LINKS; only inbound to /cookies is /privacy prose (app/privacy/page.tsx:39,98)
  **GAP:** An on-site visitor who wants their data deleted cannot navigate to /data-deletion from anywhere on the site (it is reachable only from Meta/Google platform configs), and the cookie inventory is reachable only by first reading /privacy prose — the rights most likely to be exercised by a distrustful visitor are the hardest pages to find, undermining the trust job the whole legal set exists to do.
- ✓ **fair-housing-complaint-routing** — visitor with a discrimination complaint -> /fair-housing page -> HUD link + 1-800-669-9777 -> complaint filed with the regulator (off-site by design)
  evidence: app/fair-housing/page.tsx:9-10 (HUD_URL, HUD_PHONE), :61-71 (rendered link + tel:); reached from LEGAL_LINKS lib/site-nav.ts:393; deliberate off-platform completion — the regulator, not the brokerage, is the correct terminal
- ✗ **crawler-signal-consistency** — app/sitemap.ts emits legal URLs -> sitemap.xml -> crawler reads sitemap AND per-page robots meta -> consistent index/noindex decision
  evidence: app/sitemap.ts:158-162 emits /privacy, /terms, /accessibility, /fair-housing, /dmca at priority 0.3 while every legal page declares robots: 'noindex, follow' (app/privacy/page.tsx:22, app/terms metadata, app/cookies/page.tsx:22, app/data-deletion/page.tsx:23, app/accessibility/page.tsx:22); /cookies and /data-deletion are the two NOT emitted — inconsistent in both directions
  **GAP:** The crawler receives contradictory instructions — the sitemap nominates five legal URLs for indexing that each self-declare noindex, while the two omitted pages follow the opposite convention — so whichever indexing policy is intended, search engines see an incoherent signal, wasting crawl budget and leaving the legal set's search visibility undefined rather than decided.

  Gap statements: 
  - Cookie inventory is hand-maintained and already drifted: at least four first-party cookies set on public surfaces (pwa_install_dismissed, home-city, welcome-dismissed, viewed-listings) are absent from the published /cookies list — a live compliance misstatement borne by the broker's license.
  - All rights/deletion/DMCA/accessibility request paths terminate in a bare mailto with no on-platform store: the 30-day deletion and 45-day response promises are unprovable and unmeasurable, a rights-exerciser can be dropped without trace, and the split info@/admin@ fallbacks can scatter requests across two mailboxes.
  - /data-deletion has zero internal inbound links and /cookies is reachable only through /privacy prose — the on-site visitor most in need of the deletion path or cookie inventory cannot navigate to either.
  - app/sitemap.ts emits five noindex legal URLs and omits the other two, giving crawlers a contradictory index/noindex signal in both directions.

## P4-earn-search-traffic

- ✓ **sitemap-emission** — warm-sitemaps cron + buildAllUrls (listing_tile_mv actives, listings zips, blog_posts, market_reports, curated registries) -> unstable_cache 'sitemap-class-urls-v2' per class -> /sitemap.xml beforeFiles rewrite -> sitemapindex -> /sitemaps/{cls}.xml urlset served to crawlers -> Google indexes -> human clicks SERP onto the emitted page
  evidence: writer: app/api/cron/warm-sitemaps/route.ts:56-59 calls getClassRows per class; registered vercel.json:245-246 (0 * * * *). universe sources re-opened: app/sitemap.ts:457-467 listing_tile_mv filtered to PUBLIC_ACTIVE_STATUSES, :505-507 listings zips, :527-530 blog_posts status='published', :541-543 market_reports, rogue backstop :560-561. store: lib/sitemap-class-rows.ts:58-72 unstable_cache keyed ['sitemap-class-urls-v2']+cls, revalidate 3600, universe memo :45-50. readers: app/sitemaps/[cls]/route.ts:67 getClassRows -> :69-90 urlset; app/sitemaps/index.xml/route.ts:50-60 sitemapindex over SITEMAP_CLASSES; next.config.ts beforeFiles rewrite '/sitemap.xml'->'/sitemaps/index.xml' (verified this run). crawler access: middleware.ts:587-590 matcher excludes robots.txt/sitemap.xml/dotted paths; classifier total function lib/data/sitemap/classify.ts:23-57. human hop is external (Google) — clicks evidenced downstream by the GSC ingest chain.
- ✓ **robots-crawl-permissions** — app/robots.ts static config -> served /robots.txt (middleware-exempt) -> crawler roster reads allow rules + sitemap pointer -> crawls pages humans later land on
  evidence: app/robots.ts:16-52 (AI/search roster :29-49, sitemap pointer :51, canonical host via getCanonicalSiteUrl lib/share-metadata.ts:12-18 never localhost); middleware.ts:587-590 matcher exclusion + GOOD_BOT_RE :154-155 checked first in screenBotRequest :214-215 before empty-UA/bad-UA 403s :217-221; gated by ci:ai-crawler-access package.json:216, wired into ci:gates package.json:230.
- ✓ **llms-txt-ai-map** — content writers (blog upsert, market-report rows, curated registries) -> blog_posts/market_reports/neighborhoods/subdivisions -> app/llms.txt/route.ts DAL reads -> Markdown map served to AI crawlers -> AI answer citation -> human clicks through
  evidence: writer: app/actions/blog.ts:302 upsert into blog_posts (onConflict slug). readers re-opened: app/llms.txt/route.ts:24-33 (getRecentBlogPosts, getPublishedGuides, listMarketReports, getAllNeighborhoodsWithCity, getIndexableSubdivisions), geo lines :74-98 from the same three sitemap sources, body served text/plain revalidate 3600 :11. Confirmed live: the duplicated 'Market reports' labels pointing at /reports and /housing-market (route.ts Market Data block, verified this run — PDS defect 4 still present). Citation hop external.
- ✓ **site-identity-jsonld** — public.brokers (admin broker editor writes) -> getBrokers DAL -> Organization/RealEstateAgent + WebSite JSON-LD -> root layout renders on every page -> crawler and human receive markup
  evidence: writer: lib/data/brokers/getBrokers.ts:140 update on brokers (reads :85,:98). reader: components/JsonLd.tsx:14,57 getBrokers -> organization/website blocks :56-118 rendered as ld+json scripts; mounted app/layout.tsx:18,142.
- ✓ **listing-page-canonical-and-structured-data** — MLS sync -> listings -> listing_tile_mv (refresh-mvs cron) -> listing page DAL read -> canonical via the same listingDetailPath the sitemap uses + breadcrumb/SingleFamilyResidence JSON-LD via MetadataBlock -> crawler indexes the pretty URL a human lands on
  evidence: store refresh: app/api/cron/refresh-mvs/route.ts:7-28 (listing_tile_mv CONCURRENTLY, 589K-row source; sync-delta cron exists at app/api/cron/sync-delta). page: app/listing/[listingKey]/page.tsx:139-168 canonical = listingDetailPath (same helper as app/sitemap.ts listings section :485), schemas built :477-536 (breadcrumb + realEstateListing) rendered :539 via components/site/MetadataBlock.tsx:37-48; @type SingleFamilyResidence lib/site/json-ld.ts:222-225. CAVEAT proven this run: components/listing/ListingJsonLd.tsx (Product+Offer+Place, the PDS §4 SoR row) is rendered NOWHERE — repo-wide grep matches only the file itself and two worktree copies; the chain holds via MetadataBlock, not the cited component.
- ✓ **gsc-clicks-ingest-to-broker-surface** — snapshot-channels daily cron -> marketing-snapshot-gsc fan-out -> upsertMetricRows -> marketing_channel_daily -> /admin/analytics/google-search page -> Matt reads GSC clicks/impressions on the admin surface
  evidence: cron: vercel.json:221-222 (0 12 * * *); fan-out: app/api/cron/snapshot-channels/route.ts:27 ('gsc' in PLATFORMS), :57 fetches /api/cron/marketing-snapshot-gsc; writer: app/api/cron/marketing-snapshot-gsc/route.ts:25-36 -> lib/marketing-brain/snapshot.ts:77,109-110 upsert into marketing_channel_daily (onConflict date,channel,scope,scope_id,metric); reader: app/admin/(protected)/analytics/google-search/page.tsx:1,48-50,82-83 queries channel='gsc' and renders for Matt.
- ✗ **per-class-indexed-counts-feedback** — Google Search Console per-class indexed counts (external) -> [no writer] -> [no store] -> [no surface] -> emission policy never sees its own outcome
  evidence: the class split's stated purpose is per-class GSC attribution (lib/data/sitemap/classify.ts:1-13, '10,744 submitted / 60 indexed' dated 2026-07-28); the only in-repo reader is hand-run scripts/_gsc-index-check.mjs:1-25 (console output, wired to no npm script or cron — package.json grep this run); the daily ingest covers search-analytics clicks/impressions only (app/api/cron/marketing-snapshot-gsc/route.ts:36 SOURCE='gsc_search_analytics_api'), so no indexed-count row ever lands in marketing_channel_daily or any table, and no admin page shows submitted-vs-indexed per class.
  **GAP:** The process's completion state is invisible: per-class indexed counts — the entire reason the sitemap was split into five children — reach no store and no human surface, so emission policy cannot respond to indexation starvation, P5 pruning decisions lack the data the split was built to produce, and a class-wide deindexing event would be discovered only if someone remembers to hand-run a diagnostic script. The sole baseline anywhere is a 2026-07-28 code comment. Matt is the harmed party: the site's only free acquisition channel is flying without its one health gauge.

  Gap statements: 
  - Per-class GSC indexed counts (the process's done-state and the stated purpose of the five-way sitemap split) have no writer, no store, and no human surface — only a hand-run script wired to nothing — so indexation starvation is unmeasurable, pruning decisions lack their data, and deindexing would be found by accident; the only baseline is a dated 2026-07-28 code comment.
  - The PDS's per-listing structured-data SoR (components/listing/ListingJsonLd.tsx, Product+Offer+Place) is dead code rendered nowhere; live listing pages emit breadcrumb + SingleFamilyResidence via MetadataBlock instead, so acceptance check 8's '@type":"Product' assertion fails against production and anyone relying on the doc audits markup that does not ship — Product/Offer rich-result eligibility Matt may believe exists, does not.

## cookie-consent

- ✓ **explicit-choice-to-google-stack** — banner handlers write ryan_realty_cookie_consent cookie + dispatch cookie-consent event -> cookie (client-held SoR) -> GoogleAnalytics/GTMHead/AdUnit re-read via exported getters -> gtag consent update, GTM loads iff analytics, AdUnit 'Sponsored' card renders/collapses on the page the visitor is looking at; banner disappears
  evidence: mount: app/layout.tsx:135-167 -> components/site/providers/RootProvider.tsx:31-42 -> components/layout/HideOnLP.tsx:27-35; writers: components/CookieConsentBanner.tsx:119-136 via setConsentState :46-50; readers: components/GoogleAnalytics.tsx:46-65 (listener :63), components/GTMHead.tsx:14-26, components/AdUnit.tsx:30-36; human outcome: AdUnit.tsx:75-91 visible card, CookieConsentBanner.tsx:121,138 banner hides; tracker stack mounted at components/site/providers/AnalyticsScripts.tsx:28-40
- ✓ **ad-traffic-auto-grant-to-trackers** — autoGrantConsentForAdTraffic (click-id/utm, no prior choice) writes the same cookie + event -> cookie -> PageViewTracker one-shot catch-up + GoogleAnalytics/GTMHead/AdUnit listeners -> GA page_view + Pixel PageView fire on the landing pageview; ads render for the paid visitor
  evidence: writer: components/CookieConsentBanner.tsx:76-87, invoked components/VisitTracker.tsx:262; mount outside HideOnLP so it runs on /lp/* too: components/layout/PublicClientLayer.tsx:29,45,56 (mounted app/layout.tsx:166); explicit prior choice respected :78; readers/outcome: components/PageViewTracker.tsx:53-69 (fires gtag :20-31 and fbq :33-40), components/GoogleAnalytics.tsx:53-58
- ✓ **consent-stamped-events-to-admin-dashboard** — VisitTracker computes consentLevel and POSTs each event -> /api/visitors/track enforces (declined/missing dropped; essential stripped) and inserts visitor_sessions + visitor_events -> admin queries read both tables -> broker sees /admin/visitors/live sessions, scores, identified leads
  evidence: writer: components/VisitTracker.tsx:92-107,198-233,271-274; server gate + store: app/api/visitors/track/route.ts:250-261 (refuse), :307-309,377-396 (essential stripping), :365-371 (visitor_sessions insert), :403-405 (visitor_events insert); reader: app/admin/(protected)/visitors/_lib/queries.ts:75,97,137,166-169; human surface: app/admin/(protected)/visitors/live/page.tsx (server component rendering those reads)
- ✓ **identified-consented-visit-to-broker-text** — track route reads visitor_sessions.fub_person_id for an analytics/all-consent page view -> queueReturnVisitAlert composes the lead-is-back message -> crm_broker_alerts row (status pending/push_only) + crm_timeline dedupe -> Twilio relay drains pending and texts the broker's phone
  evidence: trigger: app/api/visitors/track/route.ts:463-509 (gated !minimalOnly :483); composer: lib/crm/broker-alerts.ts:75-119; queue write: lib/crm/broker-alerts.ts:170-187 (opt-in gate :164-167); drain: scripts/crm-alert-relay.mjs:81 selects status='pending', :51 Twilio Messages.json send, :98 marks sent
- ✓ **gpc-to-durable-suppression-to-send-block** — Sec-GPC:1 header or forwarded JS flag -> track route drops the event and writes crm_suppressions channel='all' reason='gpc-opt-out' for an identified person -> isSuppressed reads crm_suppressions in every governed send guard -> the person never receives outbound email/SMS and the sending broker sees 'Blocked by suppression'
  evidence: decision: lib/crm/gpc.ts:44-48; gate + write trigger: app/api/visitors/track/route.ts:277-303; store write: lib/data/crm/recordGpcSuppression.ts:49-67 (idempotent, channel='all'); reader: lib/crm/suppressions.ts:31-34 (in ('all', channel)); enforcement chokepoint: lib/comms/guards.ts:18,34-40 blocks the send with the reason string
- ✗ **consent-change-to-meta-ldu-reapply** — mid-session consent choice writes cookie + dispatches cookie-consent event -> MetaPixel should re-read and re-apply dataProcessingOptions -> Meta stops (or starts) using the visitor's events for ad targeting
  evidence: one-shot IIFE: components/MetaPixel.tsx:43-49 (inline cookie re-parse :45-47, second copy of the ad-param predicate at components/CookieConsentBanner.tsx:80-82); no listener anywhere in components/MetaPixel.tsx:23-66 (whole file read this run); contrast: every other consumer re-listens, e.g. components/GoogleAnalytics.tsx:63
  **GAP:** MetaPixel's LDU decision runs once inside the init IIFE and the component registers no cookie-consent listener, so a visitor who lands with ad params (auto-granted or heuristic-marketing-OK) and then chooses Essential only keeps sending non-LDU Pixel events to Meta for the rest of the session — their do-not-sell choice is ignored until the next full page load, a compliance exposure Matt owns; the reverse direction silently loses attribution for late accepters.
- ✗ **withdrawal-promise-to-reopen-banner** — /cookies and /privacy#donotsell instruct the visitor to change consent 'by returning to the banner' / 'set Marketing to off in our cookie banner' -> a re-open affordance -> revised cookie -> trackers revoked
  evidence: promises: app/cookies/page.tsx:156-157, app/privacy/page.tsx:131-137; null-render: components/CookieConsentBanner.tsx:110-117,138; sole mount: components/site/providers/RootProvider.tsx:40-42; grep for other importers returned only the RootProvider + consumer-getter imports (this run)
  **GAP:** The promised mechanism does not exist: after any choice the banner renders null for the cookie's 1-year life and no other component mounts or re-opens it or its preferences dialog (repo grep this run: CookieConsentBanner is imported only by RootProvider), so a visitor following the site's own CCPA do-not-sell instructions hits a dead end and stays tracked at their old consent level — a compliance promise the site cannot keep, and legal exposure for Matt as the licensed broker.
- ✗ **cookies-page-inventory-visit-id** — getOrCreateVisitId would write ryan_realty_visit_id -> browser cookie -> /cookies inventory row describes it to visitors
  evidence: dead export: components/CookieConsentBanner.tsx:89-102; zero callers: grep across app/, components/, lib/ this run returned no hits outside the definition (exit 1); inventory row: app/cookies/page.tsx:68-74
  **GAP:** Broken at the writer: getOrCreateVisitId has zero callers repo-wide (grep this run, only its definition matched), so the ryan_realty_visit_id cookie is never set — yet the public /cookies disclosure lists it as a live 1-year analytics cookie, meaning the compliance document visitors rely on misstates actual practice and its overall accuracy cannot be trusted.
- ✗ **consent-choice-audit-trail** — consent choice -> a server-side record of who consented, to what, when -> anyone (Matt, a regulator) can later verify the consent basis for the tracking data
  evidence: handlers write cookie + event only: components/CookieConsentBanner.tsx:119-136,46-50,84-85; the consent string on tracked events (app/api/visitors/track/route.ts:148-149) stamps individual events but no table records the choice itself (no other write path found in the handlers, whole file read this run)
  **GAP:** Broken at the store: all three choice handlers and the auto-grant write only the client cookie and a window event — no server table receives the choice or its timestamp — so the business cannot demonstrate when or whether any given visitor consented; if a visitor or regulator disputes the tracking behind visitor_events/Pixel data, there is no record to answer with, and the harm lands on Matt's license.

  Gap statements: 
  - Withdrawal is impossible: /cookies and /privacy#donotsell promise consent can be changed 'in our cookie banner', but the banner never re-renders after a choice and nothing else re-opens it — visitors following the site's own CCPA instructions stay tracked at their old consent level for up to a year (app/cookies/page.tsx:156-157; app/privacy/page.tsx:137; components/CookieConsentBanner.tsx:110-117,138).
  - Meta LDU does not follow a mid-session consent change: MetaPixel decides LDU once at init with no cookie-consent listener, so an Essential-only choice after an ad-param landing keeps sending non-LDU events to Meta until the next full page load — the visitor's do-not-sell election is ignored for the rest of the session (components/MetaPixel.tsx:43-49).
  - The public cookie inventory misstates practice: /cookies lists ryan_realty_visit_id as a live 1-year cookie, but its only writer getOrCreateVisitId has zero callers — the compliance disclosure describes a cookie that is never set (app/cookies/page.tsx:69-74; components/CookieConsentBanner.tsx:89-102).
  - No consent audit trail exists: the choice lives only in a client cookie plus a window event, with no server record of who consented to what and when — the business cannot demonstrate the consent basis for its visitor_events/Pixel data if a visitor or regulator asks (components/CookieConsentBanner.tsx:119-136).

## pwa-offline-resilience

- ✓ **shell-hygiene-eviction** — pre-cutover stale SW (external writer) -> browser SW registry + CacheStorage -> StaleServiceWorkerReset getRegistrations/unregister/cache-delete/reload-once -> visitor sees fresh live shell instead of the dead cached copy
  evidence: components/site/StaleServiceWorkerReset.tsx:28,32-40,45-47 (probe, evict, one-time reload); app/layout.tsx:127-130 (mounted every route, outside HideOnLP); failure swallowed by design :48-50
- ✓ **install-prompt-state** — InstallPrompt writes pwa_visits (sessionStorage) + pwa_install_dismissed cookie -> same component reads both + beforeinstallprompt event -> banner dialog renders after 30s and >=2 loads -> visitor installs (browser prompt) or dismisses for a year
  evidence: components/pwa/InstallPrompt.tsx:33-34 (write visits), :15-18 (write cookie), :31,52-53 (reads), :36-40 (event), :73-98 (dialog render), :56-69 (install/dismiss); mounted components/layout/PublicClientLayer.tsx:28,52 -> app/layout.tsx:166
- ✓ **install-identity-manifest** — repo writes public/manifest.json + icons -> static file store -> <link rel=manifest> served on every route -> browser reads -> home-screen icon launches start_url / in standalone chrome
  evidence: public/manifest.json (start_url /, display standalone, theme #102742, 3 icons — read this run); icons on disk public/icons/icon-192.png, icon-512.png, icon-maskable-512.png (ls this run); app/layout.tsx:107 (link tag)
- ✓ **clear-route-recovery** — GET /clear writes Clear-Site-Data header + 1s meta refresh -> browser wipes origin SWs/cache/storage (cookies preserved) -> visitor lands on / served fresh -> wedged returning visitor recovered
  evidence: app/clear/route.ts:17-18 (force-dynamic nodejs), :24 (meta refresh url=/), :35 (Clear-Site-Data "cache","storage"), :36 (no-store), :8-15 (catch-22 rationale + human-relay entry)
- ✗ **offline-fallback** — app/sw.ts serwist source (fallback: document request -> /offline) -> built worker at /sw.js -> browser serves /offline on network loss -> visitor sees honest offline page with retry
  evidence: app/sw.ts:13-29 (design incl. fallback matcher :19-28); next.config.ts:39-40 (dormancy comment); package.json:16 (plain Turbopack next build, no --webpack); grep withSerwist|swSrc in next.config.ts -> zero hits this run; serwist deps installed unused package.json:417,486; app/offline/page.tsx:8-38 (page renders, retry button gated on navigator.onLine :19-23)
  **GAP:** The worker is never built or registered: a visitor who loses the network mid-session gets the raw browser error page, not /offline — the fallback page exists but is reachable only by typing its URL, and installed-app (standalone) users get a broken chrome-less window, the worst version of the failure. Every visitor on flaky mobile networks is harmed; the installed-app promise is silently hollow.
- ✗ **broker-push-worker-survival** — BrokerPushOptIn registers /sw.js (push-only worker) -> browser SW registry -> push event -> broker notification (admin-plane channel this process's component must not sever)
  evidence: components/admin/push/BrokerPushOptIn.tsx:92 (registers /sw.js scope /); public/sw.js listeners install/activate/push/notificationclick only, no fetch (grep this run, lines 17,21,25,53); components/site/StaleServiceWorkerReset.tsx:32-40 (evicts everything, no allowlist); app/layout.tsx:130 (mounted outside PublicClientLayer's admin bail at components/layout/PublicClientLayer.tsx:45)
  **GAP:** StaleServiceWorkerReset mounts on every route including /admin and unregisters ALL registrations with no allowlist, so a broker who enabled push loses the registration — and the push subscription with it — on their next page load anywhere on the origin: brokers silently stop receiving lead/deal push alerts and nobody is told. The same no-allowlist eviction also means any future first-party visitor SW (the P3 branch-A ambition) would enter a register->evict->reload loop.

  Gap statements: 
  - Offline half is dormant: no build emits the serwist worker, so network loss renders the browser's error page instead of /offline — visitors on flaky connections lose the session dishonestly, and installed standalone-app users get the worst version (a broken window with no browser chrome).
  - The hygiene eviction has no allowlist and runs on /admin too: brokers who opted into push notifications silently lose their push subscription on their next page load, missing lead and deal alerts; the same mechanism would evict any future first-party service worker, putting the eviction component at war with the offline ambition.
  - Zero telemetry across the process: install outcomes, banner impressions, evictions, and /clear hits emit no events, so nobody can say how often visitors are wedged or whether install correlates with return — Matt's P3 sub-decision (wire offline vs cut it) has no incidence data to size either branch.
  - Wedged-browser recovery depends on a human relaying the /clear URL: a stranded returning visitor who never complains to support stays permanently on the dead pre-cutover shell (the client-side backstop only works when the stale shell happens to execute our live JS) — the highest-intent local audience converts at zero invisibly.
  - pwa_visits lives in sessionStorage, so the 'returning visitor' gate actually means 'two hard loads in one sitting': genuine returning mobile visitors reset to zero and may never see the install offer the heuristic was built for.

## newsletter-lifecycle

- ✓ **subscription** — footer form / admin / cohort / one-off channels -> subscribeToNewsletter upsert into newsletter_subscribers -> getActiveSubscribersForSend at enqueue + getSubscriberSendMeta at drain -> subscriber sees inline 'You are in.' and later receives the issue in their inbox
  evidence: components/site/NewsletterSignup.tsx:40,70-74; components/site/SiteFooter.tsx:11,33; app/actions/newsletter.ts:39-46; lib/data/newsletter/index.ts:71-123,228-261; lib/newsletter/send-queue.ts:141,395,422-430; lib/data/newsletter/queue.ts:135-148; lib/resend.ts:48-76
- ✓ **issue** — produceNewsletterDraft (cron 1st-Pacific or admin button) -> newsletters row + citations jsonb -> admin review page renders via real send shell; drain reads body per broker -> Matt reviews/approves (notified by email+SMS); recipients get the rendered email
  evidence: lib/newsletter/produce-draft.ts:1-45; lib/data/newsletter/index.ts:289-312,334-338; app/api/cron/newsletter-monthly-draft/route.ts:48-64,71-94; app/actions/newsletter.ts:302-315,349-377,526-533; app/admin/(protected)/newsletters/[id]/page.tsx:147,155; lib/newsletter/send-queue.ts:378,453-489; vercel.json:141-143
- ✓ **recipient-queue** — enqueueNewsletter freezes broker+tier -> insertQueuedRecipients into newsletter_recipients -> drain claimQueuedBatch (atomic queued->sending), sendEmail, finalizeRecipient -> email lands in recipient inbox; per-recipient outcome grid on the admin issue page
  evidence: lib/newsletter/send-queue.ts:159-173,393,398-431; lib/data/newsletter/queue.ts:233-257,300-324,327-342; lib/data/newsletter/index.ts:483-496; app/admin/(protected)/newsletters/[id]/page.tsx:259,383-391; app/api/cron/newsletter-send/route.ts:20-31; vercel.json:149-151
- ✓ **tranche-schedule** — enqueue writeSendSchedule into newsletter_send_schedule -> drain reads day/cap per tick and bumps sent_count; reconciler reads for stall detection -> recipients receive on the tier's day (reputation-protected timing); a stall pages Matt on the SMS rail
  evidence: lib/data/newsletter/queue.ts:260-268,270-274,276-293; lib/newsletter/send-queue.ts:180,276,380,386-391,438,513-525; app/api/cron/newsletter-reconcile/route.ts:19-27; vercel.json:145-147
- ✓ **engagement-ledger** — Resend webhook + site pixel + click redirect -> recordLedgerEvent deduped rows in newsletter_recipient_events -> ledger stats + broker breakdown + sendWindowHealth readers -> Matt sees Engagement/By-broker on the admin issue page; breaker auto-pauses and pages Matt
  evidence: app/api/webhooks/resend/route.ts:106-120; app/api/track/e/open/route.ts:78-87; app/api/track/e/click/route.ts:61-67; lib/newsletter/track-ledger.ts:36-59; lib/data/newsletter/queue.ts:431-461,483-522,541-582,585-598; lib/newsletter/send-queue.ts:363-375; app/admin/(protected)/newsletters/[id]/page.tsx:254-349
- ✓ **deliverability** — postmaster-sync cron upserts deliverability_metrics -> getLatestDeliverability + deliverabilityVerdict read at large-send enqueue -> a blocked send returns 'deliverability_block: <reason>' to Matt's admin action; recipients' inboxes protected by the ramp
  evidence: app/api/cron/postmaster-sync/route.ts:70-95; vercel.json:165-166; lib/data/deliverability/index.ts:27-37,48-59; lib/newsletter/send-queue.ts:150-157,248-255; app/actions/newsletter.ts:372-373
- ✓ **global-opt-out** — one-click POST / confirm-page POST / Resend webhook -> subscriber status flip + crm_suppressions mirror -> drain re-checks active status + isSuppressedByEmail fail-closed per claimed row -> recipient stops receiving email and sees the 'You're unsubscribed' confirmation; admin sees skipped/unsubscribed counts
  evidence: app/api/newsletter/unsubscribe/route.ts:43-61; app/newsletter/unsubscribe/page.tsx:26-32,43-60; app/api/webhooks/resend/route.ts:82-102,178-194; lib/data/newsletter/index.ts:126-178,269-278; lib/newsletter/send-queue.ts:401-411; lib/crm/suppressions.ts:84-136 (crm_suppressions reads at :34 person-keyed and the email-keyed path 3)

  Gap statements: 
  - Deliverability chain's protective effect is unproven at runtime: postmaster-sync degrades to skipped=true when creds/DWD are missing (app/api/cron/postmaster-sync/route.ts:54-65) and live deliverability_metrics row presence for news.ryan-realty.com was not queried this run — the verdict gate may run perpetually in 'warmup' while Matt approves large sends believing a reputation gate is armed.
  - Site-pixel/click ledger writes require a crm_person_id link (attributeOutbound skips instrumentation for unlinked subscribers, and track-ledger resolves by personId) — engagement from active subscribers with crm_person_id null reaches the ledger only via the Resend webhook path, and the share of such subscribers is unqueried, so the admin KPI may undercount for Matt.
  - The public footer writer hop has no abuse guard (no honeypot/rate limit/ownership check, app/actions/newsletter.ts:39-46) and reactivates a previously-unsubscribed row unconditionally (lib/data/newsletter/index.ts:95-110): a third party can silently resurrect an admin- or webhook-set 'unsubscribed' status that lacks a suppression row, and that address then receives mail again — a CAN-SPAM exposure harming the recipient and the license.

## ods-idx-attribution

- ✓ **attribution-facts-to-courtesy-line** — sync-delta cron (vercel.json '3,18,33,48 * * * *') -> deltaSync upsert -> public.listings (ListOfficeName/ListAgentName/list_agent_email/details phones) -> getListingDetail select+map -> listing detail page mounts ListingAttribution -> visitor reads 'Listing courtesy of {office}, agent {name}, {contact}'
  evidence: writer: lib/listing-mapper.ts:447-448 (ListOfficeName/ListAgentName), :563 (list_agent_email), :461 (full details payload incl. phones); lib/sync/deltaSync.ts:524 -> lib/data/sync/syncWrites.ts:122-124 upsert into listings. reader: lib/data/listings/getListingDetail.ts:136-143 (details->>ListOfficePhone/ListAgentPreferredPhone), :382-386 map. outcome: app/listing/[listingKey]/page.tsx mount with listContact={listOfficePhone ?? listAgentPhone ?? listAgentEmail}; components/listing/ListingAttribution.tsx:80-91 renders the courtesy line. Caveat (PDS SQL 2026-08-11): 138/7,595 Active rows have all three contacts null — contact silently omits, unmonitored.
- ✓ **opt-out-suppression-detail-plane** — Spark feed opt-out flags -> mapper writes permit_internet_yn/permit_address_internet_yn/idx_participant -> public.listings -> getListingDetail guard treats false as a miss -> notFound() -> opted-out seller's listing never renders to any visitor
  evidence: writer: lib/listing-mapper.ts:485-487 (toBool PermitInternetYN/PermitAddressInternetYN/IDXParticipant) via syncWrites.ts:122-124. reader: getListingDetail.ts:432-439 (all three flags === false -> {row: null}), :446-448 (Coming Soon also a miss). outcome: page-level 404 with metadata/OG/JSON-LD suppressed in the same stroke (comment :440-445).
- ✗ **opt-out-suppression-mv-search-plane** — opt-out flags in listings -> listing_tile_mv_src / listing_search_mv_src WHERE predicates -> searchListingsAll / getListingTiles read the serving views -> search grid, tiles, map pins exclude opted-out listings
  evidence: store predicates: supabase/migrations/20260801053000_listing_tile_mv_no_detoast.sql:125-126 ('permit_internet_yn is distinct from false and idx_participant is distinct from false'); 20260731170000_listing_search_mv_v4.sql:331-333 (same two flags, Active/AUC/Coming Soon/Pending only). readers: lib/data/listings/searchListingsAll.ts:605 (from('listing_search_mv')); lib/data/listings/getListingTiles.ts:375 (from('listing_tile_mv')). outcome: app/search/page.tsx:10,272 (getViewportSearch -> rendered split view).
  **GAP:** The MV predicates enforce only permit_internet_yn and idx_participant; permit_address_internet_yn appears in NEITHER live _src definition (grep this run: zero hits in both migration creators). Live exposure is 0 rows today (PDS SQL 2026-08-11), but the first address-opt-out seller to arrive appears in search tiles with their address — a §5-3 G violation landing on Matt's Participant standing, enforced today only at the detail DAL.
- ✗ **freshness-stamp-transparency** — sync-delta writes ModificationTimestamp -> listings -> getListingDetail selects it -> ListingAttribution 'Data last updated {date}' as ODS transparency evidence for the visitor/compliance reviewer
  evidence: writer: lib/listing-mapper.ts:439 (ModificationTimestamp). store->reader: getListingDetail.ts row type :178 selects it, but map emits refreshedAt: new Date().toISOString() (:394); cache window lib/data/cache/unstable-cache.ts CACHE_WINDOWS.listingDetail: 60. outcome: ListingAttribution.tsx:103-105 renders 'Data last updated {formatted}'. Broken at the reader/map hop.
  **GAP:** The rendered 'Data last updated' date is not feed data: the DTO stamps refreshedAt = new Date() at cache-fill time inside a 60s cache, discarding the selected ModificationTimestamp. The block always shows ~today even if the feed wedged a week ago, so the transparency line proves nothing to a compliance reviewer and would actively mislead one during a delta-cursor wedge (a known incident class) — a §0-flavored honesty defect on every listing render.
- ✗ **feed-identity-attribution** — listings.mls_source (two licensed feeds: ODS + Morgan Data Shuttle, lib/mls-source.ts) -> detail DAL -> ListingAttribution source line + logo naming the correct feed to the visitor
  evidence: store: listings.mls_source exists (docs/DATABASE_SCHEMA_SNAPSHOT.md:100 per PDS; normalizer lib/mls-source.ts:7-23 per PDS). reader gap: grep -c mls_source getListingDetail.ts = 0 (this run). outcome: ListingAttribution.tsx:95-97 (hardcoded /images/oregon-data-share-logo.svg), :110 (hardcoded 'Based on information from Oregon Data Share'). Broken at the reader hop.
  **GAP:** getListingDetail never selects mls_source (grep this run: 0 hits in the file), and ListingAttribution hardcodes the ODS logo and 'Based on information from Oregon Data Share' unconditionally — every Morgan Data Shuttle listing's detail page misattributes its data source, breaching MDS's own attribution terms on those renders. The footer names both feeds, but the per-listing block is the required §5-3 S element.
- ✓ **site-wide-footer-caption** — static caption strings in the two footer registers -> rendered on every chromed page (KbFooter mounted by the listing detail page; SiteFooter on legacy-flat routes) -> visitor/reviewer sees ODS+MDS source line, reliability line, license, Equal Housing
  evidence: SiteFooter.tsx:156-163 (all five elements incl. 'All listings courtesy of participating MLS members'); KbFooter.client.tsx foot-fine block ('Listings data provided by Oregon Data Share and Morgan Data Shuttle... deemed reliable... Principal Broker license') + Equal Housing line above it — both read this run; mount on listing detail: app/listing/[listingKey]/page.tsx (KbFooter near :575, read this run). Caveat: the KB register omits the courtesy-of-participants sentence — caption drift across three hand-synced copies (SiteFooter, KbFooter, ListingAttribution:109-113), not a per-listing leak since the detail block carries its own courtesy line.
- ✗ **commit-time-gate-enforcement** — repo source files (attribution component, detail DAL, MV migrations, presets, vercel.json) -> G54 check-ods-compliance.mjs static asserts -> ci:ods-compliance inside ci:gates -> red commit stops Matt/agents from shipping a regression
  evidence: reader: scripts/check-ods-compliance.mjs (read this run) — attribution/page/DAL asserts, MV ratchet regex `CREATE MATERIALIZED VIEW (IF NOT EXISTS )?public\.${mv}\b`, sold-preset + statusFilter asserts, sync-delta registration assert. Live creators it misses: 20260801053000 creates listing_tile_mv_src_new (:~125) and recreates listing_tile_mv as a plain VIEW (:~261); 20260731170000 same for search (:331,468) — both read this run. wiring: package.json:96 (script), :230 (in ci:gates). outcome: commit blocked on regression — but only for regressions in files the gate actually reads.
  **GAP:** The gate's MV ratchet reads the wrong store: its regex matches only 'CREATE MATERIALIZED VIEW public.listing_tile_mv\b', but both serving MVs were rebuilt as plain VIEWS over *_src materialized views, so the predicates the gate pins live in creator files the gate never reads. The predicates ARE live today, but a future _src rebuild dropping them would ship green — gate-green-for-the-wrong-reason, on the one chain whose failure is a licensing leak. Secondary: the asserts are string/regex ('attribution.includes(listContact)') and stay green if the prop exists but never renders.
- ✗ **sold-data-stays-out-of-public-idx** — REQUIRED outcome: Closed rows never reach an indexable public surface (§5-4 A.4 as recorded in the gate header). Actual: /sold footer link -> 307 to /homes-for-sale?status=Sold -> rewrite to bare app/search/page.tsx -> getViewportSearch Sold branch -> getListingTiles standard_status='Closed' -> Closed rows SSR to any anonymous visitor on a page emitting robots index,follow
  evidence: next.config.ts:210 (/sold -> /homes-for-sale?status=Sold), :299 (/homes-for-sale -> /search rewrite) — read this run. app/search/page.tsx generateMetadata (:183-208 region read this run): no robots key, canonical WITH query string; app/layout.tsx:62 robots 'index, follow'. Data path: app/actions/search.ts:263-264 (filters.status==='Sold' -> getViewportListings statusFilter:'closed'); getListingTiles.ts:300-301 (eq standard_status 'Closed'). Noindex helper consumers (grep this run): only app/search/[...slug]/search-metadata.ts:76 and app/search/[...slug]/page.tsx:320. Broken at the page-metadata hop.
  **GAP:** An indexable public sold surface exists today: the bare search page sets no robots override (inherits app/layout.tsx robots 'index, follow') and canonicalizes the query URL onto itself, while status=Sold genuinely serves Closed rows through the legacy tile path — breaching the shop's recorded §5-4 A.4 interpretation (sold is VOW-only). G54 cannot see it: it asserts only 'no sold preset slug' and 'statusFilter in seo-routing', and shouldNoIndexSearchVariant is consumed only by the [...slug] preset route, never the bare page. Risk lands on Matt's ODS Participant standing.

  Gap statements: 
  - Freshness line is self-referential: 'Data last updated' renders cache-fill wall-clock (getListingDetail.ts:394, 60s cache), not the feed watermark — it shows fresh even during a wedged feed, misleading compliance reviewers and visitors on every listing render.
  - Morgan Data Shuttle listings are misattributed to ODS on every detail page: the DAL never selects mls_source and ListingAttribution hardcodes the ODS logo and source sentence — a per-feed attribution breach on MDS renders.
  - G54's MV ratchet asserts against stale creator files (the serving MVs are now views over *_src matviews the gate never reads): a future _src rebuild dropping the opt-out predicates would ship green, silently opening an opt-out leak into search/tiles.
  - permit_address_internet_yn is enforced only at the detail DAL — neither live _src MV predicate carries it, so the first address-opt-out seller appears in search tiles with their address (latent today: 0 live rows).
  - An indexable public sold surface is live: /sold -> /homes-for-sale?status=Sold SSRs Closed rows on a page emitting robots index,follow — breaching the recorded §5-4 A.4 'no indexable public sold surface' interpretation, invisible to G54.
  - The rendered 'ODS logo' is a 312-byte homemade text placeholder (gray rect + system-ui text), not the ODS-distributed mark the logo policy requires; the gate only asserts the file exists, so the logo obligation is green-but-unmet.
  - 138 of 7,595 Active rows carry no participant contact at all (no office phone, agent phone, or email), so the §5-3 P contact silently omits on those pages with no monitor counting the gap.
  - The compliance caption text is hand-synced across three components and has already diverged: the courtesy-of-participants sentence exists only in SiteFooter, leaving the KB footer register with four of the five caption elements; components/legal/MLSAttribution.tsx is a zero-consumer duplicate of the same idea.
  - Non-static ODS obligations (per-seller AVM/comment disable §5-3 L, 48h accuracy-correction channel VOW P, §7-3 advertising notice on ODS-derived stats) have no mechanism at all — nothing maps them to the surfaces that trigger them.

## sign-transaction-documents

- ✓ **credential (tokenized link)** — sendEnvelope/advanceOrSeal/resendRecipientInvite mint token + write sha256 -> tc_envelope_recipients.auth_token_hash (raw token only in the invite email) -> getSigningSession hash lookup -> /sign/[token] renders SignFlow or a state screen for the party
  evidence: writers app/actions/tc-envelopes.ts:602-604,670-671 + lib/tc/seal-envelope.ts:62-63; store docs/DATABASE_SCHEMA_SNAPSHOT.md:4567; email carrying the raw link app/actions/tc-envelopes.ts:606-614 + lib/tc/signing-emails.ts:54-61; reader app/actions/tc-sign.ts:59-65; human render app/sign/[token]/page.tsx:23-58
- ✓ **envelope lifecycle status** — sendEnvelope ('sent') / advanceOrSeal ('partially_signed') / sealAndCompleteEnvelope ('completed') / decline+void ('voided') -> tc_envelopes.status -> getSigningSession fan-out + getEnvelopesOverview + getEnvelopesForCycle -> signer state screens, /admin/signing StateWord+counts, deal-page DealEnvelopes
  evidence: writers app/actions/tc-envelopes.ts:592,708-711; lib/tc/seal-envelope.ts:45,191-200; app/actions/tc-sign.ts:276-279; store docs/DATABASE_SCHEMA_SNAPSHOT.md:4584; readers app/actions/tc-sign.ts:78-94, app/actions/tc-envelopes.ts:749-792,116-157; human app/sign/[token]/page.tsx:34-57, app/admin/(protected)/signing/page.tsx:91-96,106-151, app/admin/(protected)/deals/[key]/page.tsx:390,452
- ✓ **signature evidence (field values)** — submitSigning writes value+signed_at+signed_ip per field -> tc_envelope_fields -> sealAndCompleteEnvelope loads fields, sealEnvelope flattens PNG/text/date/checkbox onto the pages -> sealed PDF attached to every party's completion email; also getEnvelopeDetail shows values to the broker
  evidence: writer app/actions/tc-sign.ts:232-240; reader lib/tc/seal-envelope.ts:90,101-129; flatten lib/tc/seal-pdf.ts:86-121 (y-flip :70-84); human outcome lib/tc/seal-envelope.ts:211-226 + lib/tc/signing-emails.ts:78-84 (attachment); broker view app/actions/tc-envelopes.ts:203-216
- ✓ **per-signer audit trail** — getSigningSession stamps viewed_at+ip+ua, recordSigningConsent stamps consented_at+ip, submitSigning stamps completed_at -> tc_envelope_recipients -> seal builds SealRecipientSummary, certificate page prints Consented/Viewed/Signed + IP; mapRecipient feeds admin surfaces -> certificate inside the emailed sealed PDF; /admin/signing signed counts; deal-page envelope panel
  evidence: writers app/actions/tc-sign.ts:96-103,180-186,242-245; store docs/DATABASE_SCHEMA_SNAPSHOT.md:4568-4574; readers lib/tc/seal-envelope.ts:141-151 -> lib/tc/seal-pdf.ts:191-209 (prints Consented/Viewed/Signed and IP lines), app/actions/tc-envelopes.ts:100-113; human: sealed PDF email lib/tc/seal-envelope.ts:211-226; app/admin/(protected)/signing/page.tsx:94
- ✓ **executed document** — sealAndCompleteEnvelope uploads __signed.pdf, inserts tc_documents row (cycle_id), stamps sealed_sha256/executed_document_id/certificate_storage_path -> Storage tc-documents + tc_documents + tc_envelopes -> emailed to every unique party + broker notice; getTcDeal reads tc_documents by cycle -> deal-page cycle documents with DownloadButton
  evidence: writer lib/tc/seal-envelope.ts:162-200; human email lib/tc/seal-envelope.ts:211-237 + lib/tc/signing-emails.ts:64-111; broker surface app/actions/tc.ts:114-117 -> app/admin/(protected)/deals/[key]/page.tsx:212-239 (DownloadButton per doc)
- ✓ **event ledger — envelope_sent / envelope_completed** — sendEnvelope and sealAndCompleteEnvelope insert tc_events WITH deal_id -> tc_events -> getTcDeal reads .eq('deal_id') limit 15 -> deal-page events feed renders actor/action
  evidence: writers app/actions/tc-envelopes.ts:617-627 (deal_id included), lib/tc/seal-envelope.ts:202-209 (deal_id included); reader app/actions/tc.ts:124; human render app/admin/(protected)/deals/[key]/page.tsx:456-462
- ✗ **event ledger — recipient_signed / declined / seal_failed / reminder_sent / voided** — tc-sign.ts + seal-envelope.ts + tc-envelopes.ts insert these five event types with cycle_id ONLY (deal_id omitted -> NULL) -> tc_events -> the sole reader filters .eq('deal_id', deal.id), which a NULL row never matches -> no human surface ever renders them
  evidence: writers with deal_id omitted: app/actions/tc-sign.ts:247-252 (envelope_recipient_signed), :280-285 (envelope_declined); lib/tc/seal-envelope.ts:131-138,168-175 (envelope_seal_failed); app/actions/tc-envelopes.ts:689-694 (envelope_reminder_sent), :715-720 (envelope_voided); deal_id nullable no default docs/DATABASE_SCHEMA_SNAPSHOT.md:4601; only reader in app/lib is app/actions/tc.ts:124 (grep this run found no other tc_events select); deal-page feed app/admin/(protected)/deals/[key]/page.tsx:456-462 therefore never shows these rows
  **GAP:** Five of the seven envelope event types are write-only: envelope_seal_failed — the ONLY record of terminal state (e), where every signer finished, the last signer was told 'a completed copy is on its way', yet no sealed PDF exists and no copies were sent — reaches no screen and no email, so an all-signed envelope silently stalls in sent/partially_signed until a broker manually investigates; envelope_declined likewise never renders, so the PDS §6 claim 'the broker learns from the event ledger' is false in practice — the broker discovers a dead envelope only by happening to see the Voided status word on /admin/signing, with the decline reason surfaced nowhere. Harmed: the broker (blind to a stalled or killed legal transaction) and every party (believing executed copies are coming that never arrive).

  Gap statements: 
  - tc_events rows for envelope_recipient_signed, envelope_declined, envelope_seal_failed, envelope_reminder_sent, and envelope_voided are inserted with deal_id NULL while the only tc_events reader (getTcDeal, app/actions/tc.ts:124) filters .eq('deal_id', ...) — these ledger rows are unreadable from every human surface. What breaks: a failed seal (all signers done, no executed PDF, no copies emailed) is completely silent — the last signer was already told a copy is coming, and the broker's dashboard shows a still-active envelope with full signed counts and no alert; a decline reaches the broker only as an unexplained 'Voided' status word if they happen to open /admin/signing. Who is harmed: the broker supervising a legally binding transaction, and every transaction party awaiting executed copies that will never arrive.

## join-the-brokerage

- ✓ **hero-photograph** — curator patch via admin media board -> public.asset_library (approval + surface_tags) -> getSurfaceImages A/B hero query -> /join hero render seen by the recruit
  evidence: app/actions/asset-curation.ts:48-64 (update asset_library); app/admin/(protected)/media/photos/page.tsx:61-65 (board); lib/data/media/getSurfaceImages.ts:27-38 (reader); app/join/page.tsx:204-208,257 (render, static fallback :63,207)
- ✓ **section-scroll-telemetry** — KbSectionTracker beacon on /join -> public.visitor_events -> admin visitors session queries -> Matt sees the session's browsing path in /admin/visitors
  evidence: components/site/kb/KbSectionTracker.client.tsx:14-16,46-49 (writer, mounted app/join/page.tsx:212); app/api/visitors/track/route.ts:403-405 (insert); app/admin/(protected)/visitors/_lib/queries.ts:135-140 (fetchSessionEvents reader); app/admin/(protected)/visitors/[sessionId]/page.tsx:75 (render)
- ✓ **written-lead-capture** — submitContactForm -> sendEvent/ensureNativeLead -> public.crm_people (+crm_contact_points) -> broker notification email + /admin/crm list in front of Matt
  evidence: app/contact/actions.ts:31,93-112,140 (writer + email call); lib/followupboss.ts:101,129 (sendEvent delegates to ensureNativeLead); lib/data/crm/ensureNativeLead.ts:254-255,265 (insert); lib/resend.ts:103-124 (email to ADMIN_EMAIL); lib/data/crm/buildCrmPeopleQuery.ts:293 + app/admin/(protected)/crm/page.tsx:57,133 (CRM UI reader)
- ✗ **recruit-intent** — /join CTA ?inquiry=Join%20the%20team -> contact form Select -> submitContactForm inquiryType -> recruit-tagged crm_people row Matt recognizes as recruiting
  evidence: app/join/page.tsx:264,415 (CTAs); app/contact/page.tsx:81 (param read); app/contact/ContactForm.tsx:19-25,136 (value absent from INQUIRY_OPTIONS, unlisted defaultValue); app/contact/actions.ts:25-29,35 (buyer inference + default); lib/crm/enroll.ts:28-32 (audience:buyer -> buyer master sequence, rule :31)
  **GAP:** The recruit's intent dies at the process boundary: 'Join the team' is not a contact-form option, the server defaults to 'General Inquiry', and inferAudience maps it to buyer — so a licensed agent applying for a job is captured as a consumer audience:buyer lead, eligible for the buyer home-shopper drip, and reaches Matt as an unmarked General Inquiry he must decode from the message body. The machine works against the recruit and against the brokerage's highest-value conversion.
- ✗ **phone-completion-leg** — tel: CTA tap -> (no writer) -> (no store) -> (no reader) — only the offline call itself reaches Matt
  evidence: app/join/page.tsx:418-420 (bare anchor, number from lib/brand/contact.ts:81-82); components/site/kb/KbSectionTracker.client.tsx:46-49 (only section_view/scroll_depth telemetered — no click events exist on the page)
  **GAP:** The likely-majority completion channel writes nothing anywhere: the call CTA is a bare tel: anchor in a server component with no click event and no store, so recruit phone-leg starts are invisible, the process has zero funnel signal, and the registry's 'rare' cadence claim is unverifiable — Matt cannot measure or improve recruiting from data.
- ✓ **pitch-content** — repo-committed constants (LISTING_SUPPORT/HOW_IT_WORKS/FAQ_ITEMS) -> route file -> server render -> recruit reads the pitch
  evidence: app/join/page.tsx:65-105 (constants), 288+ (cards), 440-450 (FAQ dl), 235 (FAQPage JSON-LD projection)

  Gap statements: 
  - Recruit intent is destroyed at the /join -> /contact handoff: the join CTA's inquiry value is not a form option, the server defaults to General Inquiry and infers audience buyer, so the recruit is captured as a consumer buyer lead eligible for the home-shopper drip and Matt receives an unmarked General Inquiry — the brokerage's highest-value visitor is actively mishandled by its own machine.
  - No recruit marker exists anywhere in the capture path, so recruit conversations can never be counted, recruits cannot be suppressed from consumer sequences, and the recruiting process has no funnel metric at all.
  - The phone completion leg leaves zero trace (bare tel: anchor, no click events on any of the page's five CTAs), making the likely-majority completion channel invisible and the process's 'rare' cadence claim unverifiable.

## track-outbound-engagement

- ✓ **email-open → broker activity feed** — open pixel route upserts crm_timeline email_open -> crm_timeline -> getContactActivityFeed classifies 'Email opened' -> broker reads it on the person page
  evidence: writer app/api/track/e/open/route.ts:41-56 (dedupe track:open:{pid}:{emailKey}); store crm_timeline; reader lib/data/crm/getContactActivityFeed.ts:61 (email_open → 'Email opened'); human app/admin/(protected)/people/[id]/page.tsx:154 (getContactActivityFeed(idNum,30)) and :568 (feed.map render)
- ✓ **email open/click → reporting spine** — open/click routes call recordEmailEvent -> insertEmailEvent upserts email_events -> getEmailReporting reads + summarizes -> Matt/brokers read /admin/reports/emails
  evidence: writers app/api/track/e/open/route.ts:65-72 and app/api/track/e/click/route.ts:47-55 → lib/crm/email-events.ts:219-272 (recordEmailEvent) → lib/data/crm/insertEmailEvent.ts:50-53 (.from('email_events').upsert onConflict dedupe_key); reader lib/data/crm/getEmailReporting.ts:365,644 (.from('email_events')), summarizeEngagement :291-329; human app/admin/(protected)/reports/emails/page.tsx:166-171 (getEmailSendLog/getEmailEngagementSummary rendered)
- ✓ **email click → recipient lands on promised page** — compose-time instrumentEmailHtml signs the destination into the token -> the sent email is the store -> click route verifies token and 302s to the signed URL -> recipient sees the promised page
  evidence: mint lib/crm/gmail.ts:444-445 → lib/email-tracking.ts:139-144 (instrumentEmailHtml wraps links to /api/track/e/click); reader app/api/track/e/click/route.ts:18-19 (verifyEmailToken, target = signed URL else site root) → :72 (302 redirect); anti-open-redirect by construction (target never from request)
- ✓ **SMS tap → redirect + broker feed** — SMS composer mints crm_short_links row -> /r/[code] resolves code, 302s recipient to stored target, upserts sms_click to crm_timeline -> activity feed labels 'Text link clicked' -> broker reads it on the person page
  evidence: writer lib/data/crm/shortLinks.ts:59-66 (crm_short_links insert, minted from instrumentSmsLinks :96-103); reader lib/data/crm/shortLinks.ts:145-149 (select by code) + app/r/[code]/route.ts:23 (bot UA filter), :27-33 (302); sms_click write shortLinks.ts:161-173 + counters :179-186; feed lib/data/crm/getContactActivityFeed.ts:55; human app/admin/(protected)/people/[id]/page.tsx:568
- ✓ **click landing → identity stitch → visitors dashboard** — identity-bridge sets rr_pid + backfills visitor_sessions.identified_via -> visitor_sessions -> visitors queries read identified fields -> broker sees the named visitor on /admin/visitors/live
  evidence: writer app/actions/identity-bridge.ts:83-90 (rr_pid cookie 90d), :115-121 (backfillSessionToFub with retry) → lib/visitor-backfill.ts:202-205 (identified_via, fub_person_id written); reader app/admin/(protected)/visitors/_lib/queries.ts:75-77 (.from('visitor_sessions') selecting identified_via, crm_person_id, identified_email); human app/admin/(protected)/visitors/live/page.tsx:189-193 (renders identified_email + 'via email_click_pid' with CRM link); rr_pid also read forward by LP actions via getPersonIdFromCookie (app/lp/seller-home-value/actions.ts:126)
- ✓ **newsletter engagement → per-issue analytics (queue sends)** — open/click routes call recordNewsletterEngagement -> recordLedgerEvent upserts newsletter_recipient_events -> brokerAnalytics aggregates -> broker reads /admin/newsletters/analytics
  evidence: writer app/api/track/e/open/route.ts:78-87 / click/route.ts:61-67 → lib/newsletter/track-ledger.ts:43-58 → lib/data/newsletter/queue.ts:21 (EVENTS='newsletter_recipient_events'), :446-459 (upsert, recipient-scoped dedupe nl:<id>:sub:<key>:<event>:<url>); reader lib/data/newsletter/brokerAnalytics.ts:72,121 (.from(EVENTS)); human app/admin/(protected)/newsletters/analytics/page.tsx:90 (getBrokerNewsletterAnalytics), :97-100,:129 (opened/clicked rendered)
- ✗ **newsletter engagement → per-issue analytics (broker 1:1 sends)** — 1:1 send signs emailKey newsletter:<id>:p<personId> -> parseNewsletterEmailKey yields '<id>:p<pid>' which matches no newsletter UUID -> getRecipientForPerson no-ops -> no ledger row ever written
  evidence: lib/newsletter/track-ledger.ts:23-28 (parse slices everything after 'newsletter:', returning '<uuid>:p<pid>'), :46-47 (recipient null → return false, zero writes); mint app/actions/contact-newsletter.ts per PDS §2.2 (emailKey newsletter:<id>:p<personId>)
  **GAP:** Opens and clicks on a broker's 1:1 newsletter resend never reach newsletter_recipient_events, so the per-issue analytics page under-counts engagement for exactly the people a broker personally followed up with — the broker cannot see that their manual resend worked, while crm_timeline/email_events silently disagree with the newsletter dashboard

  Gap statements: 
  - Cross-rail double-entry: one real open/click on Resend-sent mail lands twice in email_events under two dedupe keyspaces (emailKey-anchored pixel row vs messageId-anchored webhook row with sendType 'other'), so /admin/reports/emails counts it twice under two send types — Matt and brokers see inflated engagement rates and misjudge send performance
  - Broker 1:1 newsletter engagement never reaches the newsletter_recipient_events ledger (emailKey 'newsletter:<id>:p<pid>' fails the parse in track-ledger.ts:23-28), so /admin/newsletters/analytics under-reports opens/clicks for personally-resent issues and contradicts the person's timeline
  - The open pixel has no proxy/prefetch filter (app/api/track/e/open/route.ts:31-93 logs the UA but never classifies it), so Apple MPP / Gmail-proxy prefetches record as human opens — 'Email opened' on a person view can be a phantom, and a broker may follow up on interest that never existed; the inflation share is unmeasured
  - A misconfigured production tracking secret 500s the RECIPIENT on every tracked click (verifyEmailToken throws outside the try, app/api/track/e/click/route.ts:18) — the hottest contact in the funnel gets an error page instead of the page their broker's email promised, while the open route absorbs the same throw harmlessly

## view-client-valuation-doc

- ✓ **cma-document-serve** — CMA builder writes html_content+render_args -> public.cmas -> getCmaHtmlBySlug + publication/register gate -> immersive/print document rendered to the verified client
  evidence: writer lib/cma/build.ts:867-903 (upsertCmaRowBySlug, html_content:890, render_args:893, exported lib/data/sync/syncWrites.ts:844); reader lib/data/cma/documents.ts:42-64; serve app/cma/[slug]/route.ts:55 (row read), 60-73 (status gate), 147-162 (immersive render from render_args), 168-194 (print fallback + tracker injection)
- ✓ **identity-consent-gate** — Register POST writes custom.cmaConsent/cmaClaimedBy/Registered-email -> crm_people -> getCmaAccessIdentity + decideCmaAccess read it -> consented recipient gets zero-shell repeat serves; wrong person 403s
  evidence: writer app/api/cma/register/route.ts:69-92 (cmaConsent:70-77, claim:81-88, update:89-92); store read lib/data/cma/documents.ts:150-160 (consentRecorded = Boolean(custom.cmaConsent):160); decision lib/cma/register-gate.ts:30-53 (serve on consentRecorded:52); wiring app/cma/[slug]/route.ts:79-117
- ✓ **consent-audit-to-broker** — Register POST inserts audited crm_timeline row (dedupe cma-register:<slug>:<personId>) -> crm_timeline -> getContactActivityFeed -> person-detail page: broker reads who registered and the exact consent decision
  evidence: writer app/api/cma/register/route.ts:95-104; reader lib/data/crm/getContactActivityFeed.ts:147-157 (.from('crm_timeline'):151); surface app/admin/(protected)/people/[id]/page.tsx:20,154
- ✓ **email-open-click-receipts** — attributeOutbound/instrumentEmailHtml embed pixel+wrapped links (emailKey cma:<slug>) -> recipient open/click hits /api/track/e/{open,click} -> crm_timeline (email_open/email_click) + email_events -> person timeline feed AND getDocEngagement -> getCmaPerformance -> /admin/reports/cma-performance: broker reads the funnel
  evidence: link writer lib/cma/send.ts:332-338 (emailKey `cma:${slug}`) + delivered_at stamp :384; deliver path lib/cma-deliver.ts:255-281 (cmaLink stamp), 315-323 (instrumentEmailHtml); wrapper lib/crm/attributed-links.ts:60-90; event writers app/api/track/e/open/route.ts:41-72 and app/api/track/e/click/route.ts:26-55; readers lib/data/prospecting/engagement.ts:53-74 (email_events `cma:<slug>`) and lib/data/crm/getContactActivityFeed.ts:151; report lib/data/cma/getCmaPerformance.ts:83-156; page app/admin/(protected)/reports/cma-performance/page.tsx:49
- ✓ **document-read-telemetry** — Serve-time rr-doc-tracker posts page_view (page_category client-document) -> /api/visitors/track writes visitor_sessions+visitor_events -> getDocEngagement counts /cma/<slug> page_views -> reportViews column on the CMA performance report the broker reads
  evidence: injection app/cma/[slug]/route.ts:151-152,180-183 and app/bpo/[slug]/route.ts:77-78; tracker public/rr-doc-tracker.js:40-56; writer app/api/visitors/track/route.ts:365-371 (session insert), 403-405 (event insert); reader lib/data/prospecting/engagement.ts:101-127 (page_category='client-document', event_type='page_view':108-109); surfaced via lib/data/cma/getCmaPerformance.ts:120-156 on app/admin/(protected)/reports/cma-performance/page.tsx
- ✓ **identity-stitch** — Tracker passes ?_pid to /api/track/e/identify -> identifyPersonFromEmailClickNative sets rr_pid cookie + backfillSessionToFub writes visitor_sessions.crm_person_id/identified_at -> getContactBehaviorSummary joins stitched sessions -> PersonEngagementRegion on the person workspace: broker sees the client's web behavior under their name
  evidence: tracker public/rr-doc-tracker.js:58-74; endpoint app/api/track/e/identify/route.ts:36-39; bridge app/actions/identity-bridge.ts:67-127 (cookie :83-90, backfill :115-121); writer lib/visitor-backfill.ts:141-199 (identified_at/crm_person_id post-conditions :132-137); reader lib/data/crm/getContactBehaviorSummary.ts:1-53; surface components/admin/crm/person-detail/PersonEngagementRegion.tsx:26,66 -> PersonWorkspaceBody.tsx:58,572 -> app/admin/(protected)/people/[id]/tools/page.tsx
- ✓ **published-cma-token-delivery** — registerForCmaDocumentAction writes cma_document_registrations (SHA-256 token_hash) + CRM lead/task -> resolveCmaDocumentByToken re-checks publish flag, stamps first_delivered_at/delivery_count -> /api/cma-document/[token] serves the full document to the registrant; broker gets a 15-min Follow Up task
  evidence: writer app/actions/cma-download.ts:156-189 (registration :156-168, createNativeTask :183-189) -> lib/data/cma/getPublishedCma.ts:487-531 (insert :514-527); reader lib/data/cma/getPublishedCma.ts:547-587 (publish re-check :563-568, delivery stamp :574-580); serve app/api/cma-document/[token]/route.ts:24-55
- ✓ **bpo-serve-and-receipts** — BPO builder writes html_content -> broker_price_opinions -> getBpoHtmlBySlug + offer-strategy strip (fails closed) -> client-safe document rendered to any link holder; send rail's open/click rows (emailKey bpo:<slug>) reach the broker via the person timeline feed
  evidence: writer lib/bpo/build.ts:534,582-583 via upsertBpoRowBySlug lib/data/bpo/reads.ts:134-140; reader lib/data/bpo/reads.ts:120-130; serve app/bpo/[slug]/route.ts:25-88 (final gate :36-49, strip fails closed :56-67, tracker injection :77-78); send lib/bpo/send.ts:376-381 (attributeOutbound emailKey `bpo:${slug}`), last_sent_at :416-418; open/click land on crm_timeline (app/api/track/e/open/route.ts:41-56) read by getContactActivityFeed.ts:151 on the person page. NOTE: per-doc BPO engagement never reaches the performance report (see gaps) but the timeline hop completes the human outcome
- ✗ **first-open-broker-alert** — (intended) delivery email embeds /api/cma/<slug>/track?e=open|view -> route queues crm_broker_alerts -> crm-alert-drain cron texts/pushes the broker within ~1 min of first open
  evidence: queue caller exists only at app/api/cma/[slug]/track/route.ts:45-53; repo grep finds ZERO producers of its ?e=open/?e=view URLs (live emails instrument via lib/cma/send.ts:332-338 + lib/cma-deliver.ts:315-323, whose handlers app/api/track/e/open/route.ts:31-92 and click/route.ts:17-72 never call queueBrokerAlert); its e=view redirect targets dead legacy /cmas/<slug>/cma.html (track/route.ts:57); the visitors/track return-visit alert is consent-gated !minimalOnly (app/api/visitors/track/route.ts:483-509) and rr-doc-tracker posts consent:'essential' (public/rr-doc-tracker.js:54), so no alert fires from document views either; drain cron runs (vercel.json:21-22) but receives nothing from this process
  **GAP:** The broker is never pushed a first-open notice: a client opening the valuation — the single highest-intent moment this funnel produces — is visible only if the broker happens to check the person timeline or the performance report, so the strike-while-hot follow-up window is routinely missed and the machine objective's 'alert the broker the document landed' clause is unmet
- ✗ **on-document-click-exits** — (intended) rr-doc-tracker cta_click per anchor tap -> visitor_events -> a per-document exits readout the broker can act on
  evidence: writer proven: public/rr-doc-tracker.js:80-107 posts cta_click (but :99 records link TEXT as pageTitle; the href/destination is dropped for every labeled anchor) -> app/api/visitors/track/route.ts:403-405 inserts the row; reader gap: the only per-document aggregation filters event_type='page_view' (lib/data/prospecting/engagement.ts:109), so client-document cta_click rows are aggregated by NO per-doc surface
  **GAP:** Which exits a client tapped from their valuation (market node, broker contact, sell plan) is unmeasurable: the destination is discarded at capture and no reader aggregates client-document clicks, so the 'document is a graph node, not a dead end' objective cannot be scored and exit-link changes ship blind

  Gap statements: 
  - First-open broker alert is orphaned instrumentation (D6): the only queueBrokerAlert feeder for CMA opens is a route no email ever links to, and the live /api/track/e handlers queue nothing — the broker gets no push when a client first opens their valuation, so the highest-intent follow-up moment is only discoverable by manually checking the timeline or the performance report
  - On-document click exits are unmeasurable (D4 + reader gap): rr-doc-tracker records link text instead of destination (public/rr-doc-tracker.js:99) and no per-document reader aggregates client-document cta_click rows (engagement reader filters to page_view only), so nobody can see which doors out of the document clients actually use
  - BPO engagement is invisible on the performance report: getCmaPerformance offers a 'bpo' docType filter (lib/data/cma/getCmaPerformance.ts:59) but queries only the cmas table, whose doc_type is restricted to 'cma'|'expired-audit' (lib/cma/build.ts:131; migration 20260714223354), and the engagement reader builds email keys as cma:<slug> only (lib/data/prospecting/engagement.ts:53) while BPO sends key bpo:<slug> (lib/bpo/send.ts:379) — a broker filtering the report to BPOs sees an empty funnel and per-doc BPO opens/views are surfaced nowhere, only as raw timeline rows
  - Consent flag is person-global and last-writer-wins (D3): the gate reads Boolean(custom.cmaConsent) (lib/data/cma/documents.ts:160) and each registration POST overwrites the consent object (app/api/cma/register/route.ts:69-77) while the timeline audit is first-writer-wins per (slug, person) (:103), so the live flag and its audit trail can diverge after a re-registration — the consent record a compliance question would be answered from is not guaranteed to match what the person last chose
  - Google-only door (D1): the register shell offers exactly one identity path (lib/cma/register-gate.ts sign-in shell; app/api/cma/register/route.ts:29-31 hardcodes 'google'), so a recipient whose on-file email is not a Google account is locked out of the web experience (their different Google address 403s as wrong-person) and falls back to the emailed PDF, losing the consent capture, identity stitch, and read telemetry this process exists to produce

---

## Completeness critic

Missing / cross-process observations:
- Cross-process convergence the single-process agents each rediscovered alone: marketing_assignments is a write-only ledger found independently by FIVE processes (get-home-value.written-cma, plan-a-sale, save-and-return.guest-alert-capture, contact-form-inquiry, P4-capture-and-attribute). This is ONE defect with one fix (wire a routing-audit reader or delete recordMarketingAssignment and its six callers), not five — and the stale in-code comments claiming dashboards read it are part of a broader 'docs lie' class (dead ListingJsonLd named as the structured-data SoR in the PDS; getUserEvents.ts docstring crediting the wrong writer).
- CONTRADICTION between agents on partial_address: arrive-from-ad marks its partial-address chain HOLDS ('admin /admin/visitors queries + backfill replay onto crm_people on later identify') while get-home-value.written-cma and plan-a-sale both verified zero readers of visitor_events metadata.partial_address. Both cannot be true. QUESTION: does any /admin/visitors surface actually render metadata.partial_address, and does an identify-time replay onto crm_people exist? Resolve before queueing either the delete-the-writer fix (D2/D3 no-orphan-saves) or the wire-a-reader fix.
- QUESTION on instant-alert gate scope: contact-form-inquiry proved the instant broker text is dead for native crm_people rows (enroll.ts:315 gates on fub_created_at, which nativeCreate never sets), yet arrive-from-ad and P4-capture-and-attribute report their instant-broker-alert-sms chains as holding. Which capture doors, if any, actually set fub_created_at post-FUB-cutover? If none, every 'instant' alert chain across all 28 processes is actually the 15-minute crm-auto-enroll sweep, and the sub-minute SLA claim is false system-wide.
- No single email-engagement source of truth: three ledgers (email_events, newsletter_recipient_events, crm_timeline) under three keyspaces produce cross-rail double-counting (track-outbound-engagement), 1:1-newsletter undercounting (newsletter emailKey parse failure, found by two processes), null-person orphan rows for guest alerts (guest-alert-capture), and phantom proxy opens — no process owns reconciling 'did this human receive/open our email' into one answer, and Matt reads inflated or contradictory numbers depending on which admin page he opens.
- Missing 'lead-door contract': capture doors demonstrably diverge on attribution (opt-in per door, D1/D2), SMS consent handling (/sell/valuation discards it; double-writer race), task creation (alert signups get a 5-min task; hotter intent-LP/rental/recruit leads get none), broker notification (ADMIN_EMAIL-only vs assigned broker), and enrollment (portal leads undetermined). There is no registry of lead doors and no mechanical gate asserting the invariant set per door — the analogue of check-page-dal for capture paths does not exist, which is why the same defect class recurs door by door.
- No global outbound-delivery failure surface: many chains terminate at 'Resend/Twilio delivers' with fire-and-forget error handling (sendContactNotification .catch(()=>{}) losing inquiries invisibly; alert/CRM/digest rails unexamined) — only the newsletter rail has a breaker and stall pager. QUESTION: do Resend bounce/failure webhooks on the transactional, alert, and CRM-sequence rails write anywhere a human sees, or is the newsletter the only rail that can notice its own outage?
- The search-outcome feedback loop is absent across three processes and owned by none: P4-earn-search-traffic emits sitemaps with no per-class indexed-count read-back, measure-search-traffic-gsc stores only top-25 slices so per-route equity for P5 cut decisions cannot exist, and read-content accumulates engagement rows no ranking reader consumes plus a dark analyzeSEO chain — the site publishes into a void and its pruning/content policy runs blind. One 'search feedback' workstream covers all three.
- tc seal-failure silence is the single highest-severity finding in the set and needs a reconciler no process proposed: an envelope where all recipients signed but no executed_document_id exists is a detectable invariant violation (all-signed AND not-completed AND age > N minutes), independent of fixing the NULL deal_id ledger rows. A cron cross-check would catch it even if event plumbing regresses again.
- QUESTION — unqueried runtime conditions behind 'holds' verdicts: several chains pass on code shape but depend on live rows never queried this run (deliverability_metrics presence for news.ryan-realty.com, the crm_automation_rules portal-lead row, public search_areas row count, crm_person_id-null subscriber share). The P12 phase should open with one live-DB verification sweep so these conditional 'holds' become facts before fixes are prioritized.

### Gap buckets (queue for a correctness phase)

**Write-only stores and phantom instrumentation — wire a reader or delete the writer**
- marketing_assignments upserted by six callers, zero readers repo-wide (get-home-value.written-cma, plan-a-sale, guest-alert-capture, contact-form-inquiry, P4-capture-and-attribute)
- visitor_events metadata.partial_address written on seller step-1 abandon, reader contested between agents — resolve the contradiction, then delete-writer (per D2/D3 no-orphan-saves) or wire remarketing (get-home-value, plan-a-sale, arrive-from-ad)
- engagement_metrics.view_count write-only; dead consumers (getFeaturedListings, DemandIndicators, ShowcaseStickyBar) orphaned in tree (find-a-home)
- market_narratives written daily under a CI gate, only reader is a zero-caller export API — governed narrative never reaches a visitor while an ungoverned inline one ships (explore-market-knowledge)
- saved_searches public-mirror rows + getPopularPublicSearches + /api/public-search/click: opt-in checkbox promises a feature that renders nowhere (save-and-return.search-alerts)
- tc_events recipient_signed/declined/seal_failed/reminder_sent/voided inserted with NULL deal_id, unreadable by the only reader (sign-transaction-documents)
- CMA first-open broker alert: the only queueBrokerAlert feeder is a route no email links to; live track handlers queue nothing (view-client-valuation-doc)
- Per-class GSC indexed counts: the process's stated done-state has no writer, no store, no surface — inverse orphan, the promised store was never built (P4-earn-search-traffic)
- No-writer funnels: tel:/sms: taps (broker-direct-call-text, join-the-brokerage), internal ?source= CTA params (arrive-from-ad), PWA install/eviction/clear telemetry (pwa-offline-resilience)

**Lead-door contract — capture, attribute, consent, notify, task: one invariant set enforced per door**
- Broker notification goes only to static ADMIN_EMAIL with no crm_tasks timer on the hottest doors: contact-form, intent-LPs (plan-a-purchase), rental calculator (run-the-numbers) — while weaker alert-signups get a 5-minute task
- Instant broker text dead for native rows via fub_created_at gate (enroll.ts:315) — every door waits on the 15-min sweep; scope across doors unresolved (contact-form-inquiry, cross-check arrive-from-ad/P4-capture)
- Attribution opt-in per door: paid clicks converting on /contact, /sell/valuation, saved-search, RSVP, homepage, sign-in lose channel labels and ?agent= routing (P4-capture-and-attribute D1/D2)
- /sell/valuation collects then discards checked SMS consent — consenting seller stays suppressed (P4-capture D11); consent double-writer race on canonicallyTagLead doors (D3)
- Recruit intent destroyed at /join→/contact: captured as consumer buyer, eligible for home-shopper drip, no recruit marker anywhere (join-the-brokerage)
- Tetherow LPs tag cma-requested but never queue a cmas draft or content:cma row — promised deliverable invisible to the pipeline (arrive-from-ad)
- Listing-detail call/text ignores rr_agent_attribution cookie; lead assigns to the dialed line's owner, not the attributing broker (broker-direct-call-text)
- Portal-lead enrollment undetermined: no audience tag, no fallback rule; depends on an unqueried crm_automation_rules row (P4-capture D6)
- Lead enrichment lives only inside after(); if it dies the lead is captured but never tagged/routed/enrolled and no cron repairs it (contact-form-inquiry PDS D2)
- Conversion-moment identity breaks: password sign-in never claims guest alerts (guest-alert-capture D4); anonymous user_events never join the account so the portal opens empty at conversion (save-and-return.portal); native leads' stitched web events invisible in the main timeline (P4-capture)

**Compliance plane — consent, cookies, CAN-SPAM/TCPA, ODS/IDX obligations**
- Consent withdrawal impossible: banner never re-opens despite /cookies and /privacy promising it; Meta LDU ignores mid-session changes; no server-side consent audit trail exists (cookie-consent)
- Cookie inventory drifted in both directions: four live cookies undisclosed (serve-legal-pages) and ryan_realty_visit_id disclosed but never set (cookie-consent)
- Newsletter footer subscribe unconditionally resurrects admin/webhook-unsubscribed rows with no abuse guard — CAN-SPAM exposure (newsletter-lifecycle)
- Rights/deletion/DMCA requests terminate in bare mailtos with no store; 30/45-day promises unprovable; /data-deletion and /cookies barely reachable; sitemap emits contradictory index/noindex signals (serve-legal-pages)
- Indexable public sold surface live via /sold → /homes-for-sale?status=Sold, breaching recorded §5-4 A.4, invisible to G54 (ods-idx-attribution)
- MDS listings misattributed to ODS on every detail page (mls_source never selected, logo/sentence hardcoded); rendered 'ODS logo' is a homemade placeholder (ods-idx-attribution)
- G54 ratchet asserts stale creator files — a _src MV rebuild dropping opt-out predicates ships green; permit_address_internet_yn enforced only at detail DAL, not in either _src predicate (ods-idx-attribution)
- 138 Active rows with zero participant contact silently omit the §5-3 P contact with no monitor; caption text hand-synced across three components and already diverged; non-static ODS obligations (per-seller AVM disable, 48h correction channel, §7-3 notice) have no mechanism (ods-idx-attribution)
- CMA consent flag person-global and last-writer-wins while its audit trail is first-writer-wins — live flag and audit can diverge on the exact record a compliance question would use (view-client-valuation-doc D3)

**Numbers that lie — §0 read-path integrity, silent failure, and double-counting**
- app_config mortgage/tax/insurance rates seeded once 2026-04-14 with no ongoing writer — the site's highest-trust tool presents a frozen rate as current (run-the-numbers)
- FAQ market claims have no re-verification mechanism against live data — stale factual prose served under reviewer-only enforcement (read-content)
- ODS 'Data last updated' renders cache-fill wall-clock, not the feed watermark — shows fresh during a wedged feed (ods-idx-attribution)
- GA4 generate_lead double-fires (server MP + client gtag, no eventId dedup) inflating the lead volume Matt reads (contact-form-inquiry PDS D8)
- email_events cross-rail double-entry (pixel row + webhook row per real open/click) inflates /admin/reports/emails; open pixel has no proxy/prefetch filter so MPP prefetches read as human interest (track-outbound-engagement)
- Tracked click 500s the RECIPIENT on tracking-secret misconfig — hottest contact gets an error page instead of the promised destination (track-outbound-engagement)
- Swallowed-error surfaces: app/actions/blog.ts returns confident empty states on Supabase error; sendContactNotification .catch(()=>{}) loses inquiries with no record (read-content, contact-form-inquiry)
- GSC measurement plane: marketing-brain search chain dark on a scope/metric name mismatch; red health grade never emailed; two admin surfaces show different 'GSC truth' unannotated; top-25-only storage destroys long-tail route equity evidence for P5 cuts (measure-search-traffic-gsc)
- Deliverability gate may run perpetually in 'warmup' (postmaster-sync degrades to skipped; live rows unqueried) while Matt approves large sends believing it is armed (newsletter-lifecycle)
- market_pulse_live carries no ZIP rows and misses neighborhood scopes — the finest-grain public surfaces answer with city-relabeled figures (evaluate-a-place)
- BPO engagement invisible on the performance report: docType filter queries a table that cannot contain BPOs and the engagement reader builds only cma: keys (view-client-valuation-doc)
- tc seal-failure completely silent: all signers done, no executed PDF, no emails, dashboard shows an active envelope with full signed counts — add an invariant reconciler independent of the ledger fix (sign-transaction-documents)
- CMA 24h delivery promise measured but breach never pushed to the accountable broker — visible only if Matt opens oversight (get-home-value.written-cma)

**Broken feedback loops — outcomes never re-enter the system**
- Search-outcome loop absent end to end: no per-class indexation read-back into emission policy (P4-earn), no per-route equity store for cut-list decisions (measure-gsc D2), no engagement-based content ranking so editorial never compounds (read-content)
- Drawn-area pages have no inbound path (no sitemap, nav, or index) — broker-published areas may serve zero humans; live search_areas count unqueried (evaluate-a-place)
- Standalone FAQ pages absent from sitemap with zero inbound links — the per-question ranking outcome the route exists for cannot occur (read-content)
- Blog family pays per-request dynamic rendering for a deleted FUB consumer; categories hardcoded so empty-category dead-ends ship (read-content)
- evaluate-a-place reshaped without ever reading back GA4 device mix or field CWV for its routes; community boundary quality frozen at a known-bad baseline (evaluate-a-place)
- PWA process at war with itself: offline worker never built so /offline is dead, hygiene eviction kills broker push subscriptions and any future first-party SW, wedged-visitor recovery depends on a human relaying /clear, install heuristic resets every session — and zero telemetry exists to size Matt's wire-vs-cut decision (pwa-offline-resilience)
- On-document click exits unmeasurable (link text recorded, not destination; no per-doc reader) so the doors clients use out of valuations are invisible (view-client-valuation-doc D4)
- Intent classification frozen at mortgage-only URLs: rental and appreciation calculator use never becomes a CRM intent chip, blinding brokers to the investor/seller segments the tools exist to mark (run-the-numbers)


_Totals: 239 chains, 191 hold, 48 broken._
