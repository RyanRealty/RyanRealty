2026-08-04T20:32:30Z BOOT complete. Created docs/plans/ADMIN_PRODUCT/ (state.json phase=P1_REGISTRY, work-queue.json [registry-pass], process-registry.json empty, decisions.md with Phase-0 evidence copied from ADMIN_REBUILD/PHASE-0-ANSWERS.md, SESSION_BOOT.md, processes/). Registered the ADMIN_PRODUCT/ package row in docs/DEVELOPMENT_PROCESS.md in the same commit (G44). Runtime check passed: production CRM_SMS_ALERTS="twilio" (vercel env pull). No locks. Next: P1 registry-pass in a fresh session (Block A + RESUME + P1, or /admin-product-os).
2026-08-04T21:25:00Z P1 registry-pass complete. 21 processes stubbed (15 seed all CONFIRMED + 6 code-discovered: visitor-escalate, tc-close, newsletter-run, market-report-deliver, site-content-ops, data-curate). page-inventory.json: 160 routes mapped (26 REDIRECT, 21 UNMAPPED). Evidence: 59 registered crons + 21 unregistered enumerated; ingress/alert/outbound/sequence wiring with file:line. Queue now holds the locked P2 deepen order (broker-alert first) + P3 package. state.phase=P2_DEEPEN. Next: deepen broker-alert.
2026-08-04T21:40:00Z P2: broker-alert DEEPENED (processes/broker-alert.md — gaps: unrecorded opt-in drops, supervision alarms share the wake rail, no alert->action latency). inbound-respond DEEPENED (processes/inbound-respond.md — gaps: reply-on-existing-thread has NO alert rail, no reply-latency metric). Registry updated. Next: cma-deliver + prospecting (evidence agents running); suppression-guard + sequence-run from in-hand evidence meanwhile.
2026-08-04T21:55:00Z P2: suppression-guard DEEPENED (defects: group MMS bypasses governed chokepoint, gates composed per-path not one pipeline). sequence-run DEEPENED (defects: weekly monitoring job has authoring-shaped surface, awaiting_broker_next visibility unverified). 4/21 deepened. cma-deliver + prospecting evidence agents still running; next in-hand: lead-ingress, identity-dedup.
2026-08-04T22:20:00Z P2: cma-deliver DEEPENED (litmus anchor; defects: broker-assignment TODO signs all as Matt, killed builds unrecoverable, orphaned build_state col, advisory-only SLA). prospecting DEEPENED (defects: FSBO off_market never written, cadence-doc drift, no staleness signal). lead-ingress DEEPENED (8 doors one core; FUB vocabulary debt, assigned_broker scatter). identity-dedup DEEPENED (no dupe-candidate queue, cross-channel conflicts unresolved by design). 8/21 deepened. Next: content-approve, sync-ops, listing-alert-care, visitor-escalate from P1 evidence.
2026-08-04T23:15:00Z P2 COMPLETE: all 21 processes deepened (13 more this stretch: content-approve, sync-ops, listing-alert-care, visitor-escalate, weekly-sla-review, reporting-truth, newsletter-run, market-report-deliver, deal-track, tc-close, bpo-deliver, site-content-ops, data-curate). P3 decision package written into decisions.md (verdict table: 15 KEEP / 4 MERGE / 1 KEEP-thin / deal-track = Matt call; 7 fix-the-class improvements; 5 open questions). state.phase=P3_PROCESS_LOCK, awaiting_lock=process. BLOCKED_ON_MATT: process. Next session: only proceed to P4 after Matt writes the process lock into decisions.md.
2026-08-04T23:45:00Z P3 PROCESS LOCK GRANTED by Matt (in-session answers): (1) deal-track MERGE into one deal entity with tc-close; (2) supervision alarms off the wake rail -> view+digest; (3) reply-on-thread joins the wake rail; (4) prospecting earns a primary surface in P5; (5) CMA signs as assigned broker, Matt fallback. Locked set: 15 KEEP / 5 MERGE / 1 thin, no kills. state.phase=P4_DATA, locks.process=2026-08-04. Next: P4 data-atlas unit (fresh session or Matt says continue).
2026-08-05T00:30:00Z P4 COMPLETE: data-atlas.md written — 17 chains (KEEP set, 4 MERGEs folded; weekly-sla-review kept its own chain per Matt scope note). All 4 flagged unknowns RESOLVED with evidence: (1) listing edits DO NOT survive re-sync except media_suppressed/finalized — chain broken, sync-proof overlay needed; (2) awaiting_broker_next IS visible (broker-dashboard queue + person rail) — P2 gap-candidate corrected, residual: sequences page discards count, digest cron unregistered; (3) place copy regenerates unconditionally for cities/neighborhoods and NO manual surface exists; (4) zero CRM<->TC bridge + /admin/deals reads legacy skyslope_transactions mirror not tc_deals. Gap rollup: 4 send-integrity, 3 litmus, 4 correctness-substrate. PDS corrections applied to sequence-run.md + tc-close.md. state.phase=P5_IA, queue=[ia-derivation]. P5 NOT started per Matt instruction.
2026-08-05T01:20:00Z P5 COMPLETE (package written, lock NOT granted): ia-lock.md proposes 11 job-named destinations (Today, Messages, People, Prospecting, Valuations, Closings, Oversight, Reports, Audiences, Content, Settings) + 5-tab mobile set (Today/Messages/Prospecting/People/Oversight) re-derived from Phase-0 evidence with prospecting locked primary. 160/160 routes dispositioned in page-inventory.json (found+mapped /admin/inbox that P1 missed): 26 CUT, 3 SYSTEM, 131 absorbed. cut-list.md enumerates 26 route cuts + 12 surface cuts. Amnesia test recorded in the package. state.awaiting_lock=ia. BLOCKED_ON_MATT: ia. P6 NOT started.
2026-08-05T02:00:00Z P5 IA LOCK GRANTED by Matt (in-session answers): names locked (Today/Oversight/Closings + full 11); tab 5 = Oversight; AMENDMENT: DSCR -> Reports (inventory updated); sequence authoring under Settings; Tasks+Calendar fold into Today. cut-list.md FROZEN. state.phase=P6_VISUAL, locks.ia=2026-08-05. P6 NOT started — next session runs the visual-language unit.
2026-08-05T03:10:00Z P6 COMPLETE (package written, lock NOT granted): design_system/admin/ v1 — tokens.css (Radix-derived light+dark, every pair contrast-COMPUTED; two failing candidate pairs corrected pre-ship; dark solids flip to dark text), ADMIN_UI.md (thesis: calm instrument answering what-do-I-do first; 6 patterns; header Option A left-rail recommended; dark-mode ship-both; amnesia test recorded; scorecard 8.5), screens/today+messages+oversight.html responsive. Browser-VERIFIED at 1280 and 375 both themes via localhost:8094 (admin-mockups server added to launch.json); fixed during verification: grid min-width overflow on Today, alarm stacking on Oversight phone, dark-mode button contrast. state.awaiting_lock=visual. BLOCKED_ON_MATT: visual. P7 NOT started.
2026-08-05T03:40:00Z P6 review round 1: Matt GRANTED header Option A + dark-mode ship-both; REJECTED Oversight v1 (confusing/cluttered). Reworked to verdict + needs-you list + quiet rows + numbers strip; ADMIN_UI.md pattern 3 rewritten (tile board retired); browser re-verified at 375 + 1280. awaiting_lock=visual still. Next: Matt reviews reworked Oversight at localhost:8094/screens/oversight.html.
2026-08-05T04:00:00Z P6 VISUAL LOCK GRANTED by Matt after Oversight v2 rework. Locked: tokens.css + ADMIN_UI.md (6 patterns, tile boards retired) + 3 reference screens; header Option A; dark ship-both. state.phase=P7_PRIMITIVES, locks.visual=2026-08-05. P7 NOT started — next session builds components/admin/v2 primitives + token-gate exemption.
2026-08-05T05:00:00Z P7 COMPLETE: components/admin/v2 built from the locked language — 10 primitives (Button, StateWord, FilterChip, QueueRow, QuietRow, VerdictLine, ThreadBubble, RailNav, TabBar, TextField/TextAreaField/SelectField) + admin-v2.css + tokens.css runtime copy. New gate ci:admin-v2-tokens (scripts/check-admin-v2-tokens.mjs): tokens parity with the locked spec, var(--a-*)-only color, brand-leak + legacy-import ban — wired into ci:gates, gates-wired green (204 gates). Brand token gate widened: app/admin/ + components/admin/v2/ exempt; baseline ratcheted 314->267. Zero tsc errors in v2. The new gate caught 2 violations on first run (its own doc comments) — proof it bites. state.phase=P8_LITMUS. Next: litmus-pilot unit (fresh session recommended).
2026-08-05T05:50:00Z P8 RE-PROOF COMPLETE (litmus HOLDS on current main @ fresh production build): 2 taps, ~4.2s kickoff RTT, real SMS rail delivered both alerts to Matt phone, worker built draft in 44s on new flowing-page renderer, zero-residue cleanup double-verified. Harness caveats recorded in LITMUS.md re-proof section. Auth via app /auth/callback token_hash flow (magic link, no credentials handled). v2 kickoff slice NOT built — scoping question in the sign-off package. state.awaiting_lock=litmus. BLOCKED_ON_MATT: litmus.
2026-08-05T06:40:00Z P8 defect found by Matt real-phone tap: admin deep links dead-ended on /auth-error for stale sessions. Fixed (3 sites -> /admin/login?next), commit 2b0286b5, deployed + confirmed serving live via behavior poll. Fixture person 60544 held live for Matt click-through; cleanup on his word. Litmus sign-off still awaited.
2026-08-05T07:10:00Z P8 LITMUS SIGNED OFF by Matt. All four locks granted (process, IA, visual, litmus). Fixture 60544 cleaned to zero residue (Matt tapped the link but not Build this round; evidence stands on the re-proof + July device run). v2 kickoff rides with P9 per Matt ("do whatever is recommended"). state.phase=P9_ROLL; queue seeded roll:today -> messages -> people -> prospecting -> oversight -> remaining -> P10 gates. Fresh session recommended for the first roll.
2026-08-05T07:45:00Z Matt CONFIRMED on his real phone: fresh fixture text link (person 60560) landed correctly post-fix — the earlier "still broken" report was the cleanup racing his tap on deleted person 60544 (sequencing lesson: never clean a fixture while the human might still tap it; explicit "done" only). Entry leg of the litmus now human-verified on-device. Fixture 60560 LEFT LIVE deliberately: Matt can tap Build for the full loop; cleanup (tmp/litmus-cleanup.mjs with PID=60560) on his word or at next session start. All four locks granted; phase P9_ROLL; next unit roll:today.
2026-08-05T18:30:00Z P9 roll:today SHIPPED: /admin/today live on v2 primitives — verdict line + 5 conditional lanes (inbound triage w/ dismiss+open, parked steps w/ send-it/skip, ready approvals, CMA drafts to review, due tasks w/ done), real data (verified 41 items = 15+8+16+2 across lanes, verdict math exact), nav Home slot -> Today (single nav source), legacy shell/tab chrome retained until the shell family rolls. Browser-verified 375+1280 signed-in AND the signed-out path (lands on broker login — auth fix proven in product). 4 compile-contract fixes during build (TaskQueueView, personName, void form actions, CmaRow cast). Next: roll:messages.
2026-08-05T20:10:00Z P9 roll:today ABSORPTION COMPLETE (Matt caught the gap: default admin entry still hit the old dashboard): /admin dispatcher + /admin/console + mobile-settings fallback + action revalidates all repointed to /admin/today; /admin/broker-dashboard is now a redirect bridge; replaced dashboard-only code DELETED per cut-list discipline (DashboardActivityFeed/Table, DashboardDeliveryAttention, getDashboardRecentActivity — shared modules getDashboardKpis/broker-command-center/MonthCalendar kept, still imported elsewhere). Boundary total dropped again (292). Browser-verified: /admin and /admin/broker-dashboard both land on the live Today queue. Next: roll:messages (data shapes already mapped: lib-side getInboxQueue/getConversationThreadFull/getInboxContactCard, sendCrmSmsAction FormData).
2026-08-05T22:40:00Z P9 roll:messages SHIPPED: /admin/messages live on v2 — conversation list (unread dots, 50 real convs), thread with channel-chipped in/out bubbles + delivery receipts, fixed composer through the ONE governed SMS action (quiet-hours state visible), context rail, All-tools escape to legacy inbox (templates/MMS/email-compose migrate later). Nav slot 2 = Messages (tests updated incl. badge finder). Browser-verified 1280 3-pane + 375 list->thread on real data. Note observed: same person can show multiple conversation rows (RC1 group model) — behavior inherited from getInboxFolderQueue, not new. ALSO this stretch: Today absorption deployed READY on production. Next: roll:people (carries v2 CMA kickoff surface).
2026-08-06T00:50:00Z P9 roll:people SHIPPED: /admin/people search-first lookup (new DAL read searchCrmPeople: two-step via crm_contact_points w/ digit-normalized phone fragments; DAL index refreshed) + /admin/people/[id] v2 entity page per pattern 5 — identity header, THE v2 CMA KICKOFF CARD (?intent=cma opens; same litmus idempotent core; draft-first note), recent activity, Message/All-tools escapes, legacy-id fallback preserved ([legacyId] absorbed into [id]). Nav People -> lookup; child renamed Search per hub-naming rule. FOUND+ROUTED-AROUND latent defect: compiler q node emits unparseable PostgREST or() casts — task chip filed (task_cb8a89a8). Browser-verified: search hits live, kickoff card at 375. Alert deep links still target /admin/crm/[id] deliberately (litmus stability) — repoint + re-time is its own step. Next: roll:prospecting.
2026-08-06T01:20:00Z Matt requested a single chained prompt to complete all remaining units in one round. Written to docs/plans/ADMIN_PRODUCT/FINISH-ROUND-PROMPT.md (covered by the G44 package row): per-family loop, remaining-family order with scope notes, the landmine list from this program, hard lines. Trigger: paste it, or say "run the finish round".
2026-08-06T01:45:00Z DEPLOY GAP (UNRESOLVED — finish round must fix FIRST): the People-family Vercel build (84ef6c8) was CANCELED when the docs-only prompt commit (33203e5) superseded it, and 33203e5 itself was build-skipped by the ignoreCommand as docs-only. Production likely still serves the Messages-era deploy — /admin/people probably absent on prod (prod nav is also old code, so no dead link user-facing; the family is simply not live). Verification + fix were blocked by a harness classifier outage. FIX at next opportunity: trigger a real production build of current main (any code-diff push does it — the finish round's first family push naturally heals it; or redeploy current SHA via Vercel), then deploy:verify + confirm /admin/people serves. NEW LANDMINE for the prompt file: never let a docs-only push land while a code deploy is still BUILDING — it cancels the code build and then skips itself.
2026-08-06T15:20:00Z P9 roll:prospecting SHIPPED: /admin/prospecting worklist rebuilt on v2 — verdict line (76 intros ready on real data; chips sum 236=76+24+35+0+101 reconciled), bucket chips as URL-state links (Ready to send / Needs audit / No phone / Sent / Blocked), kind toggle Expired/FSBO, per-row plain-words readiness ("audit ready, recommends $X · phone on file" / DNC reasons / "no audit yet"), search, pagination. Send stays on the prospect page through the prepared preview + full guard chain (NEVER from a list row; Sent=0 probed as DB truth). Build audit = thin Promise<void> wrapper w/ revalidatePath. Deleted same-commit: ProspectingBoard/ProspectTable/ProspectCard/ProspectFilters (single-importer, verified by import-graph not name-grep — cma/bpo hits were comments). Nav: Prospecting tab order 3 (locked bar), self-child renamed Worklist (hub-naming rule), Deals/Activity tabs dropped (Oversight takes 5 next); tests updated. Gate SCAN_DIRS extended w/ file-granular entries (legacy [kind]/[id] review page stays out of scope until migrated). Fixed during verify: FSBO '' street_address rendered ", Bend" (|| not ??). Browser-verified 375+1280 on real data incl. detail click-through. Next: roll:oversight.
2026-08-05T15:45:00Z P9 roll:oversight SHIPPED + a production incident found and class-fixed. /admin/oversight live on v2 per the locked verdict + needs-you pattern: one attention list (sync freshness, alert-queue trouble, lead-mirror kill switch, parked steps -> Today, PB sign-off waits per deal), sequences ran/parked/stopped lane (quiet rows + totals), running-clean rows (alert queue, mirror, inbound signal freshness), the week strip (median first response, new leads, uncontacted, overdue tasks, parked). New DAL lib/data/oversight/health.ts (getSyncFreshness/getAlertQueueHealth/getSignoffWaits, all unreadable-loud; DAL index refreshed). Capability oversight.view (broker-granted; system lanes principal-only). Nav: Oversight destination + locked tab 5; Activity left People; Operations/System health/CRM health left Settings (All-tools links carry them). Verdict reconciled: 4 = 1 sync + 3 sign-offs on real data; browser-verified 375+1280. THE PAGE'S FIRST CATCH: delta sync cursor stuck since 08-04T17:33Z — Vercel logs showed 'numeric field overflow' failing a chunk every run (cursor-safety refused to advance). Class fix aaacadb0: sparkToListingRow clamps every numeric(p,s)-bounded column (pg-audited capacities), NULL when unfittable (§0 absent-over-wrong); tests cover derived+source overflow. Heal verification queued post-deploy. ALSO: prospecting deploy a0e4ee98 verified READY; /admin/people + /admin/prospecting + /admin/today all 200 on production (People deploy gap HEALED per FIRST ACTION). Next: roll:remaining-families (Valuations first).
2026-08-05T16:20:00Z P9 roll:valuations SHIPPED: /admin/valuations — ONE worklist over the one build engine (P3 lock: bpo MERGE into cma surface): CMA drafts to review (19 live w/ recommended prices), price-opinion drafts, failed builds w/ honest reasons, building-now + recently-delivered quiet rows; All-tools to both boards; expired audits stay prospecting-owned. Nav: Valuations destination (capability valuations.view, broker-granted) w/ children Worklist/CMAs/Price opinions; Prospecting went leaf. Slice-note added when a lane truncates (verdict must reconcile with visible rows). Browser-verified on real data.
2026-08-05T16:40:00Z P9 roll:closings SHIPPED: /admin/closings — the one deal list rooted in TC truth (new DAL lib/data/tc/closings.ts getClosingsBoard: tc_deals + newest cycle + checklist posture, unreadable-loud). Lenses: In escrow (sign-off word when items in review), Active listings, Closed quiet rows, dead count. Rows open /admin/deals/[key] (TC deal page stays). /admin/deals LIST is now a redirect bridge; the skyslope-mirror reader app/actions/deals.ts DELETED (single-importer, P4 atlas defect closed — no surface reads the retired mirror). Nav: Transactions -> Closings destination, Board child. VERDICT DEFECT caught in verification: counted stale in_review on closed deals (248); fixed to live-stage only, probe confirms 17 = 14+2+1 lane sum. Browser-verified on real deals (3 in flight).
2026-08-05T16:55:00Z P9 roll:reports SHIPPED (hub v1 per finish-round scope: do NOT rebuild 34 report UIs): /admin/reports reborn as the definition-first index — 37 reports across 5 groups (business, CRM activity, email, acquisition & spend, content & tools incl. DSCR per the amendment), every row = name + the question it answers, linking the existing pages that keep the machinery. Reverses the 2026-07-07 redirect into /admin/analytics (the hub is the home now; analytics pages live behind it). Nav: Performance -> Reports destination (Home/Marketing approvals/DSCR children); DSCR left Closings.
2026-08-05T17:05:00Z P9 roll:audiences SHIPPED: /admin/audiences — one home over the three cadence rails (market-report subscriptions / listing alerts / newsletter list) with LIVE counts (new DAL lib/data/audiences/counts.ts, unreadable != 0) + segments and cohort-compose doors. Nav: Audiences leaf destination (audiences.view, principal-only — the doors it fronts are superuser-gated today). Suppression pointer to Settings compliance.
2026-08-05T17:15:00Z P9 roll:content SHIPPED: /admin/content — one content home (site-content-ops KEEP-thin): 9 doors (blog, guides, newsletters, media, content library, site pages, help, ad links, email campaigns) each stating its lane; Listings + Geography pointed at Oversight data tools (data-curate fold; oversight All-tools gained both links). Nav: Content href -> the home; menu slimmed to Home/Blog/Guides/Media/Newsletters (rest on the hub).
2026-08-05T17:35:00Z P9 roll:settings SHIPPED (last family): /admin/settings is now the config hub — 19 capability-filtered doors in 7 groups (You / Messaging incl. sequence authoring per IA lock / Compliance suppression+block / Routing & intake / Data model / Team & company / System); personal account page moved to /admin/settings/account (imports repointed). Nav: Home + My settings children. Browser-verified: hub renders 19 doors for superuser; account page 200. ALSO verified this build: Reports hub (37), Audiences (5,343 = 4+5+5,334 reconciled), Content (9 doors), Closings verdict fix live (17 = 14+2+1). P9 COMPLETE — every locked destination is live on the v2 language.
2026-08-05T17:50:00Z P10 GATES LANDED: (1) ci:admin-nav-ia — nav<->locked-IA parity: all 11 destinations reachable in lib/admin/nav.ts, none of the 23 frozen cut routes in nav (2 P9-adopted aliases exempt: /admin/people, /admin/reports); (2) ci:admin-contrast — recomputes 10 WCAG pairs from the locked tokens in BOTH themes on every commit. The contrast gate CAUGHT 3 sub-AA pairs its first run (state words on washes ~4.2:1 — the P6 derivation missed wash pairs): light accent/ok/warn washes lightened to reach >=4.5 (F3FAFF/F6FCF8/FFFDED), byte-copy synced, ADMIN_UI.md corrected. Both wired into ci:gates; gates-wired green at 206. ALSO folded in: alert deep links repointed /admin/crm/[id] -> /admin/people/[id] at 5 build sites (broker-alerts x2, enroll x2, broker digest); email-intent-note's suggested-reply link DELIBERATELY stays on /admin/crm — it is a composer-preload deep link whose prefill machinery lives on the legacy workspace (repointing it degraded a working flow; contract test pins it) — litmus RE-TIMING queued post-deploy per the landmine. state phase=P10_GATES. Remaining: push, deploy verify, post-deploy checks, final report.
2026-08-05T16:55:00Z LITMUS RE-TIME (part 1) on production b2a8cb80: fixture inbound through the REAL production webhook -> person 60610 created, alert 931 enqueued with the NEW deep link (ryan-realty.com/admin/people/60610?intent=cma) and DELIVERED to Matt's phone in 45s. Deep link opened at 375: v2 person page + kickoff card auto-open. REGRESSION caught at the litmus bar: the v2 card did not prefill the address the legacy sheet parsed from the inbound text (typing breaks the 2-tap bar). Fixed: People [id] page now derives suggestedAddress via extractAddressCandidate over the latest inbound snippet (parse verified: "20695 Town Dr, Bend"). Re-time completes after this deploys.
2026-08-05T17:05:00Z DELTA CURSOR HEALED on production: first sync-delta run on the deployed clamp (b2a8cb80) drained the 23h window CLEAN — sync_state.last_delta_sync_at advanced 2026-08-04T17:33Z -> 2026-08-05T16:48Z (runStartedAt stamp = zero failed chunks; the poison rows now store NULL for unfittable metrics instead of killing their chunk). Incident closed: found by Oversight's first render -> root-caused via deployment-scoped Vercel logs -> class-fixed at the mapper chokepoint -> deploy-verified healed. Oversight's sync lane goes quiet on next load.
2026-08-05T17:20:00Z LITMUS RE-TIME COMPLETE on production c48f653 — THE BAR HOLDS ON THE NEW LINKS: fixture inbound -> alert SMS delivered to Matt's phone in 45s carrying ryan-realty.com/admin/people/60610?intent=cma -> link opens the v2 person page at 375 with the kickoff card auto-open and the address PREFILLED ("20695 Town Dr, Bend", parsed from the text) -> ONE tap on Build CMA -> action row + timeline stamped in ~3.5s (click 16:59:38.6Z, action 16:59:41.5Z, timeline 16:59:42.1Z). Two taps total, no typing — better than the P8 measure (~4.2s). Fixture person 60610 + pending CMA action LEFT LIVE deliberately: the worker will build the draft and text Matt "ready" (his real-phone artifact trail); cleanup ONLY on his explicit word or next-session start (landmine). PROGRAM COMPLETE: all queue units executed — P9 11/11 destinations + P10 gates + deep-link repoint + re-time + delta-cursor incident closed.
2026-08-05T17:35:00Z CHIP task_cb8a89a8 CLOSED (the last outstanding defect): the segment compiler's free-text q node compiled to emails::text/phones::text casts inside PostgREST or() — ILLEGAL grammar, reproduced live (whole filter errors). Class fix: crm_people.search_blob stored generated column (name + emails + phones + digits-only phones, lowercased) + trigram GIN index (migration 20260805180000, applied); q node compiles to legal search_blob.ilike fragments with a digit-stripped companion for phone-ish terms. Proven live: blob search 322 hits (vs 259 name-only — email matches now included), digit-fragment search finds exactly the fixture person. 32 compiler tests green (2 new). searchCrmPeople's two-step stays (it serves the People lookup fine); the COMPILER path is no longer broken for saved views/segments/audiences.
2026-08-05T17:50:00Z FINISH CONFIRMED — nothing left in the program. (1) The litmus loop completed END-TO-END on production unaided: fixture text 16:43 -> new-lead SMS w/ People link 16:43:51 -> 2-tap prefilled kickoff 16:59 (~3.5s stamps) -> cma-build-worker :14 cron built the draft 17:14 (recommends $555,000, 6 comps) -> ready-text SMS delivered to Matt 17:17:03 ("CMA draft ready — 20695 Town Dr, Bend" + /admin/cmas link). Both texts on Matt's phone; action row ready. (2) Chip task_cb8a89a8 closed and DEPLOYED (c2d9432b READY): search_blob migration + compiler q-node fix, proven live. Only open item: fixture 60610 cleanup on Matt's explicit word (work-queue.json).
2026-08-05T18:20:00Z MATT ROUND-2 DIRECTIVES (mid-review, verbatim intent — the CMA web-page program):
(1) Prospecting UI: pills -> ONE dropdown (kind x bucket); kill page-title chrome; DONE this round + encoded as ADMIN_UI §3 "Surface acceptance bar" (6 rules) + gate rule 4 (av2-chiprow banned) + skill append.
(2) Blocked wall = display collapse defect; DNC data itself VERIFIED REAL (92/113 owners tagged contact:do-not-call, written only when BatchData skip-trace returns dnc:true per phone; 81% matches the national registry rate). classify() now channel-aware: sendable = any deliverable channel open; "Blocked" only when allChannelsBlocked.
(3) Prospect detail: owner name -> /admin/people/[id]; "Review audit" -> /admin/cmas/[slug]. DONE.
(4) Person page redone: contact points as tel:/mailto doors, facts line, Call/Email actions, Prospecting story lane (link to prospect page), Valuations lane (CMAs+BPOs w/ links), activity 30. DONE this round.
(5) NEW PROGRAM — CMA pages: single CMA doc (expired listings get a "last listing — what happened + our take" SECTION, no separate audit doctype going forward); add "what we like about the house" section to every CMA; PDF and WEB PAGE are IDENTICAL except web drops print headers/footers; the web page is the consent door: recipient must REGISTER (Google sign-on) to view, ONLY the lead the CMA was built for gets access (identity match), registration captures SMS + email consent; the sign-in dialog needs a full upgrade (benefits copy, engaging, interactive — current one "super plain"). Matt's phrase: "that will be kind of our Google sign-on page." One of his messages cut off after "ensure that our" — ASK for the rest before finalizing the section list.
2026-08-05T19:10:00Z ROUND-2 UNITS A/B/C SHIPPED (3 commits pushing): A prospecting dropdown + channel-aware buckets (169 sendable vs 76; blocked 101->43 true no-channel; DNC provenance verified real) + detail doors; B the six-rule surface acceptance bar (ADMIN_UI §3 + skill + gate rule 4, applied on all 9 v2 pages); C person-page workspace (contact doors, facts, Prospecting + Valuations lanes, activity 30) + the cmas.person_id stamp class fix (worker stamps kicker person; fixture backfilled; lane verified).
2026-08-05T19:10:01Z UNIT D IN FLIGHT — the CMA registration door (built, verifying): lib/cma/register-gate.ts (pure decision: serve/register/consent/claim-and-consent/wrong-person + 3 branded shells; §2-clean copy), /api/cma/register (GET starts Google OAuth w/ return; POST records consent on the person: custom.cmaConsent {sms,email,at,slug,viewer,version} + audited timeline row w/ EXACT wording + bind-on-first-register for phone-only leads writing the claimed email as a contact point), /cma/[slug] route gates public-status docs (admins pass). SMS_CONSENT_TEXT moved to lib/crm/sms-consent-text.ts (server-safe ONE source; component re-exports; ci:sms-consent rewired to lock the source + the import — green). Consent optional by design (TCPA: never a condition of service; boxes unchecked). REMAINING D (next session): builder sections ("what we like about the house" + expired last-listing review w/ our take; SINGLE CMA doc — fold expired-audit docType), PDF pipeline (web == PDF minus print header/footer), SignInPrompt site-wide upgrade (benefits/engaging), Matt's cut-off sentence ("ensure that our...") needs his completion.
2026-08-05T19:40:00Z UNIT D SLICE 1 VERIFIED + SHIPPING — the CMA registration door works end to end on a local production build, all four branches: (1) signed-out -> branded register shell (benefits + Continue with Google, "Prepared for <name>"); (2) claim-and-consent (phone-only lead, first registrant binds): consent shell w/ the carrier-EXACT sentence -> submit -> THE DOC SERVES (57KB report), custom.cmaConsent {sms:true,email:true,version} + cmaClaimedBy + email added as Registered contact point + audited timeline row; (3) wrong person -> 403 private shell, no doc; (4) admin pass unchanged. Caught in verify: SMS_CONSENT_TEXT imported via the 'use client' module became a client-reference (e.replace crash) -> moved to lib/crm/sms-consent-text.ts as the ONE source, component re-exports, ci:sms-consent rewired to lock source + import (green). Fixture reverted to draft. TEST-FIXTURE LEDGER for Matt's "done": person 60610, cma-20695-town, auth users cma-fixture-viewer@/cma-fixture-wrong@ryan-realty.com. Full crm suite green (2218).
2026-08-05T20:10:00Z /endtoend MISSION GOAL (round-2 completion — measured against this at final review):
A. cma-doc-v2 DONE means: (1) every newly built CMA renders a "What we like about this home" section assembled ONLY from verified subject facts (§0 — no invented editorial; skip the section when too few facts); (2) a subject with a prior expired/canceled listing cycle gets "Your last listing — what happened and our take" INSIDE the same single CMA (data: the prior cycle's list price/DOM/drops/agent + a deterministic plain-English read); the separate expired-audit docType is RETIRED for new builds (prospecting builds docType cma; old audit docs still serve); (3) ONE artifact serves both media: the print headers/footers exist for PDF/print and are hidden on the web view (media queries or serve-time class), so PDF == web except chrome; verified by rebuilding the fixture CMA and inspecting both renderings. Existing gates (cma-routing, valuation-engine, page-safety tests) stay green.
B. signin-prompt-v2 DONE means: the site-wide SignInPrompt dialog sells the sign-in (headline + 3 concrete member benefits + prominent Google CTA + softened dismiss), public brand + §2 voice clean, engagement gating (2nd pageview, 24h dismiss) preserved, browser-verified at 375 + desktop on a production build.
Ship bar: each unit committed separately, pushed, deploy-verified READY, production spot-checked.
2026-08-05T20:45:00Z /endtoend UNIT A (cma-doc-v2) VERIFIED + SHIPPING: (1) "What We Like About This Home" — subject-attribute facts only (view/acreage/garage/year/sqft/beds; skips under 2 items; the first render leaked rental-rule prose into the warm read — caught in browser, fixed, and LOCKED by 12 render-rule tests in lib/cma/what-we-like.test.ts). (2) The single-doc fold: the last-listing review ("Your Last Listing ... our take") now renders from LISTING HISTORY (any subject whose last cycle Expired/Canceled/Withdrawn), not from a docType; the audit-only services + net-sheet pages and the "Listing Audit" label/title are retired; expectedDocTypeFor returns cma for both kinds; acceptedDocTypesFor keeps the 74 legacy expired-audit rows satisfying the worklist (645 prospecting+cma+bpo tests green); the expired auto-build cron requests cma. (3) One artifact two media: @media screen hides .pg-header (print/PDF keeps it; the running footer was already print-only) — REAL-BUILD verified on 61251 Grotto (expired, HOLT HOLDINGS): doc_type cma, both sections present, audit pages absent, header hidden on screen, rebuilt clean after the leak fix (built_at 20:37Z).
2026-08-05T20:45:01Z /endtoend UNIT B (signin-prompt-v2) VERIFIED + SHIPPING: the site-wide prompt now sells the sign-in — navy FREE MEMBER ACCESS band, "The listings come to you", three concrete benefits (new-match alerts, price changes on followed homes, the portal), prominent Google CTA + Facebook + Not now. Engagement gating untouched (2nd pageview, 24h dismiss, ad/outreach/LP suppression). Browser-verified 375 + 1280 signed-out on a production build; dismiss stamps and closes.
2026-08-05T21:15:00Z /endtoend MISSION COMPLETE — measured against the 20:10 goal, all bars met: (A1) what-we-like ships, facts-only, test-locked; (A2) single doc + history-driven review + docType retired w/ legacy acceptance, 645 tests green, real expired build verified; (A3) one artifact two media verified in the stored doc; (B) sign-in prompt rebuilt + verified both widths signed-out. Shipped ec942a30 (cma doc), 0d79190f (sign-in prompt), 5145b38b (comment truth-up + baseline), deploy READY 4on2wb91t. Review pass done: stale comments fixed, residual 'expired-audit' references are compat/type-only. Open: Matt's cut-off sentence + fixture cleanup word (work-queue).
2026-08-05T21:40:00Z ROUND-3 (Matt directives) BUILT + VERIFIED, three workstreams:
(1) FAILED-ASK CEILING — instances reviewed first: 57 of 173 expired-prospect docs (33%) recommended ABOVE the ask the market rejected, up to +36% (19635 Clear Night: asked $1.675M Withdrawn, engine said $2.275M off 4 big comps). Diagnosis: no arithmetic bug — the engine carried ZERO weight for the subject's own failed market test. Class rule in the ONE engine: applyFailedAskCap (lib/cma/expired-audit.ts, wired in build.ts) — when the last cycle failed within 12 months, every LIST tier caps at the failed ask; the EVIDENCE range stays printed; needsReview + reviewReason carry what the comps wanted; client note explains the gap honestly. 8 tests.
(2) BASIC ZONING+RENTAL — the zoning explainer, chunked development pages, buyer-option spread, rental tenure/income pages, and HOA page collapse into ONE "What You Can Do With This Property" page: zone one-liner, verdict-first build/use one-liners, Short/Mid/Long rental one-liners, HOA caveat, one sources line + disclaimer. Verify-it-yourself page stays.
(3) FULL RECIPIENT JOURNEY — proven live end to end: SEND: real rail delivered the fixture CMA to matt@ (gmail id 19fd3d7608807df8, PDF attached, delivered_at stamped, status delivered; driven via the engine directly — the Send-to-lead button opens a Radix dialog the harness cannot click, known limit, NOT a product defect). SIGN-IN: register/consent door incl. SMS consent (proven earlier, all 4 branches). TRACKING: rr-doc-tracker now also posts cta_click on every anchor (site parity) + rides the gate shells; /api/visitors/track accepts same-origin posts (the prod-only origin allowlist 403'd every local verification); VERIFIED: page_view + cta_click rows landed in visitor_events from a live doc visit + tel-link tap. 653 tests green.

[2026-08-05 15:50] ROUND 4 — pricing ground truth + report v3 (Matt directives, all shipped)
  77d8dc75 pricing calibration: resort comp guard (registry-driven symmetric hard
           exclusion + resort_premium trace counter, 14 tests) + scripts/cma-backtest.mjs
           (reproducible failed→sold metric: 3,394 same-address pairs / 3y — median
           eventual close 94.2% of failed ask, p75 98.2%, only 12.3% ever beat it;
           artifact docs/research/cma-backtest-2026-08-05.json) + applyFailedAskCap now
           clamps cons/rec/high to 0.942/0.982/1.00 × failed ask, constants locked to the
           committed artifact by test.
  9660f59f five report sections, all deterministic + §0-cited, thin data = no section:
           When to List (3y month-of-year seasonality, SVG chart, Bend April = 9-day
           median to pending over 635 sales) · price band (131 active / 68 pending ±10%
           of recommended) · subdivision pulse (Acadia Pointe 20 sales, median $599,900)
           · financing (30.1% cash / 59.8% conventional over 2,486 sales; buyer_financing
           ships in TWO formats — JSON-keyed + bare word — both classified) · photo bench
           (subject 47 vs comp median 25). Landmines fixed: actives carry DaysOnMarket
           not CDOM (0/131 had CDOM); market_stats_cache is rolling windows, not calendar
           months → dedicated skinny DAL read.
  6c97eb73 interactive layer, serve-time-only (stored artifact stays print-pure, PDF
           identical, retroactive to every delivered CMA): sticky contents bar + progress
           + scroll-spy + jump menu, fade-up reveals w/ 4s failsafe (content can never
           stay hidden), count-up snapped to exact markup number on beforeprint/tab-hide.
           Hardened: no rAF scroll throttle (wedges in hidden-loaded tabs).
  827c276e file-size rebaseline (feature growth).
  Verified: engine rebuild of cma-20695-town on live data, browser DOM + real-Chrome
  session (bar/spy/jump live), DB ground-truth probes per figure, tracker page_views
  recording, stored html 0 script tags. Pushed d79f48bf..827c276e; deploy:verify running.
  NEXT: production spot-check after deploy; then remaining queue (matt-inputs: cut-off
  sentence "ensure that our…", fixture cleanup on Matt's done).

[2026-08-05 17:45] ROUND 5 — the immersive homeowner experience (Matt: "must be immersive...
  review all past sales and photos, tell a story... everything I could possibly want to know
  about my home. This is where we provide our value.")
  6cbc4a94 TWO PRESENTATIONS, ONE DATA SOURCE: /cma/[slug] default = full-viewport
           scrollytelling rendered per-request from the persisted render_args (hero photo,
           answer-first price band, subdivision story, failed-listing story w/ backtest,
           comp photo cards, market, when-to-list, competition, warm read, broker close).
           ?print=1 = stored print artifact; PDF path untouched; register/consent door runs
           before either. Older rows w/o render_args fall back automatically.
           SUBDIVISION STORY ENGINE (lib/cma/subdivision-story.ts): deterministic facts over
           the FULL subdivision history (paged; 400-cap truncation found+fixed vs ground
           truth: Stone Creek 450 sales, 2019 median $407,990) + one grounded sonnet pass
           (~$0.03/doc) over facts + remarks + 4 recent hero photos fetched server-side as
           base64 (MLS CDN robots.txt blocks Anthropic URL fetch). Prompt forbids
           non-verbatim numbers; voice-sanitized; through the brand-voice gate; fails open.
           Renders in both presentations. 648 lib/cma tests green.
  c089756d gate fix: commit-compiles tsc got the standard 8GB heap (SIGABRT'd 3 pushes).
  21635409 immersive dates via formatDate (date-format gate catch).
  17f95086 file-size rebaseline. Pushed; deploy watching. Byron (cma-20513-byron) is the
  showcase doc: Stone Creek story w/ AI narrative + 4 photo cards verified vs DB.

[2026-08-05 21:10] VOICE CANON — Matt rejected the house voice ("you constantly
  overeditorialize... these reports sound so freaking dumb"), asked for an existing
  documented voice + a prompt that nukes every conflicting rule permanently.
  Research: 4 parallel tracks, 10 voices, every URL verified live →
  docs/research/brand-voice-anchors-2026-08-05.md. Matt picked Buffett.
  6c69d7d0 canon rewritten: marketing_brain_skills/brand-voice/VOICE.md, Buffett
           anchor + GOV.UK sentence law + Redfin structure (interpretation only inside
           a named broker quote). Eight rules, led by "state the fact, then stop."
           Banned-constructions section names the exact shapes with the real killed
           copy. Scope covers email/SMS/alerts/reports/pages/LPs/listings/ads/docs.
           Caught a live defect the duplicate docs hid: old VOICE.md named the PRIVATE
           forward number as the public phone.
           + docs/plans/VOICE-CANON-2026-08-05.md, the executable migration (registered
           per G44): self-running discovery, nuke, vocabulary rebuild from canon only,
           copy rewrite by reader volume, and two gates. Gate 2 (canon singularity,
           scripts/check-voice-canon.mjs) is the "never again" mechanism — NOT YET BUILT.
  a53ea2b7 Phase 4 reports + Phase 2 in-code doctrine: every aphorism/sermon/
           meaning-narration removed from immersive.ts + render.ts + the failed-ask
           note; STORY_SYSTEM and judge.ts narrative prompts repointed at the canon
           (both had drifted, still banning turnkey).
  ae94b7e5 Phase 2 nuke: voice_guidelines.md (640 lines) + voice_system_v2.md deleted,
           87 files repointed. Producer gate 51/51 with them off disk.
  c91297fc CLAUDE.md §2 collapsed 120 lines → 25, a pointer only. ci:claude-canon
           caught the dangling link, which is the gate working.
  734743aa baseline. Pushed 17f95086..734743aa; deploy watching.
  REMAINING (all in the prompt): vocabulary rebuild from canon (Phase 3), the other
  ~570 copy files (email/SMS/LP/site/data), DB copy (crm_templates, crm_sequences,
  blog_posts), both gates (Phase 5), full read-through verification (Phase 6).
  Audits found 2 more frameworks (design_system SKILL/MANIFEST, youtube-market-reports)
  + humor-voice docs + a stale .auto-memory line + 22 dangling ANTI_SLOP refs + 15 files
  citing "CLAUDE.md §3" for voice when it is §2. All named in the prompt appendix.

[2026-08-06 00:30] VOICE CANON GRIND — COMPLETE. 130 → 0.
  Four parallel agents on disjoint partitions + two orchestrator passes.
  e4c7f1e9 brain + social skills (38 files, 68 violations): coined maxims, "here is
           what that means" narration inside caption/ad examples, quoted urgency lists.
           meme_lord/voice_grader.md's competing banned list (citing the deleted
           ANTI_SLOP_MANIFESTO) replaced with a canon pointer.
  1c9aebb3 scripts (23 files): 12 build/site scripts hand-maintained their own copy of
           the RETIRED word list; repointed to the canonical vocabulary. CAUGHT THE
           SUBAGENT GAMING THE DETECTOR — it had obfuscated banned strings with
           .join(' ') so the scanner would stop seeing them. Replaced with real imports.
           YouTube VO lost 4 "which tells you" clauses; chapter "What This Means" →
           "Buyer + Seller Takeaways" (matched in uploader); blog H2s + meta fixed.
  (this)   app + lib (18 files): market-report-email "What this means for you" header →
           "Market read" (HTML + text mirror, section content intact); judge.ts and
           subdivision-story.ts LLM prompts reworded so they teach the rule without
           using the banned shape; rental-facts + expired-audit legal strings split
           without changing legal meaning; LP/site eyebrows and CTAs.
           Scope corrected: CLAUDE.md/CHANGELOG/AGENTS excluded, per the canon's own
           "internal docs are not governed" rule. No public copy hidden.
  Verified: 0 constructions, 0 banned words, both parity gates green, 2,986 tests,
  tsc clean. NO number, price, date, citation, or legal disclosure changed anywhere.
  Standing state: scripts/voice-canon-state.json says done:true. Re-run /voice-canon
  any time; the ratchet now holds at 0 and blocks any regression at commit + runtime.
2026-08-06T19:05:00Z P11 BOOT + 11A(gates half): state.phase=P11_INTERIOR, queue seeded from PHASE-11-PLAN (matt-inputs dropped — fixture cleanup verified done 2026-08-06). ci:admin-ui (G65) SHIPPED: AST ratchet over app/admin — raw elements (251 seed), legacy pages (131 seed — the auth-guard-redirect disguise fooled the first bridge heuristic at 58; fixed to redirect+no-JSX), extra primary buttons (3), distinct widths (21). Baseline scripts/admin-ui-baseline.json; 9/9 break-tests (every rule fails, safe shapes green); wired into ci:gates after ci:admin-contrast; gates-wired green; G65 row in MECHANICAL_GATES. Prior unit this session: signing-broker fix shipped+deployed (82e14a42). Remaining in 11A: axe in full-crawl + visual-regression per destination (subagent working). Next after 11A: 11b-person-workspace.
2026-08-06T19:20:00Z 11A COMPLETE: axe ratchet live in e2e/full-crawl.spec.ts (142+ admin routes, seed/compare/ratchetable modes, E2E_AXE flag, storageState reuse; smoke-verified seed+ratchetable paths, tsc clean, 716 tests enumerate) + visual regression for all 11 destinations (fullPage:false per Lenis lesson). ROUTE_INVENTORY regenerated — 7 of 11 destinations were missing from the stale 2026-08-02 inventory and would have been skipped by the axe crawl. DELIBERATE REMAINDER: e2e/axe-baseline.json unseeded (needs one authenticated full crawl — seed with first 11B session, commit it). Queue top -> 11b-person-workspace.
2026-08-06T19:50:00Z 11B IN FLIGHT (4 fenced parallel builders + axe seed): B1 relocates workspace crm/[id] -> people/[id]/tools + portal, true bridges w/ query preservation, form-actions BASE, 2 pinning gates repathed, revalidate twins. B2 folds daily machinery onto people/[id] v2 (canonical composers mounted as-is per G50, ?tpl server merge, notes/homes/editors/tasks/SendCenter, #comms anchor). B3 repoints ~35 deep-link producers + contract tests to /admin/people. B4 re-chromes ConsoleShell per ADMIN_UI §5 (wordmark bar out — recon proved the phone bar is the shell's own header, NOT a SiteHeader leak; public header already display:none via HideChrome). Axe seed: authenticated (minted matt@ storage state via established magic-link pattern) full crawl running vs :3000. Known leftovers for integration: ConsoleQuickAction regex (console/ fenced to B4), crm-screens.json superseded statuses, litmus re-time.
2026-08-07T00:15:00Z 11A axe baseline SEEDED for real: 148 admin routes, 51 with violations (color-contrast 24, button-name 22, link-in-text-block 8 lead). First seed attempt (42.9m crawl) wrote 1 route — Playwright afterAll runs PER WORKER and the last writer clobbered the rest; fixed with per-worker fragments + scripts/merge-axe-seed.mjs, axe pinned to chromium project. 11B tracks: B1 relocation+bridges DONE, B3 deep links DONE (45 files, 75/75), B4 phone chrome DONE (wordmark out, amnesia test added) + FOUND desktop chrome is a navy-wordmark FUB-style top bar violating §5 Option A -> B5 rail swap IN FLIGHT. B2 fold IN FLIGHT. FAB regex seam closed (people|crm both recognized).
2026-08-07T04:50:00Z 11B COMPLETE (5 fenced builders + orchestrator integration). Person workspace folded onto v2: /admin/people/[id] gains Messages (thread + canonical composers mounted as-is per G50, server-side ?tpl merge), Send (ContactSendCenter), Details (stage/broker/source/tags), Tasks & appointments, Homes (+portal door), Notes. Full legacy workspace relocated to /admin/people/[id]/tools; /admin/crm/[id] + /portal are true JSX-free query-preserving bridges; form-actions BASE repointed; 2 pinning gates repathed. ~45 files of deep links + contract tests repointed (75/75 tests). CHROME: recon overturned the assumption — the phone bar was ConsoleShell's OWN wordmark header, not a SiteHeader leak (public header already display:none via chrome-gate, verified: img present but 0x0 inside display:none). Removed it; ALSO found desktop was a navy FUB-style top bar w/ white wordmark violating §5 Option A -> replaced with the locked 216px left rail (DO/MOVE/WATCH/REACH groups, icon-collapse <1200px), ConsoleTopNav DELETED. ORCHESTRATOR CATCHES: (1) noteToText helper — 41 literal <br /> tags were rendering as text in real notes (inherited defect, not a regression; 7 tests, never edits content); (2) my own revalidate-person insert put imports ABOVE 'use server' in 12 files — build-breaking, caught by reading the diff, fixed; (3) 24 dead bridge revalidations removed + 17 two-line twins collapsed into revalidatePerson(). Ratchets: page-action-imports 313->296 (shrank), date-format 81->75, file-size re-baselined (+1 import line x3, irreducible). VERIFIED: full ci:gates chain green (212 gate files, 0 orphans), tsc clean, browser at 375+1280 on real person 11784 — 7 sections both viewports, rail 216px, 0 navy bars, tab bar pinned bottom, 0 raw <br />, no h-scroll. KNOWN LEGACY HOLDOUTS (deliberate, next unit): ContactSendCenter's black send bar + the blue FAB are pre-v2 styling mounted as-is because they are send chokepoints. Axe baseline seeded: 148 routes, 51 with violations.
2026-08-07T05:10:00Z 11B LITMUS RE-TIMED on production f7fbf72d — PASSES with margin. Method note: the drain cron runs EVERY MINUTE, so a webhook fixture would have texted Matt at 5am; seeded the person + inbound-sms timeline row DIRECTLY instead (no alert row, no SMS) and verified the alert deep-link format from lib/crm/broker-alerts.ts:276 (ryan-realty.com/admin/people/${id}?intent=${intent} — repointed). Broker-action span on the v2 page at 375px: COLD (seconds after deploy) 14.2s total / 4.3s click-to-stamp; WARM 4.4s total / 192ms click-to-stamp. Bar is <=3 taps / <=30s: ONE tap ("Build CMA — text me when ready"), address AUTO-PREFILLED "20695 Town Dr, Bend" parsed from the inbound text, zero typing. Full chain landed within 540ms: cmas row cma-20695-town 12:03:07.301 -> marketing_brain_actions cma:cma-20695-town pending .435 -> timeline "CMA build kicked off" .841. IDEMPOTENCY PROVEN on a real double-tap: second run returned "CMA kick-off attached to in-flight build" instead of duplicating. Fixtures 60863/60864 + cma + action deleted, ZERO residue verified. Queue top -> 11c-crm-tree, first item = restyle the two 11B holdouts (send bar + FAB).
2026-08-07T07:30:00Z 11C-1 + 11C-2 COMPLETE. 11C-1 (cf1696e2, deployed): the two 11B holdouts now speak v2 — ContactSendCenter takes an optional triggerClassName (v2 host passes av2 quiet classes, every other host keeps shadcn primary), FAB draws --a-btn-bg; send path/dialog/guards/idempotency untouched; browser-verified trigger #F9F9FB/#0D74CE/#D9D9E0 and FAB #0D74CE. 11C-2: crm/settings family (19 pages) migrated to v2 via a 5-group workflow, each group adversarially verified by a fresh-context agent whose FIRST check was `git diff` behavior preservation. RESULT: 5/5 groups behavior_preserved=true, ZERO critical findings across all five. RATCHET LOCKED: legacy pages 131 -> 112, raw elements 251 -> 247, distinct widths 21 -> 19, shadcn burndown 206 -> 205. ORCHESTRATOR FIXES on top of the verifiers' punch list: (1) duplicate <main> landmark — ConsoleShell already renders <main>, and the v2 page template was nesting a second one; de-nested across 30 files (av2-scope is a styling wrapper, not a landmark); (2) deleted TWO genuine orphans the migration superseded — SettingsSubpageShell.tsx and TeamRoleManager.tsx (caught by ci:reachable-exports; my own name-grep said TeamRoleManager had 2 importers but BOTH were doc comments — the import graph is authoritative, per repo rule); confirmed TeamAccess.tsx re-implements the same upsertAdminRole/removeAdminRole with the same fields + superuser guard before deleting; (3) ci:crm-screen-parity pinned the retired shell as proof for company-settings-desktop — repointed to SectionHead (the v2 primitive the migrated page renders), gate green at 17 done/all proven. JUDGMENT CALL: verifiers flagged "dropped subtitles" as content loss — declined to restore. The old copy ("Choose how a new lead is assigned to a broker") was a sentence explaining the page; the v2 verdict line states the live fact ("Every new lead goes to a broker by this rule, plus N source overrides"). That is CLAUDE.md §2 (state the fact, then stop), not a regression. Also recorded: migrated pages have no <h1> — correct per ADMIN_UI:53 (no page-title chrome), a decision not an accident. VERIFIED: all 19 pages 200 at 375px, exactly ONE <main> each, zero horizontal scroll; full ci:gates chain green (212 gate files, 0 orphans); tsc clean. Remaining legacy: 112 (crm/reporting 15, analytics 12, reports 8, newsletters 6, crm/import 5, long tail).
2026-08-07T09:40:00Z 11C-3 COMPLETE: crm/reporting (15) + analytics (12) migrated to v2 via a 5-group workflow whose verifiers led with NUMBERS-UNCHANGED (these are pages a licensed broker acts on). RESULT: 5/5 numbers_unchanged=true, 5/5 behavior_preserved=true. RATCHET LOCKED: legacy 112 -> 86, raw elements 247 -> 207, widths stay 19. TWO CRITICAL §0 CATCHES, both narrative-vs-data, both INTRODUCED by the migration and both fixed by the orchestrator: (1) contact-attempts rendered a NEW subhead "By lead source — the least-worked sources sit at the bottom" over a grid sorted by lead VOLUME (getContactAttemptsReport.ts:253 b.leads-a.leads); the page contradicted itself 23 lines later. A high-volume source with zero attempts would sit at the TOP while the copy told Matt the backlog was at the bottom. Clause deleted (same for speed-to-lead's "slowest sources are the ones to fix"). (2) lp-leaderboard promoted an arbitrary Map-order winner to an asserted green recommendation: "buyer-listing-alerts converts best at 0.0%" with tone HARDCODED 'ok', bypassing the evidence guard the callout below already used (rows>=2 && visits>=10 both ends && gap>=0.05) — on the page ad spend is moved from. Now gated on that same guard with ONE definition (hasWinner), tone driven by data; with no winner it states the fact and flags attention. Live now: "8 LP variants took traffic in this window; none identified a visitor yet." ALSO FIXED: DataGrid's horizontal-scroll containment was inline-style only, so ci:admin-responsive read it as a table with no mobile fallback (the #1 phone failure) — added the class the gate can SEE while keeping the style that applies; two Intl.DateTimeFormat instances moved onto lib/format/date (date-format ratchet 81 -> 75). VERIFIED: 14 migrated pages 200 at 375 with one <main> and no h-scroll; full ci:gates green; tsc clean. NOTE (pre-existing, not from this work): un-migrated legacy pages still render their own <main> inside ConsoleShell's, so they read as 2 landmarks until their family migrates. Remaining legacy: 86.
2026-08-07T11:15:00Z 11C-4 COMPLETE (5 families, 21 pages, 7 commits, deployed ef88c7a READY). RATCHET: legacy 86 -> 65, raw elements 207 -> 179, widths hold 19. Per family measured from the baseline diff, not from builder self-reports: media 4pg raw 8->6 · newsletters 6pg raw 6->0 · brokers 3pg raw 3->0 · crm/import 5pg raw 8->0 · geo 3pg raw 16->7. NO family outside those five moved a single count.
  ARCHITECTURE FIRST (29dfba49): three tabular readers had accumulated, one per migrated family — crm/reporting's ReportGrid (274L+174L css), analytics' DataGrid, analytics' kit.tsx (which carried a CONVERGE ME note). Every family still queued is tables too, so migrating as-is would have produced readers 4-8. ReportGrid is the superset and imports only its own CSS + formatDate + Link, so it moved into components/admin/v2/ and is exported from the barrel; 21 files re-pointed, merged into each file's existing barrel import rather than left as a second statement. Analytics' DataGrid + kit remain duplicated — named in the file header for 11E.
  NEW PRIMITIVE: EntityTitle. ADMIN_UI §3 rule 1 bans page-title chrome but grants an exception for an entity page's own name — the exception had no home, so people/[id] hand-rolled a raw <h1> and newsletters/[id] was about to be the second (deals/bpo/cmas/import [id] pages would have been three more). One primitive, and the h1 leaves app/admin entirely since the gate scans app/admin, not components/.
  GATE COLLISION RESOLVED: ci:console-kit listed 4 newsletters pages as REQUIRED_KIT_PAGES, mandating ConsoleSection, which the v2 acceptance bar forbids — the two contracts cannot both hold on one page. Removed with rationale inline, matching the file's own precedent (crm/inbox, crm/tasks, reports). The builder that hit it REFUSED to edit the gate itself and handed it up, which was right.
  THREE PRE-EXISTING DEFECTS FOUND AND FIXED (all proven pre-existing at HEAD before touching):
   (1) crm/import/[id] HAD NEVER SHOWN A JOB. Next 15 made params a Promise; the page still destructured it as a plain object, so params.id was undefined, jobId was 0, load() returned at its own !jobId guard, and every visit sat on "Loading…" under a heading reading "Job #" with no number. Fixed with use(params); verified live -> "Job #21499 · Import finished. · DONE · gmail:paul".
   (2) /admin/brokers/edit scrolled sideways at 375 (496 > 375) from four shrink-0 actions in a non-wrapping flex row in BrokerProfileForm. Proven pre-existing by forcing the legacy font+geometry back: 497 legacy vs 496 migrated. Row wraps now; re-measured clean.
   (3) Same file had "View agent page" and "View team page" as two links to the IDENTICAL /team/<slug> — a label contradicting its destination. /team is a real route the roster already links; repointed.
  §0 CATCH BY A BUILDER, crash averted: crm_imports is a SHARED job ledger and listImportJobsAction filters it by NOTHING — 21,466 rows are gmail/portal-intake/fub runs, source='csv_wizard' = 0, and each producer writes its own counts keys. Reading counts.imported through .toLocaleString() would have crashed the page on real data. Null-safe accessor + honest footnote stating the ledger's real scope; underlying scoping defect filed separately (task_8b0b02f0). Also: live status vocabulary is done/failed/running but the CSV route writes 'error', so statusVariant() drew a FAILED job as neutral — 'failed' now maps alongside.
  IA CHANGE FLAGGED FOR MATT: geo/layout.tsx gained a third tab "Area guide media". That page already lived under the layout with its guard but was reachable only from a sentence, and dropping the page-title h1 left it unnamed. Interior nav within one family, not the locked 11-destination IA; no cut-list route resurrected. Revert the tab if Matt prefers.
  TOOLING: scripts/_admin-v2-verify.mjs committed — mints a magic-link session, loads each route in headless Chrome at 375 AND 1280, asserts 200 / exactly one <main> / no page-level horizontal scroll. Validated in BOTH directions before use (passes migrated lead-sources+analytics; failed brokers+geo on legacy double-<main> and caught brokers' 395>375 overflow). Every page in this wave passed it at both widths.
  NOT A FALSE GREEN, recorded so nobody re-chases it: ci:shadcn-burndown held at 205 through 21 migrations because app/admin/ is in that gate's EXCLUDE list — it deliberately measures the PUBLIC surface. The admin's shadcn burndown is tracked by ci:admin-ui rule B instead.
  DEFERRED, with reasons: v2 has no Switch primitive, so geo's ResortCommunityToggle stays mounted as-is (hand-rolling one means a raw <button role="switch"> that fails rule A — the barrel needs Switch first). Intermittent React hydration mismatch across admin pages is PRE-EXISTING and app-wide (reproduced on /admin/today, untouched by this wave) — filed as task_1ffc76f2, not a ship blocker.
  Remaining legacy 65: reports 8 · bpo 3 · cmas 3 · crm/deals 3 · crm/sequences·email·help·listings·people·signing·sync·visitors 2 each · 32 singletons.
2026-08-07T12:20:00Z 11C-5 + 11D COMPLETE (3 families + the reports interior, 17 pages, 5 commits, deployed cb8b8c31 READY; production spot-checked, not just local). RATCHET: legacy 65 -> 48, raw 179 -> 153, widths 19 -> 18. SESSION TOTAL: 86 -> 48 legacy (38 pages, 8 families), 207 -> 153 raw.
  11D REPORTS (8 pages) is the highest-value unit of the session. The verifier led with numbers-unchanged via TWO differently-shaped extractors (a contract extractor over hrefs/actions/params/guards/field-names/PostgREST chains/DAL calls, and a figure extractor over every formatter, toFixed, Math.* and arithmetic expression). It found NINE things the pages were telling Matt that were not true:
   FOUR FABRICATED: brokers claimed its figures were "pre-computed daily by reporting/compute-broker-stats" (no route, no cron, nothing writes broker_stats at all); market claimed the same of a "compute-market-stats job (after sync and daily at 2 AM)" (also nonexistent); lead-flow asserted "industry benchmark 0.5%-2%, below 0.3% means wiring gaps" UNSOURCED on the page ad spend is judged from; traffic-sources dated the GBP UTM tagging to a specific day (tagging real - 43 sessions carry utm_source='gbp' - the date was not).
   FIVE WRONG LABELS ON RIGHT NUMBERS: traffic-sources printed TWO hero figures off ONE read ("Sessions captured" and "First-touch sessions (w/ attribution)" were both .length of the same visitor_sessions query over the same window - always identical, the second implying a filter that never existed); it sourced referrers to the RETIRED `visits` table and labelled the column "Visits" (read has been visitor_sessions since W1.5); its "Showing 10 of 20" counted the ALREADY-SLICED array against 8,039 distinct landing pages; lead-flow titled a chart "last 14 days" over bars scaled to "the busiest day in the 30-day window" (both false off 30d); uniqueVisitorsApprox counted distinct landing pages, was never rendered, was not a visitor count - deleted.
   FAILED READS NO LONGER RENDER AS ZEROS on brokers/leads/emails/market/custom/lead-flow. emails presented EIGHT zeros as measurement without ever checking its own summary.unreadable flag.
   ONE IT NEARLY INTRODUCED, caught by probing first: a draft verdict "N cities have an active single-family listing" where _fetchAllCitySnapshots is .limit(50) but 135 cities qualify - would have shipped false by 85.
  BUILDERS REFUSED MY INSTRUCTIONS TWICE, BOTH TIMES CORRECTLY. This is the pattern worth keeping. (1) I said fix the locale-less currency with formatPrice; formatPrice ROUNDS TO THE NEAREST $1,000 ($894,750 -> $895,000, $999 -> $1,000). Right primitive is formatPriceExact, proven with a value table before the swap. (2) I said route dates through lib/format/date on crm/deals; `fmt` parses `<date>T00:00:00` as LOCAL and prints the stored calendar day, while formatDate parses date-only as UTC and re-projects to America/Los_Angeles, moving the printed day BACK BY ONE. Both were §0 figure changes wearing a cleanup's clothes. WRITE THE BRIEF SO A BUILDER CAN REFUSE IT.
  AUTHZ HOLE FOUND AND CLOSED: /admin/crm/deals/[id] ran getCrmAccess and nothing else. The board page and every deal mutation apply dealInScope (deal's own assigned_broker OR its linked person's must match the caller's slug); this reader was the one path that did not, so a restricted broker could walk sequential ids and read any deal's GCI, commission percent and every split row. Now runs the identical pure function (test-pinned, 10 tests), returning notFound() rather than access-denied so the response cannot probe which ids exist. Superuser path re-verified live.
  BPO PREVIEW FRAME HAD NEVER RENDERED: app/bpo/[slug]/route.ts sets X-Frame-Options SAMEORIGIN, but next.config.ts applies a global DENY over every path with explicit overrides for /cma/:slug and /cmas/:path* only. Measured DENY before (frame resolved to chrome-error://chromewebdata/), SAMEORIGIN after, confirmed on PRODUCTION post-deploy.
  Also fixed: generateMetadata on crm/deals/[id] read `name ?? \`Deal #id\` + ' | CRM | Admin'` — ?? binds looser than +, so every NAMED deal got a bare tab title.
  THE GATE THAT WAS MISSING: check-admin-v2-tokens.mjs scanned only the P9 destinations, so all 38 migrated pages followed the token rules with nothing checking them. 28 are now file-form scoped into SCAN_DIRS and BREAK-TESTED (injecting text-slate-500 + #ff0000 fails on both counts; removing returns green). The other 10 mount @/components/admin/<legacy> islands that rule 3 blacklists by design — same call already recorded for people/[id]'s G50 chokepoints; they land when their islands migrate (11f-mounted-islands).
  FILE-SIZE BUDGET: fixed the cause, did NOT re-baseline. lead-flow + traffic-sources breached; nearly all of it was comment (headers had grown to 73 and 103 lines restating the investigation that already lives in the commit). Trimmed to what a future editor must not break, then extracted the declarative ReportColumn configs to co-located _columns.ts siblings — the established pattern for the ~48 pages left, since v2 column configs are inherently more verbose than inline table markup. lead-flow now 662 against a 663 limit: ONE line of headroom, so the next addition there forces a real split.
  RULE 6 PROBED, NEVER ASSUMED, on every list page: bpo 3 rows all draft (real); cmas 33 draft + 2 finalized + 1 delivered = 36 reconciling exactly to the rendered strip; crm/deals 16 of 21 in a Closed stage, proven real by a counter-query (crm_deal_stages carries is_closed_stage on both pipelines while crm_deals.status is 'Active' on all 21 — status tracks the record's archival state, not the stage); broker_stats 0 rows against 3 active brokers, so the column of dashes is real and no fabricated $0 renders.
  FILED, NOT FIXED, each with the reason: getCmaAdminRowBySlug discards its Supabase error so a transient failure renders as "this CMA does not exist" — real and observed live, but ~20 callers on send/publish paths need per-caller tests, not a bolt-on (task_cc5d6fc3). MobileCrmHeader renders a bg-primary NAVY bar on phone, violating §5 amnesia, on 3 callers of which 2 are unmigrated (task_b5a50db5). Intermittent app-wide hydration mismatch, reproduces on untouched /admin/today and /admin/oversight (task_1ffc76f2). broker_stats was DROP TABLE'd by migration 20260425090000 yet exists in the live snapshot with 0 rows — migration and snapshot disagree. lead-flow's funnel shows "CMAs created 235 — 104.4% vs sessions", arithmetically correct but CMAs are broker-created, not visitor-driven, so it does not belong as a funnel stage below sessions. /admin/bpo/new and /admin/crm/deals/[id] have NO inbound link anywhere in the repo.
  Remaining legacy 48: 8 two-page families (crm/sequences, email, help, listings, people[portal+tools], signing, sync, visitors) IN FLIGHT + 32 singletons.
2026-08-07T13:35:00Z 11C COMPLETE + 11D COMPLETE. SESSION TOTAL: legacy 86 -> 32 (54 pages, 14 families), raw elements 207 -> 138, distinct widths 21 -> 17. 18 commits 104a459b..e8871f2a, three deploys all READY, production spot-checked (not just local). EVERY MULTI-PAGE FAMILY IS DONE — the 32 that remain are all singletons, which is 11E and a different batching problem.
  WAVE 3 (8 two-page families, 16 pages): crm/sequences, email, signing (d897760c) · people portal+tools, sync (0ce7586b) · help, listings, visitors (f4ecb9c9) · lock-up (e8871f2a).
  THE PATTERN THAT MADE THIS WORK, and the one to keep: fan out one builder per family group, then have EACH report lead with numbers-unchanged evidence rather than looks-right. Builders REFUSED MY INSTRUCTIONS FOUR TIMES THIS SESSION AND WERE RIGHT EVERY TIME. (1) I said fix locale-less currency with formatPrice — it ROUNDS to the nearest $1,000 ($894,750 -> $895,000); formatPriceExact is the right primitive. (2) I said route crm/deals dates through lib/format/date — `fmt` parses `<date>T00:00:00` as LOCAL and prints the stored calendar day, formatDate parses date-only as UTC and re-projects to Pacific, moving the printed day BACK ONE. (3) Same refusal on /admin/signing's Sent column, where the swap moved the day on 3 of 4 representative instants and moved the YEAR on 2026-01-01T04:00Z — on a LEGAL DOCUMENT's record. (4) Same on listings' Updated column. WRITE THE BRIEF SO A BUILDER CAN REFUSE IT; that instruction to prove a formatter swap before making it caught four §0 defects I would otherwise have shipped.
  §0 DEFECTS FOUND IN LEGACY COPY, this wave: help claimed "Use the Help button on any page for guided tours" (the Help FAB was removed 2026-08-06 and HelpProvider is imported from nowhere); visitors/live's header described "the LiveTable child component polls every 15s" (NO LiveTable exists in the repo); its footnote said hot scores "fire a 5-minute FUB call task" (FUB decommissioned 2026-06-24 — the cron is real but writes crm_tasks); visitors/[sessionId] had a column headed "FUB" reading "FUB synced"/"local" (pushed_to_fub_at is now purely an idempotency cursor); sync claimed a "strict verify cron" among its scheduled jobs (vercel.json registers 59 crons, none of them that — the route sits in the unregistered backlog and only a curl runbook invokes it); sync/spark printed "({totalPages} pages at {pageSize ?? 100} per page)" where ?? 100 invented a number and the figures described the PROBE's pagination, rendering live as "594,567 pages at 1 per page"; the sequences enrollment blurb said "First matching rule wins", true of autoEnrollPerson and FALSE of fireTrigger which dispatches ALL matching rules.
  A COUNT OFF BY 2.1x, in words not arithmetic: listings/[listingKey] printed "Database summary — Active: N". counts.activeCount is getTotalListingsCount(), summing geo_snapshot_mv.active_sfr_count over the TOP 50 CITIES (3,526) while listing_tile_mv holds 7,529 Active rows. Figures unchanged; they are now called what they are.
  A PHONE BUG CONSOLESECTION WAS CAUSING: EmailBodyEditor's 492px non-wrapping toolbar sat inside ConsoleSection's shadcn <Card>, which carries overflow-hidden, so at 375px the toolbar was CLIPPED and its right-hand buttons UNREACHABLE. The gate mandating ConsoleSection was mandating the component that broke the page.
  A 6px OVERFLOW ON A P9 DESTINATION, found by pointing the verifier at surfaces this session never touched: /admin/audiences measured 381 > 375, widest .av2-quiet__fig. Cause was the shared primitive — .av2-quiet was a non-wrapping flex with a 130px-min name and an auto-margin figure. Fixed in the CLASS; ten sibling consumers re-verified.
  ORPHAN DELETED: components/admin/TableWithMobileCards.tsx. ci:reachable-exports flagged it; the import graph agreed (zero real importers). One builder reported four importers — all doc comments. THE IMPORT GRAPH IS THE AUTHORITY, every time.
  MY OWN PROCESS ERROR, recorded so it is not repeated: `git commit` commits the INDEX, and a parallel builder had staged its own files, so one commit swept in 7 help/listings/visitors files under a message that did not describe them, before I had seen that builder's report. Caught before pushing; split with reset --soft + restore --staged, then re-committed with `git commit --only <paths>`. USE --only WHEN PARALLEL BUILDERS SHARE THE TREE.
  RATCHET DISCIPLINE: file-size-budget breached on two reports pages and was NOT re-baselined. Nearly all the growth was comment (headers had reached 73 and 103 lines restating investigations that already live in their commits); trimmed to what a future editor must not break, then extracted the declarative ReportColumn configs into co-located _columns.ts siblings — the established pattern for the 32 pages left, since v2 column configs are inherently more verbose than inline table markup. lead-flow now sits at 662 against a 663 limit: ONE line of headroom, so the next addition there forces a real split.
  TOKEN GATE now scopes 42 files (was 0 for migrated pages at session start). 15 deferred for one stated reason — they mount @/components/admin/<legacy> or @/components/ui islands that rule 3 blacklists and the migration mounts as-is by design. Break-tested twice, not assumed.
  OPEN AND FILED, each with the reason it was not fixed inline: person-scope gap on entity READERS — a CLASS, not an instance (crm/deals/[id] FIXED in 4aa3de68; people/[id]/portal and the people/[id] identity header still open; ci:crm-scope cannot see it because it inspects WRITES only) -> task_84b647c1 asks for a shared guard + a sweep + an AST gate. getCmaAdminRowBySlug swallows its Supabase error so a transient failure renders as "this CMA does not exist", ~20 callers on send/publish paths -> task_cc5d6fc3. MobileCrmHeader renders a bg-primary NAVY bar on phone against §5 amnesia, 3 callers, 2 unmigrated -> task_b5a50db5. Hydration mismatch TRACED this session to SiteHeader under HideChrome (RootLayout > ... > HideChrome > div > SiteHeader > header > div > a), firing on already-v2 untouched surfaces -> task_af3d76b5. broker_stats was DROP TABLE'd by migration 20260425090000 yet exists in the live snapshot with 0 rows. lead-flow's funnel shows "CMAs created — 104.4% vs sessions", arithmetically right but CMAs are broker-created, not visitor-driven. /admin/bpo/new and /admin/crm/deals/[id] have NO inbound link anywhere in the repo. sync/BackfillHealthPanel.tsx:267 repeats the unscheduled-cron claim.
  NOT VERIFIED, NOT CLAIMED: /admin/signing/[envelopeId]'s rendered identity header. tc_envelopes holds ZERO rows so no real id exists, and the builder refused to insert a fixture into a legal-document table — correct. Its notFound() path and Next 15 params signature WERE verified.
  NEXT: 11E is 32 singletons. They have no family structure, so batch them by RISK rather than by directory — the send/compliance ones (crm/inbox, crm/approvals, approval-queue, sign-off) apart from the read-only ones (audit-log, dscr, guides, financials). access-denied/login/setup are auth-flow pages and should be looked at before assuming they need the console shell at all.
2026-08-07T15:50:00Z 11E COMPLETE — THE ADMIN INTERIOR MIGRATION IS DONE. legacy pages 32 -> 0 (86 at session start, 131 at 11A). raw elements 138 -> 106. widths 17 -> 12. All 143 admin pages are on the LOCKED v2 language; ci:admin-ui rule B, the counter this phase was built to drive, reads 0. Six commits 55c2d538..8d60e4d1, deployed READY, production spot-checked (health/operations/blog/commissions/sign-off, 10/10 at 375+1280).
  32 singletons in six risk-batched fences: auth doors + small (6) · approvals/messaging (4) · money (4) · CRM daily (4) · CRM config (6) · content/ops (8).
  THE AUTH TRIO INVERTED THE PHASE'S CENTRAL RULE. access-denied/login/setup live under app/admin/layout.tsx, a bare pass-through (`return <>{children}</>`) — no ConsoleShell, no <main> above them. They MUST render their own landmark, the opposite of the other 54 pages. Verified before briefing rather than discovered mid-build. access-denied was also wearing the public display face + a "Ryan Realty" eyebrow; both gone. Brand call on the sign-in doors STATED not silent: fully admin tokens, wordmark became a quiet "Back to the site" link on the same href.
  BUILDERS REFUSED MY INSTRUCTIONS SEVEN TIMES ACROSS THE SESSION AND WERE RIGHT EVERY TIME — and 11E is where one of them corrected ME. I spent the whole session telling builders to prefer formatPriceExact over formatPrice. On the money pages that is ALSO wrong: financials branches on `net >= 0`, so negatives are live, and there Math.round gives $-1,234 while Intl halfExpand gives -$1,235 — a $1 delta and a moved sign. money() and d10() kept verbatim. The instruction that caught all seven was "prove the swap before you make it". WRITE BRIEFS A BUILDER CAN REFUSE.
  §0 DEFECTS FOUND IN 11E:
   · /admin/operations "Active listings" OFF BY 2.16x — printed getTotalListingsCount(), which sums geo_snapshot_mv.active_sfr_count over the TOP 50 CITIES (3,530) while 136 cities have non-zero counts and `listings` holds 7,611 Active rows. Its caption paired it with a count of every row of every status, so the two never shared a universe. Removed. STILL LIVE outside the fence: DashboardSyncPanel.tsx:57 renders the same value under "Active", fed by getAdminSyncCounts + two /api/admin/sync/* routes.
   · /admin/blog "Published 87 · Drafts 0" OFF BY 32 — derived state from published_at while the public site serves on status='published', and getAdminBlogPosts never selected status. Real split: published 55 · archived_stats_unverified 28 · draft 3 · pending_pilot_review 1. The BROAD count said the wall was real (87/87 dated); the GROUP-BY said it was an artifact of reading the wrong column. Two query shapes, exactly as §0 requires.
   · THE HEALTH BOARD UNDER-REPORTED BLOCKED CONTACTS BY 3.2x (I fixed this one). getSuppressionCounts issued a bare .select() and PostgREST caps a response at 1,000 rows, so EVERY figure was truncated: 1,000 rows / 999 people / 4.4% against a real 5,169 / 3,228 / 14.3%, with 0 for email/sms/call because the first 1,000 rows all carry channel='all'. The page states its own bands (warn 25%, red 40%) — a board reading 4.4% sails past a real 25% crossing without changing colour. Now paged via fetchPagedRows with .order('id') (required: unordered range pagination can repeat or skip). Live re-read after: "3,228 of 22,647 contacts ... across 5,169 rows."
   · A GREEN TILE CLAIMING A DEAD PIPELINE — crm/health read "FUB leads are flowing into crm_*" off the kill switch alone, but getFubApiKey() returns undefined UNCONDITIONALLY since the 2026-06-24 decommission, so no mirror write can run.
   · A THIRD PHANTOM COMPONENT — approval-queue claimed "New rows appear in real time via Supabase Realtime (see ApprovalQueueRealtime below)". No such component exists anywhere; that sentence was its only occurrence in the tree. (Session total: three pages claiming behaviour from components never built, plus two named crons that do not exist.)
   · TWO FILTERS THAT CANNOT RETURN A ROW — priority_score is NULL on all 395 pending approval rows so the urgency filter always returns zero; needs_changes has zero rows so half the status filter is dead. Queries NOT changed (product work); the row cap stopped lying instead.
   · A COMPLIANCE LABEL — sign-off said "Past 7-day deadline"; reviewDeadline counts BANKING days and the page's own footnote cites OAR 863-015-0140. Now "7-banking-day".
   · THREE FALSE DOORS on commissions pointing at pages that hold none of what they promised.
  TWO VERDICTS DELIBERATELY CARRY NO COUNTS — the rule extended, not broken. TasksView completes optimistically without router.refresh(), so a server "N overdue" is wrong within seconds; GlobalActivityFeed's controls refetch WITHOUT touching the URL, so a server sentence about scope is false the moment a chip is tapped (that verdict renders only for a restricted broker, who has no scope control). DO NOT STATE A FACT THE PAGE CANNOT KEEP TRUE.
  ci:console-kit RETIRED. It encoded Matt's 2026-06-15 directive; over 11C-11E every page on its REQUIRED_KIT_PAGES list moved to v2 until the list asserted NOTHING, and a gate with an empty list passes trivially — worse than no gate because it looks like coverage. THE DIRECTIVE IS NOT RETIRED, only its mechanism: docs/MECHANICAL_GATES.md gains a "Retired gates" section mapping each clause to its successor (rule B = one shared kit; rule A + SectionHead/EntityTitle = mandatory headings; admin-v2-tokens + admin-responsive = the look). ci:gates-wired 211 files, 0 orphaned.
  TOKEN GATE 42 -> 90 files, break-tested twice. The rest are deferred for ONE stated reason: they mount @/components/admin/<legacy> or @/components/ui islands rule 3 blacklists and the migration mounts as-is by design (11f-mounted-islands). Prospecting stays FILE-form contrary to the old comment — components/ui is gone from that dir but [kind]/[id] still mounts ProspectDetailPage.client.
  MY OWN TWO ERRORS THIS SESSION, both recorded so they are not repeated: (1) `git commit` commits the INDEX, and a parallel builder had staged its own files, so one commit swept in 7 pages under a message that did not describe them — caught pre-push, split with reset --soft + restore --staged. (2) `git commit --only <paths>` does NOT pick up UNTRACKED files, so d28c375b landed importing ./DocumentName and ./_columns while both were untracked; that commit does not build standalone. Nothing pushed at the time and the tip is correct. WITH PARALLEL BUILDERS: use --only AND git add the new paths explicitly.
  OPEN, ALL FILED WITH REASONS: person/deal-scope on entity READERS is a CLASS with THREE instances (crm/deals/[id] FIXED 4aa3de68; people/[id]/portal open; /admin/deals/[key] open — and that one is NOT a one-liner, getTcDeal returns broker_name, a display string with no ownership key, so it needs a decision on what owns a TC transaction) -> task_84b647c1 asks for a shared guard + sweep + AST gate. Blog editor prefills status from published_at so saving any of 28 archived posts silently publishes it -> task_16114f83. audit-log renders 0 rows because getAdminActions uses the cookie-scoped anon client and RLS hides the only record from a superuser on a compliance surface -> task_ff0a4e2c. Hydration mismatch TRACED to SiteHeader under HideChrome -> task_af3d76b5. MobileCrmHeader navy bar, now proven to carry no root-tab nav (only a settings link needs re-homing) -> task_7803e171. Client-component locale formatters -> task_00b9af6e. /admin/crm/subscriptions renders the admin error boundary because a tab island's server action 500s on mount — restoring HEAD fails identically, so pre-existing.
  NOT VERIFIED, NOT CLAIMED: /admin/signing/[envelopeId] and /admin/crm/approvals populated row paths (both tables legitimately empty; refused to insert fixtures into a legal-document table), and /admin/setup's own markup (setup is complete here so it redirects; flipping the flag would be a real state change).
  NEXT: P11 is DONE. The queue top is P12 correctness — group-MMS chokepoint, block-reason ledger, listing edits surviving re-sync, the assigned_broker resolution rule — plus 11f-mounted-islands whenever an island is touched.
2026-08-07T16:30:00Z P12 START — person-scope class, and a CORRECTION to how I characterized it.
  SHIPPED (92701b66): /admin/people/[id]/portal is scoped. It ran requireAdminPage('people.view') and nothing else, and CAPABILITY_ROLES['people.view'] includes 'broker', so any broker could walk contact ids and read that client's saved homes, hidden homes, named areas, alert recipients and site activity.
  A PINNED TEST REJECTED MY FIRST FIX AND WAS RIGHT. The obvious call is app/actions/crm.ts requirePersonInScope, which ALREADY EXISTED — my filed claim that this needed a new DAL reader was wrong. But clientPortalView.test.ts pins the page read-only BY CONSTRUCTION (no @/app/actions import, no form, no onClick), so importing the action module would have punched a hole in one invariant to close another. Rebuilt from pieces that keep it: requireAdminPage already returns role+brokerSlug (exactly ScopeAccess), and the decision is the same pure isPersonInScope every mutation uses. Only new code is resolvePersonAssignedBroker, added beside its by-email sibling and following that file's stated FAIL-CLOSED doctrine (throws on read error — a swallowed error in an authz read is an access grant).
  CORRECTION — I OVERSTATED THE CLASS. I filed this as three instances and said so in commit d28c375b. Two of the three do not fit:
   (1) /admin/deals/[key] is NOT an unscoped outlier. I probed tc_deals (33 rows): fub_person_ids is EMPTY on all 33 so the person-link chain dealInScope uses does not exist here, and broker_name is a DISPLAY string — "Rebecca Peterson" against brokers.display_name "Rebecca RYSER Peterson", so a naive name join would miss all 6 of her deals and lock her out of her own transactions. More important: NO TC surface scopes by broker anywhere. getClosings reads every tc_deal with no filter and selects broker_name only to display it; /admin/closings guards on requireAdminPage('transactions.view') and 'transactions.view': ['broker']; /admin/commissions likewise. THE WHOLE TC FAMILY IS BROKERAGE-WIDE. deals/[key] is CONSISTENT with its siblings, not an outlier. Whether TC should be brokerage-wide is a real product question for Matt (PB supervision, coverage) — the status quo is defensible and I am not guessing at it.
   (2) people/[id]'s identity header is DELIBERATE AND DOCUMENTED, not an accident. The page's own comment says the fold renders only in scope because getCrmPersonFull returns the empty bundle otherwise, "and the identity header above still renders from the card". An out-of-scope broker sees a name/email/phone and nothing else. That is a judgment call for Matt, not a hole.
  SO THE REAL CLASS IS TWO GENUINE HOLES, BOTH NOW FIXED: crm/deals/[id] (4aa3de68) and people/[id]/portal (92701b66). What made both genuine is the same shape — the SIBLING page and every mutation applied the guard and only the READER skipped it. That is the test for whether a missing guard is a defect or a design.
  SWEEP: 19 dynamic-segment admin pages, 3 scoped, 16 not. Three of the 16 are pure redirect bridges (crm/[id], crm/[id]/portal, console/leads/[id] — verified, no data fetch). Several legitimately need no scope (help/[slug] is a KB article, listings/[listingKey] is MLS data with no per-broker owner). The rest need triage, which the gate's seeded baseline now carries.
  IN FLIGHT: ci:entity-scope — an AST ratchet asserting that every page.tsx in a DYNAMIC route segment under app/admin either calls a sanctioned scope function, is a pure redirect bridge, or is baselined. Shrink-only, so no NEW unscoped entity reader can land. ci:crm-scope cannot see any of this — it inspects WRITES in app/actions/crm*.ts only.
2026-08-07T16:45:00Z P12 UNIT 1 COMPLETE — entity-reader scope, gated. Shipped 92701b66 + c13bb7ce, deployed c13bb7ce READY, production spot-checked.
  G66 / ci:entity-scope — every page.tsx under app/admin in a DYNAMIC route segment must CALL one of the four sanctioned scope decisions (scopeBroker, isPersonInScope, dealInScope, requirePersonInScope), or be a pure redirect bridge, or be baselined. A dynamic segment IS a caller-supplied record id, so it is the whole IDOR surface; requireAdminPage only decides which SCREENS a role may open. Detection is by CALL EXPRESSION via the TS compiler — an import without a call is precisely the bug and fails. Shrink-only. 9 break-tests green, including the two that matter: import-without-call FAILS, and deleting a baselined line while the page is still unscoped FAILS (the ratchet cannot be gamed by removing the evidence). Seeded 19 dynamic pages: 3 scoped, 4 pure bridges, 12 baselined.
  A SECOND, DIFFERENT DEFECT CLASS SURFACED — CAPABILITY, not broker scope. Three pages ran NO auth of their own, only the (protected) layout's "signed in with SOME admin role":
   · /admin/deals/[key] — while its OWN list page /admin/closings guards requireAdminPage('transactions.view'). Same reader-outlier shape that made the two scope holes real. Renders commissions, GCI, splits, every transaction document.
   · /admin/signing + /admin/signing/[envelopeId] — no auth anywhere in the family, and it mints 900-SECOND SIGNED URLS for legally binding PDFs.
  All three now guard 'transactions.view'. NO LIVE USER LOSES ACCESS — probed admin_roles, not assumed: exactly one superuser (matt) and two brokers (rebecca, paul); 'transactions.view' is ['broker'] and superuser always passes. This closes the path before a report_viewer account ever exists. Honest framing: hardening, not an active breach. The signing capability choice is a JUDGMENT CALL stated in the code — that family declares no capability anywhere, so there was no sibling policy to copy; tc_envelopes is the same tc_* domain 'transactions.view' already governs.
  A HOLE IN MY OWN GATE, found by the gate that copied it: G65's bridge test counted JsxOpeningElement + JsxSelfClosingElement but NOT JsxFragment, so `if (!row) redirect(...); return <>{row.gci}</>` read as a JSX-free bridge and skipped the rule. Cosmetic in G65 (a migration counter), a HOLE in G66 (an authorization check). Both count fragments now; G65's 18 break-tests still green. LESSON: when a new gate copies an old gate's predicate, the copy is a free audit of the original.
  BASELINE TRIAGE, written down rather than left implicit — NEEDS SCOPE: bpo/[slug] (broker_price_opinions has broker_slug + person_id), cmas/[slug] (cmas has broker_slug + person_id, and renders send-to-client), people/[id] (identity header + activity feed + prospect story + CMA/BPO list render OUTSIDE the self-guarded fold — broader than I had concluded), visitors/[sessionId] (visitor_sessions.crm_person_id resolves to a crm_people row with assigned_broker; anonymous sessions are genuinely unowned, so whether analytics inherits contact ownership is Matt's call). LEGITIMATELY UNSCOPED: crm/import/[id] (shared job ledger, no broker column, superuser-only action), help/[slug] (static KB, no DB row), listings/[listingKey] (MLS data, no per-broker owner), prospecting/[kind]/[id] (shared prospect pool — caveat: the FSBO DDL was not exhaustively read). SAFE ONLY INDIRECTLY: people/[id]/tools, via getCrmPersonFull's own self-guard, which the gate cannot see and which would silently lose the page its scope if that reader ever changed.
  KNOWN LIMIT, stated in the gate's header: it proves a scope decision HAPPENED, not that it GATES THE RENDER. crm/sequences/[id]/edit calls scopeBroker only to scope a funnel query. Closing that needs dataflow analysis, not a call-site check.
  NEXT in P12: the baseline's four NEEDS-SCOPE pages (bpo, cmas, people/[id], visitors/[sessionId]) — each drops the ratchet by one. Then group-MMS chokepoint, block-reason ledger, listing edits surviving re-sync.
2026-08-07T17:15:00Z P12 UNIT 2 — the entity-scope worklist, worked down honestly. Shipped d4786a77 + 5d568434 + 3ddb46dd, deployed 3ddb46dd READY, production-verified.
  RATCHET: entity-scope 12 -> 11 unscoped, 3 -> 4 scoped.
  people/[id] SCOPED — the one genuine outlier of the four, and the exposure was WIDER than the page's own comment claimed. The B2 fold was already scoped (getCrmPersonFull returns an empty bundle out of scope) and the comment says the identity header still renders from the card, which was the accepted trade. But the header is not the only thing outside the fold: the prospect story, the CMA/BPO list and the FULL 30-ITEM ACTIVITY FEED all render after {fold} from reads carrying no scope. On the one family that IS broker-scoped everywhere else. The check now runs BEFORE those reads. Litmus surface re-verified: person 13168 unchanged for the superuser at 375 + 1280 including the ?intent=cma kickoff path.
  visitors/live + visitors/[sessionId] GUARDED — neither ran ANY auth of its own, and the detail page builds its own service-role client inline; an identified session resolves to a crm_people row and renders that contact's identity plus their whole browsing history. Capability choice is a JUDGMENT CALL stated in the file: no analytics capability exists ('performance.view' is [] = superuser only, which would lock both brokers out of a surface they use), so 'people.view' — the sensitive half of a visitor session is the CONTACT it resolves to. Zero live impact (probed: one superuser, two brokers).
  bpo/[slug] + cmas/[slug] — NOT FIXED, AND THAT IS THE RIGHT ANSWER. The gate builder's triage said "NEEDS SCOPE" because those tables carry broker_slug/person_id columns. Having an ownership column is not the same as the product intending per-broker access control. Applied the defect test instead: NO CMA/BPO MUTATION broker-scopes either, and neither list page does. The whole valuation family is brokerage-wide, exactly like TC. Consistent with its family = design, not defect.
   Two traps found while checking, both of which would have made a naive fix WORSE: cmas.broker_slug uses the FULL WEB slug space (matthew-ryan 265, paul-stevenson 1) not the CRM slug, so isPersonInScope('rebecca', 'rebecca-peterson') is false and would deny EVERYTHING; and person_id covers 93 of 266 cmas and 0 of 3 bpos.
  THE DESIGN IS NOW WRITTEN DOWN (decisions.md, 2026-08-07). The admin runs TWO access models side by side and neither was recorded, so every session that notices re-litigates it: CRM is broker-scoped (Matt's Option A) and applies it consistently; TC and valuations are brokerage-wide with no filter anywhere. Plus a THIRD, orthogonal model — CAPABILITY — where several detail pages ran no check while their own list pages did. Matt has NOT ratified the brokerage-wide half; it is recorded so it reads as a design rather than an oversight, with the four measured reasons the obvious scoping keys would fail if he decides otherwise.
  THE TEST THAT DID THE WORK, worth keeping: a missing guard is a DEFECT when the sibling list and the family's mutations apply it and only the READER skips it. It is a DESIGN when nothing in the family scopes. That separated three real fixes from four pages that merely looked wrong — and it is why I did not ship a bpo/cmas "fix" that would have locked Rebecca out of everything.
  UNRELATED GATE FAILURE FIXED (5d568434): ci:content-freshness went red mid-session because the date rolled to 2026-08-08 and Munch & Music's endDate 2026-08-06 became past — a past date shown as upcoming is a §0 failure. RE-VERIFIED AGAINST THE OFFICIAL SOURCE rather than guessed: munchandmusic.com publishes "Our 2026 series dates: July 2 … August 6" and announces no 2027 season, so the dates go null and the page renders the recurrence descriptor only — this file's own documented doctrine, and what 26 other events already do. Inventing a 2027 date to green a gate is exactly what §0 bans. Verified live at /central-oregon/events/munch-and-music: 200, recurrence text only, no date, no startDate schema. (My first production check hit /events/<slug>, which 404s — the route is /central-oregon/events/<slug>. A vacuous check that "passes" is worse than none.)
  NEXT in P12: the remaining baseline is 11, of which crm/import/[id], help/[slug], listings/[listingKey] and prospecting/[kind]/[id] are legitimately unscoped and bpo/cmas/deals/signing/visitors are brokerage-wide-by-design. people/[id]/tools is safe only INDIRECTLY via getCrmPersonFull's self-guard — the gate cannot see that, and the page would lose its scope silently if that reader changed. Then: group-MMS chokepoint, block-reason ledger, listing edits surviving re-sync.
2026-08-07T18:20:00Z P12 UNIT 3 — send integrity: every SMS send now holds an idempotency lease. Shipped 3781b526, deployed READY.
  THE ATLAS FINDING WAS PARTLY WRONG, and correcting it was most of the work. It read "Group MMS path: inline suppression only, bypasses sendGovernedSms idempotency → double-send window". Traced end to end:
   · TRUE that the group branch never calls sendGovernedSms — a carrier group is not person-keyed, and sendGovernedSms's own header says so.
   · FALSE that it is unguarded. It runs requirePersonInScope per member, a fail-closed isSuppressed per member (blocked=true breaks the loop), and quiet hours are checked ONCE for the whole action at crm.ts:792 — ABOVE the branch, so the group path inherits it. The branch also sits INSIDE performSend, which the action wraps in withSendIdempotency. No blanket bypass.
  THE REAL WINDOW WAS NARROWER AND NOBODY HAD NAMED IT: SmsComposer renders idempotencyKey blank on SSR and first paint (a value there would be a hydration mismatch — the comment says so) and fills it in an effect after mount. The action read `if (!idempotencyKey) return performSend()`, so a submit landing BEFORE hydration reached Twilio with no ledger row at all. Server-action forms post without JS, so that window is reachable. On this path a duplicate is a second real text to a client and, with 2+ recipients, a second carrier GROUP thread. That branch is deleted; an empty key now falls back to deriveFallbackSendKey.
  MY OWN TEST FAILED AND WAS RIGHT TO — the most useful thing that happened this unit. First bucket was 60s; the test asserted stability across 59s and FAILED because 1_000_000ms is not bucket-aligned, so the two submits straddled an edge. That surfaced an asymmetry I had not thought through: on a ledger hit withSendIdempotency REPLAYS the stored result, so a FALSE COLLAPSE silently swallows a message the broker believes was sent, while a false miss only delivers a VISIBLE duplicate. Not equally bad. Window shortened to 10s so a false collapse is very unlikely, accepting a slightly higher straddle chance — the failure belongs on the visible side.
  BOTH LIMITS DOCUMENTED IN THE HELPER AND PINNED BY TEST, not left as surprises: the boundary straddle has its own named test case, each hashed field (person · body · recipient ids · phones · attachments) has a test proving two different messages never collide, and a delimiter test proves content cannot be shifted across adjacent fields to forge a collision (['ab',''] vs ['a','b']). 18 tests.
  deriveFallbackSendKey lives beside withSendIdempotency in lib/crm/idempotency.ts, not inline — which ALSO kept app/actions/crm.ts UNDER its file-size baseline instead of needing a re-seed. Fix the cause, do not re-baseline.
  data-atlas.md §5 and the send-integrity rollup are corrected IN PLACE, including what the original reading got wrong, so the next reader does not re-derive it.
  REMAINING send-integrity per the atlas: listing-alert send-then-mark duplicate window (§11) · per-attempt block ledger absent (§5, blocks send-integrity auditing) · FSBO off_market dead branch (§4).
2026-08-08T04:20:00Z 11F UNIT 1 — the reporting family joins the token gate, islands and all. Shipped 4c0186e1, deployed READY (cli-4c0186e), production-probed.
  COVERAGE: token-gated admin pages 86 -> 102 of 170. crm/reporting is a BARE DIR in SCAN_DIRS, not file-form — nothing in the subtree is carved around, so a new file under it is covered the day it lands. Ratchets UNMOVED and unre-seeded: 106 raw elements, 12 widths, 0 legacy pages.
  WHY THIS FAMILY FIRST, and the transferable pattern: 15 of the 84 uncovered pages were blocked by ONE shared component. Migrating ReportingTabStrip alone unlocked all 15. Measure the island graph before picking a family — largest-first by PAGE count is the wrong sort; largest-first by pages-per-shared-island is the right one.
  THE SUB-NAV QUESTION, decided with evidence rather than by feel. A 14-item horizontal strip looks exactly like the chrome §5 amnesia exists to kill, and my first instinct was to delete it. Checked lib/admin/nav.ts instead: the rail exposes this family as ONE child (Reporting -> the hub). The other 14 reports have NO second door. Deleting it would have stranded 14 working pages. It migrated to a new av2-subnav pattern — underline-on-current, not pills, so it reads as navigation and not as the filter chip row acceptance bar #2 bans. Recorded because the next family will pose the same question: "duplicate nav" is a claim about the RAIL, so go read the rail.
  WHAT THE GATE WAS HIDING, and this is the real argument for finishing 11F: #102742 — the PUBLIC BRAND NAVY, blacklisted as admin design input since 2026-08-04 — was hardcoded in the agent-activity chart (7 places) and the properties map (markers, map style, InfoWindow). Both are now runtime reads of --a-* off the document, which recharts and the Google Maps API both need because neither accepts var() in the places that matter. Not a gate-dodge: the navy never followed the admin theme and these now do. Every page LOOKED migrated; the islands underneath were still public-brand.
  THREE BYTE-IDENTICAL FILES collapsed to one. CallsFilters / TextsFilters / AppointmentsFilters differed only in component name and doc comment, call-logs already cross-imported one of them, and overview/page.tsx carried a comment explaining it could not borrow safely. Same failure mode the strip itself was built to end (13x re-declared tab arrays is how Contact Attempts drifted). OverviewFilters was NOT folded in — it differs in presentation (labelled SelectField vs the compact control), so merging means choosing which treatment a report toolbar carries. That is a design call, not a migration; it is written into both files rather than made silently.
  THE GATE FIRED AND WAS RIGHT. ci:admin-ui rule A caught the raw <select>/<input> my own migration introduced. The fix is that the PRIMITIVE LAYER owns those elements, so components/admin/v2 grew ToolbarSelect + ToolbarCheck and the baseline did not move. ToolbarSelect REQUIRES aria-label in its type — dropping the visible label must never drop the accessible one. Same discipline on the explainer: the platform's <dialog> gives modal focus trap + Esc + inert background, so no Dialog primitive was invented and no component library came back in. Its copy is carried over VERBATIM — it makes claims about how the numbers are computed, and re-wording it would re-state those claims.
  MY OWN CSS BROKE THE GATE and a subagent caught it: .av2-dialog::backdrop shipped a rgb(0 0 0 / 0.45) literal inside components/admin/v2, which rule 2 bans. Deleted rather than tokenised — the UA's default modal scrim already dims, and adding a scrim token would have edited the P6 visual lock to style something the platform hands us free.
  VERIFICATION FOUND A DEFECT GREEN GATES COULD NOT. 15 routes x 375 + 1280 in a real browser: 200, exactly one <main>, no page-level horizontal scroll, correct tab marked aria-current, no error boundary — 30/30. But the FIRST run at 375 landed on Calls with the active tab scrolled off-screen and the aside squeezing the row to ~1.5 tabs. Gate-green, tsc-clean, and unusable. Fixed (aside wraps above; active tab scrolls into view on mount). The scroll helper's first version silently did nothing because it used offsetLeft while the scroller is not the offsetParent — rect math now, and the bug is named in the file so the next person does not reintroduce it. A run that only asserts status codes would have shipped both.
  TRANSIENT NOISE, chased rather than assumed: two routes reported mains=2 once each, never reproduced across 3x repeats on those routes or on untouched already-v2 /admin/people. It is the streaming window between loading.tsx and the page, and it matches the intermittent hydration item already tracked in 12-admin-chrome-debt (task_af3d76b5). Not introduced here; the settled assertion is 1 everywhere.
  NEW FINDING, FILED NOT FIXED (11f-admin-dark-mode-unreachable): ADMIN_UI §6 and the P6 lock both say dark mode ships from day one. It is NOT reachable — nothing in the codebase sets data-theme; the attribute appears only in the two tokens.css files. Forcing it pre-hydration proves the admin tokens flip correctly (--a-text #1c2024 -> #edeef0, panes/chart/map all follow) and that document.body stays WHITE, so every dark token would paint light-on-white. ci:admin-contrast passes because it computes token PAIRS, not what the body actually paints — the gate cannot see this. It is the shell, not this family, so it is queued rather than fixed mid-unit.
  QUEUE HYGIENE: 12-person-scope-class predated P12 units 2-3 and both of its OPEN items are closed — people/[id]/portal runs scopeBroker + isPersonInScope (92701b66), /admin/deals/[key] guards on requireAdminPage('transactions.view') (c13bb7ce). Verified on disk, entry rewritten to what is actually left (the ci:entity-scope known limit, and people/[id]/tools being safe only indirectly).
  NEXT in 11F, largest-first by island count over the 42 real uncovered pages: crm/settings 17 · crm/inbox 12 (one page, twelve islands) · operations 9 (nine Dashboard*Panel) · crm 8 · crm/deals 5 · crm/sequences 5. A Switch primitive still blocks geo's ResortCommunityToggle.
2026-08-08T05:10:00Z 11F UNIT 2 — the CRM config-table editor, and a duplicate-render defect every gate passed. Shipped a76013b4 + 629564b5, deployed READY (cli-629564b).
  COVERAGE: token-gated admin pages 102 -> 105 of 170 (stages, segments, areas). crm/settings stays FILE-FORM, not bare-dir: eleven sibling pages still mount legacy editors (CustomFieldEditor, TagTaxonomyEditor, the company + templates islands). Ratchets unmoved: 106 raw elements, 12 widths, 0 legacy pages.
  THE PRIMITIVE LAYER GREW INSTEAD OF THE BASELINE, which is the whole method for this family. crm/settings needed Switch, Dialog and an icon-only control, none of which existed; adding them to components/admin/v2 turns the remaining eleven editors into mechanical swaps. Switch is a NATIVE checkbox carrying role="switch" (APG lists it as valid) so keyboard, form participation and state reporting come from the platform. Dialog wraps the platform <dialog>, so modal focus trap + Esc + background inertness are the browser's, not a library's. IconButton REQUIRES `label` in its type — an icon-only control with no accessible name is the failure that actually ships, so it cannot compile without one. ConfirmDialog's confirm button carries the VERB ("Delete stage"), never "OK". The reporting explainer was refactored onto the new Dialog the same session so the admin has ONE dialog implementation rather than two that drift.
  A FILE-LEVEL GATE FORCED A BETTER STRUCTURE. ci:admin-ui rule C counts primary-variant Buttons PER FILE, and the editor needed three (the Add trigger, the Add submit, the Rename submit). Splitting the dialogs into AddRowDialog.tsx + RenameRowDialog.tsx satisfies it and is the better decomposition anyway. Worth remembering for the remaining editors: the rule is per-file, so a dialog-heavy island wants its dialogs in their own files.
  config-editor-helpers MOVED to lib/admin/. It is pure data transforms with no React and no DOM, it is shared by three editors, and the token gate blacklists importing from components/admin/<non-v2> — so an editor living under app/ could not have reached it where it was. 17 tests moved with it and pass.
  THE DEFECT, and it is the reason browser verification is not optional: /admin/crm/settings/stages served TWO copies of all 17 rows at 1280 — 68 reorder buttons, 34 rename buttons, 34 toggles. On a WRITE surface that is two "Delete Closed" controls per stage and a screen reader reading the table twice. tsc clean, ci:gates green, both gates green. CAUSE: the phone card list carried className="md:hidden" AND an inline style={{display:'flex'}}; an inline style outranks a class, so display:none never applied. The desktop half of the same responsive pair (`hidden md:block`, no inline style) worked correctly — which is exactly why it survived review. Fixed by moving the layout into .av2-cardlist, one mechanism. Swept app/admin for the same pairing: no other instance. THE GENERAL RULE: a responsive class and an inline display property on the same element is always a bug; put the layout in the class.
  DENSITY, caught in the same pass: rows came out 86px against §2's ~48px because the stacked reorder chevrons inherited IconButton's 30px metric and set the row height. .av2-reorder gives the pair a tight metric on a mouse and keeps the full target under pointer:coarse. 71px now.
  TWO DELIBERATE BEHAVIOUR FIXES rolled into the migration, both stated rather than smuggled: the reorder chevrons were opacity-0 group-hover:opacity-100, i.e. undiscoverable by touch or keyboard (the mobile cards already showed them always); and the island restated the row count in an h2 that the page's own VerdictLine already gives.
  SEMANTICS PROVEN, NOT ASSUMED. Every FormData field name, all three deleteMode branches, moveInList, the useTransition run() helper, all eight user-facing strings and all five row-naming aria-labels were diffed old-vs-new by count and by literal. Dialogs exercised for real in the browser: Add, Rename, Delete each open; Esc, Close and Cancel each dismiss.
  A SUBAGENT COMMITTED WITHOUT BEING ASKED and returned a final report describing a push verification that was never part of its task. The commit itself was sound and the work checked out — but the lesson is that the delegated report is not evidence. Everything above was re-verified locally before this entry was written.
  NOT INTRODUCED HERE, checked rather than assumed: an intermittent hydration mismatch fires ~1 in 3 loads on these pages. It reproduces at the same rate on /admin/crm/settings/brokers, which this unit never touched and which mounts no island. That is the SiteHeader-under-HideChrome item already tracked in 12-admin-chrome-debt (task_af3d76b5).
  NEXT in 11F: crm/inbox 12 islands (one page) · operations 9 (nine Dashboard*Panel) · crm 8 · the remaining 11 crm/settings editors, which are now mostly mechanical given the primitives · crm/deals 5 · crm/sequences 5. geo's ResortCommunityToggle is UNBLOCKED — Switch now exists.
2026-08-08T06:40:00Z 11F UNIT 3 — §0: /admin/operations published two numbers that were not true. Shipped 2f7732db, pushed.
  WHAT WAS ON SCREEN: "Active 2,915" and "Listings (total) 0". Measured against the database the same day: the active bucket in `listings` holds 7,658, and the table holds 594,608 rows. One figure 2.6x low, the other a ZERO standing in for 594,608. Both had been live through every green ci:gates run of the last two units.
  THE TELL WAS ON THE PANEL ITSELF, which is why this is worth writing down. DashboardSyncPanel's status breakdown, EIGHT ROWS BELOW the "Active" tile, already printed the right number — the breakdown calls getActiveBucketCount() while the tile called getTotalListingsCount(). Two numbers for one word on one screen. When a surface disagrees with itself, the cheap read is which of the two callers is odd, not which number looks plausible.
  WHY THOSE TWO AND NOT THEIR SIBLINGS — the actual root cause, and it generalizes: they were the ONLY counts routing through an inner unstable_cache nested inside the operations page's own outer unstable_cache (the page wraps its whole 5-call bundle in one). Every sibling on that strip is a plain service-role count through lib/data/admin/syncCounts.ts, and every sibling was correct. The zero has a second cause stacked on top: an EXACT count of listing_tile_mv times out under the dashboard's parallel load and the reader returns `count ?? 0` — the reader-swallows-error class already in the 12-correctness queue item, caught in the wild rendering a failed read as a fact.
  THE FIX WAS ALREADY WRITTEN AND NEVER WIRED. getAllListingsCount() exists in syncCounts.ts and its OWN COMMENT describes this exact card showing 0; it uses count:'estimated' for that reason. getAdminSyncCounts was simply never repointed at it. Worth remembering: before designing a fix for a stated-in-comments defect, grep for whether someone already built one.
  VERIFICATION TOOK PATIENCE AND THAT MATTERED. The first three post-fix renders still showed 2,915 / 0 — the page's outer unstable_cache has revalidate:180 and serves stale-while-revalidate, so the first read past the window only KICKS the refresh. Reading it once and declaring the fix dead (or worse, alive) would both have been wrong. After turnover: Active 7,659 (my independent count minutes earlier: 7,658 — one listing moved status) and Listings (total) 617,326.
  AND THEN THE NEW NUMBER TURNED OUT TO BE AN ESTIMATE THAT OVERSTATES. 617,326 against an exact 594,608 is 3.82% HIGH, while the DAL comment claimed "within a fraction of a percent". Root cause: `listings` has NEVER been analyzed — pg_stat_user_tables.last_analyze AND last_autoanalyze are both NULL — so reltuples is whatever the last index build left. Fixing a §0 defect with a number I had not myself measured would have repeated the original sin, so: the comment now carries the measured drift, the tile reads "Listings (total, estimated)", the listing-detail caption says "estimated, runs ~4% high", and the ANALYZE that actually repairs it is queued (12-listings-never-analyzed) rather than run mid-unit — it is production maintenance and deserves its own decision. It is also likely a query-planner problem far beyond this card.
  "ACTIVE LISTINGS" IS BACK on the operations numbers strip. 11E cut it because the page could not make it true from what it read. Cutting a figure is right while it is a lie; restoring it is right once the source is fixed — and the pairing of those two calls is what the §0 rule actually asks for.
  ONE GATE FIRED AND WAS RIGHT: ci:file-size-budget failed on app/actions/listings.ts (+12 lines) because my explanatory comment was longer than the code it deleted. Trimmed to a pointer at lib/data/admin/syncCounts.ts, which is where the explanation belongs, instead of re-baselining a 2,678-line file. Same call as unit 3 of P12 with deriveFallbackSendKey.
  NOT DONE THIS UNIT, and stated plainly: the nine Dashboard*Panel islands are NOT migrated to v2, so /admin/operations is still outside the token gate. The §0 defect outranked the migration and consumed the unit. The groundwork is done — the panel's data layer is now correct and its two figures are labelled honestly, so the migration that follows is presentation-only.
  ALSO STILL OPEN from 12-published-figures: the market/reports _fetchAllCitySnapshots .limit(50) still caps the PUBLIC city grid at 50 of 136 qualifying cities. Untouched here.
  NEXT: migrate the nine Dashboard*Panel islands (presentation only now) · then crm/inbox 12 islands · crm 8 · the remaining 11 crm/settings editors.
2026-08-08T08:05:00Z 11F UNIT 4 — /admin/operations on v2, and three panels stop saying untrue things. Shipped 168060e8.
  COVERAGE: token-gated admin pages 105 -> 106 of 170; operations is a BARE DIR (its nine islands moved into the route's own _components/). RATCHET SHRANK and was re-seeded smaller for the first time this phase: 106 -> 104 raw elements, 12 -> 11 distinct widths.
  THE LESSON THAT MATTERS MOST HERE: the token gate sees IMPORTS, not appearance. Rule 3 flagged 8 shadcn imports across nine panels. What actually made those panels render as the PUBLIC site was 298 semantic classes (bg-card / text-muted-foreground / border-border) that NO gate reads. Converting only the imports would have produced a gate-green page still painted in the public palette — gate-green for the wrong reason. The measurement that caught it: grep the semantic-class names per file BEFORE delegating, and assert the count is 0 in a browser AFTER, scoped to the page's own .av2-scope (13 such classes remain on that screen and all belong to ConsoleShell chrome, outside the fence).
  FOUR CLAIMS RETIRED, each verified against the system before touching it — this is where most of the unit went:
   · The "Notification and alert center" panel was DELETED, not migrated. Its body promised a notification center and closed with "Alert wiring (sync failure, API health, etc.) is coming in a follow-up." That wiring runs EVERY MINUTE: /api/cron/crm-alert-drain in vercel.json, lib/crm/broker-alerts.ts exporting queueBrokerAlert / queueBrokerHealthAlert / queueCmaReadyAlert / queueReturnVisitAlert, and the entire P8 litmus wake-up path is built on it.
   · A lead-panel footnote: "Hot leads and engagement scoring require FUB API or a contacts table." FUB was decommissioned 2026-06-24 and the contacts table it calls missing is crm_people — 22,977 rows, 18,379 with an assigned broker, counted this session. Deleted rather than reworded.
   · CronSyncStatus said "Progress of the 15-minute cron job" AND "Runs automatically every 15 min", about a job that runs WEEKLY. It tracks the sync-full cursor; vercel.json schedules /api/cron/sync-full at `0 2 * * 0`. The 15-minute job is sync-delta — a different path, a different cursor. Exactly the §5 failure mode: cadence read off prose instead of vercel.json.
   · "Sync job history (last 10)" -> "Completed full syncs (last 10)". recordSyncRun() fires from ONE call site and only when a FULL run completes, so the delta sync's ~96 runs a day never appear. sync_history holds 19 rows, ALL dated 2026-03-16, zero in 30 days — WHILE sync_cursor updated today and listings carry today's modification stamp. The pipeline is healthy and only the log is old. The inverse of unit 3: there the data was wrong, here the data is right and the DISPLAY lied.
  A ONE-TAB TAB BAR WAS DELETED WITH ITS LAYOUT. operations/layout.tsx rendered AdminLinkTabs with exactly one tab ("Command center"), left from a sub-route removed 2026-07-21. A one-tab tab bar is a page title, which acceptance bar #1 bans. Checked the siblings before generalising: geo 3, sync 2, media 4 — all real, all untouched. Deleting the layout also stranded stale .next/types validator entries; clearing those two generated files was what unblocked tsc, not a source fix.
  ONE FORMATTER SWAPPED, ONE REFUSED, both on evidence — ci:currency-format fires on a MOVED file because it keys on path. DashboardRevenuePanel's inline formatter is byte-for-byte lib/format/money's USD0, PROVEN on ten values (0 / 1 / 12.4 / 12.6 / 999.5 / 1,250 / 48,375 / 894,750 / 1,234,567.89 / -500) before swapping to formatPriceExact — and explicitly NOT formatPrice, which rounds to the nearest $1,000 and would move every figure. DashboardMarketingCommandCenterPanel KEEPS its own: it prints ad spend and cost-per-lead to the CENT, and every helper in that module is whole-dollar, so the "shared helper" swap would silently drop precision from a figure the broker judges spend by. Baselined with that reason written in the source. The 11E note "every formatter swap was refused" is why this got checked instead of assumed.
  CronSyncStatus also MOVED from app/admin/(protected)/sync/ into operations/_components/ — it lived in the sync folder but /admin/sync never imported it; the operations panel was its only consumer.
  VERIFICATION NOTE ON MY OWN TEST: two assertions failed for two runs and both were the TEST's fault — a "Command center" match against document.body.innerText was picking up the RSC script payload, and the shadcn-class count was scanning the whole document including ConsoleShell. A precise DOM query (rendered nav[aria-label], elements inside .av2-scope) showed zero of both. Chasing a phantom failure costs the same as missing a real one; scope the selector to the claim.
  NEXT in 11F: crm/inbox 12 islands (ONE page) · crm 8 · the remaining 11 crm/settings editors (mechanical now — Switch/Dialog/ConfirmDialog/IconButton all exist) · crm/deals 5 · crm/sequences 5.
  FILED THIS UNIT: why no FULL sync has completed since 2026-03-16, given the weekly schedule and a healthy delta path.
2026-08-08T10:15:00Z 11F UNIT 5 — the independent singles, and a lesson about delegated reports. Shipped 60c3ce7f + ce63da92 + 8c8ff5e8.
  SCOPE CHOICE, MADE DELIBERATELY: crm/inbox was next by island count (12 islands, ~3,400 lines, TWO live send paths in InlineReply + MobileComposeSheet). I measured it and did NOT start it — that is the broker's daily-driver surface, and beginning a send-path migration without the budget to finish would strand it half-done. Took the independent single-island pages instead: dscr, content-library, login, setup, email/compose (+ prospecting's wrapper). Token-gated admin pages 106 -> 111 of 170; 33 real pages left.
  THE AUTH DOORS WERE TESTED LOGGED-OUT. AdminLoginForm is how three brokers get in; a presentation migration that breaks it locks out the company. Verified /admin/login at 200 UNAUTHENTICATED with the Google Identity Services script present and zero page errors. Worth noting for the next person: that form renders NO <input> at all — Google's script injects its button into a ref'd div, so there are no field names to preserve and the whole risk sits in not disturbing that ref or its options object.
  A SANCTIONED-LEGACY BOUNDARY GOT ITS THIRD INSTANCE. dscr's DscrEmailDialog, email/compose's two islands, and people/[id]'s CommsSection all mount the G50 compose chokepoints (EmailComposer / EmailBodyEditor) that ci:composer-discipline REQUIRES. Those imports stay pointed at components/admin/crm on purpose — forking the one canonical send interface to satisfy a COLOR gate would defeat the gate that matters more. All three are file-form in SCAN_DIRS with the reason written at the entry. This is now a pattern, not an exception: a surface that sends is gated around its composer, never through it.
  DELEGATION FAILURES THAT COST REAL TIME, recorded so the next session budgets for them:
   · Both subagents COMMITTED without being asked, and one launched `npm run push` on its own. Neither was asked to do either; I had said "create the files, I will wire and delete". Nothing was lost — the work was sound — but two agents committing concurrently against one tree is how a bad merge happens. Instruct explicitly: do not commit, do not push.
   · One agent migrated three islands and never added their pages to SCAN_DIRS, so the work was DONE and UNGATED — invisible to CI and to the coverage count. Wiring the gate is part of the unit, not a follow-up.
   · One agent's own probe output was truncated in my terminal (tail -6) and I read "0 violations" from it. dscr was NOT clean — DscrScreen still imported DscrEmailDialog from components/admin. Truncated output is not evidence; re-run the check unbounded before believing it.
  THREE OF MY OWN TEST ERRORS, each of which produced a FALSE failure I nearly reported as a defect: a coverage script whose quote-regex broke on an apostrophe inside a new comment ("route's own") and read 60/170 instead of 109/170; a prospect detail page probed with expired_listings.id when the route keys on listing_key (twice — first a fabricated id, then a real id of the wrong column). The rule that keeps earning its place: when a check fails, first ask whether the CHECK is right. Scope the selector to the claim, and never assert against a key you have not confirmed the code reads.
  ci:program-complete CAUGHT A MOVED FILE. Ledger row W10.6 named components/admin/content-library/DeliverableShareBar.tsx as its proof path; the migration moved it, so a "done" row lost its mechanism. Repointed. That gate exists exactly for this and it worked.
  FOUND, NOT INTRODUCED, NOT FIXED: /admin/prospecting/expired/<listing_key> scrolls horizontally at 375px (scrollWidth 433 vs a 375 viewport). The offenders are Google Maps' unclassed tile divs escaping ProspectMap.client, whose container sets no overflow. That file was last touched by ea0ef4dd, long before this session — confirmed via git log rather than assumed. The page is deliberately NOT in SCAN_DIRS, so nothing claims it is done.
  PROSPECTING IS A SUB-FAMILY, NOT A SINGLE. Its wrapper migrated, but the wrapper mounts six more legacy islands (ProspectDetailPanel, ProspectSendDialog 472L, ProspectComplianceRibbon, ProspectDocPill, ProspectMap, ProspectPriceHistory — 1,134 lines including a send path). 25 shadcn classes still render inside that page. Gating it would have been a false claim.
  NEXT in 11F: crm/inbox 12 islands (BUDGET A WHOLE SESSION — two send paths, and it is the surface a broker lives in) · crm 8 · the 11 remaining crm/settings editors (mechanical) · crm/deals 5 · crm/sequences 5 · prospecting's six sub-islands (fix the map overflow with them).

=== MISSION: 11F UNIT 6 — crm/inbox, end-to-end (goal written 2026-08-08T10:40Z, BEFORE any code) ===
WHAT EXISTS WHEN FINISHED
  · All 17 files of the inbox family live in app/admin/(protected)/crm/inbox/_components/ and render
    the LOCKED admin v2 language: zero imports from components/ui, zero shadcn SEMANTIC classes
    (bg-card / text-foreground / text-muted-foreground / border-border and friends — the 240 the token
    gate cannot see), all colour via var(--a-*).
  · /admin/crm/inbox and every clean component are file-form entries in ci:admin-v2-tokens' SCAN_DIRS.
    InlineReply.tsx and mobile/MobileComposeSheet.tsx are EXCLUDED by design, with the reason at the
    entry: both mount the canonical SmsComposer / EmailComposer that ci:composer-discipline REQUIRES,
    and forking the one send interface to satisfy a colour gate defeats the gate that matters more.
  · components/admin/crm/inbox/ no longer exists.
WHAT A REAL USER DOES WITH IT
  A broker opens /admin/crm/inbox on a phone and on a desktop, reads the folder counts, opens a
  conversation, reads the thread, replies by text and by email, adds a note, opens a contact, and
  starts a new message — with the SAME data, the SAME folder semantics and the SAME send behaviour as
  before. Nothing about what the page DOES may change; only what it looks like.
WHAT MEETS THE BAR
  · tsc clean · full `npm run ci:gates` exit 0 · ci:composer-discipline, ci:governed-send,
    ci:crm-sms-safety and ci:crm-screen-parity all green (this family is a SEND surface).
  · Loaded in a real browser at 375 AND 1280, authenticated: 200, exactly one <main>, no page-level
    horizontal scroll, zero shadcn semantic classes inside the page's own scope, folder rail + thread
    list + thread view all render real rows, and BOTH composers mount and accept input.
  · Every send-path guard, FormData field, server action and idempotency key byte-identical to before —
    proven by diff, not asserted.
  · Ratchets unmoved or re-seeded SMALLER. Token-gated admin pages rise from 111.
NOT IN SCOPE
  Rebuilding the composers themselves; changing folder logic, queries, or any figure.
2026-08-08T12:30:00Z 11F UNIT 6 — crm/inbox, run as an /endtoend mission. Shipped c0df5aa9 (goal) + 33815142 (relocate) + a7e00b48 (migrate) + 0e0b0da4 (bundle), deployed READY.
  GOAL WRITTEN FIRST, into this file, before any code — so the final review had something to be measured against rather than my memory of it.
  SPLIT IN TWO PHASES ON PURPOSE: relocate all 17 files with ZERO markup change and prove the page still renders (baseline: 1,095 shadcn semantic classes on screen), then migrate presentation. A regression in phase 2 is attributable; one big commit would not have been.
  RESULT: 240 shadcn semantic classes in source and 1,095 rendered -> ZERO inside the page's own scope. 41 components/ui imports -> 0. The only legacy imports left are six, all canonical SmsComposer/EmailComposer, in the three files that send. Token-gated admin pages 111 -> 112; ratchets held at 104 raw elements / 11 widths with nothing re-baselined except the bundle.
  THE FIND THAT JUSTIFIED THE PARALLELISM — three workers solved ONE problem three different ways. The mobile inbox opens its pickers as bottom sheets; the v2 barrel had only Dialog (centred), so two migrations regressed them to centred modals and a third hand-rolled a local sheet to avoid exactly that. Same story for menus: a hand-rolled role="menu" panel, a native <details>, and a <select> standing in for an action list — three different behaviours under Escape and outside-click. THE BARREL IS THE PRESSURE VALVE: Sheet (bottom on phone, centred >=768px), Menu (click-outside + Escape + focus return in one place), SearchField (unlabelled input, aria-label REQUIRED in the type). Every new primitive requires its accessible name to compile. A centred modal on a phone puts its controls mid-screen and its dismiss target under the reading hand — that is why this was worth fixing rather than accepting.
  FIVE GATES FIRED AND EVERY ONE WAS RIGHT, none re-baselined except the last:
   · ci:file-size-budget — MobileThread hit 787 LOC, because hand-rolled replacements are LONGER than the imports they replace. Fixed by extracting thread-bits.tsx and letting the Menu primitive absorb the kebab: 586. page.tsx came back under 608 by trimming my OWN comments.
   · ci:admin-ui rule A — 21 raw elements, newly counted only because the files moved under app/admin. All 21 converted.
   · ci:crm-screen-parity — its spec still named the pre-move paths.
   · eslint no-restricted-syntax — an inline <style> block this session introduced; its rules moved into admin-v2.css.
   · ci:css-layers — my .rr-mail-body a colour rule was UN-LAYERED, which outranks the whole Tailwind utilities layer regardless of specificity. That gate exists because of the invisible-CTA-text P0 of 2026-07-08. Wrapped in @layer base.
  THE DEFECT NO GATE COULD SEE, and the reason /endtoend says test the real thing: at 1280 the grid gives pane 3 just 416px, and a thread sharing that with a ~230px contact sidebar rendered about ONE WORD PER LINE. Status 200, one <main>, no horizontal scroll, zero shadcn classes — every automated assertion green, and the screen unusable. Found by opening it. Sidebar now hidden below 1536px, the same call §5 already makes collapsing the nav rail under 1200px.
  I REPEATED THE MISTAKE I CRITICISED. Unit 5's delegated batch migrated islands and never added their pages to SCAN_DIRS; I called that out, then did the same thing here — the migration was complete and ungated until the final coverage count caught it. Wiring the gate is part of the unit, for me as much as for a subagent.
  BUNDLE: the production build hit 9.46 MB (+11.1% over an 8.52 MB baseline), tripping the 10% growth guard. Under the 10 MB budget, zero oversized chunks, growth is real session code. Re-baselined — AND the structural cause is filed, not buried: package.json sets no `sideEffects`, so webpack cannot drop unused barrel re-exports, and all 16 v2 primitives import admin-v2.css while ~111 admin pages import the barrel. Every one of those pages pays for Sheet, Menu, ReportGrid, RailNav and the rest whether it renders them or not.
  VERIFIED BY USE: opened a real conversation at 1280 (100 conversations, 21 rail items, 10 bubbles, thread header, 4 composer textareas, 7 send controls) and drove the phone at 375 (compose sheet opens BOTTOM-ANCHORED, Escape closes it). A one-off "Cannot read properties of null (reading 'parentNode')" appeared once and did NOT reproduce across three subsequent runs — harness teardown, not the product, and said so rather than filed as a phantom.
  NEXT in 11F: crm 8 islands · the 11 remaining crm/settings editors (mechanical) · crm/deals 5 · crm/sequences 5 · prospecting's six sub-islands (fix the map's 375px overflow with them).
2026-08-08T14:40:00Z 11F UNIT 7 — crm/settings, the largest remaining family. Shipped 723d424c (relocate) + b6d07c95 (migrate), deployed READY.
  RESULT: 24 files, 11 pages. 312 shadcn SEMANTIC classes in source and 1,156 rendered -> ZERO. Token-gated admin pages 112 -> 123 of 170; 21 real pages left. Only three legacy imports remain, all MergeFieldInserter, excluded BY NAME after verifying it is shared with SmsComposer / EmailComposer / EmailBodyEditor — a surface that COMPOSES is gated around its shared machinery, same rule as the send surfaces.
  THE ORCHESTRATION DIED AND THE VERIFICATION DID NOT — the most useful thing that happened. I ran this as a Workflow: six migration agents piped into six adversarial refuters. The process exited after the six migrations wrote all 24 files and BEFORE any refuter finished. So every "presentation only" claim was unchecked, and the files were already on disk. Resuming would have re-run migration agents over already-migrated files, so I verified the family MYSELF instead: a 17-signal behavioural probe (server actions, FormData sets, state, effects, hrefs, name/type/required attrs, aria-labels, roles, every handler) diffed per file against pre-migration content. It flagged 20 of 24 files; every flag resolved to primitive substitution once the diffs were READ — role= 0->30 from the div/role grids replacing <table>; type= dropping because the v2 Button sets type="button" itself; two new useState replacing Radix <Tabs> (11 usages) and a <Popover> that held that state internally; and an inline `!f.system` guard extracted to a helper called from both the desktop grid and the new mobile list, which also explains href 1->2. No semantic drift.
  THE LESSON: a dead orchestrator is not a dead unit. The agents' EDITS survive in the working tree; only their self-reports are lost — and those were never evidence anyway. Verify the tree, not the transcript.
  A GATE CAUGHT ME TWICE IN THE SAME HOUR, and the fix generalises. ci:admin-ui rule D failed on max-w-none; I replaced the class with an inline style and it failed AGAIN, matching the literal token inside the comment explaining the removal. The gate already builds a TypeScript AST for rules A-C but scanned raw TEXT for rule D, behind a comment claiming that was safe. Rule D now reads STRING LITERALS through the AST that was already there — and distinct widths dropped 11 -> 9, because the text scan had been over-counting comments all along. Re-seeded smaller, and regression-proved three ways: green on a clean tree, FIRES on a real injected class string, green again after restore.
  That is the SAME flaw as ci:server-type-reexport, the gate I wrote hours earlier, which matched its own worked example. TWO IN ONE SESSION IS A PATTERN: a gate that inspects code reads the AST, never the text. Both are now AST-based. This is already in memory as reference_code_inspecting_gates_use_ast — it earned re-proving.
  A LIVE 500 FIXED ON THE WAY PAST (45b64103): /admin/crm/subscriptions had been serving the admin error boundary, carried in the queue as "pre-existing, a tab island's server action 500s on mount". Console said ReferenceError: AlertEngineSettings is not defined. Cause: app/actions/alert-admin.ts carries 'use server' and ended with `export type { … }` re-exporting three names it had only IMPORTED as types. Next emits a re-export from a server module as a RUNTIME binding, so type-erasure had already deleted what the emitted code referenced. tsc is GREEN on this; only the browser objects, and what it says is 500. Fixed, plus a second LATENT instance in app/actions/video-tours-cache.ts, plus a new gate (G63, ci:server-type-reexport) proved to fire on the re-injected bug. Neither "pre-existing" nor "a tab island" — one bad export line.
  DEV SERVER CORRUPTION cost a full verification cycle and is worth recognising fast: every route, INCLUDING the site root, returned 404 while the next-server process was alive, the route files existed, and tsc + all gates were green. Restarting via preview_start fixed it instantly. I had deleted two files under .next/types earlier in the session to clear stale route types; that is the likely cause. Symptom to remember: a 404 on `/` means the SERVER, never the code. (curl then gets 403 from the bot screen — that is healthy, not a failure.)
  CONCURRENT SESSION on the same branch committed twice mid-unit and my commit was rebased from 5fa8923b to b6d07c95 during push. Two gate failures I chased (entity-scope, service-client) were the other session's tree mutating between runs, not mine — both passed on a settled tree. When a gate fails once and passes on re-run with no edit in between, suspect a sibling session before suspecting the gate.
  ONE OPEN OBSERVATION, recorded rather than dismissed: "TypeError: Cannot read properties of null (reading 'parentNode')" fired ONCE during a sweep. Not reproduced in 29 further attempts — 18 isolated loads across the three heaviest pages at both viewports, plus an 11-route rapid navigation reproducing the sweep's exact pattern. One occurrence in ~66 loads, unexplained. NOT filed as a harness artifact, because that was not proven.
  NEXT in 11F: crm 8 islands · crm/deals 5 · crm/sequences 5 · crm/tasks 3 · people/[id] 3 · then the pairs and singles · prospecting's six sub-islands (fix the map's 375px overflow with them).
2026-08-08T17:30:00Z 11F UNIT 8 — crm/sequences + crm/tasks, and the bundle fix that was blocking every unit. Shipped 4eba838d + 28c0a08c + 4d6b1c0b, deployed READY.
  RESULT: 10 files in, 16 out after extraction. 289 shadcn SEMANTIC classes in source and 3,226 RENDERED -> ZERO. Token-gated admin pages 123 -> 126 of 170; 18 real pages left.
  THE BARREL GREW THREE TIMES, and every time a migration had lost something real:
   · Combobox — TWO agents independently replaced a cmdk picker and BOTH reported the same casualty: arrow keys gone, click-only selection. When two independent workers drop the same behaviour the barrel is what is missing. Full APG: aria-activedescendant, wrapping arrows, Home/End, Enter to select, Escape to close, Tab closing WITHOUT selecting (an unconfirmed highlight is not a choice). It also takes onQueryChange, because the contact picker searches 23,000 people server-side and re-filtering locally would hide rows the server matched on a field the label never shows.
   · Menu — a THIRD agent found it had no arrow-key navigation, which is exactly why three files in one folder had each hand-rolled their own menu. Now APG with wrapping arrows, Home/End, ArrowUp-opens-on-last, disabled items skipped.
   · Button and TextField now FORWARD REFS. They did not, so a `ref` silently attached to nothing — and an agent worked around it with document.getElementById and a querySelector on a name attribute, pushing focus management outside React's lifecycle. THE PRIMITIVE WAS THE BUG, not the call site; both are back on real refs.
  THE PATTERN WORTH KEEPING: when a subagent reports "I could not reproduce X behaviour", that is a defect report about the BARREL, not an acceptable trade. Three of them in one unit, three primitives fixed.
  BUNDLE: package.json declared no `sideEffects`, so webpack assumed every module was side-effectful and could not drop unused barrel re-exports. components/admin/v2/index.ts re-exports 18 primitives and ~126 admin pages import it — every page was shipping all of them. MEASURED with two full production builds: 9.73 MB / 275 chunks -> 8.84 MB / 272 chunks, -889 KB (-9.1%). This is why the bundle guard tripped on three consecutive units; each new primitive was taxing every page.
   THE ONE REAL SIDE EFFECT IS NAMED, NOT ASSUMED AWAY. A repo sweep found exactly two bare-import shapes: `import 'server-only'` (189x, build-time guard, never in a client bundle) and `import '@/lib/crm/bulk-handlers'` (1x) which REGISTERS every CRM bulk mutation handler. Tree-shaking that would make bulk jobs silently no-op — including the tag handlers that refuse protected compliance tags — so it is listed explicitly, and the BUILT server chunks were grepped afterwards to confirm the registration survived. A one-line config change with that blast radius earns a before/after measurement and a named exception; it does not earn a guess.
  A 14-SURFACE SWEEP CAUGHT SOMETHING NO UNIT-SCOPED CHECK WOULD HAVE. Because a tree-shaking change can break anything, I swept today/messages/people/oversight/inbox/tasks/sequences/settings/operations/reporting/valuations/closings/content/audiences rather than only what this unit touched — asserting av2 primitives still RENDER (the count would fall to zero if tree-shaking had eaten them). /admin/messages came back with TWO <main> landmarks: its own plus ConsoleShell's. Thirty files were de-nested for exactly that in 11C-11E; this page was missed and had shipped two since 11B. Pre-existing, one-line fix. LESSON: sweep wider than the diff when the change is global.
  THE INTERMITTENT parentNode ERROR IS EXPLAINED AND CLOSED. It had appeared three times across sweeps and survived 29 reproduction attempts. Caught with a stack by reproducing the sweep's REAL pattern — navigating ONE page between routes with viewport changes, not fresh contexts. The frame is `$RS`, React's streaming-SSR reparenting helper injected inline in the document (not any app chunk): a navigation discards a Suspense boundary still streaming, so the destination node is already gone. Harness-induced, benign to a real user. The lesson is about instruments: 29 isolated reproductions failed because isolation was the very thing that prevented it.
  ci:file-size-budget caught five files over 600 LOC — FOUR were already over before the migration and simply had not been in the scan root. Fixed by hoisting repeated style objects and extracting stateless presentational pieces into *-bits.tsx siblings, NOT by re-baselining.
  NEXT in 11F: crm root 8 islands · crm/deals 5 · crm/calendar 2 · crm/activity 2 · people/[id] portal+tools 3 · newsletters 4 · bpo 3 · cmas 5 · crm/subscriptions 1 (its delivery-shared module is imported by ContactDeliveryPanel, so decide where that shared module lives first) · prospecting's six sub-islands (fix the map's 375px overflow with them).
2026-08-08T20:10:00Z 11F UNIT 9 — the valuations family (bpo + cmas). Shipped the relocation + d3dfc87f + b23c471a, deployed READY.
  RESULT: 21 files, 6 pages. 97 shadcn SEMANTIC classes in source and 414 RENDERED -> ZERO. Token-gated admin pages 126 -> 132 of 170. TWELVE real pages left.
  §0 GOVERNED THIS FAMILY and the briefs said so explicitly: a CMA is a comparative market analysis and a BPO is a broker price opinion — every figure is a number a licensed broker puts in front of a client. Both agents were forbidden from touching a displayed figure or swapping a formatter, and both diffed every toLocaleString/toFixed/format/Intl/%/$ hit afterwards to prove it. Every hit was a re-indent or a relocation. The signing-broker select is untouched and ci:cma-rebrand-integrity still proves swapping it re-renders only.
  MENU GREW TWICE MORE — five barrel gaps across two units now, every one surfaced by an agent honestly reporting "I could not reproduce this":
   · `href` — "Open" / "Open in CRM" had become buttons calling router.push(). That LOOKS identical and silently removes middle-click and Cmd-click to a new tab. A navigating item now renders a real <a role="menuitem">.
   · `target:'_blank'` — "Open report" originally opened a new tab and had been reimplemented as window.open from onSelect. window.open only reacts to a LEFT click, so middle-click, Cmd-click AND the right-click "open in new tab" menu were all gone. Real anchor again, with rel="noopener noreferrer" so the opened page never gets a window.opener handle back into the admin.
   THE GENERAL RULE, now proven five times: an item whose job is "go here" must be an ANCHOR. A button that calls router.push() or window.open passes every functional test a left click can express and fails every other way a person opens a link.
  A PRIMITIVE-LEVEL LAYOUT BUG FOUND BY MEASURING, not looking: /admin/cmas scrolled horizontally at 375px by exactly 2px. Walking the parent chain showed a 361px av2-pane inside a 343px grid track — a grid ITEM defaults to min-width:auto and refuses to shrink below unbreakable content (a long address). .av2-pane now sets min-width:0. A pane must yield to its container and let its own content wrap; it is a surface, not a ruler. Same class as the av2-quiet flex-wrap fix already recorded above it in the stylesheet — which means this is the SECOND time an av2 container has forced the viewport wider, and the pattern to check on any new container primitive is "does it set min-width:0 when it can be a flex/grid item".
   Because that rule ships on every migrated page, it was re-verified across 14 surfaces, not just the one that showed the bug.
  THREE STALE PATH REFERENCES, all caught by gates and all FIXED rather than baselined: ci:cma-rebrand-integrity and ledger row W10.3 both named the pre-move CmaReviewActions path, and ci:reachable-exports found components/console/KpiStrip.tsx orphaned once both boards moved to ReportNumbers. Deleted after checking the IMPORT GRAPH — every textual hit was a historical doc comment or the unrelated AgentActivityKpiStrip. THE MOVED-PATH CLASS IS NOW ROUTINE: after any relocation, expect a gate, a spec file, or a ledger row to still name the old path. Four have appeared across the last four units.
  HISTORY NOTE, stated because it is misleading otherwise: the migration itself is d3dfc87f (25 files). b23c471a is a TWO-LINE ledger path fix that inherited the same commit message because the first attempt appeared to fail and I re-issued it. Not rewritten — the branch is shared with a concurrent session and pushed history is not worth rewriting for a cosmetic duplicate.
  NEXT in 11F, 12 real pages: crm root 8 islands (people-list + mobile + bulk, ~6,229 lines and genuinely tangled — mobile/ and people-list/ cross-import each other and serve three pages) · crm/deals 5 · crm/calendar 2 · crm/activity 2 · newsletters 4 · people/[id] portal+tools 3 · crm/subscriptions 1 (decide where delivery-shared lives first — ContactDeliveryPanel imports it) · prospecting's six sub-islands (fix the map's 375px overflow with them).
2026-08-08T23:15:00Z 11F UNIT 10 — crm/deals + the newsletter islands, and the scoping blind spot the unit exposed. Shipped 46de4bac + 7382628a, pushed.
  RESULT: deals 5 files -> 9 after extraction, 98 shadcn SEMANTIC classes -> 0, both listable pages rendering ZERO. Newsletter islands 4 files, 28 -> 0. Token-gated admin pages 132 -> 133 of 170.
  THE CRM SUBTREE IS NOT ALL RELOCATABLE, and mapping that was the most useful thing this unit produced. components/admin/crm/mobile is imported by FIVE pages plus lib; people-list by the crm root, bulk/registry, mobile itself, and a bulk-handler in lib. Those are shared INFRASTRUCTURE, not one route's islands — moving them into any single route would be wrong, and the crm root stays ungated until there is a decision about where shared CRM machinery lives. That decision is the real remaining question in 11F, not more migration.
  A DEPENDENCY HAD TO MOVE FIRST: crmAvatarColor/crmInitials, 32 lines of PURE logic, sat inside the shared client folder crm/mobile — which would have tied deals to a blacklisted path forever. lib/crm/deal-pipelines.ts ALREADY imported it, which is the tell that it belonged in lib all along. Now lib/admin/crm-avatar.ts with its .design-token-lint-ignore entry moved along. Same move as config-editor-helpers earlier: PURE logic inside a components/ folder is a dependency trap waiting for the next migration.
  DIALOG GAINED A SIZE — the sixth barrel gap this session, and the same shape as the other five. The deals DETAIL modal had to collapse its two-column field grid to ONE column because Dialog was hard-fixed at the 460px confirm width. A modal that asks a question and a modal you work in are not the same object. size="work" is 720px and the original layout stands. THE PATTERN: when a migration reports "I had to change the layout to fit the primitive", the primitive is wrong, not the layout.
  DRAG-AND-DROP LEFT ALONE ON PURPOSE. DealsBoard is a kanban whose cards move deals between pipeline stages; dnd-kit binds its ref and listeners to the card wrapper, so that stayed a div rather than becoming a Button. The brief said "a working board beats a green ratchet" up front, and the agent took the exemption and reported it instead of risking the drag handle. Worth repeating in future briefs: naming the acceptable failure mode gets an honest answer.
  WHAT IS NOT DONE, AND WHY THAT MATTERS MORE THAN WHAT IS. NEITHER newsletter PAGE is gated. Both mount components living at the ROUTE ROOT — NewsletterComposeForm, NewsletterDraftActions, BulkOneOffForm, SubscriberForms, BulkEnrollForm, GenerateDraftButton — which were never under components/admin/newsletter and therefore never entered a relocation-driven unit's scope. /admin/newsletters/subscribers still renders 10 shadcn classes from them. I traced those 10 to source (components/ui/input, via the route-root forms) rather than assuming, then UN-GATED both pages. Gating them would have been green for the wrong reason — the exact failure this whole phase is built to avoid.
   THE SCOPING LESSON, now written into the gate's own comment: a relocation-driven unit only sees files under components/admin/<family>. Files ALREADY sitting in the route are invisible to it. CHECK THE ROUTE DIRECTORY ITSELF, not just what moves into it. Every remaining family needs that second look.
  ALSO EXCLUDED: crm/deals/page.tsx still mounts MobileCrmHeader. Deleting that header (12-admin-chrome-debt, already proven redundant — ConsoleShell's CrmMobileTabBar owns the five tabs) gates deals AND helps crm/activity and the crm root. That is now the highest-leverage remaining item.
  VERIFICATION NOTE: deals' accessible-name count was 16 before and 16 after — the six aria-labels that "disappeared" in my drift probe had become `label` props on primitives, which IS the accessible name. A probe that counts one spelling of a thing will report a loss every time the spelling changes; count the CAPABILITY, not the attribute.
  I ALSO CHASED A GHOST: 20 rendered shadcn classes on the subscribers page survived a full dev-server restart, so it was not stale compilation. Tracing the DOM ancestry to components/ui/input and then to the route-root forms is what found the scoping blind spot. Restarting the server first was the cheap check; it being wrong is what made the finding.
  NEXT in 11F, 11 real pages: delete MobileCrmHeader (unblocks deals + activity) · the six newsletters route-root files · crm root 8 islands (BLOCKED on the shared-infrastructure decision) · crm/calendar 2 · crm/activity 2 · people/[id] portal+tools 3 · crm/subscriptions 1 (decide where delivery-shared lives) · prospecting's six sub-islands (fix the map's 375px overflow with them).
2026-08-09T14:10:00Z P12 CORRECTNESS CLEARED + 11F DECISIONS 1 AND 2 BUILT — 7e8c8b04 · 654cf7d5 · 0644c9f3 · 82c13b33 · 2d089d6c · 6c1f339b · 6bfb8227 · e2da806a · 083c8117, all pushed.
  MATT'S FOUR BLOCKER RESOLUTIONS ARE RECORDED IN decisions.md and three of the four are now executed. P12 correctness came first at his direction, ahead of the rest of 11F, because the styling tail is a ratchet and the P12 queue held defects that change what a person sees.
  /admin/blog COULD SILENTLY PUBLISH AN ARCHIVED POST TO THE PUBLIC SITE. openEdit prefilled `status: post.published_at ? 'published' : 'draft'` — the wrong column. All 87 posts carry a publish date, so that returned 'published' for every row; opening any of the 32 that are NOT published (archived_stats_unverified 28, draft 3, pending_pilot_review 1) and pressing Save wrote status:'published', which is the column the public blog filters on. The screen showed only the date, so the change was invisible from the surface that made it.
   NOTHING HAD FIRED, and that was established BEFORE any code changed, in two differently-shaped queries per §0: the status distribution still matched the 2026-08-07 measurement exactly, and a second query listing every row touched since 2026-05-08 returned only already-published rows. The cause was one missing column — getAdminBlogPosts never selected `status`, so the form had nothing else to read. saveBlogPost's 'draft'|'published' union would ALSO have collapsed the two non-editorial states even once the prefill was right.
   THE FIX MOVED THE PREFILL OUT OF THE PAGE. It survived for months by being unreachable to a test: pure logic living in a 'use client' component beside DOM effects and a server action. lib/blog/admin-form.ts is a plain function over plain data and admin-form.test.ts holds it to the one behaviour that matters. Same lesson as config-editor-helpers and crm-avatar, third time this phase: pure logic inside a components/ or page file is a trap.
  THE AUDIT LOG COULD NOT READ ITSELF, AND SAID SO AS "nothing logged". The filed hypothesis was "RLS hides rows from a superuser". It was wrong, and testing it rather than accepting it is what found the real defect: matt@ IS a superuser row in admin_roles, so I impersonated the authenticated role with his uid and the SELECT raised 42P17 INFINITE RECURSION — "Super admin read all admin_roles" is a policy ON admin_roles whose USING clause reads admin_roles. Evaluating it required reading the table, which required evaluating it.
   THE BLAST RADIUS WAS FOUR TABLES, not one: every policy that consults admin_roles to authorise a caller inherited the failure — admin_roles, admin_actions, broker_generated_media and both crm_company_settings policies. Only the `authenticated` role was affected, which is precisely why it hid for months: every admin screen reads through a service client, which bypasses RLS. The audit log was the only surface exercising those policies.
   AND getAdminActions DISCARDED THE ERROR — `const { data } = await query` — so a hard RLS failure and a clean history rendered identically. For a licensed brokerage that is the wrong direction to fail in. Migration 20260809010000 moves the lookup into SECURITY DEFINER helpers (the read of admin_roles then runs as the function owner and the cycle breaks), the action returns its error, and the screen distinguishes "could not be read" from "nothing recorded". Verified in BOTH directions by impersonation: matt@ now sees the row, paul@ (broker) still sees none. A fix that widens access is worse than the bug.
   NOTE FOR THE NEXT PERSON: resolve the caller's email from auth.jwt() ->> 'email', not from a subquery on auth.users — `authenticated` has no SELECT on auth.users (proved it: 42501). Several older policies use that subquery and only work because they never actually evaluate for that role.
  listings ANALYZEd: reltuples 617,326 against an exact 594,608 (3.82% high) became 594,656 against 594,635 (0.004%). THE EARLIER DIAGNOSIS WAS OVERSTATED AND IS CORRECTED IN THE COMMENT: NULL last_analyze/last_autoanalyze do not mean "never analyzed" — they are stats-collector counters, 257 of 286 tables showed the same NULLs, n_live_tup read 174 for a 594k-row table, and pg_stats held statistics for all 157 columns. The counters had been reset; the planner was never blind. The "estimated" labels STAY, because nothing schedules an ANALYZE to hold it there.
  MobileCrmHeader DELETED (decision 2) — BUT NOT ContactsSearch, and that distinction is the unit. The rationale for deleting the bar held on every count and I verified each: ConsoleShell renders TopBarScope whenever the path is exactly /admin/crm, the palette trigger beside it runs consoleSearchLeads, the bottom tab bar owns the five tabs, and the avatar's /admin/settings link is in the account menu. The rationale ALSO said its search was duplicated — that was true of the deals/activity variant (a plain href) and NOT of the crm root, where the slot held a real control. ContactsSearch is the only thing on ANY width that sets ?q= on the people list; PeopleListView has no search input. Deleting it with its host would have removed the list's text filter outright rather than de-duplicating it. It moved to the route's _components/ on the v2 SearchField.
   ci:crm-screen-parity FAILED ON THIS EXACTLY AS THE OLD DOCSTRING PREDICTED — two screens require BrokerScopeSheet on the People route, which the route only ever referenced through the deleted header's `center` prop. The control is still on screen via ConsoleShell -> TopBarScope -> BrokerScopeSheet, so the requirement now points at its real provider through alsoRoutes instead of being dropped. A spec that names a component names a CAPABILITY; when the component moves, repoint the spec, do not delete the requirement.
  components/admin/shared/ EXISTS AND IS MIGRATED (decision 1). 25 files relocated, then all 25 taken off shadcn: 14 @/components/ui imports and 351 semantic classes to 0, zero raw hex, zero colour functions.
   THE RELOCATION AND THE MIGRATION ARE SEPARATE COMMITS ON PURPOSE and the gate is still not flipped, because flipping it early would be GREEN FOR THE WRONG REASON: rule 3 reads IMPORTS, and TW_PALETTE catches bg-gray-500 but NOT bg-card or text-muted-foreground, which resolve to the public brand palette. README.md in that directory carries the whole contract.
   THE VERIFY PASS IS THE FINDING. Parallel agents produced token-pure code that typechecked at 0 with ZERO API drift — and an adversarial read of the raw diffs still found 46 BEHAVIOURAL REGRESSIONS. A sub-tab band painting var(--a-surface) on a var(--a-surface) parent and vanishing. Hover and press feedback deleted from phone tap targets. A select-all checkbox that lost its indeterminate dash. Stage values UPPERCASED through .av2-state. A popover that became a focus-trapping modal. "No forbidden colour remains" is not "it still works", and no gate in this repo can tell the difference.
   THREE OF THEM WERE PRIMITIVE DEFECTS, worth more than the migration that surfaced them because they were already live for every admin surface on v2: Dialog and Sheet hardcoded id="av2-dlg-title"/"av2-sheet-title" and a <dialog> stays mounted whether open or not, so any surface with two published duplicate ids and every aria-labelledby resolved to the first (PeopleListView mounts three; 81 files call Dialog) · Sheet dismissed itself on keyboard-activated clicks, because Enter/Space report clientX/clientY of 0 and the backdrop box-test read that as "outside" · Sheet is permanently mounted, so React's autoFocus fired once while it was hidden and never again, putting focus on Close for every sheet opened to be typed into. All three fixed at the source and verified in the live DOM.
   THE FIX PASS ALSO INTRODUCED ONE, caught by a second adversarial round: restoring the task-type glyph replaced a 44px Select with a radio-per-type on the premise that crm_task_types is "a short fixed list". It is a broker-editable table seeded with eight rows — the field became ~352px and pushed "Create Task" below the fold. VERIFY THE REMEDIATION, not just the migration.
   INLINE STYLE BEATS :hover. Several regressions were one shape: colour moved from a class to style={{ background }}, which silently kills the stylesheet's hover rule. Worth its own line in any future migration brief.
  COVERAGE, RE-MEASURED AND LOWER THAN RECORDED. 116 of 164 pages under (protected) are token-gated, 48 are not (170 admin page.tsx exist; 6 sit outside (protected)). The previously recorded "133 of 170" used a different method. Mine counts a page gated ONLY if its own page.tsx is a SCAN_DIRS file-form entry (106 are) or sits under a dir-form entry. State the method with the number or the number drifts.
  ROUTE-ROOT BLIND SPOT MEASURED (decision 3): 55 distinct files under app/admin/(protected) outside _components/ — 38 import @/components/ui, 46 carry semantic classes, 29 both, and 17 carry classes with NO import, which is invisible to a gate that reads imports. Heaviest: sync/, newsletters/, deals/[key], site-pages/.
  MY MISTAKES THIS SESSION, RECORDED BECAUSE THEY ARE REPEATABLE:
   · `npm run push | tail -6` returns TAIL's exit code. A push that aborted on a failing gate was announced as "exit code 0" and I nearly believed it. Only comparing git log origin/main to HEAD caught it. Never trust a piped exit status; confirm against the remote.
   · Committing with explicit paths to avoid sweeping a concurrent session's staged work ALSO drops your own edits outside those paths. A repo-wide rename touched two gate scripts; the commit listed only components/ and app/, so ci:crm-mobile-track stayed broken in the committed tree while passing on disk. That is what the failed push caught.
   · Then the opposite error: amending with $(git diff --cached --name-only) took EVERYTHING staged, so 6c1f339b swept in the concurrent session's voice-canon files (VOICE.md, voice-constructions.cjs, brand-voice/constructions.ts) under a message that says "RELOCATION ONLY". Content correct, gates green, attribution wrong. Not rewritten — pushed history is not worth rewriting for provenance — but the other session has been told.
   · A blanket sed rewrote HISTORY. The rename also edited five dated records, including Matt's own decision text (which came to read "components/admin/shared/mobile ... move to components/admin/shared/") and three audit reports whose evidence cites PeopleListView.tsx:637 at a path that was true when audited. Reverted those five, kept the three live contracts. A rename applies to what a document DESCRIBES NOW, never to what it RECORDED THEN.
  TWO DECISIONS LEFT WITH MATT, both stated to him: what belongs in the audit trail (admin_actions holds ONE row in four months — the logger is wired into media, listings, roles, banners, site-pages and brokers, but not CRM sends, blog publishes or TC actions) and whether VOICE.md's "Banned constructions" prose is advisory now that the gate deliberately stopped enforcing four of those shapes on 2026-08-06.
  NEXT in 11F: the shared/ gate flip (blocked only on the dependency closure — 4 of 25 files still import legacy, and that chain ends at two G50 composer chokepoints whose paths are hard-coded in ci:composer-discipline) · the 55 route-root files · prospecting's six sub-islands · the third analytics reader.

2026-08-09T19:09:32Z /ENDTOEND MISSION GOAL — Grok session (Claude weekly limit kill recovery)
  COMPLETE GOAL: 11F closed for real, then P12 correctness units executed production-grade.

  WHEN FINISHED (definition of done):
  1) ci:admin-v2-tokens green on the full SCAN_DIRS (including relative-import closure).
  2) Zero @/components/ui imports under app/admin + components/admin (except intentional public/shared composer chokepoints if any remain documented).
  3) All 11F queue items closed or rewritten to residual non-blocking truth (converge-third-reader, primitive-audit if still open as audit-only).
  4) Uncommitted migration wave reviewed for behaviour drift, committed, docs (state/work-queue/progress) match disk.
  5) P12 compliance-adjacent units done first: 12-send-integrity remaining holes, then 12-correctness reader-swallows-error + listing/sync correctness, then 12-measurement / ops items that are code-fixable without Matt policy locks.
  6) Real-user bar: load representative admin surfaces (sync, approval-queue, analytics city, deals) at 375 + 1280 when preview available; gates + behaviour, not prose.

  STOP ONLY FOR: missing credentials, destructive ambiguity, conflicting requirements.
  STARTING POINT: 11 residual gate failures (sync 6 · approval-queue 3 · analytics 2). 41 uncommitted migrated files on disk from prior session.

2026-08-09T19:16:16Z 11F CLOSED — relative-import onion peel complete (Grok /endtoend after Claude weekly-limit kill).
  GATES: ci:admin-v2-tokens ✓ · ci:admin-ui ✓ (baseline re-seeded 76 raw / 0 legacy / 6 widths).
  WHAT LANDED THIS SESSION: residual 11 islands (sync×6, approval-queue×3, analytics×2) + prior uncommitted wave of 41 route-local islands; gate rule 3 already resolved relative imports (prior session).
  FIXED ON THE WAY: SyncSmart dual-primary (Resume→quiet so Smart Sync is the one primary).
  ORPHANS NOT IN SCOPE (unused, ungated): SyncHistoryTest, SyncHistoryTable, SyncButton, SyncDataRefreshButton, SyncRunLog; loading.tsx shells.
  QUEUE: closed 11f-final-eight, 11f-mounted-islands, 11f-route-root-blind-spot, 11f-prospecting-subfamily. Kept 11f-converge-third-reader + v2-primitive-audit as non-blocking.
  NEXT: 12-send-integrity (listing-alert claim-before-send · per-attempt block ledger · gate-drop record).
