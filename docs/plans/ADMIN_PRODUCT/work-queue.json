[
  {
    "id": "12-person-scope-class",
    "phase": "P12",
    "note": "SECURITY, filed as task_84b647c1. RE-READ 2026-08-08: this note predated P12 units 2-3 and its two OPEN items are now CLOSED \u2014 people/[id]/portal runs scopeBroker + isPersonInScope (92701b66) and /admin/deals/[key] guards on requireAdminPage('transactions.view') (c13bb7ce), both verified on disk. crm/deals/[id] was fixed in 4aa3de68 and people/[id] in d4786a77. What remains is the KNOWN LIMIT stated in the ci:entity-scope gate header: it proves a scope decision HAPPENED, not that it GATES THE RENDER \u2014 crm/sequences/[id]/edit calls scopeBroker only to scope a funnel query. Closing that needs dataflow analysis, not a call-site check. Also still true: people/[id]/tools is safe only INDIRECTLY via getCrmPersonFull's self-guard, which the gate cannot see and which would silently lose the page its scope if that reader changed. See progress.txt 2026-08-07T17:15Z for the design/defect test that separated the three real fixes from the four brokerage-wide-by-design pages."
  },
  {
    "id": "12-send-integrity",
    "phase": "P12",
    "note": "Group MMS chokepoint, block-reason ledger, gate-drop record. Compliance-adjacent."
  },
  {
    "id": "12-correctness",
    "phase": "P12",
    "note": "Listing-edit sync overlay, assigned_broker resolution rule, fsbo off_market writer, dupe-candidate queue. Plus the reader-swallows-error class found 2026-08-07: getCmaAdminRowBySlug (~20 callers on send/publish paths, task_cc5d6fc3), getTaskQueue / listCrmPeople / getGlobalActivityFeed / getAppointments / getCommissionsRollup / getTcFinancials all console.error then return empty, so a failed read is indistinguishable from an empty result. The v2 pages mitigated by saying 'this list came back empty' (a claim about the READ, not the world); the real fix is an ok:false on those return types. Also: listImportJobsAction filters crm_imports by nothing (task_8b0b02f0); financials.closings counts commission ROWS not deals (1:1 today, double-reports if a cycle ever gets two rows); broker_stats was DROP TABLE'd by migration 20260425090000 yet exists in the live snapshot with 0 rows."
  },
  {
    "id": "12-published-figures",
    "phase": "P12",
    "note": "RESOLVED 2026-08-08 (2f7732db) for the admin surfaces, with one follow-on below. (a) getTotalListingsCount() summed geo_snapshot_mv.active_sfr_count over the TOP 50 CITIES and rendered under the heading 'Active' \u2014 measured 2,915 against an active bucket of 7,658. Root cause was not only the cap: it and totalListings were the ONLY two counts routing through an inner unstable_cache nested inside the operations page's outer unstable_cache, and they were the only two wrong. Both now call getActiveBucketCount() / getAllListingsCount() \u2014 the same DAL family every sibling on that panel uses, and the same one the panel's OWN status breakdown was already using eight rows below. getTotalListingsCount had no other caller and is deleted. Verified in a browser after the 180s cache turned over: 7,659 and 617,326. 'Active listings' is restored to the operations strip. (b) The market/reports _fetchAllCitySnapshots .limit(50) is UNTOUCHED and still caps the public-site city grid at 50 of 136 qualifying cities \u2014 that half of this item is still open."
  },
  {
    "id": "12-measurement",
    "phase": "P12",
    "note": "first-broker-action stamp, reply-latency interval, CMA SLA reader."
  },
  {
    "id": "11f-mounted-islands",
    "phase": "P11_TAIL",
    "note": "The P11 remnant: pages on v2 shells that MOUNT legacy client islands, which token-gate rule 3 blacklists. DONE: crm/reporting 15 pages (4c0186e1, bare-dir); crm/settings stages+segments+areas via the shared ConfigTableEditor (a76013b4 + 629564b5, file-form); operations' nine Dashboard*Panel islands (168060e8, bare-dir). Token-gated admin pages 86 -> 133 of 170; ratchet re-seeded smaller at 104 raw elements / 11 widths. 68 uncovered, 26 of them redirect stubs, so ~39 real pages left. Largest-first by island count: crm 8 \u00b7 crm/deals 5 \u00b7 crm/sequences 5 \u00b7 then singles/pairs. THE v2 BARREL NOW CARRIES Switch, Dialog, ConfirmDialog, IconButton, ToolbarSelect, ToolbarCheck and the av2-cardlist / av2-reorder / av2-subnav patterns, so most remaining editors are mechanical swaps. geo's ResortCommunityToggle is UNBLOCKED (Switch exists). METHOD THAT WORKS: migrate the SHARED island first (one component unlocked 15 reporting pages, another unlocked 3 settings pages); when ci:admin-ui rule A fires on a raw control, grow the primitive layer, never the baseline; rule C is PER FILE, so put a dialog-heavy island's dialogs in their own files; then LOAD EVERY ROUTE at 375 and 1280 and assert visible-control COUNTS, not just status codes \u2014 that is what caught a settings page rendering every row twice with all gates green. Also still open: fold the duplicated folder-structure explanation on geo/area-guide-upload. CRITICAL MEASUREMENT LESSON from the operations unit: the token gate reads IMPORTS, not appearance. Nine panels showed 8 import violations but carried 298 shadcn SEMANTIC classes (bg-card/text-muted-foreground/border-border) that no gate sees and that render the PUBLIC palette. Grep those class names per file before delegating, and assert 0 inside the page's own .av2-scope afterwards. UNIT 5 (60c3ce7f + ce63da92 + 8c8ff5e8) took the independent singles: dscr, content-library, login, setup, email/compose. 33 real pages left. TWO STANDING RULES LEARNED: (a) a surface that SENDS is gated AROUND its composer, never through it \u2014 DscrEmailDialog, email/compose and people/[id] all mount the G50 EmailComposer/EmailBodyEditor that ci:composer-discipline requires, so those files stay file-form in SCAN_DIRS with the reason at the entry; forking the canonical send interface to satisfy a colour gate defeats the gate that matters more. (b) WIRING THE GATE IS PART OF THE UNIT \u2014 one delegated batch migrated three islands and never added their pages to SCAN_DIRS, leaving the work done but invisible to CI. crm/inbox is next and needs a WHOLE session: 12 islands, ~3,400 lines, two live send paths, and it is the surface a broker lives in. UNIT 6 took crm/inbox (17 files, two send paths) as an /endtoend mission: 240 semantic classes and 41 ui imports to zero. It also grew the barrel with Sheet, Menu and SearchField, because three workers solved the same overlay/menu problem three different ways \u2014 when a family needs a control the barrel lacks, ADD THE PRIMITIVE; three hand-rolls is how one surface stops feeling built by one person. 32 real pages left. UNIT 7 took crm/settings (24 files, 11 pages) \u2014 the largest family. 21 real pages left: crm 8, crm/deals 5, crm/sequences 5, crm/tasks 3, people/[id] 3, then pairs and singles. TWO RULES RE-PROVEN: a gate that inspects code reads the AST, never the text (ci:admin-ui rule D matched a max-w token inside a COMMENT, the same flaw as the gate written hours earlier); and a dead orchestrator is not a dead unit \u2014 the agents' edits survive on disk, only their self-reports are lost, and those were never evidence. Verify the tree. UNIT 8 took crm/sequences + crm/tasks. 18 real pages left. THE RULE THIS UNIT PROVED THREE TIMES: when a subagent reports 'I could not reproduce behaviour X', that is a defect report about the BARREL, not an acceptable trade \u2014 it produced Combobox, keyboard nav on Menu, and forwardRef on Button/TextField. And when a change is GLOBAL (a bundler config, a shared primitive), sweep far wider than the diff: a 14-surface sweep found /admin/messages shipping two <main> landmarks since 11B. UNIT 9 took the valuations family (bpo + cmas, 21 files, 6 pages). TWELVE real pages left. TWO RULES ADDED: (a) a menu/list item whose job is 'go here' must be an ANCHOR \u2014 a button calling router.push() or window.open passes every test a left click can express and fails middle-click, Cmd-click and the right-click menu; (b) any av2 CONTAINER primitive needs min-width:0, because a grid/flex item defaults to min-width:auto and will force the viewport wider rather than let its content wrap \u2014 that has now bitten twice (av2-quiet, av2-pane). ALSO ROUTINE NOW: after any relocation, expect a gate, spec file or ledger row to still name the old path \u2014 four have appeared across the last four units (crm-screen-parity, W10.6, cma-rebrand-integrity, W10.3). UNIT 10 took crm/deals + the newsletter islands. 11 real pages left, and TWO are BLOCKED rather than merely pending: the crm ROOT cannot be relocated (crm/mobile is imported by five pages plus lib, people-list by four callers including a lib bulk-handler \u2014 shared infrastructure needing a home decision, not a migration), and NEWSLETTERS has six un-migrated files at the ROUTE ROOT which no relocation-driven unit could see. THE SCOPING RULE: a relocation unit only sees components/admin/<family> \u2014 CHECK THE ROUTE DIRECTORY ITSELF. HIGHEST-LEVERAGE NEXT STEP: delete MobileCrmHeader per 12-admin-chrome-debt \u2014 the single blocker on crm/deals/page.tsx, and it helps crm/activity and the crm root too."
  },
  {
    "id": "11f-converge-third-reader",
    "phase": "P11_TAIL",
    "note": "analytics/_components/v2/DataGrid.tsx + that folder's kit.tsx still duplicate components/admin/v2/ReportGrid for the six analytics pages. Two of three readers merged in 11C-4; this is the third. Keep kit's phone-stacking behaviour plus DataGrid's ARIA table roles and keyboard-reachable scroll region."
  },
  {
    "id": "12-admin-chrome-debt",
    "phase": "P12",
    "note": "MobileCrmHeader renders a bg-primary NAVY bar on phone against \u00a75 amnesia \u2014 now PROVEN to carry no root-tab nav (ConsoleShell's CrmMobileTabBar owns the five tabs; its scope control and search are already duplicated by TopBarScope and ConsoleCommandPaletteTrigger), so only its avatar's /admin/settings link needs re-homing before deletion (task_7803e171). Intermittent hydration mismatch TRACED to SiteHeader under HideChrome, firing on already-v2 untouched surfaces (task_af3d76b5) \u2014 re-measured 2026-08-08 at ~1 in 3 loads on /admin/crm/settings/* including brokers (no island, untouched), 0 in 3 on /admin/people and /admin/crm/reporting/calls, so it is route-family-correlated rather than universal; that asymmetry is the lead worth pulling. Client-component locale formatters in AdminUsersList + DscrScreen.client (task_00b9af6e). /admin/crm/subscriptions RESOLVED 2026-08-08 (45b64103): not a tab island and not acceptable-pre-existing \u2014 app/actions/alert-admin.ts carried 'use server' and re-exported three types it had only IMPORTED, which Next emits as a runtime binding, so the erased type threw ReferenceError at module evaluation behind a GREEN tsc. Gate G63 (ci:server-type-reexport) now blocks the class. /admin/content-library takes 28-44s to render because signDeliverableDownload runs once per row across 402 deliverables. /admin/bpo/new and /admin/crm/deals/[id] have no inbound link anywhere in the repo."
  },
  {
    "id": "3b-fub-identifiers",
    "phase": "TRACK3",
    "note": "~171 files of fubPersonId/FUB_* identifiers + fub_* residue columns -> CRM-native vocabulary. Then 3D: ci:no-fub gate over the 3C functional-string allowlist. Note 11C-11E already removed the user-facing FUB copy (a 'FUB' column header on visitors/[sessionId], a footnote claiming hot leads fire FUB tasks, a green health tile claiming FUB leads were flowing) \u2014 those were claims, not identifiers."
  },
  {
    "id": "pricing-depth-followons",
    "phase": "BACKLOG",
    "note": "Parked CMA pricing ideas (photo/vision condition scoring, deferred-maintenance factor, ratio-vs-overprice segmentation). Re-run scripts/cma-backtest.mjs quarterly."
  },
  {
    "id": "11f-admin-dark-mode-unreachable",
    "phase": "P12",
    "note": "ADMIN_UI \u00a76 says dark mode ships from day one and the P6 lock records 'DECIDED (Matt): ship both'. It is NOT reachable: nothing in the codebase sets data-theme (grepped app/, components/, lib/ \u2014 the attribute appears only in the two tokens.css files). Measured 2026-08-08 by forcing it pre-hydration: the admin's own tokens flip correctly (--a-text #1c2024 -> #edeef0, panes/charts/map all follow), but document.body stays lab(100 0 0) = WHITE, so every dark token would render light-on-white and the surface would be unusable. Two things to decide: whether the body background is the admin's to own (it comes from outside app/admin), and whether a theme control is wanted at all. ci:admin-contrast passes because it computes token PAIRS, not what the body actually paints \u2014 it cannot see this."
  },
  {
    "id": "12-full-sync-never-completes",
    "phase": "P12",
    "note": "OPERATIONS. sync_history holds 19 rows, ALL dated 2026-03-16, zero in the last 30 days \u2014 while sync_cursor.updated_at and max(listings.ModificationTimestamp) were both TODAY when checked 2026-08-08. recordSyncRun() has exactly one call site (app/actions/sync-full-cron.ts:387) and fires only when a FULL run COMPLETES with history done; /api/cron/sync-full is scheduled `0 2 * * 0` (weekly, Sun 02:00). So either no full run has completed in ~5 months, or it completes without recording. The delta sync (/api/cron/sync-delta, every 15 min, runDeltaSync) is what actually keeps listings fresh and never writes here at all. The admin panel heading was corrected to 'Completed full syncs (last 10)' so it no longer reads as all sync activity, but the underlying question is untouched: does the weekly full sync still finish? Check the cursor phase history and whether the run is timing out mid-cycle. Related: nothing anywhere logs delta-sync runs, so there is no operational record of the job that does the real work."
  },
  {
    "id": "11f-prospecting-subfamily",
    "phase": "P11_TAIL",
    "note": "prospecting/[kind]/[id] is a SUB-FAMILY, not a single. Its wrapper (ProspectDetailPage.client) migrated in unit 5, but it mounts six more legacy islands \u2014 ProspectDetailPanel, ProspectSendDialog (472L, a SEND path), ProspectComplianceRibbon, ProspectDocPill, ProspectMap, ProspectPriceHistory \u2014 1,134 lines total, and 25 shadcn semantic classes still render inside that page. It is deliberately NOT in SCAN_DIRS. FIX THE MOBILE DEFECT WITH THIS FAMILY: the page scrolls horizontally at 375px (scrollWidth 433 vs 375 viewport). The offenders are Google Maps' unclassed tile divs escaping ProspectMap.client, whose container sets no overflow \u2014 last touched by ea0ef4dd, so pre-existing, not migration fallout. A container with overflow:hidden is the likely one-line fix but it belongs with that island's migration, not patched from outside."
  },
  {
    "id": "11f-newsletters-route-root",
    "phase": "P11_TAIL",
    "note": "Six un-migrated files sit at the newsletters ROUTE ROOT, invisible to the relocation-driven units because they were never under components/admin/newsletter: NewsletterComposeForm, NewsletterDraftActions, BulkOneOffForm, SubscriberForms, BulkEnrollForm, GenerateDraftButton. /admin/newsletters/subscribers still renders 10 shadcn classes from components/ui/input via SubscriberForms + BulkEnrollForm \u2014 traced to source, not assumed. BOTH newsletter pages are therefore deliberately NOT in ci:admin-v2-tokens SCAN_DIRS; gating them would be green for the wrong reason. The four newsletter _components ARE migrated and gated. These files are small (114L and 118L measured) and route-local already, so it is presentation-only work \u2014 but the pages SEND EMAIL, so the compose/draft/bulk forms carry CLAUDE.md section 1 per-action approval semantics."
  },
  {
    "id": "11f-route-root-blind-spot",
    "phase": "P11F",
    "note": "Matt 2026-08-08 decision 3: audit every admin route ROOT, not just components/admin/<family>. Relocation units only ever looked at components/admin/ and app/.../_components/, so files sitting DIRECTLY in a route directory were never scoped. MEASURED 2026-08-09 across app/admin/(protected): 38 route-root files import @/components/ui/*, 47 carry shadcn SEMANTIC classes (bg-card, text-muted-foreground, border-border, border-input, text-primary, bg-background, bg-muted, text-foreground), 29 are in both sets, and 18 carry semantic classes with NO ui import at all \u2014 that last group is invisible to ci:admin-v2-tokens, which reads IMPORTS, so nothing flags them. 56 distinct files total. Heaviest clusters: sync/ (11), newsletters/ (8), deals/[key] (7), site-pages/ (5), media/ (4), crm/deals/[id] (4). Regenerate the list with: grep -rln \"from '@/components/ui/\" 'app/admin/(protected)' | grep -v /_components/ and the semantic-class variant. Relocate into _components/, migrate to v2, then add the family to SCAN_DIRS."
  },
  {
    "id": "11f-shared-gate-flip",
    "phase": "P11F",
    "note": "Matt 2026-08-08 decision 1, LAST STEP. components/admin/shared/ exists, holds all 25 files, and every one is now on the v2 language (0 shadcn imports, 0 semantic classes, verified 2026-08-09). What remains is the gate flip Matt approved: add 'components/admin/shared' to SCAN_DIRS in scripts/check-admin-v2-tokens.mjs AND widen its LEGACY_IMPORT negative lookahead from (?!v2(?:\\/|['\\\"])) to (?!(?:v2|shared)(?:\\/|['\\\"])) so a gated page may import it. BLOCKED ON THE CLOSURE, not on the files: 4 of 25 still import legacy components/admin/* and would fail rule 3 \u2014 MobileCalendarTab -> crm/calendar/AppointmentSheet, MobileCommsTab -> crm/ConversationFeed, PeopleListView -> crm/BulkActions, PeopleSidebar -> crm/saved-view-grouping. saved-view-grouping.ts is the cheap one: 108 LOC, its only import is a TYPE from lib/crm/segment-ast, so it moves into shared/ as-is. The other three are real components with their own migrations, and BulkActions drags the bulk/* chain to EmailBodyEditor + MergeFieldInserter, whose PATHS are hard-coded in ci:composer-discipline's regex \u2014 moving them silently disarms G50. Options: (a) move the 21 clean files' families and add SCAN_DIRS in FILE form for them, leaving 4 documented exclusions \u2014 the gate already uses file-form entries for the composer chokepoints, so there is precedent; or (b) migrate ConversationFeed/AppointmentSheet/BulkActions first and flip the whole directory at once. Recompute the closure before starting; do not trust this list to have stayed still. The payoff either way: /admin/crm and 4 more pages become gate-eligible for the first time."
  }
]
