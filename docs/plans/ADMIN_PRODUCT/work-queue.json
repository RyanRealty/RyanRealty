[
  {
    "id": "11e-singletons",
    "phase": "P11",
    "note": "32 remaining legacy pages, ALL singletons — every multi-page family shipped 2026-08-07. No family structure left, so batch by RISK not directory. Send/compliance-adjacent: crm/inbox, crm/approvals, approval-queue, sign-off, crm/tasks, crm/calendar. Read-only/low-risk: audit-log, dscr, guides, financials, commissions, blog, site-pages, content-library, broker-links, forms, users, operations, deals, prospecting, settings, crm (root), crm/activity, crm/health, crm/new, crm/referrals, crm/reporting, crm/subscriptions, crm/workflows. LOOK BEFORE MIGRATING: access-denied, login, setup are auth-flow pages and may not want the console shell at all — decide that first."
  },
  {
    "id": "11e-converge-third-reader",
    "phase": "P11",
    "note": "analytics/_components/v2/DataGrid.tsx + that folder's kit.tsx still duplicate components/admin/v2/ReportGrid for the six analytics pages. Two of the three readers were merged in 11C-4; this is the third. The merge worth keeping takes kit's phone-stacking behaviour plus DataGrid's ARIA table roles and keyboard-reachable scroll region."
  },
  {
    "id": "11e-switch-primitive",
    "phase": "P11",
    "note": "The v2 barrel has no Switch. geo's ResortCommunityToggle stays mounted as-is because hand-rolling one means a raw <button role=\"switch\"> that fails ci:admin-ui rule A. Add Switch to components/admin/v2, then migrate the toggle."
  },
  {
    "id": "11f-mounted-islands",
    "phase": "P11",
    "note": "Client islands mounted as-is behind migrated shells, still carrying legacy markup and @/components/ui imports — this is why 15 of the 54 migrated pages are NOT yet in check-admin-v2-tokens SCAN_DIRS. AdminMediaManager, PhotoCurationBoard, StockPhotosPicker, GenerateBannersButton, AreaGuideUploadClient, ResortCommunityToggle, the newsletter compose/enroll/bulk/preview panels, BpoBoard, CmaBoard, DealsBoard/DealDetailModal, ManagePipelines (still renders its own h1 + shadcn primary), ListingsCsvExport, AdminListingEditor. Each island migrated = flip its page to the bare-dir scope. Also fold the duplicated folder-structure explanation on geo/area-guide-upload (page + island both print it)."
  },
  {
    "id": "12-person-scope-class",
    "phase": "P12",
    "note": "SECURITY, filed as task_84b647c1. A CLASS of missing broker-scope guards on admin entity READER pages. crm/deals/[id] FIXED 2026-08-07 (4aa3de68). STILL OPEN: people/[id]/portal gates only on requireAdminPage('people.view') and CAPABILITY_ROLES includes 'broker', so any broker reads any contact's saved/hidden homes, named areas, alert recipients and site activity; people/[id]'s identity header (getInboxContactCard) is also unscoped though the fold below it is scoped. ci:crm-scope cannot see either — it inspects WRITES in app/actions/crm*.ts. Needs a light DAL reader for assigned_broker, a shared page guard on the existing pure isPersonInScope, a sweep of every [id]/[slug] reader, and an AST gate seeded as a shrink-only ratchet."
  },
  {
    "id": "12-send-integrity",
    "phase": "P12",
    "note": "Group MMS chokepoint, block-reason ledger, gate-drop record. Compliance-adjacent — may run parallel to P11."
  },
  {
    "id": "12-correctness",
    "phase": "P12",
    "note": "Listing-edit sync overlay, assigned_broker resolution rule, fsbo off_market writer, dupe-candidate queue. Plus, found 2026-08-07: getCmaAdminRowBySlug discards its Supabase error so a transient read failure renders as 'this CMA does not exist' — ~20 callers on send/publish paths, needs per-caller tests (task_cc5d6fc3). listImportJobsAction filters crm_imports by nothing, so /admin/crm/import lists mailbox syncs and lead-intake runs as 'Past imports' (task_8b0b02f0). broker_stats was DROP TABLE'd by migration 20260425090000 yet exists in the live snapshot with 0 rows."
  },
  {
    "id": "12-measurement",
    "phase": "P12",
    "note": "first-broker-action stamp, reply-latency interval, CMA SLA reader."
  },
  {
    "id": "12-admin-chrome-debt",
    "phase": "P12",
    "note": "MobileCrmHeader renders a bg-primary NAVY bar on phone, violating §5 amnesia, on crm/deals (migrated) + crm and crm/activity (not yet) — task_b5a50db5; check what nav it carries before deleting, ConsoleShell's v2 TabBar may not cover it. Intermittent hydration mismatch TRACED to SiteHeader under HideChrome, firing on already-v2 untouched surfaces — task_af3d76b5. lead-flow's funnel shows 'CMAs created — 104.4% vs sessions', arithmetically right but CMAs are broker-created, not visitor-driven. /admin/bpo/new and /admin/crm/deals/[id] have no inbound link anywhere. sync/BackfillHealthPanel.tsx:267 claims a strict-verify cron that is not scheduled."
  },
  {
    "id": "3b-fub-identifiers",
    "phase": "TRACK3",
    "note": "~171 files of fubPersonId/FUB_* identifiers + fub_* residue columns -> CRM-native vocabulary. Then 3D: ci:no-fub gate over the 3C functional-string allowlist. Note 11C already removed several USER-FACING FUB references (a 'FUB' column header on visitors/[sessionId], a footnote claiming hot leads fire FUB tasks) — those were copy, not identifiers."
  },
  {
    "id": "pricing-depth-followons",
    "phase": "BACKLOG",
    "note": "Parked CMA pricing ideas (photo/vision condition scoring, deferred-maintenance factor, ratio-vs-overprice segmentation). Re-run scripts/cma-backtest.mjs quarterly."
  }
]
