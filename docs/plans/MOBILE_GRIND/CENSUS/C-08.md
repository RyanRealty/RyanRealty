# C-08 · Third-party "go ask them" fallback (+ C-06 remainder)

**census_count = 8 · fixed_count = 8**

| instance | before | after |
|---|---|---|
| `data/resort-community-tetherow.json` ×6 | `"Confirmed at office"` → rendered `Ask the club · 844-431-9701` | `—` |
| `:323` | `Typically waitlisted · ask current status` | `Typically waitlisted` |
| `:349` | `Generally open · check current cap` | `Generally open` |

Tetherow is the only community with structured `membership_tiers`, so this fixes the TEMPLATE the other
12 inherit.

## The renderer was the multiplier

`KbResortOverview.tsx:278` substituted a tel: link for every row whose value was the placeholder, so one
stored phone number printed **six times** inside one block. A design audit had added that branch because
the placeholder "read as a placeholder with no next step" — the right answer was one contact line, not a
phone number in every row. **A single foot line already existed** four lines below it.

Removed the branch. Unpublished figures now render the canon's em-dash (VOICE.md Mechanics: "Unavailable
renders as an em-dash placeholder"), never an instruction.

The foot line was strengthened to name what it is for and made tappable:
`Membership questions: Tetherow membership office 844-431-9701`.

## Verification (§0) — `/communities/tetherow`, membership block

| | before | after |
|---|---|---|
| phone occurrences | 7 | **1** |
| "Ask the club" | 6 | **0** |
| em-dash placeholders | 0 | 6 |

## NOT DONE — the sourcing pass (D6)

The six figures (3 tiers × initiation + monthly dues) are **still unsourced**. D6 says web-sourceable
only, with an `as_of` stamp, and unsourceable renders as an em-dash rather than blocking — which is
exactly the state now. §0 applies: these are consumer-facing financial figures about a third party, so
each needs a source URL, a fetch date, a rendered as-of, and a refresh owner before it ships.

**For Matt:** the structural fix is live and honest. Publishing actual dues needs either a web sourcing
pass against Tetherow's published fee schedule, or a call to the membership office — which I cannot make.

## C-06 note

`← DRAG / SWIPE →` is already gone (closed by the voice session). The remaining `Swipe`/`Drag` strings in
the repo are all admin CRM surfaces, which VOICE.md explicitly does not govern ("Not governed: … admin
screens"). No further C-06 work on public surfaces.
