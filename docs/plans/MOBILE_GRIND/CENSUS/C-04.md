# C-04 · Canon → vocabulary projection gap

**Closed by the parallel voice session before this census. Verified, not redone.**

`scripts/brand-voice-vocabulary.cjs` now carries all six canon self-praise terms, projected as the
PHRASINGS that constitute the claim ("honest answers", "trusted advisor", …) rather than as bare
adjectives — correct, because VOICE.md bans claiming the virtue, not the word. "An honest empty state"
in a docstring is not a brag.

More importantly it added `PROJECTION_REQUIRED`, consumed by `check-brand-voice.mjs:86`, which hard-fails
when a canon term has no projecting phrase in the vocabulary. That is the mechanical guarantee this
class needed.

**census_count = 0 remaining · fixed_count = 0 (by this session)**

## Proof run here

Removed `'honest answers'` from `SELF_PRAISE` → `check-brand-voice.mjs` **exit 1**. Restored → **exit 0**,
file byte-identical. The gate does what it claims.

Live instances of the four previously-unprojected terms in user-facing props: **0**.
