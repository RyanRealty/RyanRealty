# C-17 · Duplicate/stacked sections of the same subject

Census: every page rendering both `<KbMarketHud>` and `<MarketCoreCharts>`.

| file | charts nested in the HUD? |
|---|---|
| `app/communities/[slug]/page.tsx:816` | YES — adopted 2026-07-29, the day Matt asked |
| `app/cities/[slug]/page.tsx:491` | **NO — sibling section → FIXED** |

Checked and out of scope: the other five `KbMarketHud` callers render no charts at all
(`app/page.tsx`, `housing-market/central-oregon`, `housing-market/[...slug]`, `zip/[zip]`,
`cities/[slug]/[neighborhoodSlug]`).

**census_count = 1 · fixed_count = 1**

## Fix
`MarketCoreCharts` moved inside `<KbMarketHud>` as `children`, matching the communities pattern:
dropped the wrapping `<section className="section">` and the `wrap py-10 sm:py-14` cream band, used
`pt-10`. `KbMarketHud` already exposed `children` for exactly this — its docstring names the directive.

## Verification (§0)
`/cities/bend` at 375×812 — market headings on the page:
`["Is Bend a buyer's or seller's market?", "Latest market activity"]`.
The separate "Bend market trends" H2 is gone; "Latest market activity" is the unrelated KbActivity rail.

## Gate — G61 `ci:market-section-nesting`
Decidable: in any file rendering both tags, `<MarketCoreCharts` must appear before `</KbMarketHud>`.
A self-closing `<KbMarketHud ... />` cannot hold children, so its presence alongside the charts fails
automatically.

**Proven**: reintroduced the sibling form → gate exit 1 with the correct message → restored → exit 0.
Wired into `ci:gates`; `ci:gates-wired` reports 210 gate files, 0 running nowhere.
