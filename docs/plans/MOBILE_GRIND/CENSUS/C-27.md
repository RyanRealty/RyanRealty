# C-27 · Remove the admin help FAB

Census: `HelpProvider` mounted once, `app/admin/(protected)/layout.tsx:97`. One instance.

**census_count = 1 · fixed_count = 1**

## Fix
Removed the mount plus its two now-unused imports (`HelpProvider`, `getHelpArticleIndex`).

## Verified consequences (grepped, not assumed)
- `/admin/help` **survives** — also linked from `app/admin/(protected)/content/page.tsx:21` ("Help center").
- Guided tours **do not**: `startTour` and `toursForPathname` are referenced nowhere outside
  `components/admin/help/`. The tour system is now unreachable code.
- D9: `components/admin/help/tours` left parked, not deleted, so a future trigger can pick it back up.
