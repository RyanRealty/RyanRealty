[
  {
    "id": "FUNNEL-2",
    "refuted": false,
    "confidence": 0.8,
    "reasoning": "REPRODUCE (own script reverify.mjs/reverify2.mjs via scratch sb.mjs, window 2026-08-23T20:06Z..2026-09-22T20:06Z): crm_people created in 30d, deleted=false, non-fleet = 279 (reader 278; one row created since); assigned_broker matt 278 / paul 1; site-door people 88 (contact 47, alerts 30, join 7, seller 2, signin 1, Website booking 1), all 88 assigned matt; response-clock:untouched on 75/88. Outbound timeline rows on the 88: 160 (email_out/app 83, email_out/sequence 74, twilio 2, gmail 1); non-machine touches = 3 people, same as the reader, but reading those three timelines weakens them further: 63951 is the LEAD's own inbound call + voicemail (kind call/voicemail, source twilio, title 'Inbound call', no outbound callback row); 63551 'Appointment: Buying — Quinn Alias' is an appointment-invite email with no provenance stamp on an internal-looking alias (ryan-realty.com email); 63952's gmail row lands 12 s after create and is the seller confirmation synced from Gmail. So confirmed human outbound replies to site leads in 30d is 0-1, not 3. crm_broker_alerts created >= 2026-09-15: 254 rows (reader 253), broker matt on all, to_phone ending 6706 on all, status sent on all (drain status; Twilio delivery receipts not checked), median created->sent 22.6 s, p95 52 s; body kinds untouched 123 (over 70 distinct site leads), new-lead 63, cma-ready 25, health 22, reply 11, task-digest 7, looking-at 3; 36.3/day. crm_assignment_config: {strategy all_to_one, default_broker matt, updated 2026-06-25}; lead_flows: 0 rows; brokers table: notify_sms true only for matt (rebecca/paul false). lib/crm/response-clock-run.ts:357-372 sends the 24h escalation to 'matt' regardless of assignee; lib/crm/broker-notify-prefs.ts categoryForAlertKind maps untouched-5m/24h to the new_lead switch. SEVERITY: the observation is real under the shop's own rule (SITE-09, Matt 2026-09-07: every site submit incl. idx-registration gets a 5-minute clock), but the conversion impact claimed is thin in this window. Of the 23 non-bot site leads, 19 are alerts-lane signups with no phone and no message (they receive their alert emails automatically; 3 of them are the named examples); the 4 with a phone are 63940 (Instagram-management vendor pitch, later replied to the drip), 63773 (virtual-assistant vendor pitch), 63951 (inbound caller, voicemail, no callback recorded) and 63952 (real /sell seller who booked an appointment and got a CMA-ready text). No real buyer inquiry with contact details is shown going unanswered; the 'untouched' population is bots + alert-only signups + vendor spam. Downgrade p0 -> major. FIX CHECK: (a) round_robin/by_source would move leads to rebecca/paul but queueBrokerAlert (lib/crm/broker-alerts.ts:149-153) falls back to matt unless TWILIO_FORWARD_<BROKER> is set, and their notify_sms is false so their alerts queue push_only; routing alone moves leads into silence, needs their opt-in first; (b) health alarms bypass prefs and 'always route to Matt' by design (broker-alerts.ts:259, queueBrokerHealthAlert) so moving them off the rail is its own change; (c) collapsing new-lead + untouched-5m + untouched-24h alters SITE-09, a Matt ruling -> Matt decision; (d) lib/data/loop/signals.ts has no response-clock/first-touch read (grep empty), so the scoreboard row is valid and the cheapest part.",
    "corrected_claim": "The machine half of follow-up runs (confirmation <=7 s, drip ~7 min, broker text median 23 s, 5-min and 24-h clocks) but no site lead in 30 days received a confirmed human outbound reply (3 candidate rows are the lead's own inbound call, an appointment-invite email and a Gmail-synced confirmation); 75 of 88 site arrivals carry response-clock:untouched. Everything routes all_to_one to Matt (278/279 people; lead_flows empty) whose one phone received 254 alert texts in 7 days (36/day: 123 untouched, 63 new-lead, 25 CMA-ready, 22 health, 11 reply, 7 task digests). However, the untouched population is 65 bot rows, 19 alerts-only signups with no phone, two vendor pitches, one inbound voicemail and one real seller who was handled; the window shows no real buyer/seller inquiry with contact details left unanswered, so this is a rule-compliance and noise problem more than a demonstrated conversion loss.",
    "corrected_severity": "major",
    "corrected_fix": "After FUNNEL-1 quarantines suspect rows: add 'site leads human-touched within 5 min / 24 h' (from lib/crm/response-clock.ts isHumanTouch, not the first_broker_action stamp) and 'alert texts per day by kind' to collectCompanyScoreboardSignals first, so the loop can see the rail. Then Matt decisions, in this order: whether alert-only signups without a phone need a human touch at all (SITE-09 scope), whether new-lead + untouched-5m + untouched-24h collapse into one text per real lead, and whether Rebecca/Paul share site leads; if yes, set their notify_sms + TWILIO_FORWARD_* before changing crm_assignment_config, otherwise queueBrokerAlert silently falls back to Matt or queues push_only. Health alarms and task digests need their own channel (email/admin) since queueBrokerHealthAlert routes to Matt by design.",
    "evidence_checked": "reverify.mjs + reverify2.mjs + reverify3.mjs output (people30d.byBroker, site.byDoor, humanTouchesOnSiteLeads30d touchedDetail, brokerAlerts7d byKind/byStatus/byBroker/byPhoneLast4/latency, siteLeadsHumanVsBot nonBotDetail, assignmentConfig, brokerPrefs, leadFlows, timelines 63551/63951/63952/63940); lib/crm/lead-routing.ts:23-25,85-88; lib/crm/broker-alerts.ts:149-153,166-176,259; lib/crm/broker-notify-prefs.ts:56-63; lib/crm/response-clock-run.ts:330-372; lib/crm/response-clock.ts:16-60,124-134; grep of lib/data/loop/signals.ts for response-clock/first_broker_action (no hits)."
  },
  {
    "id": "FUNNEL-3",
    "refuted": false,
    "confidence": 0.9,
    "reasoning": "REPRODUCE: crm_sequence_enrollments created >= 2026-08-23: 80 rows; sequence 2: suppressed 73 (all at step 1), running 2 (step 0 and step 1), stopped 1 (step 0); all 76 seq-2 persons are site leads and all 76 carry a crm_suppressions row channel sms reason no-sms-consent (118 rows). All-time seq 2: 87 enrollments, 79 suppressed. crm_sequences id 2 steps = [email 'Your Bend home search is set, %first%', sms delayMinutes 30 (carries fallbackEmailBody/fallbackEmailSubject), task confirm:true]. Engine app/api/cron/crm-sequence-engine/route.ts:312-321: on an sms step `isSuppressed(person.id,'sms')` -> `finish({ status: 'suppressed' })` + log 'halted — suppressed' + continue, BEFORE the phone/A2P branch; the fallback-email branch at :434-470 only runs when A2P is not live, so a suppressed person gets neither the text nor the fallback email. lib/crm/enroll.ts:305-310 adds the sms suppression unless opts.smsConsent === true. Callers: app/actions/search-alert-capture.ts:161,328,503 pass no opts; app/communities/[slug]/_v3/place-value-actions.ts:280 passes {smsConsent:false}; lib/canonical-lead-tagger.ts:261 passes none; app/contact/actions.ts:191 passes the checkbox value; the /lp/* actions pass submission.smsConsent. No code path resumes a 'suppressed' enrollment (grep 'suppressed' in app/api/cron + lib/crm: only the engine's finish and lib/crm/suppressions/audience). Timeline 64105 and 63940 both show 'Sequence \"Buyer Lead — Master Workflow\" halted — suppressed (sms:no-sms-consent'. NUANCE: even with consent the task step is confirm:true, so after the SMS the engine parks the enrollment as awaiting_broker_next (route.ts:609-613) and the crm_tasks row is only written when a broker confirms (route.ts:485-491); the task was never automatic, so the fix cannot promise a task on a broker's list without that confirmation. SEVERITY holds (major): every organic buyer's drip is one email. FIX CHECK: 'skip a suppressed channel step and advance' leaves the consent suppression itself untouched, and scripts/check-sms-consent-compliance.mjs locks the consent sentence, the checkbox and enroll.ts's suppress-unless-consent (its :76 comment), not the engine's halt, so it is not gate-blocked. The alternative 'reorder to email -> task -> sms' would park at the confirm:true task after touch 1 and stop touch 2 from auto-firing, contradicting the Matt directive recorded at enroll.ts:158-161 (2026-06-13: touch 1 + 2 send automatically) — drop that option. A smaller variant: let the sms step's existing fallbackEmailBody fire when the channel is suppressed (the copy is already on the step).",
    "corrected_claim": "73 of 76 buyer enrollments in 30 days (79 of 87 all-time) ended 'suppressed' at step 1 because the engine finishes the whole enrollment when the SMS channel is suppressed (no-sms-consent), the alerts sheet, place-page ask and canonical tagger never pass consent, and nothing resumes a suppressed enrollment. The buyer drip is therefore one email for essentially every organic buyer; the SMS step's own fallback email never fires either (the suppression gate precedes it), and the final task step is confirm:true so it would only park as a recommended next step even for consented leads.",
    "corrected_severity": "major",
    "corrected_fix": "In the engine treat a suppressed channel step as 'skip and advance' (log it) — or send that step's fallbackEmailBody when the channel is suppressed — instead of finish({status:'suppressed'}); keep the fail-closed consent suppression in enroll.ts untouched (ci:sms-consent). Do NOT reorder to email -> task -> sms: the confirm:true task step parks the enrollment and would stop touch 2 auto-firing (Matt 2026-06-13, enroll.ts:158). Add a unit check that no active sequence can end before its last non-confirm step on a channel suppression.",
    "evidence_checked": "reverify.mjs output enrollments30d/sequences/suppressionsOnSeq2People/timeline64105; reverify3.mjs timelines 63940; app/api/cron/crm-sequence-engine/route.ts:312-321,395-470,485-491,585-616; lib/crm/enroll.ts:158-161,288-314; app/actions/search-alert-capture.ts:160-161; app/communities/[slug]/_v3/place-value-actions.ts:279-282; app/contact/actions.ts:43,190-193; lib/canonical-lead-tagger.ts:260-263; scripts/check-sms-consent-compliance.mjs:1-60,76; grep -rn suppressed app/api/cron lib/crm lib/data/crm."
  },
  {
    "id": "FUNNEL-4",
    "refuted": false,
    "confidence": 0.75,
    "reasoning": "REPRODUCE: lib/crm/send-event.ts:82-83 calls ensureNativeLead({ name, email, phone, source: params.source, tags, assignedBroker }) — sourceUrl/pageUrl/pageTitle/message/campaign are accepted (:43-58) and dropped. lib/data/crm/ensureNativeLead.ts mergeReuseEnrichment: `if (hasSource) update.source = input.source!.trim()` on every reuse (confirmed in the :323-366 block); person 63951 shows the effect (tags source:inbound-call + source:contact-form + source:Manual entry). Own aggregate over the 88 site leads: source_url null 88/88; custom carries no utm/landing/first_touch key on 88/88 (customKeys = response_clock, first_broker_action_*, leadTier, moveTimeline, sellerPropertyAddress...); crm_people.source = 'ryan-realty.com' on 76, website-signup 9, Website booking 2, Manual entry 1. visitor_sessions joined by crm_person_id: 115 rows for 87 of 88 site leads; referrer null 108/115; utm_source direct 103, crm 6, google 3, ryan-realty 1; identified_via form_submit 99, rr_vid_carryover 11, email_click_pid 5. Broad check, newest 1,000 sessions (2026-09-21..22): referrer null 961, utm_source direct 960 / google 32 / chatgpt.com 2 / bing 2 — so the reader's 'referrer null on every sampled row' is the norm (94-96%), not a sample artifact. MITIGATIONS the finding omits: (1) canonicallyTagLead writes a 'Lead origin' crm_timeline note (lib/lead-origin-note.ts; contact/actions.ts:179-185) — 95 notes on 77 of 88 site leads — carrying the door label ('Contact form (Selling)'), a page (hard-coded /contact for that door), the visitor's message and a Campaign line when the page URL carried UTMs; the message is not lost, it is on the timeline. (2) visitor_sessions already holds utm_*, landing_page, referrer, gclid/fbclid, engagement per session and is joinable to the person (87/88), so a report CAN say door/landing/utm per lead today; what it cannot say is organic vs direct, because document.referrer is empty on ~96% of first loads (components/VisitTracker.tsx:137-141 infers 'direct'), and a person-level freeze would copy the same nulls. (3) the seller LP already resolves paid source from UTM via resolveLeadSource (lib/crm/lead-source.ts:37). SEVERITY: the claim that 'no report can say which lead came from Google' is mostly a capture-rate problem upstream of the CRM, and the proposed medium fix would not change it; what is genuinely missing on the person is a door-level source and a frozen first touch. Keep major (tracking is the loop's eyes) but the fix should start with the join and the door label, not a new custom blob.",
    "corrected_claim": "sendEvent forwards only name/email/phone/source/tags/broker (send-event.ts:82-83) and every site door stamps source 'ryan-realty.com' (76 of 88 site leads); ensureNativeLead's reuse path overwrites source with the latest touch; source_url is null and custom holds no attribution on 88/88 site leads. The message and door label do survive in a 'Lead origin' timeline note (77/88), and visitor_sessions rows joined by crm_person_id exist for 87/88 with utm/landing/gclid — but referrer is null on 94% of those sessions and 96% of all recent sessions, so organic-vs-direct is unknowable at every layer today, and utm_source reads 'direct' on 960 of the last 1,000 sessions.",
    "corrected_severity": "major",
    "corrected_fix": "Smallest first: write source as the door label ('contact-form', 'idx-registration', 'seller-lp', 'place-page', 'join') instead of the host and stop overwriting source on reuse (keep first, append a tag for later doors); add a DAL read that joins the earliest visitor_sessions row per crm_person_id (landing_page, utm_*, referrer, gclid/fbclid) for the lead-source report instead of duplicating it into custom; extend ci:crm-lead-integrity to require a door source. Separately investigate why referrer is empty on ~96% of first loads (referrer policy / bot share) — without that, freezing first-touch on the person only freezes 'direct'.",
    "evidence_checked": "lib/crm/send-event.ts:38-89; lib/data/crm/ensureNativeLead.ts:323-366; lib/lead-origin-note.ts:1-80; app/contact/actions.ts:79-116,171-186; components/VisitTracker.tsx:112-141; app/api/visitors/track/route.ts:379-387; lib/crm/lead-source.ts:37-41; scripts/check-crm-lead-integrity.mjs:10,76-78; reverify.mjs people30d.site (sourceUrlNull 88, noAttributionInCustom 88, customKeys); reverify2.mjs leadOriginNotes (95/77) ; reverify3.mjs visitorSessionsForSiteLeads (115 rows, referrerNull 108, byUtmSource) and visitorSessionsRecent (1000 rows, referrerNull 961)."
  },
  {
    "id": "FUNNEL-5",
    "refuted": false,
    "confidence": 0.8,
    "reasoning": "REPRODUCE: 'Stage: Lead → Nurture' timeline rows on the 88 site leads: 80, source sequence-enroll 78 / first-outbound 2; delta from created_at median 3.2 s (min 2.2, max 101.7). Site leads by stage: Nurture 79, Lead 8, Engaged 1; 30-d Lead = 162 of which 143 are expired-listing-cron rows that never auto-enroll (outreach-list block, enroll.ts:58-61), so 'Lead' does mean 'not enrolled' in practice. Code: enroll.ts:177-179 advanceJourneyStage({trigger:'sequence-enroll'}); journey-advance.ts:15,67-68. Timeline 64105: created 17:21:35.913 -> stage row 17:21:40.060 (sequence-enroll). first_broker_action stamped on 78/88 site leads; matching each stamp to the timeline row with the same ts: 47 are a system confirmation (contact:confirmation 26, alert:confirmation 21, initiator=system, 0 ms delta), 28 are the sequence drip email, 3 are other rows (call/twilio, gmail, app) — so the reader's 64099 example generalises. MECHANISM the reader missed: the send rails already refuse to stamp for a system initiator (lib/comms/sendGovernedEmail.ts:241-250 'FIRST BROKER ACTION IS A HUMAN'S (SITE-09, Matt 2026-09-07)'; lib/crm/record-message.ts:250-254), but app/api/cron/crm-health-check/route.ts:235-238 calls backfillFirstBrokerActionStamps({sinceDays:14, limit:100}) which takes the FIRST outbound crm_timeline row of ANY source/initiator and stamps it (lib/data/crm/backfillFirstBrokerAction.ts:40-56) — that backfill is what writes the confirmation as the first broker action, defeating the SITE-09 guard; recordSequenceOutbound (lib/crm/sequence-outbound.ts:33) stamps drip sends by G3 design. SEVERITY ATTACK: (1) 'Nurture = enrolled' is not an accident: R-163 is a VERIFIED requirement (docs/plans/ENTERPRISE_MAP/REQUIREMENTS.md:228; VERSION-1.md:59 G3 DONE 2026-08-16 'sequence-enroll + first-outbound advance Lead → Nurture') locked by scripts/check-stage-truth.mjs (ci:stage-truth, in ci:gates); the proposed stage change is a requirement change that needs Matt, not a bug fix. (2) 'Brokers and the scoreboard cannot see which leads are waiting for a person' is already mitigated: lib/crm/response-clock.ts (SITE-09) is the one definition of a human touch, tags response-clock:untouched (75/88 site leads) and drives the admin panel and alerts; its header explicitly says the stamp-based speed-to-lead 'means nothing'. (3) The polluted stamp only feeds getMeasurementSnapshot -> /admin/oversight (super-only, app/admin/(protected)/oversight/page.tsx:67) via replyLatencySeconds. Net: the stage semantics are by ruling; the actionable defect is the backfill re-introducing system sends as the first broker action, which contradicts a recorded Matt lock but has a small blast radius. Downgrade to minor.",
    "corrected_claim": "Lead -> Nurture advances on auto-enroll a median 3.2 s after create (78 of 80 stage rows on site leads), which is the locked R-163/G3 design ('Nurture' = in a workflow), so the stage mix says 'enrolled or not', not 'answered or not'; the response clock (SITE-09) already carries the human-touch truth. The real defect: custom.first_broker_action_at is stamped by a system confirmation on 47 of 78 stamped site leads and by the drip email on 28 — the send rails guard against this (SITE-09, Matt 2026-09-07) but the crm-health-check cron's backfillFirstBrokerActionStamps stamps from the first outbound timeline row regardless of initiator, so the oversight speed-to-lead reads seconds.",
    "corrected_severity": "minor",
    "corrected_fix": "Fix the backfill: in lib/data/crm/backfillFirstBrokerAction.ts select the first outbound row that passes lib/crm/response-clock.ts isHumanTouch (source, broker, payload.initiator/purpose) instead of the first row of any kind, and clear the 47 stamps set on system rows; add that predicate to check-stage-truth or a small unit test. Leave sequence-enroll -> Nurture alone unless Matt re-opens R-163; if he wants 'Nurture' to mean human-touched, change the requirement and the gate together, and record sequence-enroll as a tag/timeline row.",
    "evidence_checked": "reverify.mjs stageFlipTiming/firstBrokerActionOnSiteLeads/timeline64105/person64099; reverify5.mjs stampSource (contact:confirmation 26, alert:confirmation 21, sequence 28); lib/crm/enroll.ts:171-182; lib/crm/journey-advance.ts:1-70; lib/data/crm/advanceJourneyStage.ts; lib/crm/first-broker-action.ts:14-47; lib/crm/sequence-outbound.ts:33; lib/comms/sendGovernedEmail.ts:119-180,225-260; lib/crm/record-message.ts:250-272; lib/data/crm/backfillFirstBrokerAction.ts:1-60; app/api/cron/crm-health-check/route.ts:235-238; scripts/check-stage-truth.mjs:1-80; docs/plans/ENTERPRISE_MAP/REQUIREMENTS.md:228; docs/plans/ENTERPRISE_MAP/VERSION-1.md:59; docs/MECHANICAL_GATES.md:101; lib/crm/response-clock.ts:1-60; app/admin/(protected)/oversight/page.tsx:19,67."
  },
  {
    "id": "FUNNEL-6",
    "refuted": false,
    "confidence": 0.8,
    "reasoning": "REPRODUCE: seller-tagged people created in 30d (audience:seller, deleted=false, non-fleet) = 157: expired-listing-cron 143, fsbo-cron 2, expired-outreach-queue 1, fleet hosts 8, and only 3 from the production site: 63580 (source:seller-lp, 2026-08-26, ryan-realty.com email — an internal alias 'Rowan Alias'), 63952 (real /sell submit 2026-09-14, booked an appointment) and 63773 (contact-form 'Selling', a virtual-assistant vendor pitch per its Lead origin note); the reader's 2 undercounts by the contact-form row, which is spam anyway. valuation_requests: 30d = 16 (11 x https://ryanrealty.vercel.app/sell fleet, 1 x localhost, 3 x https://ryan-realty.com/lp/seller-home-value on 08-26 all with ryan-realty.com emails, 1 x https://ryan-realty.com/sell on 09-14 gmail); 90d = 24 (8 /sell, 4 /lp/seller-home-value on the canonical host). CMA-ready alerts in 7d: 25 (reader 24), person source expired-listing-cron on 25/25. Live counts with a Chrome UA: 'Value my home' on / 6, /sell 18, /cities/bend 7, /communities/tetherow 8, /contact 7 (reader 6/17/7/8) — confirmed, noting these are string occurrences including nav, footer, JSON-LD and aria copies, not 18 visible buttons; V3Chrome.tsx:40-44 already enforces one filled ask per Sell leaf and V3StickyAsk.client.tsx:11 shows the sticky ask only after the target ask leaves the viewport. REFUTED PART: 'every recent Seller-sequence enrollment comes from expired-listing-cron prospecting rows' is false — expired-cron rows never auto-enroll (enroll.ts:58-61) and the 4 seq-1 enrollments in 30d belong to 63425 (Manual entry), 63580, 63773 and 63952; ALL 4 stopped at step 0 with 'Sequence \"Seller Lead — Master Workflow\" stopped — unresolved merge tokens (%address%)' (timelines 2026-08-26 18:13, 09-02 14:13, 09-14 15:43). Cause in code: the seller LP stores the address as custom.sellerPropertyAddress (app/lp/seller-home-value/actions.ts:486) while renderCrmMerge resolves %address% from custom.customSellerPropertyAddress / customPropertyAddress / addresses (lib/crm/merge.ts:221-224,273) and buildMergeContext maps no property address (merge-context.ts) — so the seller drip's first email has never sent to a site seller and the engine's fail-closed token guard stops the enrollment (route.ts:284-291). That is a stronger, mechanical seller-lane defect than the ask placement. Also: the seller funnel events the fix cites (address_submit/qualify/submit) go to gtag/GA4 (lib/tracking.ts:131; SellValueForm.tsx:341), not visitor_events (0 such rows in 30d; 7d event types are page_view/scroll_depth/listing_view/cta_click 1/join_convert 4), so the scoreboard row needs the GA4 read; lib/data/loop/signals.ts has no seller/valuation signal today (grep empty) — that part of the claim holds. SEVERITY: keep major; the corrected claim carries the drip defect.",
    "corrected_claim": "In 30 days the production site produced 3 seller people (one real /sell submit on 2026-09-14 who booked an appointment, one internal alias from /lp/seller-home-value, one vendor pitch via the contact form) and 4 canonical-host valuation_requests (1 /sell + 3 internal /lp rows); 11 more are fleet tests from ryanrealty.vercel.app. All 25 'CMA ready' texts in 7 days are expired-listing-cron prospecting rows. The Seller Master sequence has 4 enrollments in 30 days, all site/manual seller people, and every one stopped at step 0 with 'unresolved merge tokens (%address%)' because the seller LP writes custom.sellerPropertyAddress while the merge reads customSellerPropertyAddress — no site seller has received the seller drip. 'Value my home' appears 6-18 times as a string per public page (one filled ask per Sell leaf by chrome rule, sticky ask after the target leaves the viewport), and the loop has no weekly seller-funnel number.",
    "corrected_severity": "major",
    "corrected_fix": "First fix the merge key so the seller drip sends: map custom.sellerPropertyAddress into renderCrmMerge's address resolution (or write customSellerPropertyAddress at the LP) and re-run the 4 stopped enrollments; add a unit test that the seller LP's stored key resolves %address%. Then put the seller funnel on the scoreboard: GA4 address_submit/qualify/submit for /sell and place pages (trackEvent -> gtag, not visitor_events) plus valuation_requests filtered to source_url on https://ryan-realty.com and non-internal emails. Treat ask copy/placement as a measured node only after those two land; the chrome already limits the filled ask to one per Sell leaf.",
    "evidence_checked": "reverify.mjs valuationRequests/sellerLane; reverify2.mjs valuationRequestsProd30d/sellerPeopleProd30d/leadOriginNotes; reverify3.mjs seq1Stopped/timelines 63580,63952; reverify4.mjs sellPage30d/ctaEvents30d/eventTypes7d; live_home/live__sell/live__cities_bend/live__communities_tetherow/live__contact.html grep -o -i 'Value my home' counts; lib/crm/enroll.ts:56-61; app/api/cron/crm-sequence-engine/route.ts:280-291; lib/crm/merge.ts:189-224,271-273; app/lp/seller-home-value/actions.ts:486; lib/crm/merge-context.ts grep (no property address); lib/tracking.ts:131; app/sell/_v3/SellValueForm.tsx:341; components/site/v3/V3Chrome.tsx:40-44; components/site/v3/V3StickyAsk.client.tsx:11; grep seller|valuation lib/data/loop/signals.ts (no hits)."
  },
  {
    "id": "FUNNEL-7",
    "refuted": false,
    "confidence": 0.85,
    "reasoning": "REPRODUCE: crm_tasks completed_at IS NULL = 1,007 (reader 1,006; total rows 1,010, i.e. only 3 tasks ever completed), overdue 1,005; byOrigin lp-form 508, fub-import 251, smart-followup 172, twilio 41, dual-write 23, sequence 10, app 2; byBroker matt 969, rebecca 15, null 23; oldest due 2025-10-27 fub-import 'Add buyer, seller, or buyer/seller or other conditional tag' (rebecca); 248 fub-import tasks due before the 2026-06-10 enrollment epoch. Digest texts: 2026-09-22 '300 tasks need attention (299 overdue, 1 due today)', prior days 298/288/281/271 — reproduces. TWO PARTS REFUTED: (1) '507 are the 5-minute lp-form call tasks / every site submit creates one' — decomposing the 508 open origin='lp-form' tasks by name: 349 'Expired listing. <addr>' from lib/expired-listing-processor.ts:329 and 32 'FSBO <addr>' from lib/fsbo-processor.ts:222 (persons expired-listing-cron 341 / fsbo-cron 30 — prospecting rows, not site submits), 52 'New listing-alert signup' from app/actions/search-alert-capture.ts:147/321/496, 47 'Reply back — they answered your email/sms' from lib/crm/on-reply.ts:132, 28 'Hot seller intent / high engagement lead' tasks; only 78 of 508 have a 5-minute due offset; 64 of 508 belong to the 88 site leads. The contact form creates no task at all (createNativeTask callers: search-alert-capture, seller-home-value, expired/FSBO processors, on-reply, cma-download, referral actions — not app/contact/actions.ts nor lib/canonical-lead-tagger.ts), so bot contact-form submits do not add tasks; 'lp-form' is just the origin label createNativeTask stamps (ensureNativeLead.ts:508). (2) 'The digest reports 300, the table shows 1,006' is by documented design, not a filter bug: app/api/cron/crm-task-reminders/route.ts:60-73 reads open tasks with person_id not null and due_at between taskQueueBounds.staleFloor and endOfToday, where STALE_OVERDUE_DAYS = 31 (lib/data/crm/getTaskQueue.ts:64,128,149-151 'drop import cruft older than the stale floor'), and the route comment says the floor 'matches getTaskQueue / the dashboard'; my digest-shape count today is 308 vs 300 texted, and 699 open tasks sit below the floor, hidden from both the digest and the dashboard overdue view. SEVERITY: the backlog is real and nobody works tasks (3 completed ever), so major stands, but the cause is the prospecting processors and the never-purged import, not bots.",
    "corrected_claim": "1,007 open CRM tasks, 1,005 overdue, only 3 ever completed; 969 sit on Matt. Composition: 508 origin 'lp-form' = 349 expired-listing + 32 FSBO prospecting call tasks (persons from the crons), 52 listing-alert signup tasks, 47 reply-back tasks, 28 hot-lead tasks; 251 fub-import tasks from 2025 (248 pre-epoch); 172 smart-followup. Contact-form submits create no task, so the bot flood is not the driver. The daily digest's 300 is the documented 31-day stale floor shared with the dashboard (taskQueueBounds), which hides 699 older open tasks from both surfaces rather than under-reporting by mistake.",
    "corrected_severity": "major",
    "corrected_fix": "Archive (complete with a system note) the 699 open tasks older than the 31-day floor — the 251 fub-import rows and the stale expired/FSBO call tasks — so the table matches what the digest and dashboard already show; auto-complete alerts-sheet and on-reply tasks when the response clock records a human touch or the enrollment is suppressed/stopped; ask Matt whether expired-listing/FSBO detection should keep creating a call task per row (349+32 open, none worked) or only for rows a broker approves; skip createNativeTask for quality:suspect leads once FUNNEL-1 lands. Do not 'reconcile the digest filter' — it already matches the dashboard by design.",
    "evidence_checked": "reverify.mjs openTasks/taskDigestAlerts; reverify2.mjs tasksDigestReconcile (digestShapeCount 308, olderThan31dOverdue 699, lpForm fiveMinuteDue 78, onSiteLeads30d 64); reverify3.mjs lpFormTaskComposition (byWriter, byPersonSource); app/api/cron/crm-task-reminders/route.ts:1-140; lib/data/crm/getTaskQueue.ts:64,112-155; lib/data/crm/ensureNativeLead.ts:462-513; grep createNativeTask callers (lib/expired-listing-processor.ts:329, lib/fsbo-processor.ts:222, lib/crm/on-reply.ts:132, app/actions/search-alert-capture.ts:147/321/496, app/lp/seller-home-value/actions.ts:561, app/actions/cma-download.ts, referral actions); app/contact/actions.ts (no task write)."
  },
  {
    "id": "FUNNEL-8",
    "refuted": false,
    "confidence": 0.9,
    "reasoning": "REPRODUCE: crm_sequences id 2 'Buyer Lead — Master Workflow' (active) step 0 = email, subject 'Your Bend home search is set, %first%', body 'Hi %first%, thanks for reaching out to Ryan Realty. Your listing alerts are live and the first matches land within the hour. Tell me what matters most...'; step 1 SMS body also says 'Your Bend listing alerts are live'. crm_automation_rules row 4: tag_added audience:buyer -> enroll_sequence 2, no door distinction; app/contact/actions.ts:26-30 inferAudience defaults every non-seller, non-join inquiry to buyer and :171-186 applies it. Person 64105 (tags source:contact-form, listing_alerts rows for crm_person_id = 0) has timeline email_out source sequence title 'Your Bend home search is set, bJSKIwsurKTralgVeDiGblO' at 17:28:38; person 63940 (human contact-form 'General Inquiry', an Instagram-services pitch, no alert) got 'Your Bend home search is set, Tengiz' and replied. lib/crm/merge.ts:209 `first = val(person.first_name) ?? nameSplit.first ?? 'there'` and :285 '%first%': first — the only fallback is a null name, so any captured string merges verbatim. 47 contact-form arrivals in 30d were routed to this step. SEVERITY: reproduced exactly; the email asserts a service that does not exist for that door, from the brokerage's mailbox, to every contact-form buyer inquiry — an honesty defect regardless of how many recipients were bots; keep major. FIX CHECK: crm_automation_rules is tag_added -> enroll_sequence and source:contact-form is a tag canonicallyTagLead applies, so a per-door rule works, BUT 'one master sequence per person' is enforced only over MASTER_PLAN_IDS = fub_legacy_plan_id 69-72 (enroll.ts:27-34,144-156); a new contact-form sequence outside that set would let the 15-min sweep also enroll the same person into Buyer Master (audience:buyer) — the new sequence needs a master plan id or the check needs widening. The cheaper fix is to make step 0 true for both doors (or add a condition step) and to guard %first% with a name-shape check that falls back to no salutation.",
    "corrected_claim": "Buyer Master step 0 (and the step-1 SMS) is one copy for every audience:buyer door; a contact-form 'Buying'/'General' inquiry with no listing_alerts row receives 'Your Bend home search is set … Your listing alerts are live and the first matches land within the hour', and %first% merges whatever the form captured (fallback only when the name is null), so subjects like 'Your Bend home search is set, bJSKIwsurKTralgVeDiGblO' go out.",
    "corrected_severity": "major",
    "corrected_fix": "Either rewrite step 0/1 so the statement is true for both doors ('we got your note; if you asked for listing alerts they are on') or key a per-door first touch: a crm_automation_rules row on source:contact-form at a lower position than the audience:buyer rule, with the new sequence given a fub_legacy_plan_id in MASTER_PLAN_IDS (or widen the one-master check) so the 15-minute sweep cannot double-enroll into Buyer Master. In lib/crm/merge.ts fall back to no salutation when the first name fails a simple shape test (no vowel, ≥12 mixed-case letters, contains digits) and cover it with a unit test.",
    "evidence_checked": "reverify.mjs sequences (id 2 steps), automationRules (row 4), timeline64105 (listingAlertsRows 0; email_out sequence title); reverify3.mjs timelines 63940; app/contact/actions.ts:26-30,146-193; lib/crm/merge.ts:189-215,271-290; lib/crm/enroll.ts:27-34,93-156; lib/canonical-lead-tagger.ts:240-263."
  }
]
