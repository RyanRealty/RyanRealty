# p06-crm-intake

WIP branch: `claude/admiring-feynman-7lgwc3--wip-p06-crm-intake` (snapshot of the agent worktree). Original worktree: `.claude/worktrees/agent-addb9d80b7103d485`.

## Package spec (as given to the fix agent)

Read docs/plans/VISIBILITY_2026-09-22/FIX_BRIEF.md first and follow it exactly.

PACKAGE P6: lead intake quality and follow-up correctness. Verdicts: docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/funnel.json (FUNNEL-2..8) plus FUNNEL-1 in docs/plans/VISIBILITY_2026-09-22/evidence/findings/funnel.json and the FUNNEL-1 reproduction at scratchpad/verify/funnel/FUNNEL-1.reproduce.json; docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/tracking.json (TRACK-8).

1. FUNNEL-1: /contact has no honeypot; bots (random-letter single-token names, firstname+surname+letters+2digits@gmail patterns) make about 74% of site arrivals, get the confirmation, the drip, broker texts and tasks. Add a honeypot to the contact form (the alerts sheet already has `company`), a server-side quality classifier (random-token name shape, pattern emails, honeypot hit) that marks a person quality:suspect, and make enroll, broker alerts, task creation, the response clock and hot-lead escalation skip suspects. Do not delete or hide rows; tag them. Do not rely on the per-IP limiter (bots arrive ~22+ min apart). Captcha (Turnstile) is Matt's decision: do not add it.
2. FUNNEL-6: the Seller Master sequence stopped at step 0 for every site seller because the seller LP writes custom.sellerPropertyAddress and the merge resolves %address% from customSellerPropertyAddress / customPropertyAddress / addresses (lib/crm/merge.ts, lib/crm/merge-context.ts). Make the merge read the key the LP writes, with a unit test. Do NOT re-run the 4 stopped enrollments (that would send email to real people now); list them in your report for Matt.
3. FUNNEL-8: %first% merges junk strings. In lib/crm/merge.ts fall back to no salutation when the first name fails a shape test; unit test. Do not change sequence copy stored in the database; list the false 'your listing alerts are live' statement for contact-form buyers as a question for Matt with a proposed rewrite.
4. FUNNEL-5: lib/data/crm/backfillFirstBrokerAction.ts stamps the first outbound row of any kind; select only rows passing isHumanTouch (lib/crm/response-clock.ts), with a test. Do not rewrite existing stamps in the DB; report how many are wrong.
5. FUNNEL-4: write the door label as source ('contact-form', 'idx-registration', 'seller-lp', 'place-page', 'join') instead of the host, and stop ensureNativeLead's reuse path from overwriting source (keep the first, tag later doors). Extend ci:crm-lead-integrity if it pins source.
6. FUNNEL-3 (engine halts the whole buyer enrollment when SMS is suppressed): implement skip-and-advance for a suppressed channel step behind the quality gate from item 1 so suspects never advance, and keep enroll.ts's fail-closed consent suppression untouched (ci:sms-consent). Do not reorder steps. If the only way forward sends the step's fallback email, keep that code path but leave it disabled behind a clearly named flag defaulting OFF, and put the switch to Matt, because the buyer copy question in item 3 is open.
Files: app/contact/**, app/actions/search-alert-capture.ts, lib/crm/{enroll,merge,merge-context,broker-alerts,response-clock,response-clock-run,send-event}.ts, lib/data/crm/{ensureNativeLead,backfillFirstBrokerAction}.ts, app/api/cron/crm-sequence-engine/route.ts, app/api/cron/visitor-hot-lead-escalation/route.ts, tests, gates. Another agent (P7) is editing the identity/tracking side (VisitTracker, track route, link decoration, admin activity view); avoid those files.
