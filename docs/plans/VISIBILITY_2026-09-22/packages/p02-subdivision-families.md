# p02-subdivision-families

WIP branch: `claude/admiring-feynman-7lgwc3--wip-p02-subdivision-families` (snapshot of the agent worktree). Original worktree: `.claude/worktrees/agent-a1ea2f0cb8063cc0f`.

## Package spec (as given to the fix agent)

Read docs/plans/VISIBILITY_2026-09-22/FIX_BRIEF.md first and follow it exactly.

PACKAGE P2: subdivision families. Matt's directive (2026-09-23, verbatim): "We also need to be really smart about subdivisions. When there are multiple phases in a subdivision, we want all those to go into the same main neighborhood page, and then they can jump into the different phases after that. We do that for Tetherow and Broken Top, but sometimes it's not as crystal clear, so we just want to make sure that that grouping is always happening."

Verdicts: docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/seo-technical.json (SEO-7, SEO-4), docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/exposure.json (EXP-3, EXP-7), docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/voice.json (VOICE-8).

1. Learn how the Tetherow and Broken Top grouping works today (start with data/resort-communities.json, lib/market/alias-plat-graph.ts, app/communities/[slug], app/subdivisions/[slug], lib/data/subdivisions/*, boundaries rows with geo_type subdivision). Then make grouping automatic for every multi-phase family: derive the family from recorded plat names (strip Phase/Unit/No./Addition/Replat/Tract/Lot/Block tokens, roman and spelled numbers, county document numbers) and from polygon adjacency/containment where names alone are ambiguous. Print the families you derive over the full indexable plat set with counts and review a sample by hand (e.g. Ridge at Eagle Crest with 56 phase plats, Awbrey Butte Homesites, Broken Top, Tetherow, Northwest Crossing, Pronghorn, Caldera Springs). Do not invent names: the family name must be a name the county or MLS records.
2. The family's main page owns the head term: indexable, self-canonical, lists every phase with a link, and carries the family's combined inventory and sold history. Each phase page links up to the family page (breadcrumb + a visible link) and stays reachable. Where a family already has a registry community page (/communities/{slug}) that page is the main page. Today /subdivisions/ridge-at-eagle-crest (the richest page) is noindex while its phases are submitted: fix that class. Keep distinct phase pages indexable only where they have their own polygon and sold history (SITE-24 rule); exclude plat slugs equal to a city, community or neighborhood slug (bend, sisters, la-pine) from the indexable set and sitemap; strip county document tokens (pz-NN-NNNN, plldNNNN etc.) from plat <title>s while keeping the legal name in body copy.
3. Make /subdivisions the real A to Z directory: server-render an anchor for every indexable family and plat, grouped by city with phases nested under their family (V3PlaceIndex or a new v3 barrel primitive), so no sitemapped plat is an orphan. Turn the city page's existing name-only child-places rail into links where canon allows (check SITE-128 in app/cities/[slug]/page.tsx comments: no plat dump above the neighborhood bars on /cities/bend).
4. VOICE-8: on plat pages, the words 'homes for sale in {plat}' must mean one counted set; relabel the all-types boundary count in the reader's words.
5. Lock it: a gate (scripts/check-*.mjs wired into scripts/ci-lanes.json and package.json ci:gates:chain the way the other gates are, so ci:gates-wired passes) or a unit test over the family-derivation function that fails when a multi-phase family has no main page or a phase page does not link to it.
Files: app/subdivisions/**, lib/data/subdivisions/**, lib/market/alias-plat-graph.ts, lib/market/publish-plat-display-name.ts (casing only), app/cities/[slug] child rail component, app/sitemap.ts plat leg only (another agent edits the browse and types legs), design_system parity.json for any page you edit, tests, gates. Before editing an app/**/page.tsx read design_system/public/TASTE.md and PUBLIC_UI.md and keep the route's parity.json requiredComponents satisfied. If a phase-canonicalization decision would cut indexed URLs, do the linking/hierarchy part and list the cut as a question with the numbers.
