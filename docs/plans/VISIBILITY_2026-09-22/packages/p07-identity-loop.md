# p07-identity-loop

WIP branch: `claude/admiring-feynman-7lgwc3--wip-p07-identity-loop` (snapshot of the agent worktree). Original worktree: `.claude/worktrees/agent-a3c9f7704efc6eb50`.

## Package spec (as given to the fix agent)

Read docs/plans/VISIBILITY_2026-09-22/FIX_BRIEF.md first and follow it exactly.

PACKAGE P7: the known-contact identity loop. Matt's directive (2026-09-23, verbatim): "I want to ensure we're doing everything possible to track the people that are in my database, even if they haven't visited my site before. We have them in the database. If they come in through Facebook, Gmail, or Google, or whatever, and they visit my site and we can somehow see that they continue with one of those, and we see them already, we really want to be able to track that. That's super important to have that loop. When I go and see who's been active, I can see, 'Okay, this person's been active.' We try to identify that session at all times so we can always really see what that person is doing. When I go and look at them, I can see exactly what they're looking at and try and figure out how to get them to be a lead. This is all stuff that we've talked about a thousand times. We need to make sure that it's just really always encoded in our practices... I don't want to have to reinvent this every time. We just want to make sure that it's always working. Because right now, when I go into analytics, I can't see... I see a lot of unidentified things... not seeing... 'Oh, these people were here, and these were the pages they were on.'"

Context already verified (docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/tracking.json TRACK-1, TRACK-4, TRACK-8; docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/funnel.json FUNNEL-4): first-party capture is components/VisitTracker.tsx -> app/api/visitors/track/route.ts -> visitor_sessions / visitor_events with an rr_vid identity graph; identified_via today is form_submit, rr_vid_carryover, email_click_pid, google; 90 of 8,745 sessions in 7 days were identified; referrer is null on about 96% of sessions (unexplained); essential-tier consent strips user_agent; bot traffic is large and unflagged (1,154 no-referrer /contact landings in 7 days at a flat rate); lib/data/crm/getFirstTouchAttribution.ts reads a person's sessions.

Do all of this:
1. Map every channel by which a known contact reaches the site and whether the visit is identified today: CRM emails (sequence, broker Gmail sends, newsletter, alerts), SMS links, listing alert emails, CMA/report links, social DMs/links (Facebook, Instagram), Google Business Profile, paid ads (fbclid/gclid), a returning device with rr_vid, a sign-in, a form fill. For each, state the identifier carried (pid/token/hashed email/rr_vid/cookie) and trace the code path to visitor_sessions.crm_person_id. Print real counts from the DB for the last 30 days per identified_via.
2. Close every gap you can in code: every outbound CRM/broker/alert/newsletter/SMS link to our domain carries a signed person token that the track route resolves to crm_person_id (reuse the existing email_click_pid mechanism; make it one shared link-decoration helper used by every sender, and never put raw email in a URL); identify the whole visit and back-stitch earlier anonymous sessions on the same rr_vid once a person is known; carry identity across devices when a token arrives; a hashed-email match path for sign-in and Google one-tap if present. Respect the consent model in docs/TRACKING_POLICY.md: first-party identification of a person who clicked our own tracked link is allowed at essential tier if the policy says so; if the policy blocks something, do the allowed part and list the conflict.
3. Flag automation: add a server-side bot/automation classification on visitor_sessions (and exclude it from the activity view), using signals available at essential tier.
4. The view Matt asked for: an admin surface (app/admin/(protected)/..., admin register = @/components/ui, CLAUDE.md §3) listing identified people active in the last 1/7/30 days, most recent first, each with the pages they viewed (titles, listings with address and price, time on page), source of the visit, and a link to the person's CRM record; and the same per-person activity on the CRM person page. Reuse existing admin pages if one already does part of this; do not build a second one.
5. Encode it so it never has to be reinvented: a canon doc section (docs/TRACKING_POLICY.md or a new docs/IDENTITY_LOOP.md registered in docs/DEVELOPMENT_PROCESS.md's plan table if the process-canon gate requires it) stating the contract, and a mechanical gate (scripts/check-identity-loop.mjs wired into scripts/ci-lanes.json + package.json ci:gates:chain like the other gates so ci:gates-wired passes) that fails when any sender of a link to our domain bypasses the decoration helper, when the track route stops resolving the token, or when the activity view loses its query. Add unit tests for the token round-trip and the back-stitch.
6. Also fix TRACK-1's session id: the GA4 Measurement Protocol mirror in the track route should send a per-visit session id and the campaign/source/medium/referrer it already has (do NOT remove the mirror; that is Matt's decision).
Schema changes: write a migration under supabase/migrations/ if needed, and say in your report exactly what must be applied; do not apply it. Files: components/VisitTracker.tsx, app/api/visitors/track/route.ts, lib/crm/**, lib/data/crm/** (identity parts), lib/email or link helpers, app/admin/(protected)/**, docs/TRACKING_POLICY.md, new gate + tests. Another agent (P6) is editing app/contact/**, app/actions/search-alert-capture.ts and lib/crm/{enroll,merge,broker-alerts,response-clock*}.ts for intake quality; avoid those files or keep edits minimal and additive.
