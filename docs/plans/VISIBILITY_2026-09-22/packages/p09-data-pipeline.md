# p09-data-pipeline

WIP branch: `claude/admiring-feynman-7lgwc3--wip-p09-data-pipeline` (snapshot of the agent worktree). Original worktree: `.claude/worktrees/agent-a4b661087e80aadbc`.

## Package spec (as given to the fix agent)

Read docs/plans/VISIBILITY_2026-09-22/FIX_BRIEF.md first and follow it exactly.

PACKAGE P9: data pipeline. Verdicts: docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/data-pipeline.json (DATA-2, DATA-3, DATA-7, DATA-8) and scratchpad/verify/competitors/COMP-3.reproduce.json + COMP-3.severity.json (place_membership freshness).
1. place_membership: the orchestrator's reproduction found it was last refreshed 2026-08-23, public.refresh_place_membership (or the equivalent) has no caller, and about 19% of Bend actives are unassigned, which makes every neighborhood metric stale. Re-verify with the sb.mjs helper (print max(updated_at)-style evidence using a row read ordered desc, not an aggregate over a stat table), find the refresh function in supabase/migrations, and schedule it (pg_cron in a migration, after the listing refreshes it depends on) or call it from an existing sync cron. Report the numbers before and after if you can run the refresh.
2. DATA-2: the hourly Vercel cron /api/cron/refresh-mvs duplicates pg_cron. One migration: add refresh_neighborhood_year_pricing_mv to the pg_cron 15-minute DAL job after refresh_listing_search_mv, and pg_try_advisory_lock skip-on-contention in refresh_listing_boundary_xref_mv and refresh_neighborhood_year_pricing_mv. Then remove the vercel.json entry and the route, and fix docs that describe it (DATABASE_FOR_AI_AGENTS.md, snapshot-schema.mjs, SITE_SPEC.md, compare-homes.md). IMPORTANT ORDER: removing the Vercel cron before the migration is applied would stop the neighborhood pricing refresh. Find how this repo applies migrations to production (look for a scripts/ helper, a DB URL env var such as SUPABASE_DB_URL / DATABASE_URL / POSTGRES_URL in .env.local, the supabase CLI config, or docs). If you can apply migrations with a non-MCP path that the repo already uses, apply both migrations, verify with a row read of mv_refresh_state / cron.job via the helper or the same path, and only then remove the cron. If you cannot apply, commit the migrations, keep the Vercel cron, and say exactly what must be applied.
3. DATA-7: refresh_community_market_pulse writes neighborhood months_of_supply from mismatched populations (pronghorn 66.0, century-west 50.0). In a migration set months_of_supply, absorption_rate_pct and the alias-attributed sold columns to NULL for the neighborhood grain; add a minimum-close guard at both writers; route lib/data/studio/subjects.ts through publishMonthsOfSupply. Keep check-market-formula green (do not edit classify.ts).
4. DATA-3/DATA-8 (if time allows): replace the six local medianListPrice() copies in the amenity DALs with one shared median helper behind a min-n, reading on-market tiles from listing_tile_mv instead of raw listings.
After any DDL: `npm run ci:data-access -- --refresh` to regenerate docs/DATABASE_SCHEMA_SNAPSHOT.md and docs/DAL_INDEX.md (G16), and commit them. Files: supabase/migrations/**, vercel.json (refresh-mvs entry only), app/api/cron/refresh-mvs/**, docs listed above, lib/data/studio/subjects.ts, lib/data amenity DALs, scripts/cron-registered-baseline.json if it lists the route.
