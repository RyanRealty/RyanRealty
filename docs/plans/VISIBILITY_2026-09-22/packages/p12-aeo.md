# p12-aeo

WIP branch: `claude/admiring-feynman-7lgwc3--wip-p12-aeo` (snapshot of the agent worktree). Original worktree: `.claude/worktrees/agent-a7677df1fc67a035d`.

## Package spec (as given to the fix agent)

Read docs/plans/VISIBILITY_2026-09-22/FIX_BRIEF.md first and follow it exactly, including the 'MATT 2026-09-23' bullet (no past rule is permanent; change what blocks being seen, with evidence).

PACKAGE P12: answer-engine (AEO) and entity visibility. Verdicts: docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/aeo.json (AEO-1..9), docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/voice.json (VOICE-5), docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/competitors.json (COMP-8), docs/plans/VISIBILITY_2026-09-22/evidence/findings/gsc-trend.json (gsc-trend-11).
1. AEO-1 (p0, §0): on /cities/bend/<neighborhood>, the Neighborhood.additionalProperty and Dataset.variableMeasured JSON-LD publish 'Median List Price' and 'Active Listings' from the market-truth overlay while the FAQPage and the visible page publish the boundary-inventory figures under the same labels (Awbrey Butte 1,350,000/45 vs $1,312,500/54; River West 1,099,000 vs $1,569,000). Feed the JSON-LD from the same read the visible page and FAQ use (app/cities/[slug]/[neighborhoodSlug]/page.tsx ~486-498, 540-612, 801-827), or drop those variables from the Dataset. Add a unit/contract test that Dataset and FAQ values for the same label come from one read. Check the city, community, plat and housing-market templates for the same class and fix any you find.
2. AEO-5 / VOICE-5: rewrite the pipeline-provenance sentences in FAQ answers (lib/site/place-faq-extras.ts ~152-215, the activeCountNotes string in the neighborhood page ~568) so each answer is the fact in plain words (VOICE.md); provenance stays in the source line. Answer 'Who are the brokers?' on /about with the three brokers' names and roles from public.brokers (read with the sb.mjs helper), and put each broker's first bio sentence on the /team index card. Those were locked on 2026-09-14 (ABOUT_LOCK_QUOTES, about-faces tests); per Matt 2026-09-23, change the lock and its tests with the reason (answer engines need the names on the page).
3. AEO-6 / COMP-8: add Zillow, Yelp and Realtor.com profile URLs to the Organization sameAs (lib/brand/contact.ts BRAND.social or wherever JsonLd reads it) after verifying each URL resolves (curl, Chrome UA; the verifier found zillow.com/profile/Ryan%20Realty%20Bend and yelp.com/biz/ryan-realty-bend); add alternateName where the brand is written other ways; give broker RealEstateAgent nodes sameAs where a verified profile exists, and give the /team/[slug] page node the same @id as the founder/employee node.
4. gsc-trend-11: the homepage ranks ~p45 for 'ryan realty'. Put the brand in the homepage <title> alongside the market (keep the H1 contract in scripts/check-seo-shell.mjs, or change the gate if it forbids the brand in the title, citing this).
5. AEO-7: reconcile design_system/ryan-realty/SKILL.md, MANIFEST.md and README.md with lib/brand/contact.ts: the brand line is 541.703.3095, never 541.213.6706 (a private forward target).
6. AEO-2 / AEO-4: remove or repoint the /homes-for-sale/bend/northwest-crossing entries in lib/seo/ai-query-map.json, dedupe the /sell, /refer-a-client and closing-costs pillars, make scripts/check-ai-query-battery.mjs fail when a pillar is a key in data/legacy-redirects.json, add /cities/<city>/types/<type> pages with one-line descriptions to app/llms.txt/route.ts, and move the 2,642 plat lines to a linked secondary file (e.g. /llms-subdivisions.txt) so the main file leads with what matters. Look at why the Guides section dropped from 14 to 2 entries between hourly builds and fix it.
7. AEO-9: add a monthly citation battery (a cron route, CRON_SECRET-guarded, registered in vercel.json) that runs the target query set through the repo's Grok search surface (lib/grok, per CLAUDE.md §4; never call api.x.ai elsewhere) and records per-query whether ryan-realty.com is cited and which competitor domains are, into site_signal under a distinct source; surface the latest result in lib/data/loop/signals.ts with an additive block. Do not run it against production more than once to test.
Files: app/cities/[slug]/[neighborhoodSlug]/page.tsx (JSON-LD and FAQ builders only), components/JsonLd.tsx, lib/site/*faq*.ts, lib/brand/contact.ts, app/about/_v3/about-constants.ts and its tests, app/team/** (index card), app/page.tsx metadata (title only), lib/seo/ai-query-map.json, scripts/check-ai-query-battery.mjs, app/llms.txt/route.ts (+ new secondary route), design_system docs, a new cron route + lib module, vercel.json (one entry), lib/data/loop/signals.ts (additive block). Other agents own app/search/**, app/subdivisions/**, the atlas, chrome/nav and the visitor tracker.
