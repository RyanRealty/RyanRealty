# p10-loop-canon

WIP branch: `claude/admiring-feynman-7lgwc3--wip-p10-loop-canon` (snapshot of the agent worktree). Original worktree: `.claude/worktrees/agent-a67cf9bc1e01bf8c3`.

## Package spec (as given to the fix agent)

Read docs/plans/VISIBILITY_2026-09-22/FIX_BRIEF.md first and follow it exactly.

PACKAGE P10: make 'run the loop' boot on one small, true set of instructions, for Claude, Cursor and Grok alike, so no session loses or regresses another's work. Verdicts: docs/plans/VISIBILITY_2026-09-22/evidence/verdicts/process.json (PROCESS-3, 5, 6, 7, 8, 9) and docs/plans/VISIBILITY_2026-09-22/evidence/findings/process.json. Design: scratchpad/orchestrator/loop-redesign-draft.md.

Matt's words that set the objective (2026-09-22): "Our number one thing is that we need our website to be seen... both the AI models that people are searching for stuff through and through search engines... to be seen and then, from there, convert... as organically as possible." And: "We've got sessions going on in Grok and Claude... as you run out of credits in Claude, we pick up with another coding model... always writing in place... everyone's on the same page, we don't lose stuff, and we don't regress." And (2026-09-23): the known-contact identity loop and the subdivision phase grouping must be "always encoded in our practices" so they are never reinvented.

1. Write docs/RUN_LOOP.md (<=150 lines): the objective (visibility, then conversion, measured weekly by GSC clicks/position per page class plus AI referrals, gated by answered human leads; taste median, content floor, tap targets and §0 are floors that may not regress, not the score), the boot command, how to claim, the one land path, the accept test for a public page node (visibility, information, voice, floors, measurement: see the draft), the stop rule, and the standing contracts (identity loop, phase grouping, §0). Point CLAUDE.md, AGENTS.md, docs/DEVELOPMENT_PROCESS.md, .claude/skills/site-queue/SKILL.md, .cursor/skills/site-queue/SKILL.md, .cursor/rules/run-loop.mdc, docs/GROK_BOT_BRAIN.md and scripts/site-queue-routine-prompt.md at it WITHOUT restating caps, rise floor, claim path, cadence or land path in each. Register the doc wherever ci:process-canon requires. Do not change the taste rubric or taste-rule-freeze.json.
2. Fix every contradicted or dead line PROCESS-6 lists: CLAUDE.md §0.5 references (DEVELOPMENT_PROCESS.md and .husky/commit-msg), one-session-per-loop vs six workers, cap 3 vs 6 and rise floor 6 vs 3 (the code is truth: lib/data/loop/work-node.ts MAX_SITE_WORKERS, taste-rule-freeze.json riseFloor), hand claim and 'hourly' in AGENTS.md, FUB references, EXPERIENCE_SYSTEM.md in .claude/skills/growth-loop/SKILL.md, the ui_kits mockup preflight row and the CLAUDE.md sentence claiming ui_kits were deleted while 11 index.html files exist (make the sentence true to disk; do not delete the files).
3. PROCESS-7: record Matt's 2026-09-20 em-dash lock in marketing_brain_skills/brand-voice/VOICE.md and CLAUDE.md §2 so the voice doc matches the gate. Leave the other voice gates in place and list them as a question for Matt.
4. PROCESS-8: CROSS_AGENT_HANDOFF.md holds 52 stacked '# Current' blocks against a replace-one rule, and loop-brief prints 18 lines of the stack, hiding six of Matt's ten 2026-09-22 directives. Move every block but the newest into docs/archive/ (append, never delete content), leave ONE '# Current' block that carries every still-open Matt directive from the moved blocks (read them all; list each with its node or 'no node'), and add a gate (wired into ci-lanes.json + package.json ci:gates:chain so ci:gates-wired passes) that fails on more than one '# Current'. Make loop-brief print the whole Current block (touch only the handoff block of scripts/loop-brief.ts; other agents edit other blocks).
5. PROCESS-9: add path-scoped `npm run test:unit` to scripts/push-with-gates.sh so the push wrapper matches DEVELOPMENT_PROCESS.md.
6. PROCESS-5: replace scripts/site-queue-routine-prompt.md with a short prompt that points at RUN_LOOP.md and the skill.
7. CLAUDE.md is gated by scripts/check-claude-canon.mjs (no dead paths, no regrowth): keep it the same length or shorter; run that gate plus ci:process-canon and ci:loop-skills-canon and make them pass. Keep CLAUDE.md's rule that a Matt directive creating a permanent rule is written into canon, and add one line each for the identity-loop and phase-grouping contracts pointing to their owning docs (docs/TRACKING_POLICY.md or docs/IDENTITY_LOOP.md, and docs/plans/PUBLIC_PRODUCT/PLACE_PAGES.md) — other agents write those contracts; you only add the pointers, and if the target doc does not exist at your base, point at the section heading 'Identity loop' in docs/TRACKING_POLICY.md and 'Subdivision families' in PLACE_PAGES.md and add those two short sections yourself.
Files: docs/**, CLAUDE.md, AGENTS.md, .claude/skills/**, .cursor/**, .husky/commit-msg (comment only), marketing_brain_skills/brand-voice/VOICE.md, scripts/site-queue-routine-prompt.md, scripts/push-with-gates.sh, scripts/loop-brief.ts (handoff block only), new gate.
