# p14-listing-urls

WIP branch: `claude/admiring-feynman-7lgwc3--wip-p14-listing-urls` (snapshot of the agent worktree). Original worktree: `.claude/worktrees/agent-a509c8d9ec009ce91`.

## Package spec (as given to the fix agent)

Read docs/plans/VISIBILITY_2026-09-22/FIX_BRIEF.md first and follow it exactly, including the 'MATT 2026-09-23' bullet (no past rule is permanent; change what blocks the goal, with evidence).

PACKAGE P14: listing URL churn. Evidence: docs/plans/VISIBILITY_2026-09-22/evidence/findings/gsc-trend.json gsc-trend-6 and notes.md, probe16/probe20 outputs in scratchpad/gsc-trend/. Facts: listing detail is the largest click class (365 of 508 top-25 clicks in 4 weeks). The listing canonical embeds city/neighborhood/subdivision (lib/slug.ts listingDetailPath ~L180, listingCanonicalHref ~L329, lib/data/listings/getListingCanonicalPathFields.ts), so when geo classification or the URL builder changes, the canonical moves. In 08-23..09-19, 1,234 of 7,329 listing keys (16.8%, 11,694 of 31,942 detail impressions) were seen under more than one URL (June: 6.6%); 690 had an /outside-boundaries/ variant. Old variants return 200 with a rel=canonical instead of a 301 (e.g. /homes-for-sale/outside-boundaries/brasada-ranch/brasada-ranch-220220863 -> 200, canonical /homes-for-sale/powell-butte/brasada-ranch/...; /homes-for-sale/bend/1522-locksley-220226356 (1,657 impressions) -> 200, canonical /homes-for-sale/bend/mountain-view/providence/1522-locksley-220226356). Two variants timed out at 40 s cold.

Do this:
1. Re-verify two or three variants live and read the listing routes (app/listing/[listingKey]/page.tsx, app/listing/by-address/[...slug]/page.tsx, the rewrites in next.config.ts, middleware.ts) to see how a /homes-for-sale/.../{slug}-{key} URL is resolved.
2. 308 every listing path whose trailing key resolves to a listing but whose path is not the current canonical path, to the canonical, before any page render (middleware or a cheap route-level redirect that reads only the canonical path fields by key). Never 404 a real listing; unknown keys keep today's behavior.
3. Stop the canonical from moving: make a listing's canonical path stable once published. Evaluate two designs and ship the better: (a) canonical from fields that never change for a listing (MLS City + address slug + key, no polygon-derived neighborhood/subdivision segments), which re-indexes the class once; or (b) keep today's rich path but pin each listing's first-published canonical in a table (migration; apply it only through a non-MCP path the repo already uses, else write it and keep (a)-style fallback) so reclassification never moves it. Choose with evidence (how many of today's indexed listing URLs each design changes; print it) and say why. Matt asked for 'whatever it takes' to be seen; a one-time re-index that ends churn is acceptable if (b) is not workable.
4. Add a gate or unit test: for a fixed fixture set of listings, the canonical path must not change unless an explicit migration list names it; and every non-canonical variant must 308.
Files: lib/slug.ts, lib/data/listings/getListingCanonicalPathFields.ts, middleware.ts (listing redirect only), app/listing/**, next.config.ts rewrites if needed, sitemap listings leg in app/sitemap.ts (only if the canonical form changes), tests, gate. Other agents edit app/search/[...slug]/* (browse/place metadata) and other parts of app/sitemap.ts and middleware.ts (/_next/image handling); keep your hunks separate.
