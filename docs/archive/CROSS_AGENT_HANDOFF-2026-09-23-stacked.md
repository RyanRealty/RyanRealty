# Stacked Current blocks, archived 2026-09-23 (verbatim)

Seven `# Current` blocks landed on `main` on 2026-09-22/23 before `ci:handoff-current` (package p10) reached it. They were consolidated into one Current block in the visibility-pass merge (PR #352); every still-open item is carried there. Kept here verbatim for a named SHA.

# Current — 2026-09-23 (cloud grinder round 2: every registry community's area twin 301s home)

Surface: Claude cloud routine `cloud-grinder-2026-09-23-08`, `main` checkout, one lane (SITE-183 + SITE-182) cherry-picked onto main as `8b890f2` and pushed with `npm run push`. No PR. `deploy:verify` cannot run here (no `VERCEL_TOKEN`); production served the round 12 minutes after the push (10:53Z): every registry area twin 301s to its community page (Pronghorn → `/communities/juniper-preserve`), listing URLs under the twins still render, the community pages no longer link their twin, and the plat pages carry the parent door and breadcrumb (the door text renders as `Broken Top<!-- --> homes for sale`, React's adjacent-expression comment, so grep for it that way).

- **SITE-183 Broken Top + SITE-182 Tetherow (GSC "{community} homes for sale" → /communities/<slug>).** Broken Top family 414 impr: winner 48%, three plat pages 14% each, /central-oregon/golf 11%. Tetherow family 541 impr: winner 39%, the old blog slug 32% (already 308s; GSC lag), golf-homes plat 16%. Live, `/homes-for-sale/bend/broken-top` and the area twin of 13 more registry communities answered 200 with the community page's exact title/h1 — Tetherow, Sunriver, BBR and CRR had been four hand entries of one rule. `scripts/lib/registry-area-twins.mjs` derives the 301 for every registry community (26 twins / 19 communities, 20 new), `build-legacy-redirects.mjs` applies it after OVERRIDES; `lib/place-links.ts` gives a registry community a city search as its browse door plus `communityNewestListingsHref` (`…/<slug>/new-listings-30`, three segments, noindex) for the alerts strip; `community-figures.ts` drops the closing "Search {community} homes" row when it duplicates the city door; `/subdivisions/[slug]` reads its parent community from `community_subdivisions` membership (`lib/data/geo/getPlatParentCommunity.ts`), carries one opening-caption door with the parent's inventory phrase, and its breadcrumb/JSON-LD/"Other plats" follow the parent; `app/sitemap.ts` omits area URLs the legacy map 301s. Both nodes blocked to 2026-10-21.
- **SITE-181 + SITE-188 Brasada Ranch** closed on the same rule with a window: the exact query already lands 88% (28d) / 80% (90d) on the community page; the node's 3,168 impr / pos 11.7 was the "brasada" family (dominated by "brasada ranch real estate agent", 1,974 impr), not the homes-for-sale query. The one document twin `/homes-for-sale/powell-butte/brasada-ranch` now 301s. Blocked to 2026-10-21.
- **Found for the next lane (SITE-190 Bend, SITE-192 Redmond; same template).** The plain city search page, PAGE_OUTLINE's winner for "[City] homes for sale", takes the `isPlainCityBrowse` → `renderMapSplitView` early return in `app/search/[...slug]/page.tsx`, which skips `SearchPageJsonLd` (WebPage / BreadcrumbList / Place / ItemList), the city market Dataset + FAQPage, the FAQ band and the related-search doors that the grid branch renders. Verified on production AND on the local build: `/homes-for-sale/bend` and `/homes-for-sale/redmond` ship 4 ld+json scripts (layout only), zero h2s. GSC 90d: "homes for sale bend oregon" 23 impr → /cities/bend 65%, homepage GBP UTM 30%, search page 0; "redmond oregon homes for sale" 37 impr → /cities/redmond 97%; "redmond homes for sale" 59 impr → /cities/redmond 76%, search 14%. The city guide already carries one "Redmond homes for sale" door to the search page. The lane: carry the JSON-LD + Dataset/FAQ into the map-split branch without waiting on the 12 s grid RPC (skip ItemList), keep the split view.
- **Not done.** Plat pages still build `/communities/${resortSlug}` from the durable slug (a Pronghorn plat links `/communities/pronghorn`, 308) at five call sites. `/housing-market/black-butte-ranch` vs `/housing-market/sisters/black-butte-ranch` twin (round 1 note) still open, no node.
- Skills read: `.claude/skills/site-queue/SKILL.md`, CLAUDE.md §0/§1/§2/§3, PAGE_OUTLINE "One winner", SITE_PAGES_E2E "Site queue".

# Current — 2026-09-23 (cloud grinder: Bend luxury + Black Butte Ranch one winner)

Surface: Claude cloud routine `cloud-grinder-2026-09-23-08`, `main` checkout, two lanes in worktrees cherry-picked onto main and landed as `582b8a9` (SITE-185) + `738be2a` (SITE-184) on `origin/main` with `npm run push` (2b52715..738be2a). No PR. `deploy:verify` cannot run in this sandbox (no `VERCEL_TOKEN`); production served both changes 12 minutes after the push (09:31Z): `/luxury-homes-bend` 308 and `/luxury-homes-bend-oregon` 301 → `/homes-for-sale/bend/luxury` (title/h1 "Bend luxury homes for sale"); `/homes-for-sale/black-butte-ranch` (+ query-string variants) canonical → `/communities/black-butte-ranch`, title/h1 "Search Black Butte Ranch homes"; both BBR area twins 301 → community; community description opens on the query; Sunriver unchanged.

- **SITE-185 (GSC "luxury homes bend oregon" → /homes-for-sale/bend/luxury).** Family "bend luxury" 2026-08-24..09-20: 224 impr, 0 clicks, /luxury-homes-bend 64%, /housing-market/bend 31% (appreciation queries, kept), winner 0%. Root cause: every "Luxury homes in Bend" door (nav, menu, footer, /cities/bend, /cities, /communities) went to /luxury-homes-bend, which 308'd to `/homes-for-sale/bend?minPrice=1500000`; the sitemap listed the redirect source at 0.8; /luxury-homes-bend-oregon chained to `/oregon/bend%3Fminprice…` "City not found". One helper `lib/site/bend-luxury-homes.ts` (path, door label, title/h1 phrase, description): both retired URLs 301 in one hop to the winner; `app/luxury-homes-bend/**` deleted (shadowed by its own redirect); every door → winner as "Bend luxury homes for sale"; sitemap drops the source (winner already in `/sitemaps/matrix.xml`); winner title/h1 "Bend luxury homes for sale", description opens on the query naming the preset's own $1M `minPrice` filter (not a market figure); /housing-market/bend explore list gains the door. Blocked to 2026-10-21 (28-day GSC window).
- **SITE-184 + SITE-191 (GSC "black butte ranch homes for sale" → /communities/black-butte-ranch).** Family "black butte ranch": 596 impr, 0 clicks, winner 63%, condo-filtered search 16%, /housing-market/sisters/bbr 7%, /cities/bbr 6%. `lib/communities/self-city-community.ts` generalised from `slug === city_slug` to "registry community whose slug is itself a site city slug" (Sunriver, Black Butte Ranch, and Crooked River Ranch by derivation). Same set as Sunriver: plain city search (+ query-string variants) canonical → community and out of the sitemap, its title/h1 "Search Black Butte Ranch homes" (Sunriver's search now reads "Search Sunriver homes" too); both area twins (`/homes-for-sale/black-butte-ranch/black-butte-ranch`, `/homes-for-sale/sisters/black-butte-ranch`) 301 → community; city page plain doors → community (title/H1 stay "real estate"); community description + Place JSON-LD open on the query; footer Sisters cluster door. SITE-177 setting clause shortened ("Golf resort with two courses under the Cascades.") to fit the 155-char cap. Both nodes blocked to 2026-10-21.
- **Not done (follow-up, no node).** `/housing-market/black-butte-ranch` and `/housing-market/sisters/black-butte-ranch` are two self-canonical market URLs for one place (9% of the family); folding one needs a routing decision in `app/housing-market/[...slug]` + `ci:market-city-slug-canon`. Crooked River Ranch joined the self-city set by derivation; excluding it would need a registry field, not a hand list.
- **Sandbox facts.** `ci:runtime-gates` on the fresh build: route-smoke 146/146, page-payload OK; tap-targets and route-content-floor fail on the same pre-existing items as the 2026-09-23 00Z fire (team "Next slide" 28×28, footer cluster title 36×44, atlas polygon; `heroImageNatural` under floor on cities/oregon-city/place-type-community/price-drops/reviews/search via sandbox egress) — CI on main was green at `2b52715` with all of them. Playwright 1.58 fetched Chromium 1208 via `cloud-setup.sh` (fonts 2 registered). Two lanes' `npx tsc` + a dev server OOM once; run tsc solo.
- **Next lane (not started).** SITE-183 Broken Top, SITE-182/189 Tetherow, SITE-181/188 Brasada split against child plat pages (`/subdivisions/*`), `/communities`, `/central-oregon/golf`, a blog post and listing pages — a different mechanism (parent links + h1 discipline on plat pages). SITE-190 Bend and SITE-192 Redmond split against `/cities/*`, a type preset and a community; /homes-for-sale/[city] is the winner there.
- Skills read: `.claude/skills/site-queue/SKILL.md`, CLAUDE.md §0/§1/§2/§3, PAGE_OUTLINE "One winner", SITE_PAGES_E2E "Site queue".

# Current — 2026-09-22 (Matt: the assigned broker signs)

Surface: Grok Build, primary checkout. A CMA, BPO, market report, or newsletter is signed by the contact's assigned broker. Matt signs only when nobody is assigned. The published business line is the number on the document. A web slug (paul-stevenson) is that broker. Existing stored letters stay as stored until a rebuild. Nothing sent. auto_send stays off. Node: none.

# Current — 2026-09-22 (Matt: Tetherow real estate in the title)

Surface: Grok Build, primary checkout. `/communities/tetherow` title is “Tetherow real estate | Homes for Sale | Bend, OR”. The description opens “Tetherow real estate in Bend, Oregon.” The heading stays “Tetherow homes for sale.” Node: none.

# Current — 2026-09-23 (cloud grinder: Sunriver one winner + SITE-158 closed)

Surface: Claude cloud routine `cloud-grinder-2026-09-23-00`, `main` checkout, landed `fc3649b` on `origin/main` with `npm run push` (rebased once over `f81cbd5`). No PR. Vercel deploy: `deploy:verify` cannot run in this sandbox (no `VERCEL_TOKEN`); production served the new canonical 10 minutes after the push (01:41Z): `/homes-for-sale/sunriver` canonical → `/communities/sunriver`, `/homes-for-sale/sunriver/sunriver` 301, sitemap no longer lists the plain search URL.

- **SITE-187 + SITE-186 (GSC "sunriver homes for sale").** Baseline re-pulled live (query×page, 2026-08-24..09-20): 126 impr, 0 clicks; `/cities/sunriver` 64%, `/communities/sunriver-river-view` 26% (noindex, lag), `/homes-for-sale/sunriver` 7%, the winner `/communities/sunriver` 2%. Root cause was internal: two search URLs carried the winner's exact title+h1, the city page sent every plain "Sunriver homes for sale" door to the search slug, the community page handed the phrase to `/homes-for-sale/sunriver`, and chrome+footer never linked the community page. One pure helper `lib/communities/self-city-community.ts` (registry `slug === city_slug`; Sunriver is the only member): city plain doors → community page, filtered doors stay on search; community drops the search door under its own phrase, search door = `/homes-for-sale/sunriver`, meta opener "Sunriver, Oregon homes for sale."; `/homes-for-sale/sunriver` canonical → community and out of the sitemap; `/homes-for-sale/sunriver/sunriver` 301 → community (`data/legacy-redirects.json`, Tetherow rule); footer Sunriver cluster gets one "Sunriver homes for sale" door. `publishPlaceBrowseHref` accepts a bare `/communities/<slug>`. Both nodes blocked to 2026-10-21 for the 28-day GSC window (`scratchpad/gsc-qp.mts` re-pulls query×page). Not done: a 301 of the plain `/homes-for-sale/sunriver` (would bounce the search app's city switcher) is Matt's call if the canonical alone does not consolidate; the chrome Homes panel still lists Sunriver as a search city.
- **SITE-158 (load-time scroll jump) → done.** The fix built in draft PR #351 had already landed inside SITE-155's `a4f0751`. Verified on production in headless Chromium (Node-side 100 ms polling, scroll to 1400 the moment the document exceeds it): `/`, `/cities/bend`, `/homes-for-sale/bend` only grow mid-load, scroll holds. Dev-only observation: the `<header>` paints ~2456 px for ~100 ms before `V3Chrome.css` applies in `next dev`; production ships the CSS render-blocking and measured 66 px from the first sample.
- **Sandbox facts for the next cloud fire.** Playwright 1.58 wants Chromium build 1208 (`npx playwright install chromium` fetched it; the snapshot has 1194, pass `executablePath: '/opt/pw-browsers/chromium'` or install). Chromium reaches production through the agent proxy only with `args: ['--disable-http2','--disable-quic','--ignore-certificate-errors']` and `proxy: { server: HTTPS_PROXY }`; without them the proxy answers 502 or `ERR_TOO_MANY_RETRIES`, and some CSS chunk requests still come back non-CSS, which makes Next reload the page (a 16 084 → 902 height drop on `/communities/tetherow` that is not the site). `npm run ci:runtime-gates` on the fresh build: route-smoke and page-payload pass; tap-targets and route-content-floor fail on routes this diff does not touch (`heroImageNatural` under floor on cities/neighborhood/oregon-city/place-type-community/price-drops/reviews/search; new under-44 controls: team "Next slide" 28×28, footer cluster title 36×44, atlas polygon) — the image floors read remote media through the sandbox egress. `next dev` on port 3457 died under memory pressure while `ci:gates` + `tsc` ran; verify on `npm run build` + `next start` instead.
- **Next lane (not started).** SITE-184/191 Black Butte Ranch has the same three-URL shape (`/cities/black-butte-ranch`, `/homes-for-sale/black-butte-ranch` with the winner's exact title, `/communities/black-butte-ranch`) but the registry city is Sisters, so the self-city helper does not cover it; the rule to generalise is "an MLS city slug that IS a resort community slug", and the same door/canonical/301 set applies. Tetherow/Broken Top/Brasada split against child plats, `/communities`, `/central-oregon/golf` and listing pages, a different mechanism (parent links from plat pages).
- Skills read: `.claude/skills/site-queue/SKILL.md`, CLAUDE.md §0/§1/§2/§3, `docs/plans/PUBLIC_PRODUCT/PAGE_OUTLINE.md` (one winner), SITE_PAGES_E2E "Site queue".

# Current — 2026-09-22 (Matt: another brokerage's phone stays off the listing)

Surface: Grok Build, primary checkout. A listing detail for a home that is not ours shows the listing brokerage name. The listing broker's phone, email, and personal name are not on that page. A Ryan Realty listing can still name our agent and our phone. Node: none.


# Current — 2026-09-23 (cloud grinder round 3: the city search winner ships its depth; queue drained)

Surface: Claude cloud routine `cloud-grinder-2026-09-23-08`, `main` checkout, one lane (SITE-190 + SITE-192) cherry-picked onto main as `95c77df` and pushed with `npm run push`. No PR. `deploy:verify` cannot run here (no `VERCEL_TOKEN`); production served it 12 minutes after the push (11:41Z): both city search pages ship WebPage / BreadcrumbList / Place / ItemList / Dataset / FAQPage, three h2s, the five-question FAQ and the footer, first byte to done under 1 s; the Black Butte Ranch, luxury and `?view=list` controls are unchanged. §0 on Bend: ItemList 781 = the visible "781+ homes for sale", "495 on this map" is the capped frame (the rail's own trace says so), Dataset 754 = the SFR-labelled FAQ sentence.

- **SITE-190 Bend + SITE-192 Redmond (GSC "[city] homes for sale" → /homes-for-sale/[city]).** The city guide collected the inventory query (Bend 90d: /cities/bend 65%, homepage GBP-UTM 30%, search 0; Redmond 90d: /cities/redmond 97%). Root cause, verified on production and the local build: the plain city search takes the `isPlainCityBrowse` → `renderMapSplitView` early return, which skipped `SearchPageJsonLd`, the Dataset, the `buildMarketFaq` FAQ and the whole `SearchSeoTail` — the winner was a map shell with 4 layout-only ld+json scripts and no h2. The split branch now renders WebPage / BreadcrumbList / Place / ItemList (from the map's own viewport fetch; omitted when degraded) + Dataset (`app/search/[...slug]/city-market-dataset.ts`, one builder for both branches) and mounts the tail below the viewport-fit Field (market band, leftover instrument, price ladder, 5-question FAQ → FAQPage, related searches, footer); the depth read (`sections/city-split-depth.ts`) runs beside the viewport fetch with a 1.5 s grace so the map never waits. Self-city plain pages (canonical → community) skip the tail. `scripts/check-search-results.mjs` now strips `<script>` blocks before parsing the rendered count. Both nodes blocked to 2026-10-21.
- **SITE-189 Tetherow zero-click** closed on round 2's registry rule with the shared window (blocked to 2026-10-21).
- **Queue state at end of fire:** every SITE node is done or blocked; 10 GSC nodes (181–185, 188–192) sit on 28-day GSC windows re-opening 2026-10-21 (re-pull with `scratchpad/gsc-qp.mts` + `gsc-family.mts`, both re-creatable from the handoff blocks above: query×page via the service account, `dimensionFilterGroups` equals / contains). Empty of eligible is not a stop: the next fire runs the measurer / seeds per the skill.
- **Sandbox facts.** Three builds in one fire were fine one at a time (~9 min each, `--max-old-space-size=11264`, no dev server); `ci:runtime-gates` fails the same six `heroImageNatural` floors + three tap targets every run (sandbox egress; CI on main green); a lane's `tsc` beside a dev server OOMed once — run `tsc` solo. Agent worktrees stay harness-locked after the lane ends (`git worktree remove` refuses); leave them.
- **Not done / follow-ups (no node).** `/housing-market/black-butte-ranch` vs `/housing-market/sisters/black-butte-ranch` twin; plat pages build `/communities/${resortSlug}` from the durable slug (Pronghorn → 308) at five call sites; `WebPage.primaryImageOfPage` omitted on the split branch (banner read is grid-only).
- Skills read: `.claude/skills/site-queue/SKILL.md`, CLAUDE.md §0/§1/§2/§3, PAGE_OUTLINE "One winner", SITE_PAGES_E2E "Site queue".

