# Mechanical guardrails for Ryan Realty website

This catalog lists every guardrail that has been moved out of prose
(CLAUDE.md, EXECUTION_PLAN.md, handoff docs) and into mechanical
enforcement (lint rules, CI scripts, build hooks). Prose rules require
agent discipline and break under context pressure. These don't.

**Rule:** if a guardrail can be mechanized, mechanize it. If a guardrail
keeps being violated, the answer is a new gate, not more prose.

## Active gates

| # | Gate | Mechanism | Source |
|---|---|---|---|
| G1 | DAL boundary — no raw `from('<table>')` outside `lib/data/` | ESLint `no-restricted-syntax` (error) + `scripts/check-dal-boundary.mjs` (ratcheted) | Plan §4 + CLAUDE.md §6 |
| G2 | Brand voice §6.1 + §6.2 in JSX text + string attrs | ESLint plugin `rr-brand-voice/no-violations` (error) | CLAUDE.md §2 |
| G3 | Brand voice across content files | `scripts/check-brand-voice.mjs` (ratcheted) | CLAUDE.md §2 |
| **G3b** | **Voice CONSTRUCTIONS** — the sentence SHAPES a word list cannot catch: coined maxims, a sentence that explains the sentence before it, sermon clauses, data-speaks headlines, throat-clears, pandering, manufactured urgency, false certainty. Patterns in `scripts/voice-constructions.cjs`, the machine-readable form of VOICE.md. Ratcheted. Scans the site, client documents, email + SMS builders, the content registries, AND the marketing brain with every producer SKILL.md. | `scripts/check-voice-constructions.mjs` (`ci:voice-constructions`, in `ci:gates`) | `marketing_brain_skills/brand-voice/VOICE.md` |
| **G3c** | **Construction parity** — `lib/brand-voice/constructions.ts` (the in-bundle mirror the runtime checker reads) must match `scripts/voice-constructions.cjs`. Without this the CI gate and the runtime gate can silently diverge. | `scripts/gen-voice-constructions.mjs` (`ci:voice-constructions-parity`, in `ci:gates`) | same |
| **G3d** | **Runtime construction block** — the constructions run inside `lib/voice/check.ts`, the one chokepoint every content path calls and blocks on (blog save, CMA build, BPO build, social publish, CRM templates, newsletters). Matt 2026-08-05: "anytime any content / copy is created it is run through this voice. Period." Path wiring is asserted by G-runtime/`ci:voice-send-paths`. | `lib/voice/constructions.test.ts` + `scripts/check-voice-send-paths.mjs` | same |
| G4 | Design tokens — no raw hex, no retired fonts, no bg-image hero | `scripts/lint-design-tokens.js --base-diff` | CLAUDE.md §5 / Plan §10 |
| G5 | SEO route metadata + JSON-LD authoring | `scripts/check-seo-routes.mjs` + `scripts/check-seo-authoring.mjs` | Plan §1 |
| G6 | **Mockup parity** — every gated route imports every component the matching mockup contract requires | `scripts/check-mockup-parity.mjs` (ratcheted) + per-route `design_system/ryan-realty/ui_kits/<route>/parity.json` | Plan §1 + Wave 3 §9 |
| G7 | **Mockup coverage** — every mockup directory must have a `parity.json` contract | `scripts/check-mockup-coverage.mjs` (allowlisted) | Plan §1 + Wave 3 §9 |
| G8 | **Page DAL completeness** — every `app/<route>/page.tsx` reads through `@/lib/data` (positive direction) | `scripts/check-page-dal.mjs` (ratcheted) | Plan §1 |
| G9 | **`generateStaticParams` on every dynamic route** | `scripts/check-static-params.mjs` (ratcheted) | Plan Wave 3 §9 |
| G10 | **Bundle budget** — total + per-chunk JS size ceiling + growth-vs-baseline detection | `scripts/check-bundle-budget.mjs` (baselined) post-`next build` | Plan §1 |
| G11 | **Route smoke** — every canonical route returns 200, non-blank, non-404 against a live server | `scripts/check-route-smoke.mjs` + start-server-and-test | feedback memory "verify before moving on" |
| G12 | **Draft-first commit gate** — user-facing diffs require an `Approved-by: matt` or `Draft-shown: <url>` line | `.husky/commit-msg` → `scripts/check-draft-first.mjs` | CLAUDE.md §0.5 |
| G13 | First-frame thumbnail quality (video pipeline) | `scripts/check_first_frame.py` | CLAUDE.md §4 |
| G14 | TypeScript strict | `tsc --noEmit` via `next build` | Plan §1 |
| G15 | Lighthouse perf ≥ 0.90 / a11y ≥ 0.95 / BP ≥ 0.90 / SEO ≥ 0.95 / LCP ≤ 2500ms / CLS ≤ 0.10 | `npm run ci:lighthouse` (blocks PRs) | Plan §1 |
| G16 | CMA · expired audit · BPO resolve comps and price through ONE engine (`lib/cma/comps` + `lib/cma/pricing`); no build path defines its own | `scripts/check-valuation-engine.mjs` (`ci:valuation-engine`) | Brain Dump 2 A4, 2026-07-28 |
| G61 | ONE market section per page — `<MarketCoreCharts>` must be a CHILD of `<KbMarketHud>`, never a sibling | `scripts/check-market-section-nesting.mjs` (`ci:market-section-nesting`) | Matt 2026-07-29; missed on `cities/[slug]` for 8 days (C-17) |
| G62 | No section heading is a bare verb (Explore/Discover/Browse/...), including the `title = 'X'` DEFAULT-PARAM form | `scripts/check-naked-verb-headings.mjs` (`ci:naked-verb-headings`) | VOICE.md Site pages + Search-traffic rule 2; C-07 |
| G63 | A `'use server'` module may not re-export a name it only IMPORTED as a type — Next emits the re-export as a runtime binding, so the erased type becomes `ReferenceError: X is not defined` at module evaluation | `scripts/check-server-type-reexport.mjs` (`ci:server-type-reexport`), AST via the TS compiler | 2026-08-08: one line took /admin/crm/subscriptions to a 500 behind a GREEN tsc; a second latent instance sat in `app/actions/video-tours-cache.ts` |
| **G16** | **Data access discipline** — `docs/DATABASE_SCHEMA_SNAPSHOT.md` matches live Supabase + `docs/DAL_INDEX.md` matches `lib/data/`. Drift fails CI. | `scripts/check-data-access.mjs` (regenerates both via `_agent_schema_dump()` RPC + AST walk; diffs vs HEAD) | CLAUDE.md "Data Access Discipline" + feedback `no-adhoc-sql.md` |
| **G17** | **SQL column quoting** — `lib/data/*.ts` cannot call `.eq('"ColumnName"', …)` etc. with LITERAL double-quote characters inside the JS string (that's the 2026-05-28 "Listing Not Found" regression class). | `scripts/check-dal-column-quoting.mjs` | Inventory GAP-1 |
| **G18** | **`force-dynamic` + `revalidate` coexistence** — ESLint refuses both exports in the same route file (silently disables ISR cache). | `eslint-rules/no-dynamic-revalidate.js` plugin (`rr-no-dynamic-revalidate/no-dynamic-revalidate`) | Plan §0.4 + Inventory GAP-5 |
| **G19** | **Sentry `tracesSampleRate` budget** — ESLint refuses literal values > 0.2 in `sentry.*.config.{ts,js}` (silent quota drain). | `eslint.config.mjs` `no-restricted-syntax` scoped to sentry config files | Plan §0.4 + Inventory GAP-6 |
| **G20** | **Brand-voice vocabulary single source** — ESLint plugin + CI script + runtime hook all consume `scripts/brand-voice-vocabulary.cjs`. Test asserts parity. | `scripts/__tests__/brand-voice-vocabulary.test.cjs` | Inventory GAP-7 |
| **G21** | **DAL internal cache discipline** — `getMarketStats / getMarketPulse / getPriceHistory / getGeoSnapshot` etc. must `.from('<cache>')`, never `.from('listings')`. | `scripts/check-dal-internal-discipline.mjs` (with delegation support) | Inventory GAP-8 |
| **G22** | **Producer pipeline guard** — every staged `scripts/build_*.py` must call `require_action_row(payload)` from `_producer_lib` or carry `# @producer-guard-exempt: <reason>`. | `scripts/check-producer-guard.mjs` + `.husky/pre-commit` | Inventory GAP-10 + feedback `brain-pipeline-protocol` |
| **G23** | **pa11y-ci in CI** — catches WCAG 2.1 failures the Lighthouse aggregate score misses. | `npm run ci:a11y` wired into `.github/workflows/CI.yml` (PRs only) | Inventory GAP-3 |
| **G24** | **Retired-font detection** — `font-family:`, Tailwind utilities like `font-playfair`, `next/font/google` imports of Playfair/Inter/Helvetica/AzoSans/system-ui all refused. | `scripts/lint-design-tokens.js` (added `RETIRED_FONTS` set) | Plan §0.3 + Inventory GAP-4 |
| **G25** | **Design directive registry** — every rule in `docs/DESIGN_DIRECTIVES.md` must be `enforced`, `deferred`, or `wont-fix`. Any `open` directive fails CI. | `scripts/check-design-directives.mjs` | DESIGN_DIRECTIVES.md "Maintenance protocol" |
| **G26** | **Arbitrary Tailwind brackets** — `max-w-[*]`, `py-[*]`, `gap-[*]`, `p-[*]`, `rounded-[*]`, `shadow-[*]` etc. banned outside a small allowlist (`rounded-[10px]`, `rounded-[14px]`, `ring-[3px]`, `tracking-[-0.01em]`, `tracking-[-0.02em]`, `tracking-[0.08em]`, `tracking-[0.12em]`). | `scripts/lint-design-tokens.js` G26 patterns | DESIGN_DIRECTIVES.md D17/D18/D19/D20/D21/D22/D14 |
| **G27** | **Decorative gradients** — `linear-gradient(...)` banned except the navy protection scrim and the canonical photo bottom-to-transparent. | `scripts/lint-design-tokens.js` G27 patterns | DESIGN_DIRECTIVES.md D72 |
| **G28** | **Black box-shadows** — `box-shadow: ... rgba(0, 0, 0, ...)` banned. Must use `--shadow-sm/md/lg` navy-tinted vars. | `scripts/lint-design-tokens.js` G28 pattern | DESIGN_DIRECTIVES.md D23/D24 |
| **G29** | **Inline `<style>` JSX banned** — `<style>{...}</style>` and `createGlobalStyle(...)` blocked in `app/**` + `components/site/**`. Style belongs in Tailwind + globals.css + CSS modules. | ESLint `no-restricted-syntax` `JSXElement[openingElement.name.name='style']` | DESIGN_DIRECTIVES.md D32/D33 |
| **G30** | **Geo imagery canonical source** — area-tile/geo imagery on `app/cities/**`, `app/communities/**`, `components/site/**` must come from `getGeoTileImages()` (asset_library) or `GOLF_COMMUNITY_IMAGES` (`lib/geo-images.ts`). Fails on a hardcoded `/lp/...image` path or a fake `neighborhoods`/`communities` `hero_image_url` read on any geo surface. | `scripts/check-geo-imagery.mjs` (`npm run ci:geo-imagery`) | DESIGN_DIRECTIVES.md D82/D86 |
| **G31** | **Boundary-map shared DAL** — every geo page (`app/cities/[slug]/page.tsx`, `app/cities/[slug]/[neighborhoodSlug]/page.tsx`, `app/communities/[slug]/page.tsx`) that renders `<NeighborhoodMap>` or `<SiteNeighborhoodMap>` MUST import `getGeoBoundaryMapData` from `@/lib/data`. Ad-hoc polygon + pin fetches that bypass the shared DAL are banned (they produce inconsistent map data — the old per-page City/SubdivisionName string-match divergence). | `scripts/check-boundary-map.mjs` (`npm run ci:boundary-map`) | DESIGN_DIRECTIVES.md D88 |
| **G32** | **Community page completeness** — `app/communities/[slug]/page.tsx` MUST import all four: (a) `communityImage` from `@/lib/geo-images` (photo resolver covering all 14 communities), (b) `OpenHousesGrid` (open houses section), (c) `ListingCard` (homes-for-sale card grid), (d) `getGeoBoundaryMapData` from `@/lib/data` (polygon-fit boundary map). Removing any import breaks the page contract and fails CI. | `scripts/check-community-page.mjs` (`npm run ci:community-page`) | DESIGN_DIRECTIVES.md D89/D90/D91/D92 |
| **G33** | **Community content completeness** — every resort/planned community in `data/resort-communities.json` AND all 13 Bend neighborhoods MUST have a `data/resort-community-<slug>.json` config carrying `about_prose` plus at least one of `amenities` / `drive_times`. A new geo cannot ship thin: the coverage backlog (`data/community-content-baseline.json`) must shrink to zero and never grow. Content is RESEARCHED from primary sources (community / HOA / golf / county / news) and VERIFIED, never invented — a geo leaves the backlog only when a cited config lands. | `scripts/check-community-content.mjs` (`npm run ci:community-content`) | DESIGN_DIRECTIVES.md D95/D96 |
| **G34** | **AI structured-data presence (GEO)** — every key surface MUST emit machine-readable JSON-LD so AI assistants (Claude, ChatGPT, Perplexity, Google AI Overviews) surface + cite Ryan Realty. `app/layout.tsx` → `<JsonLd>` (site-wide Organization + WebSite). `app/cities/[slug]/page.tsx` + `app/communities/[slug]/page.tsx` → `MetadataBlock` + `buildMarketFaq` (entity Place + market Dataset + verified FAQPage). `app/cities/[slug]/[neighborhoodSlug]/page.tsx` → `MetadataBlock` + `buildMarketFaq` (Neighborhood Place + market Dataset + verified FAQPage). `components/site/listing-detail/ListingDetailShell.tsx` → `MetadataBlock` (RealEstateListing + BreadcrumbList). A rebuild that drops the schema fails CI instead of shipping an un-citable surface. | `scripts/check-ai-structured-data.mjs` (`npm run ci:ai-structured-data`) | DESIGN_DIRECTIVES.md D97 |
| **G35** | **Producer SKILL.md structure** — every brain-callable producer in `marketing_brain_skills/producers/`, `social_media_skills/`, `video_production_skills/` passes the 10-gate validator: 11 numbered sections, complete frontmatter, `action_types` match REGISTRY.md, 8 mandatory base refs + 4 content refs (content producers), zero em/en-dashes, canonical `target_platforms` enum, valid `**Status:**`. Capability / reference skills (caption / safe-zone / voice rule docs) skipped via `CAPABILITY_AND_BRAIN_PATHS`. | `scripts/check-producer-skills.mjs` → `scripts/validate-producer.mjs` (`npm run ci:producer-skills`, in `ci:gates`) | CLAUDE.md "Marketing Brain Architecture" + producer audit 2026-05-28 |
| **G36** | **AI-tool inline-call discipline (ratcheted)** — no NEW inline ElevenLabs / Replicate-video API calls; VO routes through `scripts/_voice_lib.py` / `lib/voice/`, Replicate video through the shared helper. Grandfathered set in `scripts/.tool-discipline-baseline.json` (27) may only shrink. (The former "Layer 1" skill-auto-load contract for `tool-mastery` + `viral-playbook` was RETIRED 2026-06-22 — both skill files were deleted, so it asserted against non-existent files and always-failed while the gate ran nowhere; the surviving inline-call ratchet was wired into `ci:gates`.) | `scripts/check-tool-discipline.mjs` (`npm run ci:tool-discipline`, in `ci:gates`) | CLAUDE.md "Marketing Brain Architecture" + organic-growth engine 2026-05-29 |
| **G37** | **Email/PDF brand tokens** — the design-token gate (G4/G24) scans only `app/` + `components/`, so the HTML/email/PDF generators in `lib/` and `app/api/` (CMA delivery email, broker digests, FSBO/expired alerts, cron report emails, map InfoWindows) were unchecked and had drifted to retired gold `#D4AF37`, old cream `#F2EBDD`, sand `#e8e2d4`, and retired web fonts (Inter/Arial/Helvetica). This gate flags retired brand HEX + retired-everywhere fonts in those dirs. Canonical tokens come from `lib/email/brand.ts` (`EMAIL_FONT_STACK`/`EMAIL_NAVY`/`EMAIL_CREAM`/`EMAIL_BORDER`). AzoSans (print-legit per DS v2) and `system-ui` (valid OS-font keyword) are deliberately not flagged. Zero tolerance, no baseline. | `scripts/check-email-brand-tokens.mjs` (`npm run ci:email-brand`, in `ci:gates`) | DS v2 palette lock 2026-05-13 + email sweep 2026-06-05 |
| **G38** | **Broker/brokerage facts centralized** — every render surface, page metadata, and JSON-LD block that names a brokerage phone, license, broker, social profile, founding date, or NAP imports from the canonical `lib/brand/contact.ts` (`BRAND`/`CONTACT`/`BROKERS`) instead of a literal. Before it, those facts were copied into ~30 files, so a phone/license change left stale copies. The gate bans the canonical phone literals (`541.213.6706`, `541.703.3095` in any format) and `ryanrealtybend` social-profile URLs from reappearing in `app/` + `components/` `.ts/.tsx` render code (`route.ts`/`actions.ts`/`lib/` excluded; emails not gated). Ratcheted per-file count baseline (`scripts/broker-facts-baseline.json`) — counts may only fall; migrate a file then `npm run ci:broker-facts:baseline`. Escape hatch: `broker-facts-ok` line suffix. | `scripts/check-broker-facts.mjs` (`npm run ci:broker-facts`, in `ci:gates`) | Cluster 5 (site-consistency audit 2026-06-04) + broker-facts sweep 2026-06-05 |
| **G39** | **AI-crawler access + llms.txt preserved** — the site's biggest forward-looking strength is being fully open to AI citation crawlers (`app/robots.ts` allows the full bot roster) and serving a Markdown content map at `/llms.txt`. A careless edit to `robots.ts` (removing a bot, adding a blanket `Disallow`) or deleting the `llms.txt` route would make Ryan Realty invisible to ChatGPT search / Perplexity / Claude / Google AI Overviews with **zero build error**. This gate asserts every required citation bot (GPTBot, OAI-SearchBot, ChatGPT-User, ClaudeBot, Claude-SearchBot, PerplexityBot, Google-Extended, Applebot, Googlebot, Bingbot) is still allowed, `/` stays crawlable, and `app/llms.txt/route.ts` still exists. | `scripts/check-ai-crawler-access.mjs` (`npm run ci:ai-crawler-access`, in `ci:gates`) | AI-exposure audit 2026-06-06 (G9) |
| **G67** | **AI query battery (F1)** — named queries ("best broker in Bend", "3-bedroom 2-bath in Northwest Crossing", "Get my home's value in Bend") plus mapped GSC extras resolve to a citable survivor URL in `/llms.txt` + JSON-LD. Fails if a 308 hop (`/reports`, `/search`, `/area-guides`) is the citation target, if `llms.txt` drops a family, or if required JSON-LD tokens disappear. Map: `lib/seo/ai-query-map.json`. | `scripts/check-ai-query-battery.mjs` (`npm run ci:ai-query-battery`, in `ci:gates`) | Broker OS Loop F / slice F1 |
| **G40** | **Canonical integrity** — (a) no page may declare an `alternates.canonical` / `openGraph.url` whose path matches a `next.config.ts` redirect `source` (a self-defeating canonical that 301s back to itself — the /reports class); (b) every indexable `app/**/page.tsx` (not robots-noindexed, not admin/api/marketing) must declare its own canonical — the root-layout blanket canonical was removed (it silently converted "missing canonical" into "wrong canonical pointing at the homepage", the /videos deindex-risk mechanism). Ratcheted with a count ceiling; baseline currently EMPTY (zero debt). | `scripts/check-canonical-integrity.mjs` (`npm run ci:canonical-integrity`, in `ci:gates`) | Site-consistency audit 2026-06-09 P1.7/P1.8/P2.8 |
| **G41** | **Dead/deprecated UI cannot return** — fails if any curated known-dead file reappears on disk (deprecated `components/Breadcrumb.tsx` alias, superseded `PhotoGallery.tsx`, orphaned geo MarketStats trio, orphaned `ListingSummary`/`ListingDetails`). Dead components with retired patterns are a re-adoption vector — they get deleted AND listed here so they stay deleted. Extend by: grep zero importers → `rm` → add path to `DEAD_FILES`. | `scripts/check-dead-ui.mjs` (`npm run ci:dead-ui`, in `ci:gates`) | Site-consistency audit 2026-06-09 P2.7 / gate 9 |
| **G42** | **Money-page content smokes** — the #1 money pages must render their commerce content, not just a 200 shell: listing-detail (formatted price + ≥1 photo URL + listing-agent name + contact CTA), buy/sell intent LPs (lead-form island + visible FAQ + talk-now block), per-city sales report (≥1 sold stat), and geo detail pages (schools/parks/subdivisions H1 + content band). Runs post-deploy against the live URL in smoke-test.yml alongside the existing content gates. | `scripts/check-listing-detail.mjs` + `scripts/check-lead-funnels.mjs` + `scripts/check-geo-detail.mjs` (`ci:listing-detail` / `ci:lead-funnels` / `ci:geo-detail`, in smoke-test.yml) | Site-consistency audit 2026-06-09 P1.9–P1.12 / gate 8 |
| **G43** | **Server-only modules stay out of the client bundle** — no `'use client'` file or `components/**` file may VALUE-import a registered server-only module (`lib/pulse-asset-library`, the asset-library manifest JSON, `pulse-lifestyle-cards.server`); `import type` is allowed. Founding case: the asset manifest grew to ~1.4 MB and shipped inside the /pulse client chunk (+42% total bundle, caught by G10). Data is resolved server-side and passed as props. | `scripts/check-server-only-imports.mjs` (`npm run ci:server-only-imports`, in `ci:gates`) | Bundle regression 2026-06-09 + G10 catch |
| **G44** | **THE LOOP canon sync** — `docs/DEVELOPMENT_PROCESS.md` (the versioned canonical development process) must be pointed at by every agent entry point (CLAUDE.md, the producer TEMPLATE, the cron system-prompt builder) with a matching "THE LOOP vX.Y.Z" version marker, and every `docs/plans/*.md` must be registered in the canon's plan table — plans are inputs to THE LOOP, never parallel processes. Pointer loss, version drift, and rogue plans each fail the build. Also asserts R-216 ship-class: sentinel prompt is ONE SHIP CLASS, loop-brief prints SHIP CLASS via `selectShipClass`, growth-loop skill names the class — one-node-one-rebuild is a fail. Also asserts R-217: walker briefs name SITE REVIEW and do not end on token match; pack header must not say END this run now. | `scripts/check-process-canon.mjs` (`npm run ci:process-canon`, in `ci:gates`) | docs/DEVELOPMENT_PROCESS.md v1.0.0 (2026-06-09); R-216 2026-08-16; R-217 2026-08-16 |
| **G45** | **RETIRED 2026-07-21** — producer-layer growth freeze, lifted by Matt ("LIFT G45", session transcript 2026-07-21). Script and baseline deleted; the registry may grow again. Producers still route through the action-row protocol, approval queue, and voice/QA gates. | — (deleted) | CLAUDE.md "Producer layer — freeze LIFTED 2026-07-21" |
| **G56** | **Version-manifest comprehensiveness** — the Company version manifest (`docs/plans/ENTERPRISE_MAP/VERSION-1.md`) may never quietly shrink: every below-Working capability (CAPABILITIES maturity bands 2/1/0) and every red integration must be accounted for in the manifest (gap row, Matt move, or explicit PARK); gap rows G1..Gmax and Matt moves M1..Mmax must be contiguous (a deleted row breaks numbering and fails); a DONE gap row must carry a date; a CERTIFIED status must carry a commit SHA. Kills the "these five things will be enough" shortcut — selection happens over the full enumerated universe. Founding case: first run caught CAP-033 dropped from the v1 manifest (2026-08-15). | `scripts/check-version-manifest.mjs` (`npm run ci:version-manifest`, in `ci:gates`) | THE LOOP v1.4.0 §Company versions; Matt directive 2026-08-15 (no shortcut assumptions) |
| **G57** | **Requirements register unshrinkable** — `docs/plans/ENTERPRISE_MAP/REQUIREMENTS.md` holds every directive Matt has issued (203 rows at creation, harvested 2026-08-15 from the full on-disk corpus), each with a disposition from a closed set. R-IDs must be contiguous (a deleted requirement fails; rows leave only by becoming SUPERSEDED in place); MISSING rows must cite a covering VERSION-manifest gap row; MISSING/PARTIAL rows cannot have empty coverage; a MISSING row citing a nonexistent gap fails. Kills "he said five things so forget the 120 others." | `scripts/check-requirements-register.mjs` (`npm run ci:requirements-register`, in `ci:gates`) | Matt directive 2026-08-15 (bring all requirements together; fill gaps automatically) |
| **G53** | **Cron reachability** — every `app/api/cron/*` route must be registered in `vercel.json`, OR carry a `cron: invoked-by <caller>` / `cron: manual-only` marker comment explaining its trigger, OR sit in the ratchet baseline (which may only shrink). Closes the dark-cron class: 23 of 72 cron routes were unregistered and invisible (2026-07-21 audit). | `scripts/check-cron-registered.mjs` + `scripts/cron-registered-baseline.json` (`npm run ci:cron-registered`, in `ci:gates`) | Platform decisions 2026-07-21 (Phase 0 reachability spine) |
| **G54** | **ODS display compliance** — pins the Oregon Data Share Rules (Aug 2024) display obligations: §5-3 P/S/T attribution block (firm + participant contact + ODS source + disclaimers) rendered on the listing page, §5-3 B/G seller opt-out + idx_participant exclusions in the detail guard and the newest public-MV migrations (address-only opt-out required in any MV migration after 2026-07-21), §5-4 A.4 sold-data-is-VOW-only (no sold preset, status variants stay noindexed), §5-3 I refresh cadence (sync-delta registered). | `scripts/check-ods-compliance.mjs` (`npm run ci:ods-compliance`, in `ci:gates`) | ODS Rules & Regulations Aug 2024; Matt directive 2026-07-21; memory `reference_ods_rules` |
| **G55** | **Export reachability** — every `components/**` and `lib/**` module must be imported by some file in the app/components/lib graph (relative or `@/` specifier), OR carry a `reachability: entry-point <reason>` marker in its first 10 lines, OR sit in the ratchet baseline (which may only shrink). Closes the orphaned-component class: SmartSearch, HeroSearchOverlay, SearchSplitView shipped dark (2026-07-21 audit); ~200 pre-gate orphans in the triage backlog. | `scripts/check-reachable-exports.mjs` + `scripts/reachable-exports-baseline.json` (`npm run ci:reachable-exports`, in `ci:gates`) | Platform decisions 2026-07-21 (Phase 0 reachability spine) |
| **G56** | **Send chokepoint (admin rebuild §A4)** — every outbound message routes through `lib/comms/sendGovernedSms` / `sendGovernedEmail` (hard-stop tags → channel suppression fail-closed → quiet hours for SMS → idempotency → provider rail → timeline). Ratchets DIRECT provider call-sites in app/ + lib/ outside `lib/comms/**` and the rail modules; baseline is per-file counts (line-drift-proof), seeded 61 sites / 47 files, shrink-only — a new file or a count increase fails. `ci:crm-sms-safety` folds in once the baselined callers migrate. | `scripts/check-governed-send.mjs` + `scripts/governed-send-baseline.json` (`npm run ci:governed-send`, in `ci:gates`) | ADMIN_REBUILD spec §A4; platform decisions 2026-07-21 (Wave B) |
| **G57** | **Market city → MLS-City canonical NAME** (§0, registry-spelling half of the market-city canon; companion to `ci:market-city-slug-canon`, which guards the slug SHAPE). Every NAME in `REPORT_CITIES` / `MARKET_REPORT_DEFAULT_CITIES` / `PRIMARY_CITIES` (`lib/data/geo/report-cities.ts`) must lower()-match ≥ 1 `listings."City"` row, OR be a documented `NON_MLS_CITY_EXEMPTIONS` entry whose `mlsCity` + `subdivisionMatch` home actually holds rows (a stale exemption that becomes a real city also fails). `compute_and_cache_period_stats` keys a city on `lower("City")=lower(slug)`, so a name matching no `City` yields a permanently-empty cache + /cities page — the latent hole that had "Crooked River Ranch" (files under Terrebonne) and "Tumalo" (files under Bend) sitting in the city tiers under a false "verified against the live table" comment. **DB-dependent → off the static `ci:gates` chain** (like G16); runs nightly in `quality.yml` + locally; SKIPS cleanly with no Supabase creds. | `scripts/check-market-city-mls-canon.mjs` (`npm run ci:market-city-mls-canon`, nightly in `.github/workflows/quality.yml`) | §0 registry-spelling audit 2026-07-24 (adjacent to slug-canon fix `96d129c6`) |
| **G58** | **Default-chrome footer scoping** — `app/layout.tsx` must NOT render SiteFooter (it used to, behind the HideChrome CSS gate, which shipped a hidden 48-link `<footer>` in the HTML of every KB/LP/admin page — dead DOM weight; Google discounts display:none links). Instead, every public `app/**/page.tsx` must resolve to exactly one footer: the KB shell (kb-root/KbFooter, paired by G53), or `<SiteFooter />` in the page / an ancestor layout / a one-level local import, or the redirect-only allowlist. A pure KB page rendering SiteFooter too (double footer) also fails; the dual-register `/housing-market/[...slug]` page is the one allowed exception. | `scripts/check-default-chrome-footer.mjs` (`npm run ci:default-chrome-footer`, in `ci:gates`) | Hidden-footer audit 2026-07-29 (three `<footer>` elements per page, SiteFooter display:none site-wide) |
| **G60** | **CI probe User-Agent** — every CI-referenced `scripts/*.mjs` that makes a raw `fetch` against an app origin must import `CI_PROBE_HEADERS` from `scripts/lib/ci-probe-ua.mjs`, or carry a `probe-ua: adversarial <reason>` marker; and that shared UA must not be matched by the LIVE `BAD_BOT_RE` in `middleware.ts` (read via the TypeScript AST, so tightening the bot screen fails this gate instead of failing CI silently). Founding case: `middleware.ts` 403s automation User-Agents; `start-server-and-test` waits via `wait-on`, which is axios-based and sends `User-Agent: axios/1.x`, which `BAD_BOT_RE` matches. Every readiness probe got 403, `wait-on` only accepts 2xx, so it timed out after 5 minutes reporting only "Timed out waiting for". **Every pull request in this repo failed on that step for ~3 months** (`main` looked green only because the step is gated on `github.event_name == 'pull_request'`). The app was healthy throughout — it answers `/` in 0.07s and passes 277/277 routes. The replacement probes passed only because Node's `fetch` defaults to `User-Agent: node` and someone picked `rr-smoke/1.0`; neither is on the block list by coincidence, and nothing recorded the constraint. Discovery is automatic (package.json scripts + `.github/workflows/*`), so a newly added probe is covered without registration. The deliberate exception is `crm-e2e-verify.mjs`, which sends `python-requests/2.31.0` on purpose to prove the A2P `COMPLIANCE_VERIFICATION_PATHS` exemption still works (Twilio error 30909 hit us twice). | `scripts/check-ci-probe-ua.mjs` + `scripts/lib/ci-probe-ua.mjs` (`npm run ci:probe-ua`, in `ci:gates`) | CI root-cause 2026-08-02 (website-audit branch); CLAUDE.md §6 |
| **G59** | **Int-test residue** (§6, recurring) — `*.int.test.ts` files write to the PRODUCTION Supabase project, and a killed run skips its `afterAll`. The 2026-07-30 survey found stranded test rows in SEVEN tables: 17 in `cmas` (five in `delivered`, inflating the count of documents actually sent to clients), 11 in `broker_price_opinions`, 22 in `crm_people` behind 27 `crm_timeline` rows, 11 in `newsletter_subscribers`, 12 in `crm_idempotency_keys`, 20 in `newsletters`, plus `marketing_brain_actions` rows stuck at `in_production` consuming CMA build-worker capacity. Blocking writes from a test context was rejected: nine of thirteen int tests exist to verify DB write contracts against real indexes, RPCs and triggers. The contract instead is ONE marker spelling on every test-written identifier (`test/int-scope.ts` — `intId()`/`intEmail()`/`intAddress()`), a registry of every table that can carry it, and a sweep in vitest `globalSetup` that runs BEFORE the run (clearing what a killed run stranded — the crash-proof half) and again at teardown. The gate asserts, by AST: no hand-rolled marker literals; every table an int test writes is registered; every writing int test imports the scope module; and the `int` project still declares `globalSetup` (renaming the project or dropping the line fails the build instead of silently disarming the net). | `scripts/check-int-test-residue.mjs` (`npm run ci:int-test-residue`, in `ci:gates`) | residue audit 2026-07-30; CLAUDE.md §6 |
| **G60** | **Confidential-key parity** — six invariants over the confidential-key lists in `lib/listing-customfields.ts` and their SQL mirror. (1) every camelCase key in `PRIVATE_DETAIL_KEYS` carries its spaced CustomFields twin; (2+4) the TS **unconditional** redaction set (`PRIVATE_DETAIL_KEYS` ∪ `CONFIDENTIAL_CF_MEMBER_KEYS`) equals `rr_private_keys()` exactly; (3) every confidential CONCEPT (remarks, showing, owner/occupant identity, lockbox/access, escrow) is covered; (4b) `CONFIDENTIAL_CF_MEMBER_KEYS` mirrors `rr_showing_member_keys()` exactly; (5) `PUBLIC_MEMBERS_IN_CONFIDENTIAL_GROUPS` is disjoint from the redaction set, so the deliberate publics ('To Be Built', 'Under Construction') are not silently over-redacted; (6) `partitionCustomFields` still tests the member LABEL set, not only the group. Born from the 2026-07-30 adversarial audit (the CF flatten emits MLS display names, so 'Private Remarks' bypassed a list holding only 'PrivateRemarks' — broker-private remarks anon-readable on ~78% of on-market rows). **Widened 2026-07-31** after a confidential key survived remediation and was live to the PUBLIC ANON KEY: the Showing-Requirements fix redacted members by GROUP, which is provenance-based, and the flattened namespace is not partitioned by provenance — 'Call Listing Agent' arrives from both 'Showing Requirements' (576/800 listings) and 'Existing Lease Type' (1/800), and the second copy landed in `details`. The old gate was blind twice over: it compared SQL against `PRIVATE_DETAIL_KEYS` alone, and its SQL reader matched the first `ARRAY[...]` after the text `rr_private_keys`, silently latching onto `rr_private_keys_BASE` once the function became composed. Both fixed; a group-conditional TS branch no longer counts as redaction. Paired with the DB-side `trg_0_redact_private_details` write trigger (migration `20260801003000`), which is the airtight half. | `scripts/check-private-key-parity.mjs` (`npm run ci:private-key-parity`, in `ci:gates`) | CF spelling leak, adversarial audit 2026-07-30 (`da2a49be`); cross-group label collision 2026-07-31 |
| **G61** | **Search registry provenance** — the search field registry must reproduce from Spark metadata + a reasoned curation file: every registry field maps to a metadata concept (or carries a curation entry with a reason), every option exists in the metadata vocabulary (or the allowlist), the committed `data/search-metadata/registry-report.json` reproduces byte-for-byte, and none of the 11 mask-dead filters (spa, carport, homeWarranty, walkScore, storiesTotal, fireplacesTotal, carportSpaces, parkingTotal, directionFaces, schoolDistrict, laundryFeatures) can be re-added by key, concept, or MV column. Static artifacts only — no network, no DB. The report also carries the P5 long-tail backlog (metadata-searchable concepts with no registry entry). | `scripts/check-search-registry-generated.mjs` + `data/search-metadata/` (`npm run ci:search-registry-generated`, in `ci:gates`) | Plan §11 P1, SEARCH_FILTER_COMPLETENESS_PLAN_2026-07-30 |
| **G62** | **TOAST read discipline** — `public.listings.details` is a ~10 KB jsonb document per row; 86% of the table's 14 GB is TOAST and TOAST is 12x `shared_buffers`. Postgres cannot read one key out of a TOASTed value, so `details->>'X'` reassembles the whole document **per candidate row** (+1.4 ms/row warm-index floor, ~3.8 ms/row seq-scan realistic) and `LIMIT` saves nothing. On 2026-07-31 one such predicate produced a **335-second query and an anon-facing outage** (`967ed3bf`), and the same expression inside `listing_tile_mv_src` / `listing_search_mv_src` is the mechanism behind the documented **8-day matview staleness** incident — a matview pays the detoast on every refresh and fails silently into stale data. Code review cannot see this class. **Four rules:** *A1 broad select* — a supabase-js `.from('listings')` read projecting `details` (bare, `details->>Key`, aliased, or a bare `.select()`/`*`) with no narrowing `.eq`/`.in`/`.match` on `"ListNumber"`/`"ListingKey"`; *A2 details predicate* — `.order`/`.filter`/`.or`/`.contains`/`.not`/`.eq`… keyed on a `details` expression on such a chain; *A3 helper columns* — a CALL SITE handing `details`/`*` into a helper whose `columns` parameter feeds an un-narrowed listings select (resolved through real `ts.resolveModuleName`, following the `@/lib/data` re-export barrel and `const { x } = await import(…)`, so a relative specifier or dynamic import cannot dodge it); *A4 sql definition* — a NEW `CREATE [MATERIALIZED] VIEW` / `CREATE FUNCTION` in `supabase/migrations/**` whose body reads `details->`/`->>`/`?`/`@>`/`jsonb_*(details)`. **Deliberately does not trip on:** a single-row lookup by primary key (that IS the listing-detail page), a pk-narrowed columns helper, `{ head: true }` count reads (A1 only — a details PREDICATE still costs), any other table's `details` column, the narrow matviews, or a migration that merely REFERENCES an existing object (REFRESH / GRANT / ALTER / COMMENT / SELECT) or names `details` only in a comment. Scope `lib/**` + `app/**` + `supabase/migrations/**`; `scripts/**` is excluded because the backfill/reconciliation scripts exist to read `details` (`backfill-virtual-tours.mjs` is named in the doc as a predicate that must NOT be "optimized"). Ratcheted against a multiset baseline keyed by `{ rule, file, normalized-expression }` — never line numbers, so swapping one violation for another in the same file is still caught. Escape hatch `// toast-ok: <reason>` / `-- toast-ok: <reason>`, reason required. AST-based; 44 break-tests. | `scripts/check-toast-read-discipline.mjs` + `scripts/toast-read-baseline.json` (`npm run ci:toast-discipline`, in `ci:gates`; re-seed with `ci:toast-discipline:baseline`) · tests `scripts/__tests__/check-toast-read-discipline.test.mjs` | [`docs/TOAST_READ_DISCIPLINE.md`](TOAST_READ_DISCIPLINE.md); 335 s search outage + matview staleness, 2026-07-31 |
| **G63** | **View-preset equivalence** — the five view SEO pages (`/search/mountain-view`, `water-view`, `river-view`, `golf-course-view`, `lake-view`) were live, indexable and permanently empty until `db537784` routed `viewContains` off the un-plannable `search_listings_advanced` path and onto `listing_search_mv.view_types` via `resolveViewContainsValues()` in `lib/search-presets.ts`. That resolver matches the term against a **hard-coded 22-value vocabulary the MLS feed owns**. The day the feed emits a value the list lacks — a new "… Mountains" spelling, a "Butte", a new body of water — the map under-returns and a live page quietly serves fewer homes, with no error, no timeout and no red build. Unit tests over hard-coded values cannot see it; they assert the map against itself. So this gate asserts it **against the data**. **Five rules:** *B0 inputs* — the module still exports what the gate reads and every preset term resolves non-null (a null resolution is the legacy-RPC fallback that emptied the pages); *B1 vocabulary* — every distinct `view_types` value on the live on-market set exists in the vocabulary (the root-cause rule, and the early warning that fires before any listing is lost); *B2 under-return* — per term, ZERO rows where `view_text ILIKE '%term%'` is true but `view_types && <resolved>` is false; *B3 widening* — the reverse direction must also be zero unless the term is declared in the gate's own `DECLARED_INTENT_TERMS` ledger with a measured reason (only `water`, because the feed never spells "Water" — it names the body of water; 0 → 332 rows), and a declared term that has STOPPED widening is flagged as a stale ledger entry; *B4 coverage* — the on-market set is non-empty, ≥ 95% of it carries a `listing_feature_flags` row, and every term matched rows on at least one side, so equality is never proven over nothing. Joins `listing_search_mv.view_types` to `listing_feature_flags.view_text` by `list_number` in-process (a matview has no FK, so a PostgREST embed is inference that can silently stop working). **DB-dependent → off the static `ci:gates` chain** (like G16 / G57): nightly in `quality.yml` + locally; SKIPS cleanly with no Supabase creds. 11 break-tests mutate the resolver in a sandbox and prove each rule turns red against live rows. | `scripts/check-view-preset-equivalence.mjs` (`npm run ci:view-preset-equivalence`, nightly in `.github/workflows/quality.yml`) · tests `scripts/__tests__/check-view-preset-equivalence.test.mjs` | Five empty view SEO pages, fixed `db537784` 2026-07-31 |
| **G46** | **Commit self-containment** — type-checks the actual committed (or staged) tree in isolation from the working tree, catching any commit that references symbols that exist only in the uncommitted working tree. Closes the two escape classes that broke production deploys 2026-06-09 night: (1) `EventName` union member added to an untracked enum while committed code consumed it; (2) `lib/data/index.ts` export added to the working tree but not yet staged while committed callers already imported it. File-existence checks cannot catch either — the files were tracked; the symbols were missing. Mechanism: `git archive HEAD \| tar -x` into `$TMP`, symlink `node_modules`, write a thin `tsconfig.check.json` extending the materialized tsconfig with `.next/` excluded, run `tsc --noEmit`, clean up. Exit 1 prints headline + first 30 error lines. `--staged` mode uses `git checkout-index -a` for pre-commit use. | `scripts/check-commit-compiles.mjs` (`npm run ci:commit-compiles`, in `ci:gates`). Locally the full `ci:gates` chain + Turbopack build run in `npm run push` (`scripts/push-with-gates.sh`) BEFORE `git push` opens the ssh connection, then stamp `<git-dir>/rr-gates-marker`; `.husky/pre-push` only verifies the marker (tree hash + freshness) in milliseconds — a long in-hook chain idles the already-open connection out and the pack write dies with SIGPIPE (observed 2026-07-17). The chain + build run against the MATERIALIZED tree of the pushed commit in a detached verify worktree (`~/.cache/ryanrealty-gates-verify`, APFS-cloned node_modules), so concurrent sessions' working-tree state can neither fail nor green-wash the run and the marker certifies exactly the pushed tree; `PUSH_GATES_IN_PLACE=1` falls back to in-place. `ci:gates` still runs in GitHub Actions. | Production deploy failures 2026-06-09 |
| **G47** | **CMA single-path routing** — every CMA routes through `marketing_brain_skills/producers/cma/SKILL.md` and a property gets exactly ONE slug (the action row's `target` slug). Fails the build if two committed `public/cmas/<slug>/` dirs normalize to the same address (including the `-usa`/country-suffix alias case, e.g. `cma-62285-deer` vs `cma-62285-deer-usa`), if a CMA dir is malformed (no `cma.html`), or if `scripts/build_cma_wrapper.py` is a live copy-and-relabel rogue builder (it is now a retired guard). `public/drafts/` (gitignored scratch) is warn-only. Born from a parallel session building a second CMA for the same property under a different slug. | `scripts/check-cma-routing.mjs` (`npm run ci:cma-routing`, in `ci:gates`) | Matt directive 2026-06-13 ("all CMAs must only route through this skill / gates") |
| **G48** | **Row-cap discipline** — no single-shot Supabase read over PostgREST's 1,000-row response cap. Every response is silently capped at 1,000 rows regardless of a larger `.limit(N)`, so `.limit(5000)` "works" in dev and silently truncates in production — and every count, tally, dedupe set, or send tier derived from it is wrong with no error anywhere. Flags literal `.limit(N)` with N > 1000 and `.range(a, b)` spans > 1000 in `lib/**/*.ts` + `app/**/*.{ts,tsx}` (comment/string/regex-literal-aware so a doc comment quoting `.limit(5000)` never trips it). Fix: paged `.range(offset, offset+999)` loop with a stable `.order()` until a short read — canonical helpers `fetchPagedRows` / `fetchAllRows` in `lib/supabase/paginate.ts` (exemplars `lib/data/crm/getCrmSources.ts`, `lib/data/newsletter/queue.ts`); `{ count: 'exact', head: true }` when only a count is shown. Opt-out pragma `// row-cap-ok: <reason>` (same or previous line) for reads provably ≤ 1,000 rows by construction. Founding cases: newsletter send tiering (`lib/data/newsletter/queue.ts`), CRM tag counts (`getCrmTags.ts`), inbox queue (`getInboxQueue.ts`), plus the 20+ analytics/dashboard/cron reads repaired in the 2026-07-14 sweep. | `scripts/check-row-cap.mjs` (`npm run ci:row-cap`, in `ci:gates`) | PostgREST 1,000-row cap (memory `reference_listing_search_mv_field_registry`) + row-cap sweep 2026-07-14 |
| **G49** | **Avatar crop discipline** — circular people-avatars must not center-crop portrait sources. The broker headshots are normalized 800×1200 portraits (head in the top half); `rounded-full object-cover` with the default (center) object-position crops them to the torso and cuts the head off — the 2026-07-15 audit found ~20 CRM surfaces beheading brokers at once because both shared primitives (`components/ui/avatar.tsx` AvatarImage, `CrmMobileKit` CrmAvatar) plus 10 inline `<img>`s inherited the default. Rule: any line in `app/**/*.tsx` + `components/**/*.tsx` carrying both `rounded-full` and `object-cover` must also carry an explicit object-position (`object-top` — correct for any photo of a person, no effect on square/landscape sources — or an explicit `object-center`/`object-bottom`/`object-[…]` choice). Opt-out pragma `// avatar-crop-ok: <reason>` (same or previous line). Founding catch on first run: `app/account/page.tsx` "Your agent" card. | `scripts/check-avatar-crop.mjs` (`npm run ci:avatar-crop`, in `ci:gates`) | Matt directive 2026-07-15 ("broker headshots are cropped in the CRM") + memory `reference_broker_headshots` (800×1200 normalization) |
| **G50** | **Composer discipline** — one compose interface for every text/email send (Matt directive 2026-07-15: "anytime a text or sms or email is sent I always want it to use the same interface"). Canonical surfaces: `components/admin/crm/SmsComposer.tsx` (FUB chat bar, every SMS), `components/admin/crm/EmailComposer.tsx` (Gmail-style form, every email), `components/admin/crm/EmailBodyEditor.tsx` (the shared editing core EmailComposer renders internally, embeddable by bulk hosts that own their audience/scheduling — ComposeToCohort, the people-list Batch Email dialog). Rule: a `.tsx` in `app/`+`components/` that references a live send action (`sendCrmSmsAction`, `sendCrmEmailAction`, `sendAdminEmail`, `sendDocEmailAction/SmsAction`, `bulkEmailCohortAction`, `dispatch/scheduleComposeCohortAction`) AND renders its own `<Textarea` must import a canonical composer. Reviewed non-compose editors go in the script's ALLOWLIST with a reason (currently: the approvals-queue inline text edit); escape hatch `// composer-ok: <reason>`. Founding consolidation: the mobile compose sheet's hand-built SMS bar, SendDocDialog (expired/FSBO), AdminEmailCompose, ComposeToCohort's inline body, and BulkActions batch email — five drifted copies replaced in one pass. | `scripts/check-composer-discipline.mjs` (`npm run ci:composer-discipline`, in `ci:gates`) | Matt directive 2026-07-15 |
| **G51** | **No toggles in resume paths** — resume/replay/retry/deep-link code must never replay a deferred intent through a toggle-style mutation. A toggle reads current state and flips it, so replaying one against stale client state inverts the user's intent — the RC7 founding case un-saved listings because the post-sign-in resume path called `toggleSavedListing` with a stale "not saved" card prop; the fix (`lib/hooks/useResumePendingSave.ts` → `resumeSaveListing`, an idempotent add) is the SAFE exemplar. Scope: any file in `app/`+`components/`+`hooks/`+`lib/` whose path matches `resume\|replay\|retry\|pending-\|deep-link`, carries the `// @resume-path` marker comment, or consumes a stashed intent (`consumePending*()`). Flags: calls to `toggle*` mutations, state setters fed a blind negation (`setX(!x)` / `setX(v => !v)`), and self-negating flip assignments (`x = !x`). Manual click handlers in ordinary components stay out of scope by design. Ratcheted vs `scripts/resume-toggle-baseline.json` (empty at birth — may only shrink); reviewed legit site escape hatch `// toggle-ok: <reason>` on the same or previous line. When writing new re-entry/replay logic without the naming, add `// @resume-path` so the gate covers it. | `scripts/check-resume-toggle.mjs` + `scripts/resume-toggle-baseline.json` (`npm run ci:resume-toggle`, in `ci:gates`) | RC7 stale-prop toggle bug (resume path un-saving listings) |
| **G64** | **THE PAGE CONTRACT — no PDF bleeds, none clips** — every document a person receives as a PDF (CMA, BPO, market report, net sheet, flyer) obeys one page box ([`lib/pdf/page-contract.ts`](../lib/pdf/page-contract.ts)): content may never enter a header, footer, or margin, and may never be silently clipped. Two constructs are banned, both measured live 2026-08-03. (1) `overflow: hidden` / `max-height` on a sheet container — clipped content is never DRAWN, so it leaves no trace in the PDF and no downstream check can see it; a delivered CMA lost 9px of a comparable's stat line at the page-5 boundary, and the dangerous copy of the rule lived in an `@media print` block that governs the PDF but reads as screen styling. (2) Reserving the header/footer bands with PADDING on a box that fragments across sheets — CSS padding applies once at the top of a box and once at the bottom, so the first sheet gets a top margin, the last gets a bottom margin, and every interior sheet gets NONE; a 5-sheet BPO printed body text 1.5pt from the paper edge on sheets 3, 4 and 5, which is how every BPO longer than two sheets had been printing. Reserve bands with `@page { margin }` and put running marks in the margin strips via `pdfRenderOptions()` (Chrome `displayHeaderFooter`), where they cannot collide with body content. **Three layers, because no single one is sufficient:** the static gate (below) catches the two banned constructs at commit time with no browser; `assertPageFit()` runs in the browser before `page.pdf()` and is the ONLY layer that can see content that would be clipped; `assertPdfPageSafety()` measures the produced BYTES with pdfjs (EDGE / STRADDLE / SIDE) inside every send path, so no CSS refactor, new renderer, or longer LLM-written narrative can route around it. Escape hatch `page-contract-exempt` in a comment above the rule; `PDF_PAGE_SAFETY=warn|off` for local iteration only. Rendered proof in `lib/pdf/page-contract.int.test.ts`, `lib/cma/page-safety.int.test.ts`, `lib/bpo/page-safety.int.test.ts` — each carries a negative control asserting the OLD model still fails. | `scripts/check-pdf-page-safety.mjs` (`npm run ci:pdf-page-safety`, in `ci:gates`) · [`lib/pdf/assert-page-fit.ts`](../lib/pdf/assert-page-fit.ts) · [`lib/pdf/assert-page-safety.ts`](../lib/pdf/assert-page-safety.ts) | [`docs/PAGE_CONTRACT.md`](PAGE_CONTRACT.md); Matt directive 2026-08-03 (CMA content bleeding into footers) |
| **G65** | **Admin interior migration ratchet** — the P11 counter that makes the 11-destination admin the ONLY admin, one-way. AST (TS compiler) over `app/admin/**/*.tsx`, four shrink-only rules ratcheted vs `scripts/admin-ui-baseline.json`: (A) raw intrinsic `<h1> <h2> <button> <input> <select> <table>` — the v2 primitives own these (251 at seed); (B) LEGACY PAGES — a `page.tsx` that neither imports `@/components/admin/v2` nor is a true redirect bridge (calls `redirect()` AND renders no JSX — an auth-guard redirect with JSX stays legacy; that disguise fooled the first draft, 58 vs the true 131 at seed); (C) more than one primary-variant v2 `<Button>` per file (primary is the DEFAULT variant — "exactly one primary action per view", ADMIN_UI.md); (D) count of DISTINCT `max-w-*` tokens (21 at seed — the visual lock names no approved width list, so the gate ratchets sprawl instead of inventing one). Counts above baseline or a NEW violating file fail; shrinkage prompts a `--write-baseline` re-seed committed like any diff. Break-tests prove every rule fails and the safe shapes (quiet/danger buttons, width-token swap, true bridge) stay green. | `scripts/check-admin-ui.mjs` + `scripts/admin-ui-baseline.json` (`npm run ci:admin-ui`, in `ci:gates`) · break-tests `scripts/__tests__/check-admin-ui.test.mjs` | `docs/plans/ADMIN_PRODUCT/PHASE-11-PLAN.md` §11A; Matt problem statement ("Frankenstein mishmash", 2026-08-06) |
| **G66** | **Admin entity-reader scope ratchet** — every `page.tsx` under `app/admin` that lives in a DYNAMIC ROUTE SEGMENT must decide whether the caller owns THIS record, not merely whether their role may open admin pages. Three holes of exactly that shape were found in one session (2026-08-07): `crm/deals/[id]` ran only `getCrmAccess`, so a restricted broker could walk sequential ids and read any deal's GCI, commission percent and split rows (fixed `4aa3de68`); `people/[id]/portal` ran only `requireAdminPage('people.view')`, and `CAPABILITY_ROLES['people.view']` includes `'broker'`, so any broker could read any contact's saved homes, hidden homes, named areas, alert recipients and site activity; `deals/[key]` was the third. `ci:crm-scope` cannot see them — it inspects WRITES in `app/actions/crm*.ts`, and nothing inspected READS. A dynamic segment IS a caller-supplied record id, which is the whole IDOR surface. **The rule:** each such page must (a) CALL one of the four sanctioned scope decisions — `scopeBroker` / `isPersonInScope` (`lib/crm/scope.ts`), `dealInScope` (`lib/crm/deal-scope.ts`), `requirePersonInScope` (`app/actions/crm.ts`); the complete reachable set, since `requireDealInScope` / `requireTaskAccess` are module-private to their action files — or (b) be a pure REDIRECT BRIDGE (imports + calls `redirect`/`permanentRedirect`, renders NO JSX — G65's bridge test, hardened so a JSX FRAGMENT also counts as rendering; an auth-guard redirect that still renders is NOT a bridge) — or (c) be baselined. Detection is by CALL EXPRESSION via the TS compiler, never import presence and never regex: a page that imports `requirePersonInScope` and never calls it is exactly the bug and must fail. Shrink-only: 19 dynamic pages at seed — 3 scoped, 4 bridges, 12 baselined. A page missing from the baseline fails, a count above baseline fails, and deleting a baselined line while the page is still unscoped fails (it simply reads as NEW). 9 break-tests, one per rule. | `scripts/check-entity-scope.mjs` + `scripts/entity-scope-baseline.json` (`npm run ci:entity-scope`, in `ci:gates`) · break-tests `scripts/check-entity-scope.test.ts` | `lib/crm/scope.ts` Option A (Matt 2026-06-22); three broker-scope holes found 2026-08-07 |
| **G-NL-14** | **Newsletter Phase 1 schema invariants** — the migrations must declare the facts the send system rests on: `newsletter_recipients` status CHECK allows `queued`+`skipped` (else the Phase 3 queue is rejected at every enqueue — the audit A1 bug), `newsletters` status CHECK allows `scheduled`+`canceled`, the `newsletter_recipient_events` ledger exists with a UNIQUE `dedupe_key`+`broker`, `newsletter_send_schedule` exists, and `newsletter_recipients` carries `broker`+`tier`. Static: reads `supabase/migrations/*.sql`, strips SQL comments so it checks DDL (not a doc-comment mentioning the value), last-`add constraint` wins so a later regression flips it red. Authoritative live-schema check is G16 nightly. First of the 20 G-NL newsletter gates (spec §12); the rest land with their phase. | `scripts/check-newsletter-schema.mjs` (`npm run ci:newsletter-schema`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §12 + Phase 0 adversarial audit 2026-07-03 |
| **G-NL-1/2/3** | **Newsletter CAN-SPAM + RFC 8058 compliance** — `lib/email/prepare.ts` must export `BROKERAGE_POSTAL_ADDRESS` with a real street-number fallback (not empty/placeholder), and `lib/email-templates/newsletter-shell.ts` must reference that constant and render an unsubscribe link (G-NL-1). Both `app/actions/newsletter.ts` and `app/actions/contact-newsletter.ts` must set the `List-Unsubscribe` + `List-Unsubscribe-Post` headers on the outbound send so Gmail/Yahoo render native one-click unsubscribe (G-NL-2). `app/actions/newsletter.ts` must derive a non-empty plain-text body via the `letter.body_text?.trim() \|\| htmlToPlainText(...)` fallback so a send never dispatches empty text (G-NL-3). Static text checks only. | `scripts/check-newsletter-compliance.mjs` (`npm run ci:newsletter-compliance`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §12 + CLAUDE.md §0 (Data Accuracy / compliance risk to Matt's license) |
| **G-NL-7** | **Newsletter shell static lint** — `lib/email-templates/newsletter-shell.ts` (the ONE place newsletter markup lives) must declare a `color-scheme` meta tag (dark-mode client safety), keep its outer content table `max-width` <= 640px (mobile-safe), set the body cell `font-size:16px` (readability floor), reference `BROKERAGE_POSTAL_ADDRESS` + render "Unsubscribe" in the footer, and use the Design System v2 navy `#102742` masthead. A regression here breaks every newsletter send, not just one. | `scripts/check-newsletter-format.mjs` (`npm run ci:newsletter-format`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §12 |
| **G-NL-4** | **Newsletter voice-gate wiring** — both newsletter send entry points must run the brand-voice gate before sending: `app/actions/newsletter.ts` (the bulk approve path) and `app/actions/contact-newsletter.ts` (the one-click path) must each call `checkNewsletterVoice(...)`. A send path that skips this check can ship a banned word or punctuation violation straight to a client inbox with no gate to catch it. | `scripts/check-newsletter-voice-paths.mjs` (`npm run ci:newsletter-voice-paths`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §12 + CLAUDE.md "Brand Voice" |
| **G-NL-6** | **Newsletter drain re-check + no-reactivate-optout** — `lib/newsletter/send-queue.ts` must re-check BOTH suppression (`isSuppressedByEmail(...)`) AND active subscriber status (`status !== 'active'`) per recipient at drain time, not just at enrollment time, so a person who unsubscribed or bounced in the gap between enqueue and drain never gets mailed. `app/actions/contact-newsletter.ts` must refuse to silently reactivate a previously opted-out subscriber (`existing` row checked against `!== 'active'`). | `scripts/check-newsletter-drain-safety.mjs` (`npm run ci:newsletter-drain-safety`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §12 + CLAUDE.md §0 (Data Accuracy / compliance risk to Matt's license) |
| **G-NL-9** | **Newsletter queue CAS lock + no sync mega-loop + reconciler exists** — `lib/data/newsletter/queue.ts` `claimNewsletterForSending` must be a conditional UPDATE (`status: 'sending'` guarded by `.in('status', ['draft', 'scheduled'])`) so two concurrent send-claims can only have one winner, not a read-then-write race that double-mails every recipient. `app/actions/newsletter.ts` `adminSendNewsletterAction` must NOT call `sendEmail(...)` directly (a synchronous loop over recipients would time out the request) and MUST call `enqueueNewsletter(...)` instead. `app/api/cron/newsletter-reconcile/route.ts` must exist so a stalled/orphaned send has an automated recovery path. | `scripts/check-newsletter-queue.mjs` (`npm run ci:newsletter-queue`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §12 |
| **G-NL-15** | **Newsletter cron wiring** — `vercel.json` `crons` array must contain entries whose `path` is `/api/cron/newsletter-send` AND `/api/cron/newsletter-reconcile`, and both route files must exist. A dropped cron entry (bad merge, unrelated cron cleanup) silently stops the queue from ever draining or self-healing, with no red build and no error — just newsletters stuck in `draft`/`sending` forever. | `scripts/check-newsletter-crons.mjs` (`npm run ci:newsletter-crons`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §12 |
| **G-NL-11/G-NL-10** | **Email tracking token safety + timeline dedup** — `lib/email-tracking.ts` `assertTrackingSecret()` must check `NODE_ENV === 'production'` and `throw` when only the insecure dev-fallback secret is set (prod hard-fail, G-NL-11); `signEmailToken` must stamp `body.exp =` + `body.n =` (sourced from `randomBytes(...)`) and `verifyEmailToken` must read `o.exp` so an expired/replayed token is rejected. Both `app/api/track/e/open/route.ts` and `app/api/track/e/click/route.ts` must upsert into `crm_timeline` with `dedupe_key` + `onConflict` + `ignoreDuplicates` so a repeat open/click collapses to one row instead of flooding the comms chain (G-NL-10). | `scripts/check-email-tracking.mjs` (`npm run ci:email-tracking`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §12 |
| **G-NL-5/G-NL-8** | **Per-broker identity swap** — the render (`lib/newsletter/send-queue.ts`) must resolve broker from the RECIPIENT's frozen slug via `normalizeBroker(recipient.broker)`, never the sender's (the core pre-build bug). The Resend webhook (`app/api/webhooks/resend/route.ts`) must resolve broker via `newsletter_recipients(resend_message_id)` and stamp `broker: eventBroker` on the event rows, so opens/clicks carry broker on BOTH engagement sources (H1). Every broker headshot in the render must be an absolute `https://` URL (email clients can't load relative `/images/brokers/` assets, G-NL-8). | `scripts/check-newsletter-broker.mjs` (`npm run ci:newsletter-broker`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §5/§12 |
| **G-NL-10/G-NL-13** | **Engagement-ledger integrity** — `EVENT_MAP` (lib/crm/resend-webhook.ts) must handle `email.unsubscribed` (T-8, else a native unsubscribe never flips the subscriber). Open/click counts must derive from the deduped `newsletter_recipient_events` ledger (`getNewsletterStatsFromLedger` in queue.ts reads it), not the inflatable recipient counters. `recordLedgerEvent` must build a RECIPIENT-scoped `dedupe_key` (`nl:<id>:sub:<...>`) not a bare message-id key (A3), and the Resend webhook must call `recordLedgerEvent` to populate it. | `scripts/check-newsletter-events.mjs` (`npm run ci:newsletter-events`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §3.1/§12 |
| **G-NL-20** | **Pre-send reputation gate** — a LARGE bulk send must consult the latest Google Postmaster snapshot before enqueuing: `lib/newsletter/send-queue.ts` enqueue calls `getLatestDeliverability` + `deliverabilityVerdict` and refuses when the verdict is `block` (Gmail reputation LOW/BAD or spam rate > 0.30%). `deliverabilityVerdict` must return `warmup` on no data (the first sends have none — defer to the ramp, never blast, never hard-block). Protects Ryan Realty's sender reputation from a bad monthly blast. | `scripts/check-newsletter-reputation.mjs` (`npm run ci:newsletter-reputation`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §6.5/§6.7 |
| **G-NL-12** | **Per-broker analytics scope (Phase 8 console)** — the newsletter analytics console (spec §9.5) must never let a restricted broker read another broker's engagement rows. Two checks: (a) `lib/data/newsletter/brokerAnalytics.ts` — every `newsletter_recipients`/`newsletter_recipient_events` query chain (`getBrokerNewsletterAnalytics`, `getBrokerWarmList`) must carry a `.eq('broker', ...)` filter in the SAME chain, not merely somewhere in the same function (a scoped helper elsewhere in the function must not "cover" an unrelated unscoped read); (b) `app/admin/(protected)/newsletters/analytics/page.tsx` must resolve a restricted broker's slug from the SESSION (`getCrmAccess()` + `scopeBroker(...)`), and the client-supplied `searchParams` broker param must only be consulted when `isSuperuser` (`restrictedSlug === null`) — a restricted broker can never widen scope by hand-editing `?broker=<slug>` in the URL. Static + comment-stripped. Red-demo verified: removing a `.eq('broker'` filter, or hardcoding `isSuperuser = true`, flips the gate red. | `scripts/check-newsletter-scope.mjs` (`npm run ci:newsletter-scope`, in `ci:gates`) | `docs/NEWSLETTER_SYSTEM_SPEC.md` §9.5/§12 |
| **G-runtime** | **Six runtime tool refusals** — Bash destructive / DB CLI / `--no-verify`; `execute_sql` against `information_schema` / DAL-covered tables; Write/Edit to `app/<route>/page.tsx` without `parity.json`; Write/Edit content with banned-voice tokens. Each can be bypassed with explicit `-- audit:` SQL comment, `# allow-destructive: <reason>` Bash trailer, `// @no-parity` or `// brand-voice:exempt` file marker, or `ALLOW_ALL_HOOKS=1`. | `.claude/settings.json` + `.claude/hooks/pre-tool-use.mjs` (26 tests) | Inventory L2 |
| **G-identity-stitch** | **Lead/sign-in doors write `visitor_identity_map.crm_person_id`** — every public capture + auth path must call `stitchFormSubmitIdentity` or `stitchVisitorIdentity` (session-only `backfillSessionToFub` is not enough). The writer must set `crm_person_id` in lockstep with `fub_person_id`. Ads CAPI hashes that id as `external_id`. Alerts get `stampListingAlertsCrmPerson`. Founding case: packet §1b showed 1/164 mapped because the stitch wrote only the legacy column. | `scripts/check-identity-stitch-paths.mjs` (`ci:identity-stitch-paths`, in `ci:gates`) | THE LOOP G2 / packet §1b / R-059 · R-134 · R-144 |
| **G-stage-truth** | **Inbound create writes Lead; named writers advance** — `JOURNEY_ENTRY_STAGE` is the string `Lead`. `buildNativePersonRow` stamps it; `ensureNativeLead` / `findOrCreatePersonByPhone` / `captureHotAnonymous` must call the builder. Enroll **awaits** `sequence-enroll`; first-outbound **awaits** the advance; the sequence engine records a real email/SMS send through `recordSequenceOutbound` (stamps first-outbound). Compose stages come from `getCrmStages`. Broker/bulk stage writes name `broker-set-stage`. Sequence `change_stage` names `sequence-change-stage`. The Lead-entry migration must not remap `crm_people`. Founding case: streamline v2 deactivated Lead and the packet showed Lead = 0 of 22,672. | `scripts/check-stage-truth.mjs` (`ci:stage-truth`, in `ci:gates`) | THE LOOP G3 / R-163 / packet stage mix |
| **G-listing-alert-enroll** | **Saved-search enrollment stamps `listing_alerts.crm_person_id`** — account / guest / buyer-LP writers pass the native `crm_people.id` from `sendEvent` / `ensureNativeLead` (via `nativeCrmPersonId`) into `upsertListingAlert`. Account path captures the person before persist. Send engine + cron read `listing_alerts` only, never `.from('saved_searches')`. Founding case: packet showed 6 active alerts for 22k people and writers treated native ids as `fub_legacy_id`. | `scripts/check-listing-alert-enroll.mjs` (`ci:listing-alert-enroll`, in `ci:gates`) | THE LOOP G4 / R-152 / packet §1b |
| **G-broker-own-book** | **Own-book fail-closed + day-one** — `scopeBroker` never returns null for a non-superuser (unmapped → `UNMAPPED_OWN_BOOK`). `getCrmAccess` resolves slug from `brokers.crm_slug` (not only `CRM_BROKER_BY_EMAIL`). Today / People / Messages use `scopeBroker`. `content.marketing` includes broker. Day-one checklist is the closed six-item set. Founding case: D17 fail-open + empty marketing arrays left CAP-022 at Skeleton. | `scripts/check-broker-own-book.mjs` (`ci:broker-own-book`, in `ci:gates`) | THE LOOP G5 / R-198 / CAP-022 |
| **G-westside-backlog** | **Westside backlog dispositions + luxury money links + review-ask drafts** — every `WESTSIDE_BACKLOG.md` ranked row carries SHIPPED/CLOSED/DONE/GATED/RE-RANKED/DEFERRED. `/luxury-homes-bend` is linked from nav, city rail, Bend `/cities` row, and `/communities`. Deal close stages a `crm_message_drafts` review-ask with `GBP_REVIEW_URL` and never sends. | `scripts/check-westside-backlog.mjs` (`ci:westside-backlog`, in `ci:gates`) | THE LOOP G7 / CAP-030 / R-124 / R-125 / R-150 |
| **G-skyslope-mirror** | **SkySlope recon mirror stays inbound and scheduled** — `/api/cron/skyslope-mirror-refresh` is registered, calls `refreshSkySlopeMirrorInbound`, and the Files client allowlists GET + login POST only (no PUT/PATCH/DELETE). Closings + the packet read `getSkySlopeMirrorFreshness`. Heartbeat `evalSkySlopeMirror` fails red after 36h. Founding case: mirror `synced_at` sat at 2026-06-10 because refresh was a Mac-only script. | `scripts/check-skyslope-mirror.mjs` (`ci:skyslope-mirror`, in `ci:gates`) | THE LOOP G8 / INT-017 |
| **G-look-walk** | **Look-walk baselines stay recorded** — `docs/plans/ENTERPRISE_MAP/look-walk-baseline.json` has the PAGE-GRADE `beat_on` public set at 390+1280 (HTTP 200 + a job noun) and a graded CMA (slug, verdict, page count). Scoreboard `collectCompanyScoreboardSignals` reads that file for CMA look + public-ux walk. Packet §1b cannot say those are UNKNOWN. Founding case: packet wrote UNKNOWN because the probe only counted `cmas` rows. | `scripts/check-look-walk.mjs` (`ci:look-walk`, in `ci:gates`) | THE LOOP G9 / R-092 / packet §1b |
| **G-meta-audience-hold** | **Meta audience hold is a named DAL** — `readMetaAudienceHold` / `computeAudienceHold` count consecutive UTC `meta_audience_log.ran_at` days. KEEP is off until 7 consecutive days end on or after 2026-08-22. Daily freshness is 36h (both crons are daily). Packet, `/admin/audiences`, `/admin/analytics/meta-health`, and `evalMetaAudienceHold` read that DAL. Founding case: INTEGRATIONS still said last LIVE 2026-06-23 while the log had weeks of daily rows, and the westside probe used an 8-day weekly hide. | `scripts/check-meta-audience-hold.mjs` (`ci:meta-audience-hold`, in `ci:gates`) | THE LOOP G11 / INT-007 |
| **G-join-conversion** | **/join convert stays a named figure** — `getJoinConversionStats` reads `visitor_events` (path `/join` visits + `event_type=join_convert`). Contact form `Join the team` writes the convert and tags `recruit:join` without buyer enroll. Scoreboard `join` + `/admin/today` + the packet cite that DAL. Packet cannot write `/join` UNKNOWN. Founding case: recruit-retain had broker count only. | `scripts/check-join-conversion.mjs` (`ci:join-conversion`, in `ci:gates`) | THE LOOP G10 / R-201 / packet recruit-retain |
| **G-video-docket** | **CAP-017 park-or-rebuild docket stays costed** — `readVideoDecisionDocket` reads `docs/plans/ENTERPRISE_MAP/video-decision-docket.json`. Park incremental vendor is $0. Rebuild cites ElevenLabs Turbo $0.05/1k and the $5/row $15/run producer cap. Packet + `/admin/loop` + scoreboard `video` read that DAL. Decision stays pending until M3. Founding case: CAP-017 sat at maturity 2 with no costed choice for Matt. | `scripts/check-video-docket.mjs` (`ci:video-docket`, in `ci:gates`) | THE LOOP G12 / CAP-017 / R-045 / M3 |
| **G-integration-health** | **Unknown-health integrations stay probed** — `readIntegrationHealth` reads `docs/plans/ENTERPRISE_MAP/integration-health-probes.json`. The eight G13 cells (INT-021/023/026/029/031/032/033/036) must each have evidence and health ≠ unknown. INTEGRATIONS health counts **unknown = 0**. Packet + `/admin/loop` + scoreboard `integrations` read that DAL. Founding case: eight runtime cells sat at unknown after the 2026-08-08 close. | `scripts/check-integration-health.mjs` (`ci:integration-health`, in `ci:gates`) | THE LOOP G13 / INT-021…036 |
| **G-search-completeness-accept** | **FILTER_COMPLETENESS accept stays complete** — `readSearchCompletenessAccept` reads `docs/plans/ENTERPRISE_MAP/search-completeness-accept.json`. A1–A11 are dispositioned, 268 long-tail concepts from `registry-report.json` are in the ledger with unexplained = 0, and prod TTFB p75 for `/homes-for-sale` and `/homes-for-sale/bend` is recorded under 600ms. Packet + `/admin/loop` + scoreboard `searchCompleteness` read that DAL. VERSION-1 G15 stays DONE. Founding case: R-099/R-100 sat MISSING while the registry and zoning already shipped. | `scripts/check-search-completeness-accept.mjs` (`ci:search-completeness-accept`, in `ci:gates`) | THE LOOP G15 / R-097…R-106 |
| **G-place-hero-grain** | **Place-page hero count names this page's grain** — `placeHeroLead` continues KbHero's "{N} homes for sale" with `in {placeName}`, never a coarser parent. City, neighborhood, community, subdivision, and ZIP pages must import it. Founding case: `/cities/bend/awbrey-butte` rendered "63 homes for sale in Bend" (fleet 97c68da5). | `scripts/check-place-hero-grain.mjs` (`ci:place-hero-grain`, in `ci:gates`) | fleet 97c68da5 / R-109 sub-city scoping |
| **G-pulse-city-remainder** | **Region SFR pulse vs city table names the omitted inventory** — `namePulseCityRemainder` names every omitted pulse city with inventory and the TIGER/MLS remainder. Hub, central-oregon, and annual-review fetch `getMarketPulseAllCitySnapshots` and pass `regionActive`. Founding case: `/housing-market` printed 1,841 next to seven city rows summing to 1,026 (fleet 5439b87e). | `scripts/check-pulse-city-remainder.mjs` (`ci:pulse-city-remainder`, in `ci:gates`) | fleet 5439b87e / R-024 same-page SFR source |
| **G-city-cache-slug** | **City URL hyphens resolve to the space-form cache key** — `canonicalCityCacheSlug` maps `/housing-market/la-pine` to `geo_slug='la pine'` before any city-tier pulse/stats read. Founding case: hub printed La Pine 175 SFR; the city report 404ed (fleet 75370225805bb52d38b151ced2dab5c1). | `scripts/check-city-cache-slug.mjs` (`ci:city-cache-slug`, in `ci:gates`) | fleet 75370225805bb52d38b151ced2dab5c1 / R-024 |
| **G-publish-mos** | **Pulse months of supply publishes only with its own numerator** — `publishMonthsOfSupply` withholds the figure when the pulse `active_count` is not the count on screen, or when implied six-month closes exceed a printed 12-month sold count. Community, neighborhood, city, homepage, housing-market geo, FAQ/Dataset, and the CRM report block must import it. Founding case: `/communities/tetherow` printed 4.6 MOS + 35 actives next to 36 sold in 12 months (fleet 5d55abbd72a67d25a5d7232b46fd2fb0). | `scripts/check-publish-months-of-supply.mjs` (`ci:publish-months-of-supply`, in `ci:gates`) | fleet 5d55abbd72a67d25a5d7232b46fd2fb0 / R-002 |
| **G-publish-place-hoa** | **One published annual HOA on a place page** — `publishPlaceHoa` prefers the master assessment, else the floor of registry estimates. Glance, FAQ, and Dataset cannot print two different annuals. Founding case: `/communities/tetherow` glance $1,464 vs FAQ $2,244 (fleet eab91ac8dfa9b833ade88640c6cce7d4). | `scripts/check-publish-place-hoa.mjs` (`ci:publish-place-hoa`, in `ci:gates`) | fleet eab91ac8dfa9b833ade88640c6cce7d4 / R-024 |
| **G-publish-median-caption** | **A published list median is labeled with the geography of the number** — `publishSellMedian` pairs a place median with `{place} median`. "Regional median" only when the figure is the region pulse. KbSell withholds a price with no caption. Empty membership facts withhold (`publishFactValue`). Public chart sources go through `toPublicCoreChartSeries` (no table names). Founding case: `/communities/tetherow` labeled $1,499,000 a Regional median (fleet 5f0ec58d60988a52e76b8a559ef22f0c). | `scripts/check-publish-median-caption.mjs` (`ci:publish-median-caption`, in `ci:gates`) | fleet 5f0ec58d60988a52e76b8a559ef22f0c / R-024 |
| **G-publish-down-payment** | **One published down-payment dollar figure on a listing** — `publishFinancingSplit` is whole-dollar down plus remainder loan. Monthly payment, rental analysis, the standalone mortgage calculator, and showcase payment must import it. Display those dollars exact, never nearest-thousand. Founding case: `/homes-for-sale/bend/61579-rockway-220226183` printed $130,000 down next to $129,800 (fleet 0b2eea305a233f4a1d246cf2e8f1a299). | `scripts/check-publish-down-payment.mjs` (`ci:publish-down-payment`, in `ci:gates`) | fleet 0b2eea305a233f4a1d246cf2e8f1a299 / R-024 |
| **G-publish-plat-inventory** | **One public SFR counted set on a registry plat** — `getPlatPublicInventory` is MLS SubdivisionName + SFR + PUBLIC_ACTIVE from `listing_tile_mv`. `/subdivisions` tiles, the plat hero, and `#homes` must share that payload. Do not publish a featured-fetch cap or an all-types pin count under the same label. Founding case: `/subdivisions/ridge-at-eagle-crest` printed 14 / listed 26 / index 12 (fleet 37d5349b2d2e55aa62df73389d8bad85). | `scripts/check-publish-plat-inventory.mjs` (`ci:publish-plat-inventory`, in `ci:gates`) | fleet 37d5349b2d2e55aa62df73389d8bad85 / R-024 |
| **G-publish-plat-figures** | **A plat page publishes only plat-grain live figures** — `publishPlatFigures` takes the counted-set median and withholds days-to-pending and 30-day sold. City/community pulse must not fill the unlabeled hero or sell tail. Founding case: `/subdivisions/ridge-at-eagle-crest` printed Pending in 19.5 days (Redmond city pulse) next to the plat $910,000 median (fleet 6a52801e3ef9e0d041b830497794290d). | `scripts/check-publish-plat-figures.mjs` (`ci:publish-plat-figures`, in `ci:gates`) | fleet 6a52801e3ef9e0d041b830497794290d / R-024 |
| **G-publish-resort-index-figures** | **A registry resort's public active count and list median are the alias-aware set** — `publishResortIndexFigures` takes `resortActiveSfrCounts` / `resortTilesForSlug`. Homepage tiles, `/communities` flagship + A-Z, and `getCommunityBySlug` overlay that pair. Literal-name snapshot counts are a different geography. Founding case: homepage Tetherow 12 ACTIVE vs `/communities/tetherow` 35 homes (fleet a7a6038f1d78857572e7e2199cf399bf). | `scripts/check-publish-resort-index-figures.mjs` (`ci:publish-resort-index-figures`, in `ci:gates`) | fleet a7a6038f1d78857572e7e2199cf399bf / R-024 |
| **G-publish-search-count** | **One labeled search count per grain** — `publishSearchCount` / `publishSearchCountPair` make filter-match the coherent number for URL filters. A map-viewport count prints only when it differs, labeled "in this map view". City browse that is all types says so. FAQ SFR names single-family in the question. 390 chips stay in the layout. Apply fires on first press. Founding case: `/homes-for-sale?maxPrice=800000&beds=3` sheet 409 vs results 318 (fleet d8f52b39ceceb240344f408d574fee27). | `scripts/check-publish-search-count.mjs` (`ci:publish-search-count`, in `ci:gates`) | fleet d8f52b39 / R-024 |
| **G-publish-regional-search-href** | **A control that names the regional inventory opens list view with no city** — `publishRegionalSearchHref` → `/homes-for-sale?view=list`. Split/map `/homes-for-sale` injects Bend. Homepage See homes, towns CTA, map Browse homes, featured view-all, and footer must import it. Founding case: homepage See homes next to 1,836 homes landed on Showing Bend only (fleet ef6af6b44156e99f0f5ca42850819b19). | `scripts/check-publish-regional-search-href.mjs` (`ci:publish-regional-search-href`, in `ci:gates`) | fleet ef6af6b4 / R-024 |

Run them all locally before pushing:
```bash
npm run ci:gates
```

That umbrella runs the gate chain defined in `package.json` → `ci:gates` (~60 gates). **That script is the single source of truth — do not re-enumerate the list in prose here; it drifts.** (The 2026-06-20 audit found this paragraph listed gates like `mockup-coverage`, `data-access`, `producer-skills`, `tool-discipline`, `geo-imagery`, `community-content`, `design-directives` as "in the umbrella" when they were never wired.) The meta-gate `ci:gates-wired` (last in the chain) now enforces two things: every `ci:*` gate is wired, AND no `scripts/check-*.mjs` runs nowhere. As of 2026-06-22, **7 gate files are an orphan backlog** tracked in `scripts/gates-wired-baseline.json` (down from the 28 the audit found; 21 since wired or fixed-then-wired). Triage the rest: wire into `ci:gates`/a workflow, or delete; the count may only shrink. DB-dependent gates (G16 `ci:data-access`) run locally + nightly, not the secret-less static chain. CI runs `ci:gates` in `.github/workflows/ci.yml` plus pa11y-ci + Lighthouse on PRs and route-smoke against `start-server-and-test`.

## The ratchet pattern

G3, G6, G7, G8 all use the same baseline-ratchet pattern: a JSON file in `scripts/` records the legacy state when the gate was first added. The gate fails on NEW violations beyond the baseline, not on the legacy backlog. As migrations land, the baseline shrinks.

To re-baseline a gate (only when intentional — e.g. accepting a deferred contract):

```bash
npm run ci:dal-boundary:baseline       # writes scripts/dal-boundary-baseline.json
npm run ci:brand-voice:baseline        # writes scripts/brand-voice-baseline.json
npm run ci:mockup-parity --write-baseline
npm run ci:page-dal:baseline
npm run ci:static-params:baseline
```

A baseline commit should always include the reason in the commit body (e.g. "Accept gap on /listing/[listingKey] until Wave 3 rebuild lands").

## G6 — mockup parity in depth

The most important gate added 2026-05-28 because the listing-detail rebuild shipped without ever reading the mockup. The plan said "build from the mockup," and the agent ignored that prose rule. The gate now enforces it.

Contract per route lives at `design_system/ryan-realty/ui_kits/<route>/parity.json`:

```json
{
  "route": "app/listing/[listingKey]/page.tsx",
  "mockup": "design_system/ryan-realty/ui_kits/listing-detail/index.html",
  "requiredComponents": [
    { "name": "ListingHero",             "section": "ld-photo-grid full-bleed hero" },
    { "name": "PriceCtaStrip",           "section": "price + CTA strip" },
    { "name": "NeighborhoodMarketContext", "section": "Live market context (Zillow beater)" }
  ]
}
```

The gate parses the parity.json, reads the route's `page.tsx`, asserts each `requiredComponents[].name` is imported. Missing imports fail CI (unless baselined).

**Adding a new gated route:**

1. Place the mockup at `design_system/ryan-realty/ui_kits/<route>/index.html` (already exists for every planned route in this repo).
2. Add `design_system/ryan-realty/ui_kits/<route>/parity.json` with the component contract.
3. Run `node scripts/check-mockup-parity.mjs` locally.
4. Fix imports in the route file until it passes, or baseline the gap with a justification.

**Updating an existing contract:**

If the mockup changes (e.g. a new section added), update the `parity.json` to reflect the new required components. The gate will fail until the route file imports them.

## G7 — page DAL in depth

Positive counterpart to G1. Every `app/<route>/page.tsx` must `import ... from '@/lib/data'` (or any subpath). Opt-out is a leading `// @data-free` comment on a page that genuinely needs no data (e.g. a static About page).

Scope excludes:
- `app/api/**` (API routes — different read pattern)
- `app/admin/**` (admin tools — lower scope per Plan §3)
- `app/account/**`, `app/dashboard/**` (auth-gated, opt out individually)

## G8 — generateStaticParams in depth

Dynamic routes (paths containing `[slug]`) must export `generateStaticParams` so Next can pre-render. Opt-out is a `// @no-static-params` comment for routes that legitimately need full SSR.

Scope excludes the same auth-gated directories as G7.

## What's NOT yet mechanized (intentionally — runtime telemetry only)

| Guardrail | Why this stays prose | Where it lives instead |
|---|---|---|
| TTFB p95 < 200ms (listing) / < 300ms (LP) | Production telemetry, not pre-deploy | Vercel Analytics dashboard + manual review |
| MV refresh successfully for 7 consecutive days | Runtime health, not pre-deploy | `monitoring_alerts` + Supabase advisor digest |
| Pixel-diff every section vs mockup (visual parity) | Subjective human-judgement step | Mockup parity (G6) handles the structural check; visual sign-off stays with Matt |
| Data accuracy (every published number traces to a verified source) | Per-content-piece runtime concern, not a static lint | Producer-side `citations.json` files + per-deliverable verification trace |
| Spark × Supabase market-data reconciliation gate | Video pipeline, not website | `scripts/_voice_lib.py` + producer pre-render gate |
| A map subtitle's prose claim matches its query's filters | Requires cross-function dataflow analysis (call site -> DAL -> predicate); not decidable in a lint | `scripts/check-map-scope-claims.mjs` covers the decidable half (no bare `<ident>.length` in `totalActive`); the claim half is the C-01 census + reviewer |
| A shared component must not discard a prop the caller passed | The decidable form ("no ternary whose false-branch is a prop and true-branch is a literal") false-positives across the codebase on legitimate defaulting | C-09 census; caught in review |
| The trailing-defense clause (fact, then a clause justifying it) | Deciding whether a trailing clause adds an actionable noun is a semantic judgement | `scripts/check-voice-trailing-defense.mjs` denylists the exact killed strings; the shape stays VOICE.md + the review test |

Everything mechanizable for the WEBSITE surface has been mechanized. Adding new gates as new failure modes surface is the pattern (see "How to add a new gate" below).

## How to add a new gate

1. Write `scripts/check-<thing>.mjs` with these CLI conventions:
   - Default exit code: 0 on pass, 1 on fail
   - `--report` flag: human-readable, always exit 0
   - `--json` flag: machine-readable
   - `--write-baseline` flag (if ratcheted): write the current state as the baseline
2. Add a `package.json` entry: `"ci:<thing>": "node scripts/check-<thing>.mjs"`
3. Add the script to the `ci:gates` umbrella in `package.json`
4. `.github/workflows/ci.yml` already runs `npm run ci:gates`, so step 3 is enough for a
   static gate. A **DB-dependent** gate must NOT go in the chain (it runs secret-less and
   offline) — add a `- run: npm run ci:<thing>` step to `.github/workflows/quality.yml`
   instead, and make the script SKIP with exit 0 when credentials are absent. Either way
   `ci:gates-wired` must stay green.
5. Add a row to the "Active gates" table above. **This table is the authority on G-numbers;
   take the next free one.** As of 2026-08-07 the highest is G66.
6. Write break-tests at `scripts/__tests__/check-<thing>.test.mjs` that prove EVERY rule can
   fail. Copy the inspected files into a sandbox, break exactly one rule per case, assert a
   non-zero exit naming that rule — and assert the deliberately-safe shapes stay green, because
   false positives are what gets a gate deleted. Exemplars: `check-search-perf-budget.test.mjs`,
   `check-toast-read-discipline.test.mjs`. Put the sandbox OUTSIDE the repo (with `node_modules`
   symlinked in): a concurrent session running `git clean -fd` deletes an untracked in-repo
   scratch dir mid-run and the failure looks like a gate bug.

Done. No prose update to CLAUDE.md needed — the gate is the rule.

### Known G-number drift (do not copy the pattern)

The table above is authoritative, but several gate scripts' header comments claim a G-number
the table assigns to a different gate: `check-kb-page-contract.mjs` claims **G52**, which the
table has no row for at all, and the same collision runs through `check-hydration-safety.mjs`
(G37), `check-csp.mjs` (G38), `check-db-timeout-guard.mjs` (G39), `check-shadcn-burndown.mjs`
(G47), `check-tracking-policy.mjs` (G48), `check-crm-lead-integrity.mjs` (G49),
`check-kb-single-source.mjs` (G50), `check-kb-overlay-hidden.mjs` (G51),
`check-kb-shared-shell.mjs` + `check-resort-definitions.mjs` (both G53). The table itself also
carries **two G16 rows** (`check-valuation-engine.mjs` and `check-data-access.mjs`). Numbers
were handed out from script headers instead of from this table, twice, in parallel sessions.
`check-private-key-parity.mjs` shows the fix: it was renumbered to G60 with the note
"(Renumbered from G58 per docs/MECHANICAL_GATES.md, which is authoritative.)" — do that when you
next touch one of the scripts above.

## Retired gates

**`ci:console-kit` (`scripts/check-console-kit.mjs`) — RETIRED 2026-08-07 (P11E).**

It encoded a Matt directive of 2026-06-15, after the lead-command-center drift: *every
broker-facing admin console surface must be assembled from the SHARED console kit so the look
lives in one place and cannot drift page-by-page*, with `ConsoleSection`'s mandatory heading
answering "a bunch of text and boxes, no headings". The gate asserted that each page in
`REQUIRED_KIT_PAGES` imported `ConsoleSection`.

**The directive is not retired. Its mechanism is.** Phase 11 migrated all 143 admin pages onto
the locked v2 language (`design_system/admin/ADMIN_UI.md`), whose acceptance bar forbids the
legacy console kit — the two contracts cannot both hold on one page. Over 11C–11E every entry
on the list moved to the v2 side, each with a dated hand-off comment, until the list asserted
nothing. A gate with an empty assertion list passes trivially and reads green, which is worse
than no gate: it looks like coverage.

The same intent is now carried, more strictly, by three gates that ARE on the chain:

| directive | now enforced by |
|---|---|
| one shared kit, no page-by-page drift | `ci:admin-ui` rule B — a `page.tsx` under `app/admin` that does not import `@/components/admin/v2` is a legacy page; the counter ratcheted 131 → 0 |
| headings are mandatory, no headless data dump | `SectionHead` / `EntityTitle` own headings; `ci:admin-ui` rule A bans raw `<h1>`/`<h2>` outright |
| the look lives in one place | `ci:admin-v2-tokens` — every colour through `var(--a-*)`, no hex, no Tailwind palette, no brand leak; plus `ci:admin-responsive` |

`ci:gates-wired` counts 211 gate files with 0 running nowhere after the deletion.
